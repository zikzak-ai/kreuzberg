{
	"id": "config_chunking_semantic_small",
	"description": "Semantic chunker with small budget produces more granular chunks",
	"tags": ["contract", "config", "chunking"],
	"call": "extract_file_sync",
	"input": {
		"path": "semantic/annual_report.txt",
		"config": {
			"chunking": {
				"chunker_type": "semantic",
				"max_chars": 200
			}
		}
	},
	"assertions": [
		{
			"type": "equals",
			"field": "mime_type",
			"value": "text/plain"
		},
		{
			"type": "min_length",
			"field": "content",
			"value": 100
		}
	],
	"skip": {
		"reason": "Requires chunking feature"
	}
}
