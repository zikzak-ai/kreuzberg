{
	"id": "chunk_text_basic",
	"category": "chunking",
	"description": "Basic text chunking with small chunk size and overlap",
	"tags": ["chunking", "text"],
	"call": "chunk_text",
	"input": {
		"text": "The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.",
		"config": {
			"max_characters": 50,
			"max_overlap": 10
		}
	},
	"assertions": [
		{
			"type": "not_error"
		}
	]
}
