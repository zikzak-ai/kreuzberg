{
	"id": "validate_dpi_negative",
	"description": "Reject negative DPI in OCR config",
	"tags": ["validation", "ocr", "dpi"],
	"call": "extract_file_sync",
	"input": {
		"path": "pdf/fake_memo.pdf",
		"config": {
			"ocr": {
				"dpi": -1
			}
		}
	},
	"assertions": [
		{
			"type": "error"
		}
	]
}
