// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ca6a5a7b51c2bc25413ddd99f2665d7d720f0e5cdba092a6d748a9580db10e9a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzberg
import RustBridge

/// E2e tests for category: mime_utilities.
final class MimeUtilitiesTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _testDocs = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent() // <Module>Tests/
            .deletingLastPathComponent() // Tests/
            .deletingLastPathComponent() // swift/
            .deletingLastPathComponent() // packages/
            .deletingLastPathComponent() // <repo root>
            .appendingPathComponent("test_documents")
        if FileManager.default.fileExists(atPath: _testDocs.path) {
            FileManager.default.changeCurrentDirectoryPath(_testDocs.path)
        }
    }

    func testMimeDetectBytes() throws {
        // Detect MIME type from file bytes
        let contentData = try Data(contentsOf: URL(fileURLWithPath: "pdf/fake_memo.pdf"))
        let contentVec = RustVec<UInt8>()
        for _byte in contentData { contentVec.push(value: _byte) }
        let result = try detectMimeTypeFromBytes(contentVec)
        XCTAssertTrue(result.toString().contains("pdf"), "expected to contain: \("pdf")")
    }

    func testMimeDetectImage() throws {
        // Detect MIME type from PNG image bytes
        let contentData = try Data(contentsOf: URL(fileURLWithPath: "images/test_hello_world.png"))
        let contentVec = RustVec<UInt8>()
        for _byte in contentData { contentVec.push(value: _byte) }
        let result = try detectMimeTypeFromBytes(contentVec)
        XCTAssertTrue(result.toString().contains("png"), "expected to contain: \("png")")
    }

    func testMimeGetExtensions() throws {
        // Get file extensions for a MIME type
        let result = try getExtensionsForMime("application/pdf")
        XCTAssertTrue(result.toString().contains("pdf"), "expected to contain: \("pdf")")
    }

}
