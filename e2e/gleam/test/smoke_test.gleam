// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5399677991254448721a4a69b6a07aca7601ab417e255aae2207f948da9fee7c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import kreuzberg

import gleam/int
import gleam/list
import gleam/string

// OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.
pub fn ocr_image_png_test() {
  let result = kreuzberg.extract_bytes("images/test_hello_world.png", "image/png", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("image/png")
  r.content |> string.length |> int.is_at_least(1) |> should.equal(True)
  r.content |> string.contains("Hello") |> should.equal(True)
  r.content |> string.contains("World") |> should.equal(True)
  r.content |> string.contains("hello") |> should.equal(True)
  r.content |> string.contains("world") |> should.equal(True)
}

// Smoke test: DOCX with formatted text
pub fn smoke_docx_basic_test() {
  let result = kreuzberg.extract_file("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  r.content |> string.length |> int.is_at_least(20) |> should.equal(True)
  r.content |> string.contains("Lorem") |> should.equal(True)
  r.content |> string.contains("ipsum") |> should.equal(True)
  r.content |> string.contains("document") |> should.equal(True)
  r.content |> string.contains("text") |> should.equal(True)
}

// Smoke test: HTML table extraction
pub fn smoke_html_basic_test() {
  let result = kreuzberg.extract_file("html/simple_table.html", "text/html", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("text/html")
  r.content |> string.length |> int.is_at_least(10) |> should.equal(True)
  r.content |> string.contains("Sample Data Table") |> should.equal(True)
  r.content |> string.contains("Laptop") |> should.equal(True)
  r.content |> string.contains("Electronics") |> should.equal(True)
  r.content |> string.contains("Product") |> should.equal(True)
}

// Smoke test: JSON file extraction
pub fn smoke_json_basic_test() {
  let result = kreuzberg.extract_file("json/simple.json", "application/json", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("application/json")
  r.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Smoke test: PDF with simple text extraction
pub fn smoke_pdf_basic_test() {
  let result = kreuzberg.extract_file("pdf/fake_memo.pdf", "application/pdf", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("application/pdf")
  r.content |> string.length |> int.is_at_least(50) |> should.equal(True)
  r.content |> string.contains("May 5, 2023") |> should.equal(True)
  r.content |> string.contains("To Whom it May Concern") |> should.equal(True)
}

// Smoke test: Plain text file
pub fn smoke_txt_basic_test() {
  let result = kreuzberg.extract_file("text/report.txt", "text/plain", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("text/plain")
  r.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Smoke test: XLSX with basic spreadsheet data including tables
pub fn smoke_xlsx_basic_test() {
  let result = kreuzberg.extract_file("xlsx/stanley_cups.xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  r.content |> string.length |> int.is_at_least(100) |> should.equal(True)
  r.content |> string.contains("Team") |> should.equal(True)
  r.content |> string.contains("Location") |> should.equal(True)
  r.content |> string.contains("Stanley Cups") |> should.equal(True)
  r.content |> string.contains("Blues") |> should.equal(True)
  r.content |> string.contains("Flyers") |> should.equal(True)
  r.content |> string.contains("Maple Leafs") |> should.equal(True)
  r.content |> string.contains("STL") |> should.equal(True)
  r.content |> string.contains("PHI") |> should.equal(True)
  r.content |> string.contains("TOR") |> should.equal(True)
  r.tables |> list.length |> int.is_at_least(1) |> should.equal(True)
  r.metadata.format.excel.sheet_count |> int.is_at_least(2) |> should.equal(True)
  r.metadata.format.excel.sheet_names |> string.contains("Stanley Cups") |> should.equal(True)
}
