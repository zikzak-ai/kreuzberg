// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cb199d953767103c39a2941968bf805a4368e755b4fe17ef1c0fc5c28571a106
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Kreuzberg;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: render.</summary>
public class RenderTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public void Test_RenderCustomDpi()
    {
        // Render PDF page with custom DPI
        var result = Kreuzberg.RenderPage("pdf/tiny.pdf", 0, null);
        Assert.True(result.IsPng);
        Assert.True(result.ByteLength >= 50, "expected >= 50");
    }

    [Fact]
    public void Test_RenderHighDpi()
    {
        // Render PDF page with high DPI setting (300 DPI)
        var result = Kreuzberg.RenderPage("pdf/fake_memo.pdf", 0, new ExtractionConfig { Dpi = 300 });
    }

    [Fact]
    public void Test_RenderIterator()
    {
        // Iterate all PDF pages as PNG images
        var result = Kreuzberg.RenderPage("pdf/tiny.pdf", 0, null);
        Assert.True(result.IsPng);
        Assert.True(result.PageCount >= 1, "expected >= 1");
    }

    [Fact]
    public void Test_RenderMultiPage()
    {
        // Render a page from a multi-page PDF document
        var result = Kreuzberg.RenderPage("pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf", 0, null);
        Assert.True(result.Result.Length >= 1000, "expected length >= 1000");
    }

    [Fact]
    public void Test_RenderOutputDjot()
    {
        // Extract PDF with Djot output format
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "djot" });
        Assert.Equal("djot", result.OutputFormat.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public void Test_RenderOutputHtml()
    {
        // Extract PDF with HTML output format
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "html" });
        Assert.Equal("html", result.OutputFormat.Trim());
        Assert.True(result.Content.ToString().Contains("<") || result.Content.ToString().Contains(">") || result.Content.ToString().Contains("</"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public void Test_RenderOutputMarkdown()
    {
        // Extract PDF with Markdown output format
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "markdown" });
        Assert.Equal("markdown", result.OutputFormat.Trim());
        Assert.True(result.Content.ToString().Contains("#") || result.Content.ToString().Contains("**") || result.Content.ToString().Contains("*"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public void Test_RenderOutputPlain()
    {
        // Extract PDF with plain text output format
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "plain" });
        Assert.Equal("plain", result.OutputFormat.Trim());
        Assert.True(result.Content.Length >= 50, "expected length >= 50");
    }

    [Fact]
    public void Test_RenderSinglePage()
    {
        // Render a single PDF page to PNG
        var result = Kreuzberg.RenderPage("pdf/tiny.pdf", 0, null);
        Assert.True(result.IsPng);
        Assert.True(result.ByteLength >= 100, "expected >= 100");
    }
}
