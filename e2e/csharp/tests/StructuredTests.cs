// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:09d858fdcf7fdac0174532493fd8cafbd07a90b9dc0f97406ad7b3cb1ae4415c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Kreuzberg;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: structured.</summary>
public class StructuredTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_StructuredCsvBasic()
    {
        // CSV data file extraction.
        var result = await Kreuzberg.ExtractFile("csv/stanley_cups.csv", null, null);
        Assert.Equal("text/csv", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_StructuredEnwBasic()
    {
        // EndNote ENW citation format extraction.
        var result = await Kreuzberg.ExtractFile("data_formats/sample.enw", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-endnote-refer") || result.MimeType.ToString().Contains("application/x-endnote+xml") || result.MimeType.ToString().Contains("text/plain"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_StructuredJsonBasic()
    {
        // Structured JSON extraction should stream and preserve content.
        var result = await Kreuzberg.ExtractFile("json/sample_document.json", null, null);
        Assert.Equal("application/json", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
        Assert.True(result.Content.ToString().Contains("Sample Document") || result.Content.ToString().Contains("Test Author"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_StructuredJsonSimple()
    {
        // Simple JSON document to verify structured extraction.
        var result = await Kreuzberg.ExtractFile("json/simple.json", null, null);
        Assert.Equal("application/json", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("{") || result.Content.ToString().Contains("name"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_StructuredNbibBasic()
    {
        // PubMed NBIB citation format extraction.
        var result = await Kreuzberg.ExtractFile("data_formats/sample.nbib", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/nbib") || result.MimeType.ToString().Contains("application/x-pubmed") || result.MimeType.ToString().Contains("text/plain"), "expected to contain at least one of the specified values");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_StructuredRisBasic()
    {
        // RIS citation format extraction.
        var result = await Kreuzberg.ExtractFile("data_formats/sample.ris", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-research-info-systems") || result.MimeType.ToString().Contains("text/plain"), "expected to contain at least one of the specified values");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_StructuredTomlBasic()
    {
        // TOML configuration file extraction.
        var result = await Kreuzberg.ExtractFile("data_formats/cargo.toml", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/toml") || result.MimeType.ToString().Contains("text/toml"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_StructuredTsvBasic()
    {
        // TSV (tab-separated values) data file extraction.
        var result = await Kreuzberg.ExtractFile("data_formats/employees.tsv", null, null);
        Assert.True(result.MimeType.ToString().Contains("text/tab-separated-values") || result.MimeType.ToString().Contains("text/plain"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_StructuredYamlBasic()
    {
        // YAML file text extraction.
        var result = await Kreuzberg.ExtractFile("yaml/simple.yaml", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/yaml") || result.MimeType.ToString().Contains("text/yaml") || result.MimeType.ToString().Contains("text/x-yaml") || result.MimeType.ToString().Contains("application/x-yaml"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_StructuredYamlSimple()
    {
        // Simple YAML document to validate structured extraction.
        var result = await Kreuzberg.ExtractFile("yaml/simple.yaml", null, null);
        Assert.Equal("application/x-yaml", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }
}
