// This file is auto-generated by alef. DO NOT EDIT.
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Kreuzberg;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: contract.</summary>
public class ContractTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_ApiBatchBytesAsync()
    {
        // Tests async batch bytes extraction API (batch_extract_bytes)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiBatchBytesSync()
    {
        // Tests sync batch bytes extraction API (batch_extract_bytes_sync)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiBatchBytesWithConfigsAsync()
    {
        // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("markdown", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_ApiBatchBytesWithConfigsSync()
    {
        // Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("markdown", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_ApiBatchFileAsync()
    {
        // Tests async batch file extraction API (batch_extract_file)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiBatchFileSync()
    {
        // Tests sync batch file extraction API (batch_extract_file_sync)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiBatchFileWithConfigsAsync()
    {
        // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("markdown", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_ApiBatchFileWithConfigsSync()
    {
        // Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("markdown", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_ApiBatchFileWithTimeoutSync()
    {
        // Tests sync batch file extraction with per-file timeout config override
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { ExtractionTimeoutSecs = 300 });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_ApiExtractBytesAsync()
    {
        // Tests async bytes extraction API (extract_bytes)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiExtractBytesSync()
    {
        // Tests sync bytes extraction API (extract_bytes_sync)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiExtractFileAsync()
    {
        // Tests async file extraction API (extract_file)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ApiExtractFileSync()
    {
        // Tests sync file extraction API (extract_file_sync)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("Mallori"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ConfigAccelerationCpuProvider()
    {
        // Tests explicit CPU acceleration provider configuration
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { Acceleration = "{\"device_id\":0,\"provider\":\"cpu\"}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 50, "expected length >= 50");
        Assert.True(result.Content.ToString().Contains("May 5, 2023") || result.Content.ToString().Contains("To Whom it May Concern"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_ConfigChunking()
    {
        // Tests chunking configuration with chunk assertions
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { Chunking = "{\"max_chars\":500,\"max_overlap\":50}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Chunks.Count >= 1, "expected at least 1 elements");
        Assert.True(result.ChunksHaveContent);
    }

    [Fact]
    public async Task Test_ConfigChunkingText()
    {
        // Tests text chunker type (generic whitespace/punctuation splitter)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { Chunking = "{\"chunker_type\":\"text\",\"max_chars\":500,\"max_overlap\":50}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Chunks.Count >= 1, "expected at least 1 elements");
        Assert.True(result.ChunksHaveContent);
    }

    [Fact]
    public async Task Test_ConfigDisableOcr()
    {
        // Tests disable_ocr configuration option - OCR is skipped for images
        var result = await Kreuzberg.ExtractFile("images/test_hello_world.png", null, new ExtractionConfig { DisableOcr = true });
        Assert.Equal("image/png", result.MimeType.Trim());
        Assert.True(result.Content.Length <= 5, "expected length <= 5");
    }

    [Fact]
    public async Task Test_ConfigDocumentStructure()
    {
        // Tests include_document_structure config produces document tree
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { IncludeDocumentStructure = true });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.False(string.IsNullOrEmpty(result.DocumentStructure?.ToString()));
        Assert.True(result.DocumentStructure.Nodes.Count >= 1, "expected at least 1 elements");
        Assert.Contains("paragraph", result.DocumentStructure.NodeTypes.ToString().ToLower());
    }

    [Fact]
    public async Task Test_ConfigDocumentStructureDisabled()
    {
        // Tests document field is null when include_document_structure is false
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
    }

    [Fact]
    public async Task Test_ConfigDocumentStructureWithHeadings()
    {
        // Tests document structure with DOCX heading-driven nesting
        var result = await Kreuzberg.ExtractFile("docx/fake.docx", null, new ExtractionConfig { IncludeDocumentStructure = true });
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.False(string.IsNullOrEmpty(result.DocumentStructure?.ToString()));
        Assert.True(result.DocumentStructure.Nodes.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ConfigEmailMsgFallbackCodepage()
    {
        // Tests MSG extraction with custom fallback codepage for Cyrillic
        var result = await Kreuzberg.ExtractFile("email/fake_email.msg", null, new ExtractionConfig { Email = "{\"msg_fallback_codepage\":1251}" });
        Assert.Equal("application/vnd.ms-outlook", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_ConfigExtractionTimeout()
    {
        // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { ExtractionTimeoutSecs = 300 });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_ConfigHtmlOptions()
    {
        // Tests extraction with HTML conversion options configured
        var result = await Kreuzberg.ExtractFile("html/complex_table.html", null, new ExtractionConfig { HtmlOptions = "{\"extract_metadata\":true}" });
        Assert.True(result.MimeType.ToString().Contains("text/html"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_ConfigHtmlStyledCustomCss()
    {
        // HTML output with inline user CSS string is embedded verbatim in the style block
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { HtmlOutput = "{\"css\":\".kb-p { color: red; }\",\"embed_css\":true,\"theme\":\"unstyled\"}", OutputFormat = "html" });
        Assert.Equal("html", result.OutputFormat.Trim());
        Assert.Contains(".kb-p { color: red; }", result.Content.ToString().ToLower());
        Assert.Contains("kb-doc", result.Content.ToString().ToLower());
    }

    [Fact]
    public async Task Test_ConfigHtmlStyledDefault()
    {
        // HTML output with default theme emits kb-* class hooks and an embedded style block
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { HtmlOutput = "{\"embed_css\":true,\"theme\":\"default\"}", OutputFormat = "html" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.Equal("html", result.OutputFormat.Trim());
        Assert.Contains("kb-doc", result.Content.ToString().ToLower());
        Assert.Contains("kb-content", result.Content.ToString().ToLower());
        Assert.Contains("kb-p", result.Content.ToString().ToLower());
        Assert.Contains("<style>", result.Content.ToString().ToLower());
    }

    [Fact]
    public async Task Test_ConfigHtmlStyledNoEmbed()
    {
        // HTML output with embed_css false emits class hooks but no style block
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { HtmlOutput = "{\"embed_css\":false,\"theme\":\"default\"}", OutputFormat = "html" });
        Assert.Equal("html", result.OutputFormat.Trim());
        Assert.Contains("kb-doc", result.Content.ToString().ToLower());
        Assert.Contains("kb-content", result.Content.ToString().ToLower());
    }

    [Fact]
    public async Task Test_ConfigImages()
    {
        // Tests image extraction configuration with image assertions
        var result = await Kreuzberg.ExtractFile("pdf/embedded_images_tables.pdf", null, new ExtractionConfig { Images = "{\"extract_images\":true}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Images.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ConfigImagesWithFormats()
    {
        // Tests image extraction on PPTX containing embedded images
        var result = await Kreuzberg.ExtractFile("pptx/powerpoint_with_image.pptx", null, new ExtractionConfig { Images = "{\"extract_images\":true}" });
        Assert.Equal("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.MimeType.Trim());
        Assert.True(result.Images.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ConfigLanguageDetection()
    {
        // Tests language detection configuration
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { LanguageDetection = "{\"enabled\":true}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Contains("eng", result.DetectedLanguages.ToString().ToLower());
        Assert.True(result.LanguageDetectionConfidence >= 0.5d, "expected >= 0.5d");
    }

    [Fact]
    public async Task Test_ConfigLanguageDetectionMulti()
    {
        // Tests language detection with detect_multiple and min_confidence options
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { LanguageDetection = "{\"detect_multiple\":true,\"enabled\":true,\"min_confidence\":0.3}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Contains("eng", result.DetectedLanguages.ToString().ToLower());
    }

    [Fact]
    public async Task Test_ConfigPostprocessor()
    {
        // Tests postprocessor config is accepted and extraction succeeds
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { Postprocessor = "{\"enabled\":true}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_ConfigProcessingWarningsEmpty()
    {
        // Tests that a clean PDF extraction produces no processing warnings
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, null);
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(string.IsNullOrEmpty(result.ProcessingWarnings?.ToString()));
    }

    [Fact]
    public async Task Test_ConfigQualityDisabled()
    {
        // Tests extraction with quality processing explicitly disabled
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { EnableQualityProcessing = false });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_ConfigSecurityLimits()
    {
        // Tests archive extraction with custom security limits
        var result = await Kreuzberg.ExtractFile("archives/documents.zip", null, new ExtractionConfig { SecurityLimits = "{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}" });
        Assert.True(result.MimeType.ToString().Contains("application/zip") || result.MimeType.ToString().Contains("application/x-zip-compressed"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_ConfigTablesContent()
    {
        // Tests table extraction with content_contains_any assertion on DOCX with tables
        var result = await Kreuzberg.ExtractFile("docx/docx_tables.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Tables.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ConfigUseCacheFalse()
    {
        // Tests use_cache=false configuration option
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { UseCache = false });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OutputFormatBytesMarkdown()
    {
        // Tests markdown output format via bytes extraction API
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "markdown" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("markdown", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_OutputFormatDjot()
    {
        // Tests Djot output format
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "djot" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("djot", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_OutputFormatHtml()
    {
        // Tests HTML output format
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "html" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("html", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_OutputFormatMarkdown()
    {
        // Tests Markdown output format
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "markdown" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("markdown", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_OutputFormatPlain()
    {
        // Tests Plain output format
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { OutputFormat = "plain" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("plain", result.OutputFormat.Trim());
    }

    [Fact]
    public async Task Test_ResultFormatElementBased()
    {
        // Tests ElementBased result format with element assertions
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { ResultFormat = "element_based" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.Equal("element_based", result.ResultFormat.Trim());
        Assert.True(result.Elements.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ResultFormatUnified()
    {
        // Tests Unified result format (default)
        var result = await Kreuzberg.ExtractFile("pdf/fake_memo.pdf", null, new ExtractionConfig { ResultFormat = "unified" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.Equal("unified", result.ResultFormat.Trim());
    }
}
