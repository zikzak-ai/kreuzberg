// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e6384a196bd09590319fbf035c0df17ab3f268b6d84e5c347079e4be982c39e2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Kreuzberg;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: token_reduction.</summary>
public class TokenReductionTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public void Test_TokenReductionAggressive()
    {
        // Tests aggressive token reduction mode significantly reduces content
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { TokenReduction = "{\"mode\":\"aggressive\"}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 5, "expected length >= 5");
        Assert.True(result.Content.Length <= 150, "expected length <= 150");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public void Test_TokenReductionBasic()
    {
        // Tests basic token reduction on PDF document
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { TokenReduction = "{\"mode\":\"moderate\"}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 5, "expected length >= 5");
        Assert.True(result.Content.Length <= 200, "expected length <= 200");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public void Test_TokenReductionLight()
    {
        // Tests light token reduction mode preserves most content
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { TokenReduction = "{\"mode\":\"light\"}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public void Test_TokenReductionReduceAggressive()
    {
        // Reduce tokens with aggressive compression level
        var result = Kreuzberg.ReduceTokens("The quick brown fox jumps over the lazy dog.", "aggressive", null);
        Assert.True(result.Result.Length >= 1, "expected length >= 1");
    }

    [Fact]
    public void Test_TokenReductionReduceBasic()
    {
        // Reduce tokens with light compression level
        var result = Kreuzberg.ReduceTokens("The quick brown fox jumps over the lazy dog. This is a test sentence.", "light", null);
        Assert.Contains("quick", result.Result.ToString().ToLower());
    }

    [Fact]
    public void Test_TokenReductionWithChunking()
    {
        // Tests token reduction combined with chunking
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { Chunking = "{\"max_chars\":500,\"max_overlap\":50}", TokenReduction = "{\"mode\":\"moderate\"}" });
        Assert.Equal("application/pdf", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 5, "expected length >= 5");
        Assert.True(result.Content.Length <= 200, "expected length <= 200");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.True(result.Chunks.Count >= 1, "expected at least 1 elements");
        Assert.True(result.ChunksHaveContent);
    }
}
