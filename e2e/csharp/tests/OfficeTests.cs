// This file is auto-generated by alef. DO NOT EDIT.
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Kreuzberg;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: office.</summary>
public class OfficeTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_OfficeBibtexBasic()
    {
        // BibTeX bibliography extraction.
        var result = await Kreuzberg.ExtractFile("bibtex/comprehensive.bib", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-bibtex") || result.MimeType.ToString().Contains("text/x-bibtex"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeCommonmarkBasic()
    {
        // CommonMark (.commonmark) text extraction.
        var result = await Kreuzberg.ExtractFile("markdown/sample.commonmark", null, null);
        Assert.True(result.MimeType.ToString().Contains("text/markdown") || result.MimeType.ToString().Contains("text/plain") || result.MimeType.ToString().Contains("text/x-commonmark"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 5, "expected length >= 5");
    }

    [Fact]
    public async Task Test_OfficeDjotBasic()
    {
        // Djot markup text extraction.
        var result = await Kreuzberg.ExtractFile("markdown/tables.djot", null, null);
        Assert.True(result.MimeType.ToString().Contains("text/x-djot") || result.MimeType.ToString().Contains("text/djot"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeDocbookBasic()
    {
        // DocBook XML document extraction.
        var result = await Kreuzberg.ExtractFile("docbook/docbook-reader.docbook", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/docbook+xml") || result.MimeType.ToString().Contains("text/docbook"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeDocxBasic()
    {
        // DOCX document extraction baseline.
        var result = await Kreuzberg.ExtractFile("docx/sample_document.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeDocxEquations()
    {
        // DOCX file containing equations to validate math extraction.
        var result = await Kreuzberg.ExtractFile("docx/equations.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeDocxFake()
    {
        // Simple DOCX document to verify baseline extraction.
        var result = await Kreuzberg.ExtractFile("docx/fake.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeDocxFormatting()
    {
        // DOCX document heavy on formatting for style preservation.
        var result = await Kreuzberg.ExtractFile("docx/unit_test_formatting.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeDocxHeaders()
    {
        // DOCX document with complex headers.
        var result = await Kreuzberg.ExtractFile("docx/unit_test_headers.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeDocxLists()
    {
        // DOCX document emphasizing list formatting.
        var result = await Kreuzberg.ExtractFile("docx/unit_test_lists.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeDocxTables()
    {
        // DOCX document containing tables for table-aware extraction.
        var result = await Kreuzberg.ExtractFile("docx/docx_tables.docx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 50, "expected length >= 50");
        Assert.Contains("simple uniform table", result.Content.ToString().ToLower());
        Assert.Contains("nested table", result.Content.ToString().ToLower());
        Assert.Contains("merged cells", result.Content.ToString().ToLower());
        Assert.Contains("header col", result.Content.ToString().ToLower());
        Assert.True(result.Tables.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_OfficeEpubBasic()
    {
        // EPUB book extraction with text content.
        var result = await Kreuzberg.ExtractFile("epub/features.epub", null, null);
        Assert.Equal("application/epub+zip", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 50, "expected length >= 50");
    }

    [Fact]
    public async Task Test_OfficeFb2Basic()
    {
        // FictionBook (FB2) document extraction baseline.
        var result = await Kreuzberg.ExtractFile("fictionbook/basic.fb2", null, null);
        Assert.Equal("application/x-fictionbook+xml", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeFictionbookBasic()
    {
        // FictionBook (.fb2) text extraction.
        var result = await Kreuzberg.ExtractFile("fictionbook/basic.fb2", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-fictionbook+xml") || result.MimeType.ToString().Contains("application/x-fictionbook"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeJatsBasic()
    {
        // JATS scientific article extraction.
        var result = await Kreuzberg.ExtractFile("jats/sample_article.jats", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-jats+xml") || result.MimeType.ToString().Contains("text/jats"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeJupyterBasic()
    {
        // Jupyter notebook extraction.
        var result = await Kreuzberg.ExtractFile("jupyter/rank.ipynb", null, null);
        Assert.Equal("application/x-ipynb+json", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeKeynoteBasic()
    {
        // Keynote document extraction baseline.
        var result = await Kreuzberg.ExtractFile("iwork/test.key", null, null);
        Assert.Equal("application/x-iwork-keynote-sffkey", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 5, "expected length >= 5");
    }

    [Fact]
    public async Task Test_OfficeLatexBasic()
    {
        // LaTeX document text extraction.
        var result = await Kreuzberg.ExtractFile("latex/basic_sections.tex", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-latex") || result.MimeType.ToString().Contains("text/x-latex"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeMarkdownBasic()
    {
        // Markdown document extraction baseline.
        var result = await Kreuzberg.ExtractFile("markdown/comprehensive.md", null, null);
        Assert.Equal("text/markdown", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeNumbersBasic()
    {
        // Numbers document extraction baseline.
        var result = await Kreuzberg.ExtractFile("iwork/test.numbers", null, null);
        Assert.Equal("application/x-iwork-numbers-sffnumbers", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeOdsBasic()
    {
        // Basic ODS spreadsheet extraction.
        var result = await Kreuzberg.ExtractFile("data_formats/test_01.ods", null, null);
        Assert.Equal("application/vnd.oasis.opendocument.spreadsheet", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeOdtBold()
    {
        // ODT document with bold formatting.
        var result = await Kreuzberg.ExtractFile("odt/bold.odt", null, null);
        Assert.Equal("application/vnd.oasis.opendocument.text", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeOdtList()
    {
        // ODT document containing unordered lists with nesting.
        var result = await Kreuzberg.ExtractFile("odt/unorderedList.odt", null, null);
        Assert.Equal("application/vnd.oasis.opendocument.text", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 30, "expected length >= 30");
        Assert.True(result.Content.ToString().Contains("list item") || result.Content.ToString().Contains("New level") || result.Content.ToString().Contains("Pushed us"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_OfficeOdtSimple()
    {
        // Basic ODT document with paragraphs and headings.
        var result = await Kreuzberg.ExtractFile("odt/simple.odt", null, null);
        Assert.Equal("application/vnd.oasis.opendocument.text", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 50, "expected length >= 50");
        Assert.True(result.Content.ToString().Contains("favorite things") || result.Content.ToString().Contains("Parrots") || result.Content.ToString().Contains("Analysis"), "expected to contain at least one of the specified values");
    }

    [Fact]
    public async Task Test_OfficeOdtTable()
    {
        // ODT document with a table structure.
        var result = await Kreuzberg.ExtractFile("odt/table.odt", null, null);
        Assert.Equal("application/vnd.oasis.opendocument.text", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
        Assert.True(result.Tables.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_OfficeOpmlBasic()
    {
        // OPML outline document extraction.
        var result = await Kreuzberg.ExtractFile("opml/outline.opml", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/xml+opml") || result.MimeType.ToString().Contains("text/x-opml") || result.MimeType.ToString().Contains("application/x-opml+xml"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeOrgBasic()
    {
        // Org-mode document text extraction.
        var result = await Kreuzberg.ExtractFile("org/comprehensive.org", null, null);
        Assert.True(result.MimeType.ToString().Contains("text/x-org") || result.MimeType.ToString().Contains("text/org"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficePagesBasic()
    {
        // Pages document extraction baseline.
        var result = await Kreuzberg.ExtractFile("iwork/test.pages", null, null);
        Assert.Equal("application/x-iwork-pages-sffpages", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 5, "expected length >= 5");
    }

    [Fact]
    public async Task Test_OfficePpsxSlideshow()
    {
        // PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
        var result = await Kreuzberg.ExtractFile("pptx/sample.ppsx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.presentationml.slideshow", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficePptmBasic()
    {
        // PowerPoint macro-enabled presentation (.pptm) extraction.
        var result = await Kreuzberg.ExtractFile("pptx/powerpoint_with_image.pptm", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/vnd.ms-powerpoint.presentation.macroEnabled.12") || result.MimeType.ToString().Contains("application/vnd.openxmlformats-officedocument.presentationml.presentation"), "expected to contain at least one of the specified values");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_OfficePptxBasic()
    {
        // PPTX deck should extract slides content.
        var result = await Kreuzberg.ExtractFile("pptx/simple.pptx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 50, "expected length >= 50");
    }

    [Fact]
    public async Task Test_OfficePptxImages()
    {
        // PPTX presentation containing images to ensure metadata extraction.
        var result = await Kreuzberg.ExtractFile("pptx/powerpoint_with_image.pptx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 15, "expected length >= 15");
    }

    [Fact]
    public async Task Test_OfficePptxPitchDeck()
    {
        // Pitch deck PPTX used to validate large slide extraction.
        var result = await Kreuzberg.ExtractFile("pptx/pitch_deck_presentation.pptx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 100, "expected length >= 100");
    }

    [Fact]
    public async Task Test_OfficeRstBasic()
    {
        // reStructuredText document extraction.
        var result = await Kreuzberg.ExtractFile("rst/restructured_text.rst", null, null);
        Assert.True(result.MimeType.ToString().Contains("text/x-rst") || result.MimeType.ToString().Contains("text/prs.fallenstein.rst"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
    }

    [Fact]
    public async Task Test_OfficeRtfBasic()
    {
        // RTF document text extraction.
        var result = await Kreuzberg.ExtractFile("rtf/extraction_test.rtf", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/rtf") || result.MimeType.ToString().Contains("text/rtf"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeTypstBasic()
    {
        // Typst document text extraction.
        var result = await Kreuzberg.ExtractFile("typst/headings.typ", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/x-typst") || result.MimeType.ToString().Contains("text/x-typst"), "expected to contain at least one of the specified values");
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeXlsLegacy()
    {
        // Legacy XLS spreadsheet to ensure backward compatibility.
        var result = await Kreuzberg.ExtractFile("xls/test_excel.xls", null, null);
        Assert.Equal("application/vnd.ms-excel", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }

    [Fact]
    public async Task Test_OfficeXlsbBasic()
    {
        // Excel binary workbook (.xlsb) extraction.
        var result = await Kreuzberg.ExtractFile("xlsx/test_xlsb.xlsb", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/vnd.ms-excel.sheet.binary.macroEnabled.12") || result.MimeType.ToString().Contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_OfficeXlsmBasic()
    {
        // Excel macro-enabled workbook (.xlsm) extraction.
        var result = await Kreuzberg.ExtractFile("xlsx/test_01.xlsm", null, null);
        Assert.True(result.MimeType.ToString().Contains("application/vnd.ms-excel.sheet.macroEnabled.12") || result.MimeType.ToString().Contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values");
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public async Task Test_OfficeXlsxBasic()
    {
        // XLSX spreadsheet should produce metadata and table content.
        var result = await Kreuzberg.ExtractFile("xlsx/stanley_cups.xlsx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 100, "expected length >= 100");
        Assert.Contains("team", result.Content.ToString().ToLower());
        Assert.Contains("location", result.Content.ToString().ToLower());
        Assert.Contains("stanley cups", result.Content.ToString().ToLower());
        Assert.True(result.Tables.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metadata.SheetCount >= 2, "expected >= 2");
        Assert.Contains("stanley cups", result.Metadata.SheetNames.ToString().ToLower());
    }

    [Fact]
    public async Task Test_OfficeXlsxMultiSheet()
    {
        // XLSX workbook with multiple sheets.
        var result = await Kreuzberg.ExtractFile("xlsx/excel_multi_sheet.xlsx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 20, "expected length >= 20");
        Assert.True(result.Metadata.SheetCount >= 2, "expected >= 2");
    }

    [Fact]
    public async Task Test_OfficeXlsxOfficeExample()
    {
        // Simple XLSX spreadsheet shipped alongside office integration tests.
        var result = await Kreuzberg.ExtractFile("xlsx/test_01.xlsx", null, null);
        Assert.Equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.MimeType.Trim());
        Assert.True(result.Content.Length >= 10, "expected length >= 10");
    }
}
