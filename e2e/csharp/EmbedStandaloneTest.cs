// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang csharp
using Kreuzberg;
using Xunit;
using System.Collections.Generic;
using System.Runtime.InteropServices;

namespace Kreuzberg.E2E;

public class EmbedStandaloneTest
{
    [Fact]
    public async Task Embed_async()
    {
        if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows) && RuntimeInformation.ProcessArchitecture == Architecture.X64)
        {
            // Skip embeddings on Windows X64 until ONNX is implemented
            return;
        }
        IEnumerable<float[]> results;
        try
        {
            results = await KreuzbergClient.EmbedAsync(new[] { "Async embedding test" }, new EmbeddingConfig { Model = "balanced", Normalize = true });
        }
        catch (Exception)
        {
            // Skip if embeddings are not supported or requirements missing
            return;
        }
        TestHelpers.AssertEmbedResult(results, 1, 768, true, true, true);
    }

    [Fact]
    public async Task Embed_batch_texts()
    {
        if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows) && RuntimeInformation.ProcessArchitecture == Architecture.X64)
        {
            // Skip embeddings on Windows X64 until ONNX is implemented
            return;
        }
        IEnumerable<float[]> results;
        try
        {
            results = KreuzbergClient.EmbedSync(new[] { "Text one", "Text two", "Text three" }, new EmbeddingConfig { Model = "fast", Normalize = true, BatchSize = 32 });
        }
        catch (Exception)
        {
            // Skip if embeddings are not supported or requirements missing
            return;
        }
        TestHelpers.AssertEmbedResult(results, 3, 384, true, true, true);
    }

    [Fact]
    public async Task Embed_empty_input()
    {
        if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows) && RuntimeInformation.ProcessArchitecture == Architecture.X64)
        {
            // Skip embeddings on Windows X64 until ONNX is implemented
            return;
        }
        IEnumerable<float[]> results;
        try
        {
            results = KreuzbergClient.EmbedSync(new string[] { }, new EmbeddingConfig { Model = "balanced" });
        }
        catch (Exception)
        {
            // Skip if embeddings are not supported or requirements missing
            return;
        }
        TestHelpers.AssertEmbedResult(results, 0, -1, true, true, false);
    }

    [Fact]
    public async Task Embed_multilingual()
    {
        if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows) && RuntimeInformation.ProcessArchitecture == Architecture.X64)
        {
            // Skip embeddings on Windows X64 until ONNX is implemented
            return;
        }
        IEnumerable<float[]> results;
        try
        {
            results = KreuzbergClient.EmbedSync(new[] { "Bonjour le monde", "Hallo Welt", "¡Hola Mundo!" }, new EmbeddingConfig { Model = "multilingual", Normalize = true });
        }
        catch (Exception)
        {
            // Skip if embeddings are not supported or requirements missing
            return;
        }
        TestHelpers.AssertEmbedResult(results, 3, 768, true, true, true);
    }

    [Fact]
    public async Task Embed_single_text()
    {
        if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows) && RuntimeInformation.ProcessArchitecture == Architecture.X64)
        {
            // Skip embeddings on Windows X64 until ONNX is implemented
            return;
        }
        IEnumerable<float[]> results;
        try
        {
            results = KreuzbergClient.EmbedSync(new[] { "Hello, Kreuzberg!" }, new EmbeddingConfig { Model = "balanced", Normalize = true });
        }
        catch (Exception)
        {
            // Skip if embeddings are not supported or requirements missing
            return;
        }
        TestHelpers.AssertEmbedResult(results, 1, 768, true, true, true);
    }

}
