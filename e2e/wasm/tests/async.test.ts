// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5bcc7c0c9c6aefab3b2b8c703b9340ac5518e7416202175fec8d8e3eac31abf6
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import init, { extractFile, extractBytes, type WasmExtractionConfig } from "kreuzberg";
await init(
  (await import("node:fs")).readFileSync(
    (await import("node:module"))
      .createRequire(import.meta.url)
      .resolve("kreuzberg")
      .replace(/\.js$/, "_bg.wasm"),
  ),
);

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("async", () => {
  it("async_extract_bytes: Async extract_bytes call on PDF document", async () => {
    const _data_content = await (await import("node:fs/promises")).readFile("pdf/fake_memo.pdf");
    const result = await extractBytes(_data_content, "application/pdf");
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
  });

  it("async_extract_bytes_empty_mime: extract_bytes empty MIME async", async () => {
    await expect(async () => {
      const _data_content = await (await import("node:fs/promises")).readFile("text/plain.txt");
      await extractBytes(_data_content, "", {} as WasmExtractionConfig);
    }).rejects.toThrow();
  });

  it("async_extract_bytes_invalid_mime: extract_bytes unsupported MIME async", async () => {
    await expect(async () => {
      const _data_content = await (await import("node:fs/promises")).readFile("text/plain.txt");
      await extractBytes(_data_content, "application/x-nonexistent", {} as WasmExtractionConfig);
    }).rejects.toThrow();
  });
});
