// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extract_file } from 'kreuzberg';

describe('rendering', () => {
  it('convert_html_basic: Convert basic HTML to Markdown format', async () => {
    const result = await extract_file('');
    expect(result.content).toContain("Title");
  });

  it('convert_html_complex: Convert complex HTML to markdown', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('convert_html_empty: Convert empty HTML to markdown', async () => {
    const result = await extract_file('');
  });

  it('convert_html_table: Convert HTML table to Markdown table format', async () => {
    const result = await extract_file('');
    expect(result.content).toContain("|");
  });

  it('djot_to_html_basic: Convert Djot format to HTML', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('djot_to_html_complex: Complex djot to HTML', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('djot_to_html_empty: Convert empty djot to HTML', async () => {
    const result = await extract_file('');
  });

  it('render_djot_basic: Render HTML document to Djot format', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_djot_formatted: Render HTML to djot', async () => {
    const result = await extract_file('');
  });

  it('render_html_basic: Render a simple HTML document to formatted output', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_html_styled: Render HTML preserving structure', async () => {
    const result = await extract_file('');
    expect(result.content).toContain("Heading");
  });

  it('render_json_basic: Render HTML document to JSON format', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_json_complex: Render HTML to JSON', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_markdown_basic: Render HTML document to Markdown format', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_markdown_complex: Render complex HTML to markdown', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_plain_basic: Render HTML document to plain text format', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('render_plain_empty: Render empty HTML', async () => {
    const result = await extract_file('');
  });
});
