// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:49042f8e7e238887ec5b93e40fc7ae69da8e400b65c3e488d696446435dafdbe
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, , type WasmExtractionConfig } from 'kreuzberg';

describe('rendering', () => {
  it('convert_html_basic: Convert basic HTML to Markdown format', async () => {
    const result = await extractFile(null, undefined, {} as unknown as WasmExtractionConfig);
    expect(result.content).toContain("Title");
  });

  it('convert_html_complex: Convert complex HTML to markdown', async () => {
    const result = await extractFile(null, undefined, {} as unknown as WasmExtractionConfig);
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it('convert_html_empty: Convert empty HTML to markdown', async () => {
    await extractFile(null, undefined, {} as unknown as WasmExtractionConfig);
  });

  it('convert_html_table: Convert HTML table to Markdown table format', async () => {
    const result = await extractFile(null, undefined, {} as unknown as WasmExtractionConfig);
    expect(result.content).toContain("|");
  });

  it('djot_to_html_basic: Convert Djot format to HTML', () => {
    const result = ({ djot: "# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2" });
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('djot_to_html_complex: Complex djot to HTML', () => {
    const result = ({ djot: "# Heading\n\nParagraph.\n\n- Item one\n- Item two" });
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('djot_to_html_empty: Convert empty djot to HTML', () => {
    ({ djot: "" });
  });

  it('render_djot_basic: Render HTML document to Djot format', () => {
    const result = ({ doc: "<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>" });
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_djot_formatted: Render HTML to djot', () => {
    ({ doc: "<h2>Section</h2><p>Text with <code>code</code>.</p>" });
  });

  it('render_html_basic: Render a simple HTML document to formatted output', () => {
    const result = ({ doc: "<h1>Test Document</h1><p>This is a paragraph with some test content.</p>" });
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_html_styled: Render HTML preserving structure', () => {
    const result = ({ doc: "<h1>Heading</h1><p><strong>bold</strong></p>" });
    expect(result.content).toContain("Heading");
  });

  it('render_json_basic: Render HTML document to JSON format', () => {
    const result = ({ doc: "<h1>Structured Data</h1><p>Content for JSON rendering.</p>" });
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_json_complex: Render HTML to JSON', () => {
    const result = ({ doc: "<h1>Title</h1><p>Text.</p>" });
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_markdown_basic: Render HTML document to Markdown format', () => {
    const result = ({ doc: "<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>" });
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_markdown_complex: Render complex HTML to markdown', () => {
    const result = ({ doc: "<h1>Title</h1><ul><li>A</li><li>B</li></ul>" });
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('render_plain_basic: Render HTML document to plain text format', () => {
    const result = ({ doc: "<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>" });
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_plain_empty: Render empty HTML', () => {
    ({ doc: "" });
  });
});
