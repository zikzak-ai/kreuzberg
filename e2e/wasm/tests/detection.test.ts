// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f39dfd4b9c796364e641d8565f82275f79955e511f92228d60ef26d617f88b75
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile, detectMimeTypeFromBytes, getExtensionsForMime, initSync } from "kreuzberg";
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";

const __filename = fileURLToPath(import.meta.url);
const wasmPath = path.join(path.dirname(__filename), "../node_modules/kreuzberg/kreuzberg_bg.wasm");
const wasmBuffer = fs.readFileSync(wasmPath);
initSync({ module: wasmBuffer });

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("detection", () => {
  it("detect_mime_bytes_html: Detect HTML MIME from bytes", () => {
    detectMimeTypeFromBytes("html/html.html");
  });

  it("detect_mime_bytes_pdf: Detect PDF MIME type from bytes", () => {
    detectMimeTypeFromBytes("pdf/fake_memo.pdf");
  });

  it("detect_mime_bytes_png: Detect PNG MIME type from bytes", () => {
    detectMimeTypeFromBytes("images/test_hello_world.png");
  });

  it("get_extensions_unknown_mime: get_extensions unknown MIME", async () => {
    await expect(async () => {
      await getExtensionsForMime("application/x-totally-unknown");
    }).rejects.toThrow();
  });
});
