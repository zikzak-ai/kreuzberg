// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extract_file, JsExtractionConfig } from 'kreuzberg';

describe('token_reduction', () => {
  it('token_reduction_aggressive: Tests aggressive token reduction mode significantly reduces content', async () => {
    const options = new JsExtractionConfig();
    options.tokenReduction = { mode: "aggressive" };
    const result = await extract_file("pdf/fake_memo.pdf", options);
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    // TODO: unsupported assertion type: max_length
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_basic: Tests basic token reduction on PDF document', async () => {
    const options = new JsExtractionConfig();
    options.tokenReduction = { mode: "moderate" };
    const result = await extract_file("pdf/fake_memo.pdf", options);
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    // TODO: unsupported assertion type: max_length
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_light: Tests light token reduction mode preserves most content', async () => {
    const options = new JsExtractionConfig();
    options.tokenReduction = { mode: "light" };
    const result = await extract_file("pdf/fake_memo.pdf", options);
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_reduce_aggressive: Reduce tokens with aggressive compression level', async () => {
    const result = await extract_file('');
    // TODO: unsupported assertion type: min_length
  });

  it('token_reduction_reduce_basic: Reduce tokens with light compression level', async () => {
    const result = await extract_file('');
    expect(result.result).toContain("quick");
  });

  it('token_reduction_with_chunking: Tests token reduction combined with chunking', async () => {
    const options = new JsExtractionConfig();
    options.chunking = { max_chars: 500, max_overlap: 50 };
    options.tokenReduction = { mode: "moderate" };
    const result = await extract_file("pdf/fake_memo.pdf", options);
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    // TODO: unsupported assertion type: max_length
    expect(result.content.length).toBeGreaterThan(0);
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    expect(result.chunksHaveContent).toBe(true);
  });
});
