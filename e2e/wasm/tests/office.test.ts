// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:584ace6533ce0d4ca2fb30d9c103e1384b8911f6640cca060347ea03bdfed994
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile, type WasmExtractionConfig } from "kreuzberg";

describe("office", () => {
  it("office_bibtex_basic: BibTeX bibliography extraction.", async () => {
    const result = await extractFile(
      "bibtex/comprehensive.bib",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["application/x-bibtex", "text/x-bibtex"].some((v) => result.mimeType.includes(v))).toBe(
      true,
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_commonmark_basic: CommonMark (.commonmark) text extraction.", async () => {
    const result = await extractFile(
      "markdown/sample.commonmark",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      ["text/markdown", "text/plain", "text/x-commonmark"].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it("office_djot_basic: Djot markup text extraction.", async () => {
    const result = await extractFile(
      "markdown/tables.djot",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["text/x-djot", "text/djot"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_docbook_basic: DocBook XML document extraction.", async () => {
    const result = await extractFile(
      "docbook/docbook-reader.docbook",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      ["application/docbook+xml", "text/docbook"].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_docx_basic: DOCX document extraction baseline.", async () => {
    const result = await extractFile(
      "docx/sample_document.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_docx_equations: DOCX file containing equations to validate math extraction.", async () => {
    const result = await extractFile(
      "docx/equations.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_docx_fake: Simple DOCX document to verify baseline extraction.", async () => {
    const result = await extractFile(
      "docx/fake.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_docx_formatting: DOCX document heavy on formatting for style preservation.", async () => {
    const result = await extractFile(
      "docx/unit_test_formatting.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_docx_headers: DOCX document with complex headers.", async () => {
    const result = await extractFile(
      "docx/unit_test_headers.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_docx_lists: DOCX document emphasizing list formatting.", async () => {
    const result = await extractFile(
      "docx/unit_test_lists.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_docx_tables: DOCX document containing tables for table-aware extraction.", async () => {
    const result = await extractFile(
      "docx/docx_tables.docx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(50);
    expect(result.content).toContain("Simple uniform table");
    expect(result.content).toContain("Nested Table");
    expect(result.content).toContain("merged cells");
    expect(result.content).toContain("Header Col");
    expect(result.tables.length).toBeGreaterThanOrEqual(1);
  });

  it("office_epub_basic: EPUB book extraction with text content.", async () => {
    const result = await extractFile(
      "epub/features.epub",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/epub+zip");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
  });

  it("office_fb2_basic: FictionBook (FB2) document extraction baseline.", async () => {
    const result = await extractFile(
      "fictionbook/basic.fb2",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/x-fictionbook+xml");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_fictionbook_basic: FictionBook (.fb2) text extraction.", async () => {
    const result = await extractFile(
      "fictionbook/basic.fb2",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      ["application/x-fictionbook+xml", "application/x-fictionbook"].some((v) =>
        result.mimeType.includes(v),
      ),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_jats_basic: JATS scientific article extraction.", async () => {
    const result = await extractFile(
      "jats/sample_article.jats",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["application/x-jats+xml", "text/jats"].some((v) => result.mimeType.includes(v))).toBe(
      true,
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_jupyter_basic: Jupyter notebook extraction.", async () => {
    const result = await extractFile(
      "jupyter/rank.ipynb",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/x-ipynb+json");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_keynote_basic: Keynote document extraction baseline.", async () => {
    const result = await extractFile(
      "iwork/test.key",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/x-iwork-keynote-sffkey");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it("office_latex_basic: LaTeX document text extraction.", async () => {
    const result = await extractFile(
      "latex/basic_sections.tex",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["application/x-latex", "text/x-latex"].some((v) => result.mimeType.includes(v))).toBe(
      true,
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_markdown_basic: Markdown document extraction baseline.", async () => {
    const result = await extractFile(
      "markdown/comprehensive.md",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("text/markdown");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_numbers_basic: Numbers document extraction baseline.", async () => {
    const result = await extractFile(
      "iwork/test.numbers",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/x-iwork-numbers-sffnumbers");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_ods_basic: Basic ODS spreadsheet extraction.", async () => {
    const result = await extractFile(
      "data_formats/test_01.ods",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/vnd.oasis.opendocument.spreadsheet");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_odt_bold: ODT document with bold formatting.", async () => {
    const result = await extractFile(
      "odt/bold.odt",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/vnd.oasis.opendocument.text");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_odt_list: ODT document containing unordered lists with nesting.", async () => {
    const result = await extractFile(
      "odt/unorderedList.odt",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/vnd.oasis.opendocument.text");
    expect(result.content.length).toBeGreaterThanOrEqual(30);
    expect(["list item", "New level", "Pushed us"].some((v) => result.content.includes(v))).toBe(
      true,
    );
  });

  it("office_odt_simple: Basic ODT document with paragraphs and headings.", async () => {
    const result = await extractFile(
      "odt/simple.odt",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/vnd.oasis.opendocument.text");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
    expect(["favorite things", "Parrots", "Analysis"].some((v) => result.content.includes(v))).toBe(
      true,
    );
  });

  it("office_odt_table: ODT document with a table structure.", async () => {
    const result = await extractFile(
      "odt/table.odt",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/vnd.oasis.opendocument.text");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.tables.length).toBeGreaterThanOrEqual(1);
  });

  it("office_opml_basic: OPML outline document extraction.", async () => {
    const result = await extractFile(
      "opml/outline.opml",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      ["application/xml+opml", "text/x-opml", "application/x-opml+xml"].some((v) =>
        result.mimeType.includes(v),
      ),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_org_basic: Org-mode document text extraction.", async () => {
    const result = await extractFile(
      "org/comprehensive.org",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["text/x-org", "text/org"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_pages_basic: Pages document extraction baseline.", async () => {
    const result = await extractFile(
      "iwork/test.pages",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/x-iwork-pages-sffpages");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it("office_ppsx_slideshow: PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.", async () => {
    const result = await extractFile(
      "pptx/sample.ppsx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.presentationml.slideshow",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_pptm_basic: PowerPoint macro-enabled presentation (.pptm) extraction.", async () => {
    const result = await extractFile(
      "pptx/powerpoint_with_image.pptm",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      [
        "application/vnd.ms-powerpoint.presentation.macroEnabled.12",
        "application/vnd.openxmlformats-officedocument.presentationml.presentation",
      ].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(
      (() => {
        const v = result.content as unknown;
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("office_pptx_basic: PPTX deck should extract slides content.", async () => {
    const result = await extractFile(
      "pptx/simple.pptx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(50);
  });

  it("office_pptx_images: PPTX presentation containing images to ensure metadata extraction.", async () => {
    const result = await extractFile(
      "pptx/powerpoint_with_image.pptx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(15);
  });

  it("office_pptx_pitch_deck: Pitch deck PPTX used to validate large slide extraction.", async () => {
    const result = await extractFile(
      "pptx/pitch_deck_presentation.pptx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(100);
  });

  it("office_rst_basic: reStructuredText document extraction.", async () => {
    const result = await extractFile(
      "rst/restructured_text.rst",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      ["text/x-rst", "text/prs.fallenstein.rst"].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("office_rtf_basic: RTF document text extraction.", async () => {
    const result = await extractFile(
      "rtf/extraction_test.rtf",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["application/rtf", "text/rtf"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_typst_basic: Typst document text extraction.", async () => {
    const result = await extractFile(
      "typst/headings.typ",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(["application/x-typst", "text/x-typst"].some((v) => result.mimeType.includes(v))).toBe(
      true,
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_xls_legacy: Legacy XLS spreadsheet to ensure backward compatibility.", async () => {
    const result = await extractFile(
      "xls/test_excel.xls",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe("application/vnd.ms-excel");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("office_xlsb_basic: Excel binary workbook (.xlsb) extraction.", async () => {
    const result = await extractFile(
      "xlsx/test_xlsb.xlsb",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      [
        "application/vnd.ms-excel.sheet.binary.macroEnabled.12",
        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
      ].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(
      (() => {
        const v = result.content as unknown;
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("office_xlsm_basic: Excel macro-enabled workbook (.xlsm) extraction.", async () => {
    const result = await extractFile(
      "xlsx/test_01.xlsm",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(
      [
        "application/vnd.ms-excel.sheet.macroEnabled.12",
        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
      ].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(
      (() => {
        const v = result.content as unknown;
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("office_xlsx_basic: XLSX spreadsheet should produce metadata and table content.", async () => {
    const result = await extractFile(
      "xlsx/stanley_cups.xlsx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(100);
    expect(result.content).toContain("Team");
    expect(result.content).toContain("Location");
    expect(result.content).toContain("Stanley Cups");
    expect(result.tables.length).toBeGreaterThanOrEqual(1);
    expect(result.metadata.sheetCount).toBeGreaterThanOrEqual(2);
    expect(result.metadata.sheetNames).toContain("Stanley Cups");
  });

  it("office_xlsx_multi_sheet: XLSX workbook with multiple sheets.", async () => {
    const result = await extractFile(
      "xlsx/excel_multi_sheet.xlsx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
    expect(result.metadata.sheetCount).toBeGreaterThanOrEqual(2);
  });

  it("office_xlsx_office_example: Simple XLSX spreadsheet shipped alongside office integration tests.", async () => {
    const result = await extractFile(
      "xlsx/test_01.xlsx",
      undefined,
      {} as unknown as WasmExtractionConfig,
    );
    expect(result.mimeType.trim()).toBe(
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });
});
