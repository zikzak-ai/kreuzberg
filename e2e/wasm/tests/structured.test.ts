// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ffa1c979e0a09a6070d6e7e0fb43ea0b1cc95a211a9a0e426c6196573b266887
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file } from 'kreuzberg';

describe('structured', () => {
  it('structured_csv_basic: CSV data file extraction.', async () => {
    const result = await extract_file("csv/stanley_cups.csv");
    expect(result.mimeType.trim()).toBe("text/csv");
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it('structured_enw_basic: EndNote ENW citation format extraction.', async () => {
    const result = await extract_file("data_formats/sample.enw");
    expect(["application/x-endnote-refer", "application/x-endnote+xml", "text/plain"].some((v) => result.mimeType.includes(v))).toBe(true);
  });

  it('structured_json_basic: Structured JSON extraction should stream and preserve content.', async () => {
    const result = await extract_file("json/sample_document.json");
    expect(result.mimeType.trim()).toBe("application/json");
    expect(result.content.length).toBeGreaterThanOrEqual(20);
    expect(["Sample Document", "Test Author"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('structured_json_simple: Simple JSON document to verify structured extraction.', async () => {
    const result = await extract_file("json/simple.json");
    expect(result.mimeType.trim()).toBe("application/json");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["{", "name"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('structured_nbib_basic: PubMed NBIB citation format extraction.', async () => {
    const result = await extract_file("data_formats/sample.nbib");
    expect(["application/nbib", "application/x-pubmed", "text/plain"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('structured_ris_basic: RIS citation format extraction.', async () => {
    const result = await extract_file("data_formats/sample.ris");
    expect(["application/x-research-info-systems", "text/plain"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('structured_toml_basic: TOML configuration file extraction.', async () => {
    const result = await extract_file("data_formats/cargo.toml");
    expect(["application/toml", "text/toml"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('structured_tsv_basic: TSV (tab-separated values) data file extraction.', async () => {
    const result = await extract_file("data_formats/employees.tsv");
    expect(["text/tab-separated-values", "text/plain"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('structured_yaml_basic: YAML file text extraction.', async () => {
    const result = await extract_file("yaml/simple.yaml");
    expect(["application/yaml", "text/yaml", "text/x-yaml", "application/x-yaml"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('structured_yaml_simple: Simple YAML document to validate structured extraction.', async () => {
    const result = await extract_file("yaml/simple.yaml");
    expect(result.mimeType.trim()).toBe("application/x-yaml");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });
});
