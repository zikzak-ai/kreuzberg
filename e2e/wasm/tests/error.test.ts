// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b49c9a20b5c4bd6da9fdf109297c3459337920816ee3ecb2b487821077bfcedd
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file, extract_bytes_sync, extract_file_sync, render_page, JsExtractionConfig } from 'kreuzberg';

describe('error', () => {
  it('error_empty_bytes: Graceful handling of empty bytes (should not error)', () => {
    extract_bytes_sync("text/empty.txt", "text/plain");
  });

  it('error_empty_mime: Error when extracting with empty MIME type', () => {
    expect(() => extract_bytes_sync("text/plain.txt", "")).toThrow();
  });

  it('error_extract_bytes_conflicting_ocr: extract_bytes force+disable OCR', () => {
    const options = new JsExtractionConfig();
    options.disableOcr = true;
    options.forceOcr = true;
    expect(() => extract_bytes_sync("SGVsbG8=", "text/plain", options)).toThrow();
  });

  it('error_file_not_found: Error when file does not exist (sync)', () => {
    expect(() => extract_file_sync("nonexistent/file.pdf")).toThrow();
  });

  it('error_file_not_found_async: Error when file does not exist (async)', async () => {
    await expect(async () => await extract_file("nonexistent/file.pdf")).rejects.toThrow();
  });

  it('error_invalid_chunk_overlap: Error when chunk overlap exceeds max_characters', () => {
    const options = new JsExtractionConfig();
    options.chunking = { max_characters: 10, overlap_characters: 100 };
    expect(() => extract_file_sync("pdf/fake_memo.pdf", options)).toThrow();
  });

  it('error_invalid_confidence: Error when OCR confidence threshold is out of range', () => {
    const options = new JsExtractionConfig();
    options.ocr = { confidence_threshold: 2.0 };
    expect(() => extract_file_sync("pdf/fake_memo.pdf", options)).toThrow();
  });

  it('error_invalid_dpi: Error when OCR DPI is invalid (negative)', () => {
    const options = new JsExtractionConfig();
    options.ocr = { dpi: -1 };
    expect(() => extract_file_sync("pdf/fake_memo.pdf", options)).toThrow();
  });

  it('error_invalid_mime_format: Error when extracting with invalid MIME type format', () => {
    expect(() => extract_bytes_sync("text/plain.txt", "not-a-mime")).toThrow();
  });

  it('error_invalid_ocr_language: Error when OCR language pack is not available', () => {
    const options = new JsExtractionConfig();
    options.ocr = { force_ocr: true, language: "zzz_nonexistent" };
    expect(() => extract_file_sync("images/balance_sheet_1.png", options)).toThrow();
  });

  it('error_pdf_corrupt: Error when PDF is truncated/corrupt', () => {
    expect(() => extract_file_sync("pdf/corrupt_truncated.pdf")).toThrow();
  });

  it('error_pdf_needs_password: Error when password-protected PDF is accessed without password', () => {
    expect(() => extract_file_sync("pdf/password_protected.pdf")).toThrow();
  });

  it('error_pdf_page_out_of_range: Graceful handling of out-of-range page extraction', () => {
    const options = new JsExtractionConfig();
    options.pages = "999-1000";
    extract_file_sync("pdf/fake_memo.pdf", options);
  });

  it('error_pdf_wrong_password: Error when PDF password is incorrect', () => {
    const options = new JsExtractionConfig();
    options.pdf = { password: "wrongpassword" };
    expect(() => extract_file_sync("pdf/password_protected.pdf", options)).toThrow();
  });

  it('error_render_corrupt_pdf: Error when rendering page from corrupt PDF', () => {
    expect(() => render_page("pdf/corrupt_truncated.pdf", 0)).toThrow();
  });

  it('error_render_invalid_page: Error when rendering page number exceeds document page count', () => {
    expect(() => render_page("pdf/fake_memo.pdf", 999)).toThrow();
  });

  it('error_security_archive_files: Error when archive file count exceeds security limit', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_files_in_archive: 0 };
    expect(() => extract_file_sync("archives/documents.zip", options)).toThrow();
  });

  it('error_security_archive_size: Error when archive size exceeds security limit', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_archive_size: 1 };
    expect(() => extract_file_sync("archives/documents.zip", options)).toThrow();
  });

  it('error_security_content_too_large: Error when content exceeds security limit for max size', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_content_size: 10 };
    expect(() => extract_file_sync("pdf/fake_memo.pdf", options)).toThrow();
  });

  it('error_security_entity_too_long: Error when an XML attribute / entity expansion exceeds max_entity_length (EntityValidator fires). Real DOCX attributes routinely exceed a 2-byte cap, so this trips on any content.', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_entity_length: 2 };
    expect(() => extract_file_sync("docx/lorem_ipsum.docx", options)).toThrow();
  });

  it('error_security_nesting_depth: Error when XML nesting depth exceeds security limit', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_nesting_depth: 1 };
    expect(() => extract_file_sync("html/complex_table.html", options)).toThrow();
  });

  it('error_security_nesting_too_deep: Error when XML/DOCX nesting depth exceeds max_xml_depth (DepthValidator fires)', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_xml_depth: 1 };
    expect(() => extract_file_sync("docx/lorem_ipsum.docx", options)).toThrow();
  });

  it('error_security_too_many_cells: Error when cumulative table cell count exceeds max_table_cells (TableValidator fires). The Stanley Cups spreadsheet has more than 1 cell, so any tabular extraction trips this dial-down.', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_table_cells: 1 };
    expect(() => extract_file_sync("xlsx/stanley_cups.xlsx", options)).toThrow();
  });

  it('error_security_too_many_iterations: Error when extractor parser-loop iteration count exceeds max_iterations (IterationValidator fires)', () => {
    const options = new JsExtractionConfig();
    options.securityLimits = { max_iterations: 1 };
    expect(() => extract_file_sync("docx/lorem_ipsum.docx", options)).toThrow();
  });

  it('error_timeout_extraction: Error when extraction times out', () => {
    const options = new JsExtractionConfig();
    options.extractionTimeoutSecs = 1;
    expect(() => extract_file_sync("pdf/large.pdf", options)).toThrow();
  });

  it('error_unsupported_mime: Error when extracting with unsupported MIME type', () => {
    expect(() => extract_bytes_sync("text/plain.txt", "application/x-nonexistent")).toThrow();
  });
});
