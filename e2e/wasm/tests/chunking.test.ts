// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f2d39978379ed1c3a1a8106dce0bb70b5f48039033079f6510a5efb09434f7f7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file, chunk_texts_batch, chunk_text, JsExtractionConfig } from 'kreuzberg';

describe('chunking', () => {
  it('chunk_batch_basic: Batch chunking of multiple texts in a single call', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 100;
    options.maxOverlap = 10;
    chunk_texts_batch(["Hello world. This is text one.", "Goodbye world. This is text two."], options);
  });

  it('chunk_text_basic: Basic text chunking with small chunk size and overlap', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 50;
    options.maxOverlap = 10;
    chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", options);
  });

  it('chunk_text_large: Text chunking with large chunk size to test handling of long content', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 1000;
    options.maxOverlap = 10;
    chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", options);
  });

  it('chunk_text_no_overlap: Text chunking with no overlap between chunks', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 50;
    options.maxOverlap = 0;
    chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", options);
  });

  it('chunk_text_overlap_exceeds: chunk_text overlap >= max', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 50;
    options.overlap = 60;
    expect(() => chunk_text("Some text.", options)).toThrow();
  });

  it('chunk_text_yaml: Chunk with YAML chunker', () => {
    const options = new JsExtractionConfig();
    options.chunkerType = "yaml";
    options.maxCharacters = 50;
    chunk_text("key1: value1\nkey2: value2\n---\nkey3: value3", options);
  });

  it('chunk_text_zero_max: chunk_text max_characters zero', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 0;
    expect(() => chunk_text("Some text.", options)).toThrow();
  });

  it('chunk_texts_batch_multiple: Batch chunk multiple texts', () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 100;
    chunk_texts_batch(["First doc text.", "Second doc text."], options);
  });
});
