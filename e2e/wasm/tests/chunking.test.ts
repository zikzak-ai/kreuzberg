// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extract_file, JsExtractionConfig } from 'kreuzberg';

describe('chunking', () => {
  it('chunk_batch_basic: Batch chunking of multiple texts in a single call', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 100;
    options.maxOverlap = 10;
    const result = await extract_file('', options);
  });

  it('chunk_text_basic: Basic text chunking with small chunk size and overlap', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 50;
    options.maxOverlap = 10;
    const result = await extract_file('', options);
  });

  it('chunk_text_large: Text chunking with large chunk size to test handling of long content', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 1000;
    options.maxOverlap = 10;
    const result = await extract_file('', options);
  });

  it('chunk_text_no_overlap: Text chunking with no overlap between chunks', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 50;
    options.maxOverlap = 0;
    const result = await extract_file('', options);
  });

  it('chunk_text_overlap_exceeds: chunk_text overlap >= max', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 50;
    options.overlap = 60;
    await expect(async () => await extract_file('', options)).rejects.toThrow();
  });

  it('chunk_text_yaml: Chunk with YAML chunker', async () => {
    const options = new JsExtractionConfig();
    options.chunkerType = "yaml";
    options.maxCharacters = 50;
    const result = await extract_file('', options);
  });

  it('chunk_text_zero_max: chunk_text max_characters zero', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 0;
    await expect(async () => await extract_file('', options)).rejects.toThrow();
  });

  it('chunk_texts_batch_multiple: Batch chunk multiple texts', async () => {
    const options = new JsExtractionConfig();
    options.maxCharacters = 100;
    const result = await extract_file('', options);
  });
});
