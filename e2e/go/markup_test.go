// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:169f1f8d6988794fbdd09cb6c71d35a98aa0689ad6874a3b0c706225e323b88d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: markup
package e2e_test

import (
	"strings"
	"testing"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_BoldBasic(t *testing.T) {
	// Create bold markdown formatting
	result, err := kreuzberg.Bold(`important`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `important`) {
		t.Errorf("expected to contain %s, got %v", `important`, result.Result)
	}
}

func Test_BoldEmpty(t *testing.T) {
	// Bold empty string
	_, err := kreuzberg.Bold(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_BoldSpecialChars(t *testing.T) {
	// Bold special chars
	result, err := kreuzberg.Bold(`hello **world** & <tag>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `hello`) {
		t.Errorf("expected to contain %s, got %v", `hello`, result.Result)
	}
}

func Test_CodeBasic(t *testing.T) {
	// Create inline code formatting
	result, err := kreuzberg.Code(`variable`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `variable`) {
		t.Errorf("expected to contain %s, got %v", `variable`, result.Result)
	}
}

func Test_CodeMultiline(t *testing.T) {
	// Code multiline
	_, err := kreuzberg.Code(`line1
line2`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_ItalicBasic(t *testing.T) {
	// Create italic markdown formatting
	result, err := kreuzberg.Italic(`emphasis`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `emphasis`) {
		t.Errorf("expected to contain %s, got %v", `emphasis`, result.Result)
	}
}

func Test_ItalicEmpty(t *testing.T) {
	// Italic empty string
	_, err := kreuzberg.Italic(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_ItalicSpecialChars(t *testing.T) {
	// Italic special chars
	_, err := kreuzberg.Italic(`emphasized *text*`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_LinkBasic(t *testing.T) {
	// Create markdown link with text and URL
	result, err := kreuzberg.Link(`Click here`, `https://example.com`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `Click here`) {
		t.Errorf("expected to contain %s, got %v", `Click here`, result.Result)
	}
	if !strings.Contains(string(result.Result), `example.com`) {
		t.Errorf("expected to contain %s, got %v", `example.com`, result.Result)
	}
}

func Test_LinkEmptyText(t *testing.T) {
	// Create markdown link with empty text
	result, err := kreuzberg.Link(``, `https://example.com`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `example.com`) {
		t.Errorf("expected to contain %s, got %v", `example.com`, result.Result)
	}
}

func Test_LinkSpecialUrl(t *testing.T) {
	// Link with query params
	_, err := kreuzberg.Link(`click`, `https://example.com?q=1&b=2`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_StrikethroughBasic(t *testing.T) {
	// Create strikethrough formatting
	result, err := kreuzberg.Strikethrough(`deleted`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `deleted`) {
		t.Errorf("expected to contain %s, got %v", `deleted`, result.Result)
	}
}

func Test_StrikethroughEmpty(t *testing.T) {
	// Strikethrough empty
	_, err := kreuzberg.Strikethrough(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_UnderlineBasic(t *testing.T) {
	// Create underline formatting
	result, err := kreuzberg.Underline(`underlined`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Result), `underlined`) {
		t.Errorf("expected to contain %s, got %v", `underlined`, result.Result)
	}
}

func Test_UnderlineEmpty(t *testing.T) {
	// Underline empty
	_, err := kreuzberg.Underline(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}
