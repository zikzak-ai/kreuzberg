// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:97d9f8d63fa11083702d47fb4434cd6b8d6cda79164e81e49e9906aecf21162b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: render
package e2e_test

import (
	"encoding/base64"
	"encoding/json"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_RenderCustomDpi(t *testing.T) {
	// Render PDF page with custom DPI
	pdf_bytesBytes, _ := base64.StdEncoding.DecodeString(`pdf/tiny.pdf`)
	result, err := kreuzberg.RenderPdfPageToPng(pdf_bytesBytes, 0)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result == nil {
		t.Fatalf("expected non-nil result")
	}
	value := *result
	if len(value) == 0 {
		t.Errorf("expected non-empty value")
	}
	assert.GreaterOrEqual(t, len(value), 50, "expected at least 50 elements")
}

func Test_RenderHighDpi(t *testing.T) {
	// Render PDF page with high DPI setting (300 DPI)
	pdf_bytesBytes, _ := base64.StdEncoding.DecodeString(`pdf/fake_memo.pdf`)
	_, err := kreuzberg.RenderPdfPageToPng(pdf_bytesBytes, 0)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_RenderIterator(t *testing.T) {
	// Iterate all PDF pages as PNG images
	pdf_bytesBytes, _ := base64.StdEncoding.DecodeString(`pdf/tiny.pdf`)
	result, err := kreuzberg.RenderPdfPageToPng(pdf_bytesBytes, 0)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result == nil {
		t.Fatalf("expected non-nil result")
	}
	value := *result
	if len(value) == 0 {
		t.Errorf("expected non-empty value")
	}
	assert.GreaterOrEqual(t, len(value), 1, "expected at least 1 elements")
}

func Test_RenderMultiPage(t *testing.T) {
	// Render a page from a multi-page PDF document
	pdf_bytesBytes, _ := base64.StdEncoding.DecodeString(`pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf`)
	result, err := kreuzberg.RenderPdfPageToPng(pdf_bytesBytes, 0)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result == nil {
		t.Fatalf("expected non-nil result")
	}
	value := *result
	assert.GreaterOrEqual(t, len(value), 1000, "expected length >= 1000")
}

func Test_RenderOutputDjot(t *testing.T) {
	// Extract PDF with Djot output format
	var config kreuzberg.ExtractionConfig
	if err := json.Unmarshal([]byte(`{"output_format":"djot"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := kreuzberg.ExtractFileSync(`pdf/fake_memo.pdf`, nil, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataOutputFormat string
	if result.Metadata.OutputFormat != nil {
		metadataOutputFormat = *result.Metadata.OutputFormat
	}
	if strings.TrimSpace(metadataOutputFormat) != `djot` {
		t.Errorf("equals mismatch: got %v", metadataOutputFormat)
	}
	assert.GreaterOrEqual(t, len(result.Content), 10, "expected length >= 10")
}

func Test_RenderOutputHtml(t *testing.T) {
	// Extract PDF with HTML output format
	var config kreuzberg.ExtractionConfig
	if err := json.Unmarshal([]byte(`{"output_format":"html"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := kreuzberg.ExtractFileSync(`pdf/fake_memo.pdf`, nil, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataOutputFormat string
	if result.Metadata.OutputFormat != nil {
		metadataOutputFormat = *result.Metadata.OutputFormat
	}
	if strings.TrimSpace(metadataOutputFormat) != `html` {
		t.Errorf("equals mismatch: got %v", metadataOutputFormat)
	}
	{
		found := false
		if strings.Contains(result.Content, `<`) {
			found = true
		}
		if strings.Contains(result.Content, `>`) {
			found = true
		}
		if strings.Contains(result.Content, `</`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_RenderOutputMarkdown(t *testing.T) {
	// Extract PDF with Markdown output format
	var config kreuzberg.ExtractionConfig
	if err := json.Unmarshal([]byte(`{"output_format":"markdown"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := kreuzberg.ExtractFileSync(`pdf/fake_memo.pdf`, nil, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataOutputFormat string
	if result.Metadata.OutputFormat != nil {
		metadataOutputFormat = *result.Metadata.OutputFormat
	}
	if strings.TrimSpace(metadataOutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", metadataOutputFormat)
	}
	{
		found := false
		if strings.Contains(result.Content, `#`) {
			found = true
		}
		if strings.Contains(result.Content, `**`) {
			found = true
		}
		if strings.Contains(result.Content, `*`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_RenderOutputPlain(t *testing.T) {
	// Extract PDF with plain text output format
	var config kreuzberg.ExtractionConfig
	if err := json.Unmarshal([]byte(`{"output_format":"plain"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := kreuzberg.ExtractFileSync(`pdf/fake_memo.pdf`, nil, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataOutputFormat string
	if result.Metadata.OutputFormat != nil {
		metadataOutputFormat = *result.Metadata.OutputFormat
	}
	if strings.TrimSpace(metadataOutputFormat) != `plain` {
		t.Errorf("equals mismatch: got %v", metadataOutputFormat)
	}
	assert.GreaterOrEqual(t, len(result.Content), 50, "expected length >= 50")
}

func Test_RenderSinglePage(t *testing.T) {
	// Render a single PDF page to PNG
	pdf_bytesBytes, _ := base64.StdEncoding.DecodeString(`pdf/tiny.pdf`)
	result, err := kreuzberg.RenderPdfPageToPng(pdf_bytesBytes, 0)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result == nil {
		t.Fatalf("expected non-nil result")
	}
	value := *result
	if len(value) == 0 {
		t.Errorf("expected non-empty value")
	}
	assert.GreaterOrEqual(t, len(value), 100, "expected at least 100 elements")
}
