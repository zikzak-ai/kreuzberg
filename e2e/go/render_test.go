// Code generated by alef. DO NOT EDIT.

// E2e tests for category: render
package e2e_test

import (
	"strings"
	"testing"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_RenderCustomDpi(t *testing.T) {
	// Render PDF page with custom DPI
	result, err := kreuzberg.ExtractFile(`pdf/tiny.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.True(t, result.IsPng, "expected true")
	if result.ByteLength < 50 {
		t.Errorf("expected >= 50, got %v", result.ByteLength)
	}
}

func Test_RenderHighDpi(t *testing.T) {
	// Render PDF page with high DPI setting (300 DPI)
	_, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigDpi(300)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_RenderIterator(t *testing.T) {
	// Iterate all PDF pages as PNG images
	result, err := kreuzberg.ExtractFile(`pdf/tiny.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.True(t, result.IsPng, "expected true")
	if result.PageCount < 1 {
		t.Errorf("expected >= 1, got %v", result.PageCount)
	}
}

func Test_RenderMultiPage(t *testing.T) {
	// Render a page from a multi-page PDF document
	result, err := kreuzberg.ExtractFile(`pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_RenderOutputDjot(t *testing.T) {
	// Extract PDF with Djot output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`djot`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.OutputFormat) != `djot` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_RenderOutputHtml(t *testing.T) {
	// Extract PDF with HTML output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`html`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.OutputFormat) != `html` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	{
		found := false
		if strings.Contains(*result.Content, `<`) {
			found = true
		}
		if strings.Contains(*result.Content, `>`) {
			found = true
		}
		if strings.Contains(*result.Content, `</`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_RenderOutputMarkdown(t *testing.T) {
	// Extract PDF with Markdown output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`markdown`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	{
		found := false
		if strings.Contains(*result.Content, `#`) {
			found = true
		}
		if strings.Contains(*result.Content, `**`) {
			found = true
		}
		if strings.Contains(*result.Content, `*`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_RenderOutputPlain(t *testing.T) {
	// Extract PDF with plain text output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`plain`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.OutputFormat) != `plain` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_RenderSinglePage(t *testing.T) {
	// Render a single PDF page to PNG
	result, err := kreuzberg.ExtractFile(`pdf/tiny.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.True(t, result.IsPng, "expected true")
	if result.ByteLength < 100 {
		t.Errorf("expected >= 100, got %v", result.ByteLength)
	}
}
