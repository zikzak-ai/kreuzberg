// Code generated by alef. DO NOT EDIT.

// E2e tests for category: contract
package e2e_test

import (
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_ApiBatchBytesAsync(t *testing.T) {
	// Tests async batch bytes extraction API (batch_extract_bytes)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiBatchBytesSync(t *testing.T) {
	// Tests sync batch bytes extraction API (batch_extract_bytes_sync)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiBatchBytesWithConfigsAsync(t *testing.T) {
	// Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_ApiBatchBytesWithConfigsSync(t *testing.T) {
	// Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_ApiBatchFileAsync(t *testing.T) {
	// Tests async batch file extraction API (batch_extract_file)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiBatchFileSync(t *testing.T) {
	// Tests sync batch file extraction API (batch_extract_file_sync)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiBatchFileWithConfigsAsync(t *testing.T) {
	// Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_ApiBatchFileWithConfigsSync(t *testing.T) {
	// Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_ApiBatchFileWithTimeoutSync(t *testing.T) {
	// Tests sync batch file extraction with per-file timeout config override
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigExtractionTimeoutSecs(300)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_ApiExtractBytesAsync(t *testing.T) {
	// Tests async bytes extraction API (extract_bytes)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiExtractBytesSync(t *testing.T) {
	// Tests sync bytes extraction API (extract_bytes_sync)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiExtractFileAsync(t *testing.T) {
	// Tests async file extraction API (extract_file)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ApiExtractFileSync(t *testing.T) {
	// Tests sync file extraction API (extract_file_sync)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ConfigAccelerationCpuProvider(t *testing.T) {
	// Tests explicit CPU acceleration provider configuration
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigAcceleration(`{"device_id":0,"provider":"cpu"}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `To Whom it May Concern`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_ConfigChunking(t *testing.T) {
	// Tests chunking configuration with chunk assertions
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigChunking(`{"max_chars":500,"max_overlap":50}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	assert.GreaterOrEqual(t, len(result.Chunks), 1, "expected at least 1 elements")
	assert.True(t, result.ChunksHaveContent, "expected true")
}

func Test_ConfigChunkingText(t *testing.T) {
	// Tests text chunker type (generic whitespace/punctuation splitter)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigChunking(`{"chunker_type":"text","max_chars":500,"max_overlap":50}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	assert.GreaterOrEqual(t, len(result.Chunks), 1, "expected at least 1 elements")
	assert.True(t, result.ChunksHaveContent, "expected true")
}

func Test_ConfigDisableOcr(t *testing.T) {
	// Tests disable_ocr configuration option - OCR is skipped for images
	result, err := kreuzberg.ExtractFile(`images/test_hello_world.png`, htmd.NewExtractionConfig(htmd.WithExtractionConfigDisableOcr(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `image/png` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: max_length
}

func Test_ConfigDocumentStructure(t *testing.T) {
	// Tests include_document_structure config produces document tree
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	if len(result.DocumentStructure) == 0 {
		t.Errorf("expected non-empty value")
	}
	assert.GreaterOrEqual(t, len(result.DocumentStructure.Nodes), 1, "expected at least 1 elements")
	if !strings.Contains(string(result.DocumentStructure.NodeTypes), `paragraph`) {
		t.Errorf("expected to contain %s", `paragraph`)
	}
}

func Test_ConfigDocumentStructureDisabled(t *testing.T) {
	// Tests document field is null when include_document_structure is false
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
}

func Test_ConfigDocumentStructureWithHeadings(t *testing.T) {
	// Tests document structure with DOCX heading-driven nesting
	result, err := kreuzberg.ExtractFile(`docx/fake.docx`, htmd.NewExtractionConfig(htmd.WithExtractionConfigIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	if len(result.DocumentStructure) == 0 {
		t.Errorf("expected non-empty value")
	}
	assert.GreaterOrEqual(t, len(result.DocumentStructure.Nodes), 1, "expected at least 1 elements")
}

func Test_ConfigEmailMsgFallbackCodepage(t *testing.T) {
	// Tests MSG extraction with custom fallback codepage for Cyrillic
	result, err := kreuzberg.ExtractFile(`email/fake_email.msg`, htmd.NewExtractionConfig(htmd.WithExtractionConfigEmail(`{"msg_fallback_codepage":1251}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.ms-outlook` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_ConfigExtractionTimeout(t *testing.T) {
	// Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigExtractionTimeoutSecs(300)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_ConfigHtmlOptions(t *testing.T) {
	// Tests extraction with HTML conversion options configured
	result, err := kreuzberg.ExtractFile(`html/complex_table.html`, htmd.NewExtractionConfig(htmd.WithExtractionConfigHtmlOptions(`{"extract_metadata":true}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `text/html`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_ConfigHtmlStyledCustomCss(t *testing.T) {
	// HTML output with inline user CSS string is embedded verbatim in the style block
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigHtmlOutput(`{"css":".kb-p { color: red; }","embed_css":true,"theme":"unstyled"}`), htmd.WithExtractionConfigOutputFormat(`html`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.OutputFormat) != `html` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	if !strings.Contains(string(*result.Content), `.kb-p { color: red; }`) {
		t.Errorf("expected to contain %s", `.kb-p { color: red; }`)
	}
	if !strings.Contains(string(*result.Content), `kb-doc`) {
		t.Errorf("expected to contain %s", `kb-doc`)
	}
}

func Test_ConfigHtmlStyledDefault(t *testing.T) {
	// HTML output with default theme emits kb-* class hooks and an embedded style block
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigHtmlOutput(`{"embed_css":true,"theme":"default"}`), htmd.WithExtractionConfigOutputFormat(`html`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	if strings.TrimSpace(result.OutputFormat) != `html` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	if !strings.Contains(string(*result.Content), `kb-doc`) {
		t.Errorf("expected to contain %s", `kb-doc`)
	}
	if !strings.Contains(string(*result.Content), `kb-content`) {
		t.Errorf("expected to contain %s", `kb-content`)
	}
	if !strings.Contains(string(*result.Content), `kb-p`) {
		t.Errorf("expected to contain %s", `kb-p`)
	}
	if !strings.Contains(string(*result.Content), `<style>`) {
		t.Errorf("expected to contain %s", `<style>`)
	}
}

func Test_ConfigHtmlStyledNoEmbed(t *testing.T) {
	// HTML output with embed_css false emits class hooks but no style block
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigHtmlOutput(`{"embed_css":false,"theme":"default"}`), htmd.WithExtractionConfigOutputFormat(`html`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.OutputFormat) != `html` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
	if !strings.Contains(string(*result.Content), `kb-doc`) {
		t.Errorf("expected to contain %s", `kb-doc`)
	}
	if !strings.Contains(string(*result.Content), `kb-content`) {
		t.Errorf("expected to contain %s", `kb-content`)
	}
}

func Test_ConfigImages(t *testing.T) {
	// Tests image extraction configuration with image assertions
	result, err := kreuzberg.ExtractFile(`pdf/embedded_images_tables.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigImages(`{"extract_images":true}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	assert.GreaterOrEqual(t, len(result.Images), 1, "expected at least 1 elements")
}

func Test_ConfigImagesWithFormats(t *testing.T) {
	// Tests image extraction on PPTX containing embedded images
	result, err := kreuzberg.ExtractFile(`pptx/powerpoint_with_image.pptx`, htmd.NewExtractionConfig(htmd.WithExtractionConfigImages(`{"extract_images":true}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.presentationml.presentation` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	assert.GreaterOrEqual(t, len(result.Images), 1, "expected at least 1 elements")
}

func Test_ConfigLanguageDetection(t *testing.T) {
	// Tests language detection configuration
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigLanguageDetection(`{"enabled":true}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if !strings.Contains(string(result.DetectedLanguages), `eng`) {
		t.Errorf("expected to contain %s", `eng`)
	}
	if result.LanguageDetectionConfidence < 0.5 {
		t.Errorf("expected >= 0.5, got %v", result.LanguageDetectionConfidence)
	}
}

func Test_ConfigLanguageDetectionMulti(t *testing.T) {
	// Tests language detection with detect_multiple and min_confidence options
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigLanguageDetection(`{"detect_multiple":true,"enabled":true,"min_confidence":0.3}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if !strings.Contains(string(result.DetectedLanguages), `eng`) {
		t.Errorf("expected to contain %s", `eng`)
	}
}

func Test_ConfigPostprocessor(t *testing.T) {
	// Tests postprocessor config is accepted and extraction succeeds
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigPostprocessor(`{"enabled":true}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_ConfigProcessingWarningsEmpty(t *testing.T) {
	// Tests that a clean PDF extraction produces no processing warnings
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if len(result.ProcessingWarnings) != 0 {
		t.Errorf("expected empty value, got %v", result.ProcessingWarnings)
	}
}

func Test_ConfigQualityDisabled(t *testing.T) {
	// Tests extraction with quality processing explicitly disabled
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigEnableQualityProcessing(false)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_ConfigSecurityLimits(t *testing.T) {
	// Tests archive extraction with custom security limits
	result, err := kreuzberg.ExtractFile(`archives/documents.zip`, htmd.NewExtractionConfig(htmd.WithExtractionConfigSecurityLimits(`{"max_archive_size":104857600,"max_compression_ratio":50,"max_files_in_archive":100}`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/zip`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/x-zip-compressed`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_ConfigTablesContent(t *testing.T) {
	// Tests table extraction with content_contains_any assertion on DOCX with tables
	result, err := kreuzberg.ExtractFile(`docx/docx_tables.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	assert.GreaterOrEqual(t, len(result.Tables), 1, "expected at least 1 elements")
}

func Test_ConfigUseCacheFalse(t *testing.T) {
	// Tests use_cache=false configuration option
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigUseCache(false)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OutputFormatBytesMarkdown(t *testing.T) {
	// Tests markdown output format via bytes extraction API
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`markdown`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_OutputFormatDjot(t *testing.T) {
	// Tests Djot output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`djot`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `djot` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_OutputFormatHtml(t *testing.T) {
	// Tests HTML output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`html`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `html` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_OutputFormatMarkdown(t *testing.T) {
	// Tests Markdown output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`markdown`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `markdown` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_OutputFormatPlain(t *testing.T) {
	// Tests Plain output format
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigOutputFormat(`plain`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.OutputFormat) != `plain` {
		t.Errorf("equals mismatch: got %v", result.OutputFormat)
	}
}

func Test_ResultFormatElementBased(t *testing.T) {
	// Tests ElementBased result format with element assertions
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigResultFormat(`element_based`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	if strings.TrimSpace(result.ResultFormat) != `element_based` {
		t.Errorf("equals mismatch: got %v", result.ResultFormat)
	}
	assert.GreaterOrEqual(t, len(result.Elements), 1, "expected at least 1 elements")
}

func Test_ResultFormatUnified(t *testing.T) {
	// Tests Unified result format (default)
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`, htmd.NewExtractionConfig(htmd.WithExtractionConfigResultFormat(`unified`)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.ResultFormat) != `unified` {
		t.Errorf("equals mismatch: got %v", result.ResultFormat)
	}
}
