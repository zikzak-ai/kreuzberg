// Code generated by alef. DO NOT EDIT.

// E2e tests for category: pdf
package e2e_test

import (
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_PdfAssemblyTechnical(t *testing.T) {
	// Assembly language technical manual with large body of text.
	result, err := kreuzberg.ExtractFile(`pdf/assembly_language_for_beginners_al4_b_en.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `assembly`) {
			found = true
		}
		if strings.Contains(*result.Content, `register`) {
			found = true
		}
		if strings.Contains(*result.Content, `instruction`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfBayesianDataAnalysis(t *testing.T) {
	// Bayesian data analysis textbook PDF with large content volume.
	result, err := kreuzberg.ExtractFile(`pdf/bayesian_data_analysis_third_edition_13th_feb_2020.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `Bayesian`) {
			found = true
		}
		if strings.Contains(*result.Content, `probability`) {
			found = true
		}
		if strings.Contains(*result.Content, `distribution`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfCodeAndFormula(t *testing.T) {
	// PDF containing code snippets and formulas should retain substantial content.
	result, err := kreuzberg.ExtractFile(`pdf/code_and_formula.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_PdfDeepLearning(t *testing.T) {
	// Deep learning textbook PDF to ensure long-form extraction quality.
	result, err := kreuzberg.ExtractFile(`pdf/fundamentals_of_deep_learning_2014.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `neural`) {
			found = true
		}
		if strings.Contains(*result.Content, `network`) {
			found = true
		}
		if strings.Contains(*result.Content, `deep learning`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfEmbeddedImages(t *testing.T) {
	// PDF with embedded images should extract text and tables when present.
	result, err := kreuzberg.ExtractFile(`pdf/embedded_images_tables.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	assert.GreaterOrEqual(t, len(result.Tables), 0, "expected at least 0 elements")
}

func Test_PdfGoogleDoc(t *testing.T) {
	// Google Docs exported PDF to verify conversion fidelity.
	result, err := kreuzberg.ExtractFile(`pdf/google_doc_document.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfLargeCiml(t *testing.T) {
	// Large machine learning textbook PDF to stress extraction length.
	result, err := kreuzberg.ExtractFile(`pdf/a_course_in_machine_learning_ciml_v0_9_all.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `machine learning`) {
			found = true
		}
		if strings.Contains(*result.Content, `algorithm`) {
			found = true
		}
		if strings.Contains(*result.Content, `training`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfNonEnglishGerman(t *testing.T) {
	// German technical PDF to ensure non-ASCII content extraction.
	result, err := kreuzberg.ExtractFile(`pdf/5_level_paging_and_5_level_ept_intel_revision_1_1_may_2017.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `Intel`) {
			found = true
		}
		if strings.Contains(*result.Content, `paging`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfRightToLeft(t *testing.T) {
	// Right-to-left language PDF to verify RTL extraction.
	result, err := kreuzberg.ExtractFile(`pdf/right_to_left_01.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}

func Test_PdfSimpleText(t *testing.T) {
	// Simple text-heavy PDF should extract content without OCR or tables.
	result, err := kreuzberg.ExtractFile(`pdf/fake_memo.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `May 5, 2023`) {
			found = true
		}
		if strings.Contains(*result.Content, `To Whom it May Concern`) {
			found = true
		}
		if strings.Contains(*result.Content, `Mallori`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_PdfTablesLarge(t *testing.T) {
	// Large PDF with extensive tables to stress table extraction.
	result, err := kreuzberg.ExtractFile(`pdf/large.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_PdfTablesMedium(t *testing.T) {
	// Medium-sized PDF with multiple tables.
	result, err := kreuzberg.ExtractFile(`pdf/medium.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_PdfTechnicalStatLearning(t *testing.T) {
	// Technical statistical learning PDF requiring substantial extraction.
	result, err := kreuzberg.ExtractFile(`pdf/an_introduction_to_statistical_learning_with_applications_in_r_islr_sixth_printing.pdf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/pdf` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `statistical`) {
			found = true
		}
		if strings.Contains(*result.Content, `regression`) {
			found = true
		}
		if strings.Contains(*result.Content, `learning`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if strings.TrimSpace(result.Metadata.FormatType) != `pdf` {
		t.Errorf("equals mismatch: got %v", result.Metadata.FormatType)
	}
}
