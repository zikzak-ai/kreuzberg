// Code generated by alef. DO NOT EDIT.

// E2e tests for category: office
package e2e_test

import (
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_OfficeBibtexBasic(t *testing.T) {
	// BibTeX bibliography extraction.
	result, err := kreuzberg.ExtractFile(`bibtex/comprehensive.bib`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-bibtex`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/x-bibtex`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeCommonmarkBasic(t *testing.T) {
	// CommonMark (.commonmark) text extraction.
	result, err := kreuzberg.ExtractFile(`markdown/sample.commonmark`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `text/markdown`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/plain`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/x-commonmark`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDjotBasic(t *testing.T) {
	// Djot markup text extraction.
	result, err := kreuzberg.ExtractFile(`markdown/tables.djot`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `text/x-djot`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/djot`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocbookBasic(t *testing.T) {
	// DocBook XML document extraction.
	result, err := kreuzberg.ExtractFile(`docbook/docbook-reader.docbook`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/docbook+xml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/docbook`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxBasic(t *testing.T) {
	// DOCX document extraction baseline.
	result, err := kreuzberg.ExtractFile(`docx/sample_document.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxEquations(t *testing.T) {
	// DOCX file containing equations to validate math extraction.
	result, err := kreuzberg.ExtractFile(`docx/equations.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxFake(t *testing.T) {
	// Simple DOCX document to verify baseline extraction.
	result, err := kreuzberg.ExtractFile(`docx/fake.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxFormatting(t *testing.T) {
	// DOCX document heavy on formatting for style preservation.
	result, err := kreuzberg.ExtractFile(`docx/unit_test_formatting.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxHeaders(t *testing.T) {
	// DOCX document with complex headers.
	result, err := kreuzberg.ExtractFile(`docx/unit_test_headers.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxLists(t *testing.T) {
	// DOCX document emphasizing list formatting.
	result, err := kreuzberg.ExtractFile(`docx/unit_test_lists.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeDocxTables(t *testing.T) {
	// DOCX document containing tables for table-aware extraction.
	result, err := kreuzberg.ExtractFile(`docx/docx_tables.docx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.wordprocessingml.document` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if !strings.Contains(string(*result.Content), `Simple uniform table`) {
		t.Errorf("expected to contain %s", `Simple uniform table`)
	}
	if !strings.Contains(string(*result.Content), `Nested Table`) {
		t.Errorf("expected to contain %s", `Nested Table`)
	}
	if !strings.Contains(string(*result.Content), `merged cells`) {
		t.Errorf("expected to contain %s", `merged cells`)
	}
	if !strings.Contains(string(*result.Content), `Header Col`) {
		t.Errorf("expected to contain %s", `Header Col`)
	}
	assert.GreaterOrEqual(t, len(result.Tables), 1, "expected at least 1 elements")
}

func Test_OfficeEpubBasic(t *testing.T) {
	// EPUB book extraction with text content.
	result, err := kreuzberg.ExtractFile(`epub/features.epub`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/epub+zip` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeFb2Basic(t *testing.T) {
	// FictionBook (FB2) document extraction baseline.
	result, err := kreuzberg.ExtractFile(`fictionbook/basic.fb2`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/x-fictionbook+xml` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeFictionbookBasic(t *testing.T) {
	// FictionBook (.fb2) text extraction.
	result, err := kreuzberg.ExtractFile(`fictionbook/basic.fb2`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-fictionbook+xml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/x-fictionbook`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeJatsBasic(t *testing.T) {
	// JATS scientific article extraction.
	result, err := kreuzberg.ExtractFile(`jats/sample_article.jats`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-jats+xml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/jats`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeJupyterBasic(t *testing.T) {
	// Jupyter notebook extraction.
	result, err := kreuzberg.ExtractFile(`jupyter/rank.ipynb`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/x-ipynb+json` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeKeynoteBasic(t *testing.T) {
	// Keynote document extraction baseline.
	result, err := kreuzberg.ExtractFile(`iwork/test.key`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/x-iwork-keynote-sffkey` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeLatexBasic(t *testing.T) {
	// LaTeX document text extraction.
	result, err := kreuzberg.ExtractFile(`latex/basic_sections.tex`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-latex`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/x-latex`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeMarkdownBasic(t *testing.T) {
	// Markdown document extraction baseline.
	result, err := kreuzberg.ExtractFile(`markdown/comprehensive.md`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `text/markdown` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeNumbersBasic(t *testing.T) {
	// Numbers document extraction baseline.
	result, err := kreuzberg.ExtractFile(`iwork/test.numbers`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/x-iwork-numbers-sffnumbers` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeOdsBasic(t *testing.T) {
	// Basic ODS spreadsheet extraction.
	result, err := kreuzberg.ExtractFile(`data_formats/test_01.ods`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.oasis.opendocument.spreadsheet` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeOdtBold(t *testing.T) {
	// ODT document with bold formatting.
	result, err := kreuzberg.ExtractFile(`odt/bold.odt`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.oasis.opendocument.text` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeOdtList(t *testing.T) {
	// ODT document containing unordered lists with nesting.
	result, err := kreuzberg.ExtractFile(`odt/unorderedList.odt`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.oasis.opendocument.text` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `list item`) {
			found = true
		}
		if strings.Contains(*result.Content, `New level`) {
			found = true
		}
		if strings.Contains(*result.Content, `Pushed us`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_OfficeOdtSimple(t *testing.T) {
	// Basic ODT document with paragraphs and headings.
	result, err := kreuzberg.ExtractFile(`odt/simple.odt`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.oasis.opendocument.text` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `favorite things`) {
			found = true
		}
		if strings.Contains(*result.Content, `Parrots`) {
			found = true
		}
		if strings.Contains(*result.Content, `Analysis`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_OfficeOdtTable(t *testing.T) {
	// ODT document with a table structure.
	result, err := kreuzberg.ExtractFile(`odt/table.odt`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.oasis.opendocument.text` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	assert.GreaterOrEqual(t, len(result.Tables), 1, "expected at least 1 elements")
}

func Test_OfficeOpmlBasic(t *testing.T) {
	// OPML outline document extraction.
	result, err := kreuzberg.ExtractFile(`opml/outline.opml`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/xml+opml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/x-opml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/x-opml+xml`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeOrgBasic(t *testing.T) {
	// Org-mode document text extraction.
	result, err := kreuzberg.ExtractFile(`org/comprehensive.org`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `text/x-org`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/org`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficePagesBasic(t *testing.T) {
	// Pages document extraction baseline.
	result, err := kreuzberg.ExtractFile(`iwork/test.pages`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/x-iwork-pages-sffpages` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficePpsxSlideshow(t *testing.T) {
	// PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
	result, err := kreuzberg.ExtractFile(`pptx/sample.ppsx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.presentationml.slideshow` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficePptmBasic(t *testing.T) {
	// PowerPoint macro-enabled presentation (.pptm) extraction.
	result, err := kreuzberg.ExtractFile(`pptx/powerpoint_with_image.pptm`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/vnd.ms-powerpoint.presentation.macroEnabled.12`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/vnd.openxmlformats-officedocument.presentationml.presentation`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_OfficePptxBasic(t *testing.T) {
	// PPTX deck should extract slides content.
	result, err := kreuzberg.ExtractFile(`pptx/simple.pptx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.presentationml.presentation` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficePptxImages(t *testing.T) {
	// PPTX presentation containing images to ensure metadata extraction.
	result, err := kreuzberg.ExtractFile(`pptx/powerpoint_with_image.pptx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.presentationml.presentation` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficePptxPitchDeck(t *testing.T) {
	// Pitch deck PPTX used to validate large slide extraction.
	result, err := kreuzberg.ExtractFile(`pptx/pitch_deck_presentation.pptx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.presentationml.presentation` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeRstBasic(t *testing.T) {
	// reStructuredText document extraction.
	result, err := kreuzberg.ExtractFile(`rst/restructured_text.rst`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `text/x-rst`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/prs.fallenstein.rst`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeRtfBasic(t *testing.T) {
	// RTF document text extraction.
	result, err := kreuzberg.ExtractFile(`rtf/extraction_test.rtf`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/rtf`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/rtf`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeTypstBasic(t *testing.T) {
	// Typst document text extraction.
	result, err := kreuzberg.ExtractFile(`typst/headings.typ`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-typst`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/x-typst`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeXlsLegacy(t *testing.T) {
	// Legacy XLS spreadsheet to ensure backward compatibility.
	result, err := kreuzberg.ExtractFile(`xls/test_excel.xls`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.ms-excel` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_OfficeXlsbBasic(t *testing.T) {
	// Excel binary workbook (.xlsb) extraction.
	result, err := kreuzberg.ExtractFile(`xlsx/test_xlsb.xlsb`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/vnd.ms-excel.sheet.binary.macroEnabled.12`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_OfficeXlsmBasic(t *testing.T) {
	// Excel macro-enabled workbook (.xlsm) extraction.
	result, err := kreuzberg.ExtractFile(`xlsx/test_01.xlsm`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/vnd.ms-excel.sheet.macroEnabled.12`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_OfficeXlsxBasic(t *testing.T) {
	// XLSX spreadsheet should produce metadata and table content.
	result, err := kreuzberg.ExtractFile(`xlsx/stanley_cups.xlsx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if !strings.Contains(string(*result.Content), `Team`) {
		t.Errorf("expected to contain %s", `Team`)
	}
	if !strings.Contains(string(*result.Content), `Location`) {
		t.Errorf("expected to contain %s", `Location`)
	}
	if !strings.Contains(string(*result.Content), `Stanley Cups`) {
		t.Errorf("expected to contain %s", `Stanley Cups`)
	}
	assert.GreaterOrEqual(t, len(result.Tables), 1, "expected at least 1 elements")
	if result.Metadata.SheetCount < 2 {
		t.Errorf("expected >= 2, got %v", result.Metadata.SheetCount)
	}
	if !strings.Contains(string(result.Metadata.SheetNames), `Stanley Cups`) {
		t.Errorf("expected to contain %s", `Stanley Cups`)
	}
}

func Test_OfficeXlsxMultiSheet(t *testing.T) {
	// XLSX workbook with multiple sheets.
	result, err := kreuzberg.ExtractFile(`xlsx/excel_multi_sheet.xlsx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	if result.Metadata.SheetCount < 2 {
		t.Errorf("expected >= 2, got %v", result.Metadata.SheetCount)
	}
}

func Test_OfficeXlsxOfficeExample(t *testing.T) {
	// Simple XLSX spreadsheet shipped alongside office integration tests.
	result, err := kreuzberg.ExtractFile(`xlsx/test_01.xlsx`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}
