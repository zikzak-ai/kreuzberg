// Code generated by alef. DO NOT EDIT.

// E2e tests for category: structured
package e2e_test

import (
	"strings"
	"testing"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_StructuredCsvBasic(t *testing.T) {
	// CSV data file extraction.
	result, err := kreuzberg.ExtractFile(`csv/stanley_cups.csv`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `text/csv` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}

func Test_StructuredEnwBasic(t *testing.T) {
	// EndNote ENW citation format extraction.
	result, err := kreuzberg.ExtractFile(`data_formats/sample.enw`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-endnote-refer`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/x-endnote+xml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/plain`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_StructuredJsonBasic(t *testing.T) {
	// Structured JSON extraction should stream and preserve content.
	result, err := kreuzberg.ExtractFile(`json/sample_document.json`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/json` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `Sample Document`) {
			found = true
		}
		if strings.Contains(*result.Content, `Test Author`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_StructuredJsonSimple(t *testing.T) {
	// Simple JSON document to verify structured extraction.
	result, err := kreuzberg.ExtractFile(`json/simple.json`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/json` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
	{
		found := false
		if strings.Contains(*result.Content, `{`) {
			found = true
		}
		if strings.Contains(*result.Content, `name`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_StructuredNbibBasic(t *testing.T) {
	// PubMed NBIB citation format extraction.
	result, err := kreuzberg.ExtractFile(`data_formats/sample.nbib`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/nbib`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/x-pubmed`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/plain`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_StructuredRisBasic(t *testing.T) {
	// RIS citation format extraction.
	result, err := kreuzberg.ExtractFile(`data_formats/sample.ris`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/x-research-info-systems`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/plain`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_StructuredTomlBasic(t *testing.T) {
	// TOML configuration file extraction.
	result, err := kreuzberg.ExtractFile(`data_formats/cargo.toml`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/toml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/toml`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_StructuredTsvBasic(t *testing.T) {
	// TSV (tab-separated values) data file extraction.
	result, err := kreuzberg.ExtractFile(`data_formats/employees.tsv`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `text/tab-separated-values`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/plain`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_StructuredYamlBasic(t *testing.T) {
	// YAML file text extraction.
	result, err := kreuzberg.ExtractFile(`yaml/simple.yaml`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	{
		found := false
		if strings.Contains(result.MimeType, `application/yaml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/yaml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `text/x-yaml`) {
			found = true
		}
		if strings.Contains(result.MimeType, `application/x-yaml`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
	// TODO: unsupported assertion type: min_length
}

func Test_StructuredYamlSimple(t *testing.T) {
	// Simple YAML document to validate structured extraction.
	result, err := kreuzberg.ExtractFile(`yaml/simple.yaml`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `application/x-yaml` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	// TODO: unsupported assertion type: min_length
}
