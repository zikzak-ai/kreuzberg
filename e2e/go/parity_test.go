// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang go

package e2e

import (
	"reflect"
	"testing"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/packages/go/v4"
)

func TestExtractionResultFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.ExtractionResult{})
	expectedFields := map[string]string{
		"Annotations":        "annotations",
		"Children":           "children",
		"Chunks":             "chunks",
		"Content":            "content",
		"DetectedLanguages":  "detected_languages",
		"DjotContent":        "djot_content",
		"Document":           "document",
		"Elements":           "elements",
		"ExtractedKeywords":  "extracted_keywords",
		"Images":             "images",
		"Metadata":           "metadata",
		"MimeType":           "mime_type",
		"OcrElements":        "ocr_elements",
		"Pages":              "pages",
		"ProcessingWarnings": "processing_warnings",
		"QualityScore":       "quality_score",
		"Tables":             "tables",
		"Uris":               "uris",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("ExtractionResult missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestExtractionConfigFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.ExtractionConfig{})
	expectedFields := map[string]string{
		"Acceleration":             "acceleration",
		"CacheNamespace":           "cache_namespace",
		"CacheTTLSecs":             "cache_ttl_secs",
		"Chunking":                 "chunking",
		"Concurrency":              "concurrency",
		"Email":                    "email",
		"EnableQualityProcessing":  "enable_quality_processing",
		"ExtractionTimeoutSecs":    "extraction_timeout_secs",
		"ForceOcr":                 "force_ocr",
		"ForceOcrPages":            "force_ocr_pages",
		"HTMLOptions":              "html_options",
		"Images":                   "images",
		"IncludeDocumentStructure": "include_document_structure",
		"Keywords":                 "keywords",
		"LanguageDetection":        "language_detection",
		"Layout":                   "layout",
		"MaxArchiveDepth":          "max_archive_depth",
		"MaxConcurrentExtractions": "max_concurrent_extractions",
		"Ocr":                      "ocr",
		"OutputFormat":             "output_format",
		"Pages":                    "pages",
		"PdfOptions":               "pdf_options",
		"Postprocessor":            "postprocessor",
		"ResultFormat":             "result_format",
		"SecurityLimits":           "security_limits",
		"TokenReduction":           "token_reduction",
		"TreeSitter":               "tree_sitter",
		"UseCache":                 "use_cache",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("ExtractionConfig missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestBoundingBoxFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.BoundingBox{})
	expectedFields := map[string]string{
		"X0": "x0",
		"X1": "x1",
		"Y0": "y0",
		"Y1": "y1",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("BoundingBox missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestExtractedKeywordFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.ExtractedKeyword{})
	expectedFields := map[string]string{
		"Algorithm": "algorithm",
		"Positions": "positions",
		"Score":     "score",
		"Text":      "text",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("ExtractedKeyword missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestPdfAnnotationFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.PdfAnnotation{})
	expectedFields := map[string]string{
		"AnnotationType": "annotation_type",
		"BoundingBox":    "bounding_box",
		"Content":        "content",
		"PageNumber":     "page_number",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("PdfAnnotation missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestProcessingWarningFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.ProcessingWarning{})
	expectedFields := map[string]string{
		"Message": "message",
		"Source":  "source",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("ProcessingWarning missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestTableFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.Table{})
	expectedFields := map[string]string{
		"BoundingBox": "bounding_box",
		"Cells":       "cells",
		"Markdown":    "markdown",
		"PageNumber":  "page_number",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("Table missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func TestURIFieldParity(t *testing.T) {
	typ := reflect.TypeOf(kreuzberg.URI{})
	expectedFields := map[string]string{
		"Kind":  "kind",
		"Label": "label",
		"Page":  "page",
		"URL":   "url",
	}
	for goName, jsonTag := range expectedFields {
		field, ok := typ.FieldByName(goName)
		if !ok {
			t.Errorf("URI missing field: %s (json: %s)", goName, jsonTag)
			continue
		}
		tag := field.Tag.Get("json")
		if tag == "" || (tag != jsonTag && !contains(tag, jsonTag)) {
			t.Errorf("field %s has wrong json tag: got %q, want to contain %q", goName, tag, jsonTag)
		}
	}
}

func contains(s, substr string) bool {
	for i := 0; i <= len(s)-len(substr); i++ {
		if s[i:i+len(substr)] == substr {
			return true
		}
	}
	return false
}
