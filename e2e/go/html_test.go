// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0dd70a0018885aeada16d1d2c10bd43eb2d6807bceb6e418561cb777d3654814
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: html
package e2e_test

import (
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_HtmlComplexLayout(t *testing.T) {
	// Large Wikipedia HTML page to validate complex conversion.
	result, err := kreuzberg.ExtractFile(`html/taylor_swift.html`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `text/html` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	assert.GreaterOrEqual(t, len(result.Content), 1000, "expected length >= 1000")
}

func Test_HtmlSimpleTable(t *testing.T) {
	// HTML table converted to markdown should retain structure.
	result, err := kreuzberg.ExtractFile(`html/simple_table.html`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(result.MimeType) != `text/html` {
		t.Errorf("equals mismatch: got %v", result.MimeType)
	}
	assert.GreaterOrEqual(t, len(result.Content), 100, "expected length >= 100")
	if !strings.Contains(string(result.Content), `Product`) {
		t.Errorf("expected to contain %s", `Product`)
	}
	if !strings.Contains(string(result.Content), `Category`) {
		t.Errorf("expected to contain %s", `Category`)
	}
	if !strings.Contains(string(result.Content), `Price`) {
		t.Errorf("expected to contain %s", `Price`)
	}
	if !strings.Contains(string(result.Content), `Stock`) {
		t.Errorf("expected to contain %s", `Stock`)
	}
	if !strings.Contains(string(result.Content), `Laptop`) {
		t.Errorf("expected to contain %s", `Laptop`)
	}
	if !strings.Contains(string(result.Content), `Electronics`) {
		t.Errorf("expected to contain %s", `Electronics`)
	}
	if !strings.Contains(string(result.Content), `Sample Data Table`) {
		t.Errorf("expected to contain %s", `Sample Data Table`)
	}
}
