// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fd0e3c097cbfcc43d516b978e9a3c6f1944a249a0d3591063f8b31ff67b3abce
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: rendering
package e2e_test

import (
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	kreuzberg "github.com/kreuzberg-dev/kreuzberg/v4"
)

func Test_ConvertHtmlBasic(t *testing.T) {
	// Convert basic HTML to Markdown format
	result, err := kreuzberg.ConvertHTMLToMarkdown(`<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Content), `Title`) {
		t.Errorf("expected to contain %s, got %v", `Title`, result.Content)
	}
}

func Test_ConvertHtmlComplex(t *testing.T) {
	// Convert complex HTML to markdown
	result, err := kreuzberg.ConvertHTMLToMarkdown(`<h1>Title</h1><p>Text with <strong>bold</strong> and <a href="https://example.com">link</a>.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 20, "expected length >= 20")
}

func Test_ConvertHtmlEmpty(t *testing.T) {
	// Convert empty HTML to markdown
	_, err := kreuzberg.ConvertHTMLToMarkdown(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_ConvertHtmlTable(t *testing.T) {
	// Convert HTML table to Markdown table format
	result, err := kreuzberg.ConvertHTMLToMarkdown(`<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Content), `|`) {
		t.Errorf("expected to contain %s, got %v", `|`, result.Content)
	}
}

func Test_DjotToHtmlBasic(t *testing.T) {
	// Convert Djot format to HTML
	result, err := kreuzberg.DjotToHTML(`# Title

Paragraph text with **bold** and *italic*.

- Item 1
- Item 2`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 10, "expected length >= 10")
}

func Test_DjotToHtmlComplex(t *testing.T) {
	// Complex djot to HTML
	result, err := kreuzberg.DjotToHTML(`# Heading

Paragraph.

- Item one
- Item two`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 10, "expected length >= 10")
}

func Test_DjotToHtmlEmpty(t *testing.T) {
	// Convert empty djot to HTML
	_, err := kreuzberg.DjotToHTML(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_RenderDjotBasic(t *testing.T) {
	// Render HTML document to Djot format
	result, err := kreuzberg.RenderDjot(`<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 5, "expected length >= 5")
}

func Test_RenderDjotFormatted(t *testing.T) {
	// Render HTML to djot
	_, err := kreuzberg.RenderDjot(`<h2>Section</h2><p>Text with <code>code</code>.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_RenderHtmlBasic(t *testing.T) {
	// Render a simple HTML document to formatted output
	result, err := kreuzberg.RenderHTML(`<h1>Test Document</h1><p>This is a paragraph with some test content.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 5, "expected length >= 5")
}

func Test_RenderHtmlStyled(t *testing.T) {
	// Render HTML preserving structure
	result, err := kreuzberg.RenderHTML(`<h1>Heading</h1><p><strong>bold</strong></p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Content), `Heading`) {
		t.Errorf("expected to contain %s, got %v", `Heading`, result.Content)
	}
}

func Test_RenderJsonBasic(t *testing.T) {
	// Render HTML document to JSON format
	result, err := kreuzberg.RenderJSON(`<h1>Structured Data</h1><p>Content for JSON rendering.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 5, "expected length >= 5")
}

func Test_RenderJsonComplex(t *testing.T) {
	// Render HTML to JSON
	result, err := kreuzberg.RenderJSON(`<h1>Title</h1><p>Text.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 5, "expected length >= 5")
}

func Test_RenderMarkdownBasic(t *testing.T) {
	// Render HTML document to Markdown format
	result, err := kreuzberg.RenderMarkdown(`<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 5, "expected length >= 5")
}

func Test_RenderMarkdownComplex(t *testing.T) {
	// Render complex HTML to markdown
	result, err := kreuzberg.RenderMarkdown(`<h1>Title</h1><ul><li>A</li><li>B</li></ul>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 10, "expected length >= 10")
}

func Test_RenderPlainBasic(t *testing.T) {
	// Render HTML document to plain text format
	result, err := kreuzberg.RenderPlain(`<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Content), 5, "expected length >= 5")
}

func Test_RenderPlainEmpty(t *testing.T) {
	// Render empty HTML
	_, err := kreuzberg.RenderPlain(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}
