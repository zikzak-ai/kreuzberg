// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: error. */
class ErrorTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testErrorEmptyBytes() throws Exception {
        // Graceful handling of empty bytes (should not error)
        var result = Kreuzberg.extractFile("", "text/plain");
    }

    @Test
    void testErrorEmptyMime() throws Exception {
        // Error when extracting with empty MIME type
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("", ""));
    }

    @Test
    void testErrorExtractBytesConflictingOcr() throws Exception {
        // extract_bytes force+disable OCR
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("", "text/plain", config));
    }

    @Test
    void testErrorFileNotFound() throws Exception {
        // Error when file does not exist (sync)
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("nonexistent/file.pdf"));
    }

    @Test
    void testErrorFileNotFoundAsync() throws Exception {
        // Error when file does not exist (async)
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("nonexistent/file.pdf"));
    }

    @Test
    void testErrorInvalidChunkOverlap() throws Exception {
        // Error when chunk overlap exceeds max_characters
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/fake_memo.pdf", config));
    }

    @Test
    void testErrorInvalidConfidence() throws Exception {
        // Error when OCR confidence threshold is out of range
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/fake_memo.pdf", config));
    }

    @Test
    void testErrorInvalidDpi() throws Exception {
        // Error when OCR DPI is invalid (negative)
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/fake_memo.pdf", config));
    }

    @Test
    void testErrorInvalidMimeFormat() throws Exception {
        // Error when extracting with invalid MIME type format
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("", "not-a-mime"));
    }

    @Test
    void testErrorInvalidOcrLanguage() throws Exception {
        // Error when OCR language pack is not available
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("images/balance_sheet_1.png", config));
    }

    @Test
    void testErrorPdfCorrupt() throws Exception {
        // Error when PDF is truncated/corrupt
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/corrupt_truncated.pdf"));
    }

    @Test
    void testErrorPdfNeedsPassword() throws Exception {
        // Error when password-protected PDF is accessed without password
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/password_protected.pdf"));
    }

    @Test
    void testErrorPdfPageOutOfRange() throws Exception {
        // Graceful handling of out-of-range page extraction
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
    }

    @Test
    void testErrorPdfWrongPassword() throws Exception {
        // Error when PDF password is incorrect
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/password_protected.pdf", config));
    }

    @Test
    void testErrorRenderCorruptPdf() throws Exception {
        // Error when rendering page from corrupt PDF
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/corrupt_truncated.pdf"));
    }

    @Test
    void testErrorRenderInvalidPage() throws Exception {
        // Error when rendering page number exceeds document page count
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/fake_memo.pdf"));
    }

    @Test
    void testErrorSecurityArchiveFiles() throws Exception {
        // Error when archive file count exceeds security limit
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("archives/documents.zip", config));
    }

    @Test
    void testErrorSecurityArchiveSize() throws Exception {
        // Error when archive size exceeds security limit
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("archives/documents.zip", config));
    }

    @Test
    void testErrorSecurityContentTooLarge() throws Exception {
        // Error when content exceeds security limit for max size
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/fake_memo.pdf", config));
    }

    @Test
    void testErrorSecurityNestingDepth() throws Exception {
        // Error when XML nesting depth exceeds security limit
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("html/complex_table.html", config));
    }

    @Test
    void testErrorTimeoutExtraction() throws Exception {
        // Error when extraction times out
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("pdf/large.pdf", config));
    }

    @Test
    void testErrorUnsupportedMime() throws Exception {
        // Error when extracting with unsupported MIME type
        assertThrows(Exception.class, () -> Kreuzberg.extractFile("", "application/x-nonexistent"));
    }

}
