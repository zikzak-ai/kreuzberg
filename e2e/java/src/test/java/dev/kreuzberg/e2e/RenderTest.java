// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: render. */
class RenderTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testRenderCustomDpi() throws Exception {
        // Render PDF page with custom DPI
        var result = Kreuzberg.extractFile("pdf/tiny.pdf");
        assertTrue(result.isPng(), "expected true");
        assertTrue(result.byteLength() >= 50, "expected >= 50");
    }

    @Test
    void testRenderHighDpi() throws Exception {
        // Render PDF page with high DPI setting (300 DPI)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
    }

    @Test
    void testRenderIterator() throws Exception {
        // Iterate all PDF pages as PNG images
        var result = Kreuzberg.extractFile("pdf/tiny.pdf");
        assertTrue(result.isPng(), "expected true");
        assertTrue(result.pageCount() >= 1, "expected >= 1");
    }

    @Test
    void testRenderMultiPage() throws Exception {
        // Render a page from a multi-page PDF document
        var result = Kreuzberg.extractFile("pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf");
        assertTrue(result.result().length() >= 1000, "expected length >= 1000");
    }

    @Test
    void testRenderOutputDjot() throws Exception {
        // Extract PDF with Djot output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("djot", result.outputFormat().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testRenderOutputHtml() throws Exception {
        // Extract PDF with HTML output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("html", result.outputFormat().trim());
        assertTrue(result.content().orElse("").contains("<") || result.content().orElse("").contains(">") || result.content().orElse("").contains("</"), "expected to contain at least one of the specified values");
    }

    @Test
    void testRenderOutputMarkdown() throws Exception {
        // Extract PDF with Markdown output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("markdown", result.outputFormat().trim());
        assertTrue(result.content().orElse("").contains("#") || result.content().orElse("").contains("**") || result.content().orElse("").contains("*"), "expected to contain at least one of the specified values");
    }

    @Test
    void testRenderOutputPlain() throws Exception {
        // Extract PDF with plain text output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("plain", result.outputFormat().trim());
        assertTrue(result.content().orElse("").length() >= 50, "expected length >= 50");
    }

    @Test
    void testRenderSinglePage() throws Exception {
        // Render a single PDF page to PNG
        var result = Kreuzberg.extractFile("pdf/tiny.pdf");
        assertTrue(result.isPng(), "expected true");
        assertTrue(result.byteLength() >= 100, "expected >= 100");
    }

}
