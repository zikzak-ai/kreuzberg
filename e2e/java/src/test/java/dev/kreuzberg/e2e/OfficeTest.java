// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e37da9d689c912e2917f792126eae3259f037d6bf4c6bc9fd4e350f15c5ab776
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: office. */
class OfficeTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testOfficeBibtexBasic() throws Exception {
        // BibTeX bibliography extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("bibtex/comprehensive.bib"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/x-bibtex") || result.mimeType().contains("text/x-bibtex"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeCommonmarkBasic() throws Exception {
        // CommonMark (.commonmark) text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("markdown/sample.commonmark"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("text/markdown") || result.mimeType().contains("text/plain") || result.mimeType().contains("text/x-commonmark"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 5, "expected length >= 5");
    }

    @Test
    void testOfficeDjotBasic() throws Exception {
        // Djot markup text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("markdown/tables.djot"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("text/x-djot") || result.mimeType().contains("text/djot"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeDocbookBasic() throws Exception {
        // DocBook XML document extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docbook/docbook-reader.docbook"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/docbook+xml") || result.mimeType().contains("text/docbook"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeDocxBasic() throws Exception {
        // DOCX document extraction baseline.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/sample_document.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeDocxEquations() throws Exception {
        // DOCX file containing equations to validate math extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/equations.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeDocxFake() throws Exception {
        // Simple DOCX document to verify baseline extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/fake.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeDocxFormatting() throws Exception {
        // DOCX document heavy on formatting for style preservation.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/unit_test_formatting.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeDocxHeaders() throws Exception {
        // DOCX document with complex headers.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/unit_test_headers.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeDocxLists() throws Exception {
        // DOCX document emphasizing list formatting.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/unit_test_lists.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeDocxTables() throws Exception {
        // DOCX document containing tables for table-aware extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/docx_tables.docx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
        assertTrue(result.content().contains("Simple uniform table"), "expected to contain: " + "Simple uniform table");
        assertTrue(result.content().contains("Nested Table"), "expected to contain: " + "Nested Table");
        assertTrue(result.content().contains("merged cells"), "expected to contain: " + "merged cells");
        assertTrue(result.content().contains("Header Col"), "expected to contain: " + "Header Col");
        assertTrue(result.tables().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testOfficeEpubBasic() throws Exception {
        // EPUB book extraction with text content.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("epub/features.epub"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/epub+zip", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
    }

    @Test
    void testOfficeFb2Basic() throws Exception {
        // FictionBook (FB2) document extraction baseline.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("fictionbook/basic.fb2"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/x-fictionbook+xml", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeFictionbookBasic() throws Exception {
        // FictionBook (.fb2) text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("fictionbook/basic.fb2"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/x-fictionbook+xml") || result.mimeType().contains("application/x-fictionbook"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeJatsBasic() throws Exception {
        // JATS scientific article extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("jats/sample_article.jats"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/x-jats+xml") || result.mimeType().contains("text/jats"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeJupyterBasic() throws Exception {
        // Jupyter notebook extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("jupyter/rank.ipynb"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/x-ipynb+json", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeKeynoteBasic() throws Exception {
        // Keynote document extraction baseline.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("iwork/test.key"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/x-iwork-keynote-sffkey", result.mimeType().trim());
        assertTrue(result.content().length() >= 5, "expected length >= 5");
    }

    @Test
    void testOfficeLatexBasic() throws Exception {
        // LaTeX document text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("latex/basic_sections.tex"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/x-latex") || result.mimeType().contains("text/x-latex"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeMarkdownBasic() throws Exception {
        // Markdown document extraction baseline.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("markdown/comprehensive.md"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("text/markdown", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeNumbersBasic() throws Exception {
        // Numbers document extraction baseline.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("iwork/test.numbers"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/x-iwork-numbers-sffnumbers", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeOdsBasic() throws Exception {
        // Basic ODS spreadsheet extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("data_formats/test_01.ods"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.oasis.opendocument.spreadsheet", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeOdtBold() throws Exception {
        // ODT document with bold formatting.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("odt/bold.odt"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.oasis.opendocument.text", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeOdtList() throws Exception {
        // ODT document containing unordered lists with nesting.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("odt/unorderedList.odt"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.oasis.opendocument.text", result.mimeType().trim());
        assertTrue(result.content().length() >= 30, "expected length >= 30");
        assertTrue(result.content().contains("list item") || result.content().contains("New level") || result.content().contains("Pushed us"), "expected to contain at least one of the specified values");
    }

    @Test
    void testOfficeOdtSimple() throws Exception {
        // Basic ODT document with paragraphs and headings.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("odt/simple.odt"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.oasis.opendocument.text", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
        assertTrue(result.content().contains("favorite things") || result.content().contains("Parrots") || result.content().contains("Analysis"), "expected to contain at least one of the specified values");
    }

    @Test
    void testOfficeOdtTable() throws Exception {
        // ODT document with a table structure.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("odt/table.odt"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.oasis.opendocument.text", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
        assertTrue(result.tables().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testOfficeOpmlBasic() throws Exception {
        // OPML outline document extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("opml/outline.opml"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/xml+opml") || result.mimeType().contains("text/x-opml") || result.mimeType().contains("application/x-opml+xml"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeOrgBasic() throws Exception {
        // Org-mode document text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("org/comprehensive.org"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("text/x-org") || result.mimeType().contains("text/org"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficePagesBasic() throws Exception {
        // Pages document extraction baseline.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("iwork/test.pages"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/x-iwork-pages-sffpages", result.mimeType().trim());
        assertTrue(result.content().length() >= 5, "expected length >= 5");
    }

    @Test
    void testOfficePpsxSlideshow() throws Exception {
        // PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pptx/sample.ppsx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.slideshow", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficePptmBasic() throws Exception {
        // PowerPoint macro-enabled presentation (.pptm) extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pptx/powerpoint_with_image.pptm"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/vnd.ms-powerpoint.presentation.macroEnabled.12") || result.mimeType().contains("application/vnd.openxmlformats-officedocument.presentationml.presentation"), "expected to contain at least one of the specified values");
        assertFalse(result.content().isEmpty(), "expected non-empty value");
    }

    @Test
    void testOfficePptxBasic() throws Exception {
        // PPTX deck should extract slides content.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pptx/simple.pptx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
    }

    @Test
    void testOfficePptxImages() throws Exception {
        // PPTX presentation containing images to ensure metadata extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pptx/powerpoint_with_image.pptx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mimeType().trim());
        assertTrue(result.content().length() >= 15, "expected length >= 15");
    }

    @Test
    void testOfficePptxPitchDeck() throws Exception {
        // Pitch deck PPTX used to validate large slide extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pptx/pitch_deck_presentation.pptx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mimeType().trim());
        assertTrue(result.content().length() >= 100, "expected length >= 100");
    }

    @Test
    void testOfficeRstBasic() throws Exception {
        // reStructuredText document extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("rst/restructured_text.rst"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("text/x-rst") || result.mimeType().contains("text/prs.fallenstein.rst"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testOfficeRtfBasic() throws Exception {
        // RTF document text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("rtf/extraction_test.rtf"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/rtf") || result.mimeType().contains("text/rtf"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeTypstBasic() throws Exception {
        // Typst document text extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("typst/headings.typ"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/x-typst") || result.mimeType().contains("text/x-typst"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeXlsLegacy() throws Exception {
        // Legacy XLS spreadsheet to ensure backward compatibility.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("xls/test_excel.xls"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.ms-excel", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testOfficeXlsbBasic() throws Exception {
        // Excel binary workbook (.xlsb) extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("xlsx/test_xlsb.xlsb"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/vnd.ms-excel.sheet.binary.macroEnabled.12") || result.mimeType().contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values");
        assertFalse(result.content().isEmpty(), "expected non-empty value");
    }

    @Test
    void testOfficeXlsmBasic() throws Exception {
        // Excel macro-enabled workbook (.xlsm) extraction.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("xlsx/test_01.xlsm"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.mimeType().contains("application/vnd.ms-excel.sheet.macroEnabled.12") || result.mimeType().contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values");
        assertFalse(result.content().isEmpty(), "expected non-empty value");
    }

    @Test
    void testOfficeXlsxBasic() throws Exception {
        // XLSX spreadsheet should produce metadata and table content.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("xlsx/stanley_cups.xlsx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mimeType().trim());
        assertTrue(result.content().length() >= 100, "expected length >= 100");
        assertTrue(result.content().contains("Team"), "expected to contain: " + "Team");
        assertTrue(result.content().contains("Location"), "expected to contain: " + "Location");
        assertTrue(result.content().contains("Stanley Cups"), "expected to contain: " + "Stanley Cups");
        assertTrue(result.tables().size() >= 1, "expected at least 1 elements");
        assertTrue(result.metadata().sheetCount().orElse(0L) >= 2, "expected >= 2");
        assertTrue(result.metadata().sheetNames().orElse(java.util.List.of()).contains("Stanley Cups"), "expected to contain: " + "Stanley Cups");
    }

    @Test
    void testOfficeXlsxMultiSheet() throws Exception {
        // XLSX workbook with multiple sheets.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("xlsx/excel_multi_sheet.xlsx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
        assertTrue(result.metadata().sheetCount().orElse(0L) >= 2, "expected >= 2");
    }

    @Test
    void testOfficeXlsxOfficeExample() throws Exception {
        // Simple XLSX spreadsheet shipped alongside office integration tests.
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("xlsx/test_01.xlsx"), null, MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

}
