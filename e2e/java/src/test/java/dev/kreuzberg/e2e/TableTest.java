// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: table. */
class TableTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testTableCsvExtraction() throws Exception {
        // CSV file extraction produces content
        var result = Kreuzberg.extractFile("csv/stanley_cups.csv");
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testTableHtmlExtraction() throws Exception {
        // HTML table extraction produces markdown format
        var result = Kreuzberg.extractFile("html/simple_table.html", config);
        assertTrue(result.content().orElse("").contains("|"), "expected to contain at least one of the specified values");
    }

    @Test
    void testTableXlsxMarkdown() throws Exception {
        // XLSX extraction produces markdown table format
        var result = Kreuzberg.extractFile("xlsx/stanley_cups.xlsx", config);
        assertTrue(result.content().orElse("").contains("|") || result.content().orElse("").contains("---"), "expected to contain at least one of the specified values");
    }

}
