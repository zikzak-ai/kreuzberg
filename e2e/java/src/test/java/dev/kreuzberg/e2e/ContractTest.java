// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: contract. */
class ContractTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testApiBatchBytesAsync() throws Exception {
        // Tests async batch bytes extraction API (batch_extract_bytes)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiBatchBytesSync() throws Exception {
        // Tests sync batch bytes extraction API (batch_extract_bytes_sync)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiBatchBytesWithConfigsAsync() throws Exception {
        // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("markdown", result.outputFormat().trim());
    }

    @Test
    void testApiBatchBytesWithConfigsSync() throws Exception {
        // Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("markdown", result.outputFormat().trim());
    }

    @Test
    void testApiBatchFileAsync() throws Exception {
        // Tests async batch file extraction API (batch_extract_file)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiBatchFileSync() throws Exception {
        // Tests sync batch file extraction API (batch_extract_file_sync)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiBatchFileWithConfigsAsync() throws Exception {
        // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("markdown", result.outputFormat().trim());
    }

    @Test
    void testApiBatchFileWithConfigsSync() throws Exception {
        // Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("markdown", result.outputFormat().trim());
    }

    @Test
    void testApiBatchFileWithTimeoutSync() throws Exception {
        // Tests sync batch file extraction with per-file timeout config override
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testApiExtractBytesAsync() throws Exception {
        // Tests async bytes extraction API (extract_bytes)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiExtractBytesSync() throws Exception {
        // Tests sync bytes extraction API (extract_bytes_sync)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiExtractFileAsync() throws Exception {
        // Tests async file extraction API (extract_file)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testApiExtractFileSync() throws Exception {
        // Tests sync file extraction API (extract_file_sync)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("Mallori"), "expected to contain at least one of the specified values");
    }

    @Test
    void testConfigAccelerationCpuProvider() throws Exception {
        // Tests explicit CPU acceleration provider configuration
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 50, "expected length >= 50");
        assertTrue(result.content().orElse("").contains("May 5, 2023") || result.content().orElse("").contains("To Whom it May Concern"), "expected to contain at least one of the specified values");
    }

    @Test
    void testConfigChunking() throws Exception {
        // Tests chunking configuration with chunk assertions
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.chunks().size() >= 1, "expected at least 1 elements");
        assertTrue(result.chunksHaveContent(), "expected true");
    }

    @Test
    void testConfigChunkingText() throws Exception {
        // Tests text chunker type (generic whitespace/punctuation splitter)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.chunks().size() >= 1, "expected at least 1 elements");
        assertTrue(result.chunksHaveContent(), "expected true");
    }

    @Test
    void testConfigDisableOcr() throws Exception {
        // Tests disable_ocr configuration option - OCR is skipped for images
        var result = Kreuzberg.extractFile("images/test_hello_world.png", config);
        assertEquals("image/png", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() <= 5, "expected length <= 5");
    }

    @Test
    void testConfigDocumentStructure() throws Exception {
        // Tests include_document_structure config produces document tree
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertFalse(result.documentStructure().isEmpty(), "expected non-empty value");
        assertTrue(result.documentStructure().nodes().size() >= 1, "expected at least 1 elements");
        assertTrue(result.documentStructure().nodeTypes().contains("paragraph"), "expected to contain: " + "paragraph");
    }

    @Test
    void testConfigDocumentStructureDisabled() throws Exception {
        // Tests document field is null when include_document_structure is false
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
    }

    @Test
    void testConfigDocumentStructureWithHeadings() throws Exception {
        // Tests document structure with DOCX heading-driven nesting
        var result = Kreuzberg.extractFile("docx/fake.docx", config);
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertFalse(result.documentStructure().isEmpty(), "expected non-empty value");
        assertTrue(result.documentStructure().nodes().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testConfigEmailMsgFallbackCodepage() throws Exception {
        // Tests MSG extraction with custom fallback codepage for Cyrillic
        var result = Kreuzberg.extractFile("email/fake_email.msg", config);
        assertEquals("application/vnd.ms-outlook", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testConfigExtractionTimeout() throws Exception {
        // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testConfigHtmlOptions() throws Exception {
        // Tests extraction with HTML conversion options configured
        var result = Kreuzberg.extractFile("html/complex_table.html", config);
        assertTrue(result.mimeType().contains("text/html"), "expected to contain at least one of the specified values");
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
    }

    @Test
    void testConfigHtmlStyledCustomCss() throws Exception {
        // HTML output with inline user CSS string is embedded verbatim in the style block
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("html", result.outputFormat().trim());
        assertTrue(result.content().orElse("").contains(".kb-p { color: red; }"), "expected to contain: " + ".kb-p { color: red; }");
        assertTrue(result.content().orElse("").contains("kb-doc"), "expected to contain: " + "kb-doc");
    }

    @Test
    void testConfigHtmlStyledDefault() throws Exception {
        // HTML output with default theme emits kb-* class hooks and an embedded style block
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertEquals("html", result.outputFormat().trim());
        assertTrue(result.content().orElse("").contains("kb-doc"), "expected to contain: " + "kb-doc");
        assertTrue(result.content().orElse("").contains("kb-content"), "expected to contain: " + "kb-content");
        assertTrue(result.content().orElse("").contains("kb-p"), "expected to contain: " + "kb-p");
        assertTrue(result.content().orElse("").contains("<style>"), "expected to contain: " + "<style>");
    }

    @Test
    void testConfigHtmlStyledNoEmbed() throws Exception {
        // HTML output with embed_css false emits class hooks but no style block
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("html", result.outputFormat().trim());
        assertTrue(result.content().orElse("").contains("kb-doc"), "expected to contain: " + "kb-doc");
        assertTrue(result.content().orElse("").contains("kb-content"), "expected to contain: " + "kb-content");
    }

    @Test
    void testConfigImages() throws Exception {
        // Tests image extraction configuration with image assertions
        var result = Kreuzberg.extractFile("pdf/embedded_images_tables.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.images().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testConfigImagesWithFormats() throws Exception {
        // Tests image extraction on PPTX containing embedded images
        var result = Kreuzberg.extractFile("pptx/powerpoint_with_image.pptx", config);
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mimeType().trim());
        assertTrue(result.images().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testConfigLanguageDetection() throws Exception {
        // Tests language detection configuration
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.detectedLanguages().contains("eng"), "expected to contain: " + "eng");
        assertTrue(result.languageDetectionConfidence() >= 0.5d, "expected >= 0.5d");
    }

    @Test
    void testConfigLanguageDetectionMulti() throws Exception {
        // Tests language detection with detect_multiple and min_confidence options
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.detectedLanguages().contains("eng"), "expected to contain: " + "eng");
    }

    @Test
    void testConfigPostprocessor() throws Exception {
        // Tests postprocessor config is accepted and extraction succeeds
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
    }

    @Test
    void testConfigProcessingWarningsEmpty() throws Exception {
        // Tests that a clean PDF extraction produces no processing warnings
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertTrue(result.processingWarnings().isEmpty(), "expected empty value");
    }

    @Test
    void testConfigQualityDisabled() throws Exception {
        // Tests extraction with quality processing explicitly disabled
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
    }

    @Test
    void testConfigSecurityLimits() throws Exception {
        // Tests archive extraction with custom security limits
        var result = Kreuzberg.extractFile("archives/documents.zip", config);
        assertTrue(result.mimeType().contains("application/zip") || result.mimeType().contains("application/x-zip-compressed"), "expected to contain at least one of the specified values");
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testConfigTablesContent() throws Exception {
        // Tests table extraction with content_contains_any assertion on DOCX with tables
        var result = Kreuzberg.extractFile("docx/docx_tables.docx");
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mimeType().trim());
        assertTrue(result.tables().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testConfigUseCacheFalse() throws Exception {
        // Tests use_cache=false configuration option
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testOutputFormatBytesMarkdown() throws Exception {
        // Tests markdown output format via bytes extraction API
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("markdown", result.outputFormat().trim());
    }

    @Test
    void testOutputFormatDjot() throws Exception {
        // Tests Djot output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("djot", result.outputFormat().trim());
    }

    @Test
    void testOutputFormatHtml() throws Exception {
        // Tests HTML output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("html", result.outputFormat().trim());
    }

    @Test
    void testOutputFormatMarkdown() throws Exception {
        // Tests Markdown output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("markdown", result.outputFormat().trim());
    }

    @Test
    void testOutputFormatPlain() throws Exception {
        // Tests Plain output format
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("plain", result.outputFormat().trim());
    }

    @Test
    void testResultFormatElementBased() throws Exception {
        // Tests ElementBased result format with element assertions
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertEquals("element_based", result.resultFormat().trim());
        assertTrue(result.elements().size() >= 1, "expected at least 1 elements");
    }

    @Test
    void testResultFormatUnified() throws Exception {
        // Tests Unified result format (default)
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertEquals("unified", result.resultFormat().trim());
    }

}
