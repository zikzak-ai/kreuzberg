// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:31b8c64992283a1acbc69070e85ee5ba01e3c5c27edb3c90b8f692aff056b702
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: async. */
class AsyncTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testAsyncExtractBytes() {
        // Async extract_bytes call on PDF document
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "TODO: implement Java e2e test for fixture 'async_extract_bytes'");
    }

    @Test
    void testAsyncExtractBytesEmptyMime() {
        // extract_bytes empty MIME async
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "TODO: implement Java e2e test for fixture 'async_extract_bytes_empty_mime'");
    }

    @Test
    void testAsyncExtractBytesInvalidMime() {
        // extract_bytes unsupported MIME async
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "TODO: implement Java e2e test for fixture 'async_extract_bytes_invalid_mime'");
    }

    @Test
    void testAsyncExtractFile() throws Exception {
        // Async extract_file call on PDF document
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pdf/fake_memo.pdf"), "application/pdf", MAPPER.readValue("{}", ExtractionConfig.class));
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
    }

    @Test
    void testAsyncExtractFileDocx() throws Exception {
        // Async extract_file call on DOCX document
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("docx/fake.docx"), "application/vnd.openxmlformats-officedocument.wordprocessingml.document", MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testAsyncExtractFileHtml() throws Exception {
        // Async extract_file call on HTML document
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("html/simple_table.html"), "text/html", MAPPER.readValue("{}", ExtractionConfig.class));
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testAsyncExtractFileImage() throws Exception {
        // Async extract_file call on image document
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("images/test_hello_world.png"), "image/png", MAPPER.readValue("{}", ExtractionConfig.class));
    }

    @Test
    void testAsyncExtractFileWithConfig() throws Exception {
        // Async extract_file call with output format and chunking config
        var config = MAPPER.readValue("{\"chunking\":{\"max_characters\":500,\"max_overlap\":50},\"output_format\":\"markdown\"}", ExtractionConfig.class);
        var result = Kreuzberg.extractFile(java.nio.file.Path.of("pdf/fake_memo.pdf"), "application/pdf", config);
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

}
