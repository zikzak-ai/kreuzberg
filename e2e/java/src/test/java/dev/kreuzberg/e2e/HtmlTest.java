// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;

/** E2e tests for category: html. */
class HtmlTest {
    @Test
    void testHtmlComplexLayout() throws Exception {
        // Large Wikipedia HTML page to validate complex conversion.
        var result = Kreuzberg.extractFile("html/taylor_swift.html");
        assertEquals("text/html", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 1000, "expected length >= 1000");
    }

    @Test
    void testHtmlSimpleTable() throws Exception {
        // HTML table converted to markdown should retain structure.
        var result = Kreuzberg.extractFile("html/simple_table.html");
        assertEquals("text/html", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 100, "expected length >= 100");
        assertTrue(result.content().orElse("").contains("Product"), "expected to contain: " + "Product");
        assertTrue(result.content().orElse("").contains("Category"), "expected to contain: " + "Category");
        assertTrue(result.content().orElse("").contains("Price"), "expected to contain: " + "Price");
        assertTrue(result.content().orElse("").contains("Stock"), "expected to contain: " + "Stock");
        assertTrue(result.content().orElse("").contains("Laptop"), "expected to contain: " + "Laptop");
        assertTrue(result.content().orElse("").contains("Electronics"), "expected to contain: " + "Electronics");
        assertTrue(result.content().orElse("").contains("Sample Data Table"), "expected to contain: " + "Sample Data Table");
    }

}
