// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;

/** E2e tests for category: archive. */
class ArchiveTest {
    @Test
    void testArchiveGzBasic() throws Exception {
        // Gzip compressed file extraction.
        var result = Kreuzberg.extractFile("archives/book_war_and_peace_1p.txt.gz");
        assertTrue(result.mimeType().contains("application/gzip") || result.mimeType().contains("application/x-gzip"), "expected to contain at least one of the specified values");
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testArchiveSevenzBasic() throws Exception {
        // 7-Zip archive extraction.
        var result = Kreuzberg.extractFile("archives/documents.7z");
        assertEquals("application/x-7z-compressed", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testArchiveTarBasic() throws Exception {
        // TAR archive extraction.
        var result = Kreuzberg.extractFile("archives/documents.tar");
        assertTrue(result.mimeType().contains("application/x-tar") || result.mimeType().contains("application/tar"), "expected to contain at least one of the specified values");
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

    @Test
    void testArchiveZipBasic() throws Exception {
        // ZIP archive extraction.
        var result = Kreuzberg.extractFile("archives/documents.zip");
        assertTrue(result.mimeType().contains("application/zip") || result.mimeType().contains("application/x-zip-compressed"), "expected to contain at least one of the specified values");
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
    }

}
