// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:395e930ed53f7fd2471414ae7bc8865628ddc7b72e0de138baaccd4b1400f730
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;

/** E2e tests for category: structured. */
class StructuredTest {
    @Test
    void testStructuredCsvBasic() throws Exception {
        // CSV data file extraction.
        var result = Kreuzberg.extractFile("csv/stanley_cups.csv");
        assertEquals("text/csv", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testStructuredEnwBasic() throws Exception {
        // EndNote ENW citation format extraction.
        var result = Kreuzberg.extractFile("data_formats/sample.enw");
        assertTrue(result.mimeType().contains("application/x-endnote-refer") || result.mimeType().contains("application/x-endnote+xml") || result.mimeType().contains("text/plain"), "expected to contain at least one of the specified values");
    }

    @Test
    void testStructuredJsonBasic() throws Exception {
        // Structured JSON extraction should stream and preserve content.
        var result = Kreuzberg.extractFile("json/sample_document.json");
        assertEquals("application/json", result.mimeType().trim());
        assertTrue(result.content().length() >= 20, "expected length >= 20");
        assertTrue(result.content().contains("Sample Document") || result.content().contains("Test Author"), "expected to contain at least one of the specified values");
    }

    @Test
    void testStructuredJsonSimple() throws Exception {
        // Simple JSON document to verify structured extraction.
        var result = Kreuzberg.extractFile("json/simple.json");
        assertEquals("application/json", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
        assertTrue(result.content().contains("{") || result.content().contains("name"), "expected to contain at least one of the specified values");
    }

    @Test
    void testStructuredNbibBasic() throws Exception {
        // PubMed NBIB citation format extraction.
        var result = Kreuzberg.extractFile("data_formats/sample.nbib");
        assertTrue(result.mimeType().contains("application/nbib") || result.mimeType().contains("application/x-pubmed") || result.mimeType().contains("text/plain"), "expected to contain at least one of the specified values");
        assertFalse(result.content().isEmpty(), "expected non-empty value");
    }

    @Test
    void testStructuredRisBasic() throws Exception {
        // RIS citation format extraction.
        var result = Kreuzberg.extractFile("data_formats/sample.ris");
        assertTrue(result.mimeType().contains("application/x-research-info-systems") || result.mimeType().contains("text/plain"), "expected to contain at least one of the specified values");
        assertFalse(result.content().isEmpty(), "expected non-empty value");
    }

    @Test
    void testStructuredTomlBasic() throws Exception {
        // TOML configuration file extraction.
        var result = Kreuzberg.extractFile("data_formats/cargo.toml");
        assertTrue(result.mimeType().contains("application/toml") || result.mimeType().contains("text/toml"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testStructuredTsvBasic() throws Exception {
        // TSV (tab-separated values) data file extraction.
        var result = Kreuzberg.extractFile("data_formats/employees.tsv");
        assertTrue(result.mimeType().contains("text/tab-separated-values") || result.mimeType().contains("text/plain"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testStructuredYamlBasic() throws Exception {
        // YAML file text extraction.
        var result = Kreuzberg.extractFile("yaml/simple.yaml");
        assertTrue(result.mimeType().contains("application/yaml") || result.mimeType().contains("text/yaml") || result.mimeType().contains("text/x-yaml") || result.mimeType().contains("application/x-yaml"), "expected to contain at least one of the specified values");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testStructuredYamlSimple() throws Exception {
        // Simple YAML document to validate structured extraction.
        var result = Kreuzberg.extractFile("yaml/simple.yaml");
        assertEquals("application/x-yaml", result.mimeType().trim());
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

}
