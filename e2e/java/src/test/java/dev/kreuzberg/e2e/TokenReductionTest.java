// This file is auto-generated by alef. DO NOT EDIT.
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: token_reduction. */
class TokenReductionTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testTokenReductionAggressive() throws Exception {
        // Tests aggressive token reduction mode significantly reduces content
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 5, "expected length >= 5");
        assertTrue(result.content().orElse("").length() <= 150, "expected length <= 150");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
    }

    @Test
    void testTokenReductionBasic() throws Exception {
        // Tests basic token reduction on PDF document
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 5, "expected length >= 5");
        assertTrue(result.content().orElse("").length() <= 200, "expected length <= 200");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
    }

    @Test
    void testTokenReductionLight() throws Exception {
        // Tests light token reduction mode preserves most content
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 10, "expected length >= 10");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
    }

    @Test
    void testTokenReductionReduceAggressive() throws Exception {
        // Reduce tokens with aggressive compression level
        var result = Kreuzberg.extractFile("");
        assertTrue(result.result().length() >= 1, "expected length >= 1");
    }

    @Test
    void testTokenReductionReduceBasic() throws Exception {
        // Reduce tokens with light compression level
        var result = Kreuzberg.extractFile("");
        assertTrue(result.result().contains("quick"), "expected to contain: " + "quick");
    }

    @Test
    void testTokenReductionWithChunking() throws Exception {
        // Tests token reduction combined with chunking
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", config);
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().orElse("").length() >= 5, "expected length >= 5");
        assertTrue(result.content().orElse("").length() <= 200, "expected length <= 200");
        assertFalse(result.content().orElse("").isEmpty(), "expected non-empty value");
        assertTrue(result.chunks().size() >= 1, "expected at least 1 elements");
        assertTrue(result.chunksHaveContent(), "expected true");
    }

}
