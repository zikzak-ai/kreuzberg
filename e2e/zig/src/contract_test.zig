// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0fd63a5e2a69221011ade693151d25a021bff8be7adad3c2b9d6168a7b74f9d8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: contract

test "config_document_structure_with_headings" {
    // Tests document structure with DOCX heading-driven nesting
    const result = try kreuzberg.extract_file_sync("docx/fake.docx", null, std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type);
    try testing.expect(result.document.len > 0);
    try testing.expect(result.document.nodes.len >= 1);
}

test "config_extraction_timeout" {
    // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
    const result = try kreuzberg.extract_file_sync("pdf/fake_memo.pdf", null, std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "config_security_limits" {
    // Tests archive extraction with custom security limits
    const result = try kreuzberg.extract_file_sync("archives/documents.zip", null, std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/zip") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-zip-compressed") != null);
    try testing.expect(result.content.len >= 10);
}

test "output_format_bytes_markdown" {
    // Tests markdown output format via bytes extraction API
    const result = try kreuzberg.extract_bytes_sync("pdf/fake_memo.pdf", "application/pdf", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.metadata.output_format);
}

test "output_format_markdown" {
    // Tests Markdown output format
    const result = try kreuzberg.extract_file_sync("pdf/fake_memo.pdf", null, std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.metadata.output_format);
}
