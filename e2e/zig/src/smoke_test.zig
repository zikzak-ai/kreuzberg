// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:36fd8eac3e06a6437c6b759bcda926be2d083a41758d72971058b9307f313a42
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: smoke

test "ocr_image_png" {
    // OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.
    const result = try kreuzberg.extract_bytes_sync("images/test_hello_world.png", "image/png", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("image/png", result.mime_type);
    try testing.expect(result.content.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.content, "Hello") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "World") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "hello") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "world") != null);
}

test "smoke_docx_basic" {
    // Smoke test: DOCX with formatted text
    const result = try kreuzberg.extract_file_sync("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type);
    try testing.expect(result.content.len >= 20);
    try testing.expect(std.mem.indexOf(u8, result.content, "Lorem") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "ipsum") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "document") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "text") != null);
}

test "smoke_html_basic" {
    // Smoke test: HTML table extraction
    const result = try kreuzberg.extract_file_sync("html/simple_table.html", "text/html", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("text/html", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "Sample Data Table") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Laptop") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Electronics") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Product") != null);
}

test "smoke_json_basic" {
    // Smoke test: JSON file extraction
    const result = try kreuzberg.extract_file_sync("json/simple.json", "application/json", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/json", result.mime_type);
    try testing.expect(result.content.len >= 5);
}

test "smoke_pdf_basic" {
    // Smoke test: PDF with simple text extraction
    const result = try kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "application/pdf", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 50);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "To Whom it May Concern") != null);
}

test "smoke_txt_basic" {
    // Smoke test: Plain text file
    const result = try kreuzberg.extract_file_sync("text/report.txt", "text/plain", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("text/plain", result.mime_type);
    try testing.expect(result.content.len >= 5);
}

test "smoke_xlsx_basic" {
    // Smoke test: XLSX with basic spreadsheet data including tables
    const result = try kreuzberg.extract_file_sync("xlsx/stanley_cups.xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", std.mem.zeroInit(kreuzberg.ExtractionConfig, .{ .output_format = kreuzberg.OutputFormat{ .plain = {} } }));
    try testing.expectEqual("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mime_type);
    try testing.expect(result.content.len >= 100);
    try testing.expect(std.mem.indexOf(u8, result.content, "Team") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Location") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Stanley Cups") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Blues") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Flyers") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Maple Leafs") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "STL") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "PHI") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "TOR") != null);
    try testing.expect(result.tables.len >= 1);
    try testing.expect(result.metadata.format.?.excel.?.sheet_count.? >= 2);
    try testing.expect(std.mem.indexOf(u8, result.metadata.format.?.excel.?.sheet_names, "Stanley Cups") != null);
}
