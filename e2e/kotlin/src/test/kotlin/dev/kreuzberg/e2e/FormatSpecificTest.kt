// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1d2c4b115b976d81ac74e7366cb7f8904a7886e8f016ef8c96fdaa30623a2982
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import dev.kreuzberg.Kreuzberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.ExtractionConfig

/** E2e tests for category: format_specific. */
class FormatSpecificTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testFormatDocxStandalone() {
        // Standalone DOCX extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(java.nio.file.Files.readAllBytes(java.nio.file.Path.of("docx/fake.docx")), "application/vnd.openxmlformats-officedocument.wordprocessingml.document", ExtractionConfig.builder().build())
        assertTrue(result.content().length >= 20, "expected length >= 20")
    }

    @Test
    fun testFormatHwpxStandalone() {
        // Standalone HWPX extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(java.nio.file.Files.readAllBytes(java.nio.file.Path.of("hwpx/simple.hwpx")), "application/haansofthwpx", ExtractionConfig.builder().build())
        assertTrue(result.content().length >= 20, "expected length >= 20")
        assertTrue(result.content().contains("Hello from HWPX"), "expected to contain: " + "Hello from HWPX")
    }

    @Test
    fun testFormatPdfText() {
        // Standalone PDF text extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(java.nio.file.Files.readAllBytes(java.nio.file.Path.of("pdf/fake_memo.pdf")), "application/pdf", ExtractionConfig.builder().build())
        assertTrue(result.content().length >= 50, "expected length >= 50")
        assertTrue(result.content().contains("Mallori") || result.content().contains("May"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testFormatPptx() {
        // PPTX presentation extraction using extract_file_sync
        val result = Kreuzberg.extractFileSync(java.nio.file.Path.of("pptx/simple.pptx"), "application/vnd.openxmlformats-officedocument.presentationml.presentation", ExtractionConfig.builder().build())
    }

    @Test
    fun testFormatXlsx() {
        // XLSX spreadsheet extraction using extract_file_sync
        val result = Kreuzberg.extractFileSync(java.nio.file.Path.of("xlsx/stanley_cups.xlsx"), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", ExtractionConfig.builder().build())
    }

}
