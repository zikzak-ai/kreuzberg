// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang rust

// Tests for keywords fixtures.
#![allow(clippy::too_many_lines)]
use e2e_rust::{assertions, resolve_document};
use kreuzberg::KreuzbergError;
use kreuzberg::core::config::ExtractionConfig;
#[test]
fn test_keywords_rake() {
    // Tests keyword extraction using RAKE algorithm

    let document_path = resolve_document("pdf/fake_memo.pdf");
    if !document_path.exists() {
        println!(
            "Skipping keywords_rake: missing document at {}",
            document_path.display()
        );
        return;
    }
    let config: ExtractionConfig = serde_json::from_str(
        r#"{
  "keywords": {
    "algorithm": "rake",
    "max_keywords": 10
  }
}"#,
    )
    .expect("Fixture config should deserialize");

    let result = match kreuzberg::extract_file_sync(&document_path, None, &config) {
        Err(KreuzbergError::MissingDependency(dep)) => {
            println!("Skipping keywords_rake: missing dependency {dep}", dep = dep);
            return;
        }
        Err(KreuzbergError::UnsupportedFormat(fmt)) => {
            println!(
                "Skipping keywords_rake: unsupported format {fmt} (requires optional tool)",
                fmt = fmt
            );
            return;
        }
        Err(KreuzbergError::Parsing { message: ref msg, .. }) => {
            println!("Skipping keywords_rake: parsing dependency unavailable: {msg}");
            return;
        }
        Err(err) => panic!("Extraction failed for keywords_rake: {err:?}"),
        Ok(result) => result,
    };

    assertions::assert_expected_mime(&result, &["application/pdf"]);
    assertions::assert_min_content_length(&result, 10);
    assertions::assert_keywords(&result, Some(true), Some(1), Some(10));
}

#[test]
fn test_keywords_yake() {
    // Tests keyword extraction using YAKE algorithm

    let document_path = resolve_document("pdf/fake_memo.pdf");
    if !document_path.exists() {
        println!(
            "Skipping keywords_yake: missing document at {}",
            document_path.display()
        );
        return;
    }
    let config: ExtractionConfig = serde_json::from_str(
        r#"{
  "keywords": {
    "algorithm": "yake",
    "max_keywords": 10
  }
}"#,
    )
    .expect("Fixture config should deserialize");

    let result = match kreuzberg::extract_file_sync(&document_path, None, &config) {
        Err(KreuzbergError::MissingDependency(dep)) => {
            println!("Skipping keywords_yake: missing dependency {dep}", dep = dep);
            return;
        }
        Err(KreuzbergError::UnsupportedFormat(fmt)) => {
            println!(
                "Skipping keywords_yake: unsupported format {fmt} (requires optional tool)",
                fmt = fmt
            );
            return;
        }
        Err(KreuzbergError::Parsing { message: ref msg, .. }) => {
            println!("Skipping keywords_yake: parsing dependency unavailable: {msg}");
            return;
        }
        Err(err) => panic!("Extraction failed for keywords_yake: {err:?}"),
        Ok(result) => result,
    };

    assertions::assert_expected_mime(&result, &["application/pdf"]);
    assertions::assert_min_content_length(&result, 10);
    assertions::assert_keywords(&result, Some(true), Some(1), Some(10));
}
