// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f39b64c8ae3433ae913cfdf9137374000b3ffefe8643f2088ec743d5d7cef936
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: html

use kreuzberg::extract_file;

#[tokio::test]
async fn test_html_complex_layout() {
    // Large Wikipedia HTML page to validate complex conversion.
    let path = r#"html/taylor_swift.html"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/html"#, "equals assertion failed");
    assert!(
        result.content.len() >= 1000,
        "expected length >= 1000, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_html_simple_table() {
    // HTML table converted to markdown should retain structure.
    let path = r#"html/simple_table.html"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/html"#, "equals assertion failed");
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Product"#),
        "expected to contain: {}",
        r#"Product"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Category"#),
        "expected to contain: {}",
        r#"Category"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Price"#),
        "expected to contain: {}",
        r#"Price"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Stock"#),
        "expected to contain: {}",
        r#"Stock"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Laptop"#),
        "expected to contain: {}",
        r#"Laptop"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Electronics"#),
        "expected to contain: {}",
        r#"Electronics"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Sample Data Table"#),
        "expected to contain: {}",
        r#"Sample Data Table"#
    );
}
