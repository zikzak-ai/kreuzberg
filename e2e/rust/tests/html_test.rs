// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: html

use kreuzberg::extract_file;

#[tokio::test]
async fn test_html_complex_layout() {
    // Large Wikipedia HTML page to validate complex conversion.
    let path = r#"html/taylor_swift.html"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/html"#, "equals assertion failed");
    assert!(
        result.content.len() >= 1000,
        "expected length >= 1000, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_html_simple_table() {
    // HTML table converted to markdown should retain structure.
    let path = r#"html/simple_table.html"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"text/html"#, "equals assertion failed");
    assert!(content.len() >= 100, "expected length >= 100, got {}", content.len());
    assert!(
        format!("{:?}", content).contains(r#"Product"#),
        "expected to contain: {}",
        r#"Product"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Category"#),
        "expected to contain: {}",
        r#"Category"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Price"#),
        "expected to contain: {}",
        r#"Price"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Stock"#),
        "expected to contain: {}",
        r#"Stock"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Laptop"#),
        "expected to contain: {}",
        r#"Laptop"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Electronics"#),
        "expected to contain: {}",
        r#"Electronics"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Sample Data Table"#),
        "expected to contain: {}",
        r#"Sample Data Table"#
    );
}
