// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: detection

use kreuzberg::{
    detect_image_format, detect_languages, detect_mime_type, detect_mime_type_from_bytes, detect_or_validate,
    get_extensions_for_mime, is_valid_format_field,
};

#[test]
fn test_detect_image_jpeg() {
    // Detect JPEG from bytes
    let data = r#"/9j/4AAQ"#;
    let _ = detect_image_format(data.as_bytes()).expect("should succeed");
}

#[test]
fn test_detect_image_png() {
    // Detect PNG image format
    let data = r#"images/hello_world.png"#;
    let _ = detect_image_format(data.as_bytes()).expect("should succeed");
}

#[test]
fn test_detect_lang_english() {
    // Detect English language in text
    let text = r#"The quick brown fox jumps over the lazy dog."#;
    let config = Default::default();
    let _ = detect_languages(&text, &config).expect("should succeed");
}

#[test]
fn test_detect_lang_french() {
    // Detect French language in text
    let text = r#"Bonjour le monde, comment allez-vous aujourd'hui?"#;
    let config = Default::default();
    let _ = detect_languages(&text, &config).expect("should succeed");
}

#[test]
fn test_detect_lang_mixed() {
    // Detect mixed languages in text
    let text = r#"Hello world. Bonjour le monde. Hallo Welt."#;
    let config = Default::default();
    let _ = detect_languages(&text, &config).expect("should succeed");
}

#[test]
fn test_detect_mime_bytes_html() {
    // Detect HTML MIME from bytes
    let content = r#"<!DOCTYPE html><html><body>Hello</body></html>"#;
    let _ = detect_mime_type_from_bytes(content.as_bytes()).expect("should succeed");
}

#[test]
fn test_detect_mime_bytes_pdf() {
    // Detect PDF MIME type from bytes
    let content = r#"pdf/fake_memo.pdf"#;
    let _ = detect_mime_type_from_bytes(content.as_bytes()).expect("should succeed");
}

#[test]
fn test_detect_mime_bytes_png() {
    // Detect PNG MIME type from bytes
    let content = r#"images/hello_world.png"#;
    let _ = detect_mime_type_from_bytes(content.as_bytes()).expect("should succeed");
}

#[test]
fn test_detect_mime_docx() {
    // Detect DOCX MIME type from extension
    let path = r#"document.docx"#;
    let check_exists = None;
    let _ = detect_mime_type(&path, check_exists).expect("should succeed");
}

#[test]
fn test_detect_mime_html() {
    // Detect HTML MIME type from extension
    let path = r#"page.html"#;
    let check_exists = None;
    let _ = detect_mime_type(&path, check_exists).expect("should succeed");
}

#[test]
fn test_detect_mime_not_found() {
    // detect_mime_type file not found
    let path = r#"/nonexistent/path/file.pdf"#;
    let check_exists = Some(true);
    let result = detect_mime_type(&path, check_exists);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_detect_mime_pdf() {
    // Detect PDF MIME type from extension
    let path = r#"test.pdf"#;
    let check_exists = None;
    let _ = detect_mime_type(&path, check_exists).expect("should succeed");
}

#[test]
fn test_detect_mime_unknown_ext() {
    // detect_mime_type unknown extension
    let path = r#"file.zzz_unknown"#;
    let check_exists = None;
    let result = detect_mime_type(&path, check_exists);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_detect_or_validate_both() {
    // detect_or_validate with both
    let path = Some(r#"file.pdf"#);
    let mime_type = Some(r#"application/pdf"#);
    let _ = detect_or_validate(&path, &mime_type).expect("should succeed");
}

#[test]
fn test_detect_or_validate_invalid_mime() {
    // detect_or_validate unsupported MIME
    let path = None;
    let mime_type = Some(r#"application/x-nonexistent"#);
    let result = detect_or_validate(&path, &mime_type);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_detect_or_validate_mime() {
    // Validate known MIME
    let path = None;
    let mime_type = Some(r#"application/pdf"#);
    let _ = detect_or_validate(&path, &mime_type).expect("should succeed");
}

#[test]
fn test_detect_or_validate_neither() {
    // detect_or_validate errors with no args
    let path = None;
    let mime_type = None;
    let result = detect_or_validate(&path, &mime_type);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_detect_or_validate_path() {
    // Detect MIME from path
    let path = Some(r#"document.pdf"#);
    let mime_type = None;
    let _ = detect_or_validate(&path, &mime_type).expect("should succeed");
}

#[test]
fn test_get_extensions_unknown_mime() {
    // get_extensions unknown MIME
    let mime_type = r#"application/x-totally-unknown"#;
    let result = get_extensions_for_mime(&mime_type);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_is_valid_format_field_false() {
    // Invalid format field
    let field = r#"nonexistent_field_xyz"#;
    let _ = is_valid_format_field(&field).expect("should succeed");
}

#[test]
fn test_is_valid_format_field_true() {
    // Valid format field
    let field = r#"content"#;
    let _ = is_valid_format_field(&field).expect("should succeed");
}
