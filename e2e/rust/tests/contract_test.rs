// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:138969ab9b4dc0bc3bd0785c415151485a8dad829ed952f5295b147a4fab2238
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: contract

use kreuzberg::{
    batch_extract_bytes, batch_extract_bytes_sync, batch_extract_file, batch_extract_file_sync, extract_bytes,
    extract_bytes_sync, extract_file, extract_file_sync,
};

#[tokio::test]
async fn test_api_batch_bytes_async() {
    // Tests async batch bytes extraction API (batch_extract_bytes)
    let items_json = serde_json::json!(null);
    let items = serde_json::from_value(items_json).unwrap();
    let config = Default::default();
    let result = batch_extract_bytes(&items, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_api_batch_bytes_sync() {
    // Tests sync batch bytes extraction API (batch_extract_bytes_sync)
    let items_json = serde_json::json!(null);
    let items = serde_json::from_value(items_json).unwrap();
    let config = Default::default();
    let result = batch_extract_bytes_sync(&items, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_api_batch_bytes_with_configs_async() {
    // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
    let items_json = serde_json::json!(null);
    let items = serde_json::from_value(items_json).unwrap();
    let config = Default::default();
    let result = batch_extract_bytes(&items, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"markdown"#, "equals assertion failed");
}

#[test]
fn test_api_batch_bytes_with_configs_sync() {
    // Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
    let items_json = serde_json::json!(null);
    let items = serde_json::from_value(items_json).unwrap();
    let config = Default::default();
    let result = batch_extract_bytes_sync(&items, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"markdown"#, "equals assertion failed");
}

#[tokio::test]
async fn test_api_batch_file_async() {
    // Tests async batch file extraction API (batch_extract_file)
    let paths_json = serde_json::json!(null);
    let paths = serde_json::from_value(paths_json).unwrap();
    let config = Default::default();
    let result = batch_extract_file(&paths, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_api_batch_file_sync() {
    // Tests sync batch file extraction API (batch_extract_file_sync)
    let paths_json = serde_json::json!(null);
    let paths = serde_json::from_value(paths_json).unwrap();
    let config = Default::default();
    let result = batch_extract_file_sync(&paths, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_api_batch_file_with_configs_async() {
    // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
    let paths_json = serde_json::json!(null);
    let paths = serde_json::from_value(paths_json).unwrap();
    let config = Default::default();
    let result = batch_extract_file(&paths, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"markdown"#, "equals assertion failed");
}

#[test]
fn test_api_batch_file_with_configs_sync() {
    // Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
    let paths_json = serde_json::json!(null);
    let paths = serde_json::from_value(paths_json).unwrap();
    let config = Default::default();
    let result = batch_extract_file_sync(&paths, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"markdown"#, "equals assertion failed");
}

#[test]
fn test_api_batch_file_with_timeout_sync() {
    // Tests sync batch file extraction with per-file timeout config override
    let paths_json = serde_json::json!(null);
    let paths = serde_json::from_value(paths_json).unwrap();
    let config_json = serde_json::json!({"extraction_timeout_secs": 300});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = batch_extract_file_sync(&paths, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_api_extract_bytes_async() {
    // Tests async bytes extraction API (extract_bytes)
    let data = Default::default();
    let mime_type = String::new();
    let config = Default::default();
    let result = extract_bytes(data, &mime_type, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_api_extract_bytes_sync() {
    // Tests sync bytes extraction API (extract_bytes_sync)
    let data = Default::default();
    let mime_type = String::new();
    let config = Default::default();
    let result = extract_bytes_sync(data, &mime_type, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_api_extract_file_async() {
    // Tests async file extraction API (extract_file)
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_api_extract_file_sync() {
    // Tests sync file extraction API (extract_file_sync)
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_config_acceleration_cpu_provider() {
    // Tests explicit CPU acceleration provider configuration
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"acceleration": {"device_id": 0, "provider": "cpu"}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"To Whom it May Concern"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_config_chunking() {
    // Tests chunking configuration with chunk assertions
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"chunking": {"max_chars": 500, "max_overlap": 50}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.chunks.is_empty(), "expected >= 1");
    assert!(result.chunks_have_content, "expected true");
}

#[test]
fn test_config_chunking_text() {
    // Tests text chunker type (generic whitespace/punctuation splitter)
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"chunking": {"chunker_type": "text", "max_chars": 500, "max_overlap": 50}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.chunks.is_empty(), "expected >= 1");
    assert!(result.chunks_have_content, "expected true");
}

#[test]
fn test_config_disable_ocr() {
    // Tests disable_ocr configuration option - OCR is skipped for images
    let path = r#"images/test_hello_world.png"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"disable_ocr": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"image/png"#, "equals assertion failed");
    assert!(
        result.content.len() <= 5,
        "expected length <= 5, got {}",
        result.content.len()
    );
}

#[test]
fn test_config_document_structure() {
    // Tests include_document_structure config produces document tree
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"include_document_structure": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(!result.document_structure.is_empty(), "expected non-empty value");
    assert!(!result.document_structure.nodes.is_empty(), "expected >= 1");
    assert!(
        format!("{:?}", result.document_structure.node_types).contains(r#"paragraph"#),
        "expected to contain: {}",
        r#"paragraph"#
    );
}

#[test]
fn test_config_document_structure_disabled() {
    // Tests document field is null when include_document_structure is false
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
}

#[test]
fn test_config_document_structure_with_headings() {
    // Tests document structure with DOCX heading-driven nesting
    let path = r#"docx/fake.docx"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"include_document_structure": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(!result.document_structure.is_empty(), "expected non-empty value");
    assert!(!result.document_structure.nodes.is_empty(), "expected >= 1");
}

#[tokio::test]
async fn test_config_email_msg_fallback_codepage() {
    // Tests MSG extraction with custom fallback codepage for Cyrillic
    let path = r#"email/fake_email.msg"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"email": {"msg_fallback_codepage": 1251}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.ms-outlook"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_config_extraction_timeout() {
    // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"extraction_timeout_secs": 300});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_config_html_options() {
    // Tests extraction with HTML conversion options configured
    let path = r#"html/complex_table.html"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"html_options": {"extract_metadata": true}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(
        result.mime_type.contains(r#"text/html"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.content.is_empty(), "expected non-empty value");
}

#[test]
fn test_config_html_styled_custom_css() {
    // HTML output with inline user CSS string is embedded verbatim in the style block
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"html_output": {"css": ".kb-p { color: red; }", "embed_css": true, "theme": "unstyled"}, "output_format": "html"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.output_format.trim(), r#"html"#, "equals assertion failed");
    assert!(
        format!("{:?}", result.content).contains(r#".kb-p { color: red; }"#),
        "expected to contain: {}",
        r#".kb-p { color: red; }"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"kb-doc"#),
        "expected to contain: {}",
        r#"kb-doc"#
    );
}

#[test]
fn test_config_html_styled_default() {
    // HTML output with default theme emits kb-* class hooks and an embedded style block
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json =
        serde_json::json!({"html_output": {"embed_css": true, "theme": "default"}, "output_format": "html"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert_eq!(result.output_format.trim(), r#"html"#, "equals assertion failed");
    assert!(
        format!("{:?}", result.content).contains(r#"kb-doc"#),
        "expected to contain: {}",
        r#"kb-doc"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"kb-content"#),
        "expected to contain: {}",
        r#"kb-content"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"kb-p"#),
        "expected to contain: {}",
        r#"kb-p"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"<style>"#),
        "expected to contain: {}",
        r#"<style>"#
    );
}

#[test]
fn test_config_html_styled_no_embed() {
    // HTML output with embed_css false emits class hooks but no style block
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json =
        serde_json::json!({"html_output": {"embed_css": false, "theme": "default"}, "output_format": "html"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.output_format.trim(), r#"html"#, "equals assertion failed");
    assert!(
        format!("{:?}", result.content).contains(r#"kb-doc"#),
        "expected to contain: {}",
        r#"kb-doc"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"kb-content"#),
        "expected to contain: {}",
        r#"kb-content"#
    );
}

#[test]
fn test_config_images() {
    // Tests image extraction configuration with image assertions
    let path = r#"pdf/embedded_images_tables.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"images": {"extract_images": true}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(!result.images.is_empty(), "expected >= 1");
}

#[test]
fn test_config_images_with_formats() {
    // Tests image extraction on PPTX containing embedded images
    let path = r#"pptx/powerpoint_with_image.pptx"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"images": {"extract_images": true}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.presentationml.presentation"#,
        "equals assertion failed"
    );
    assert!(!result.images.is_empty(), "expected >= 1");
}

#[test]
fn test_config_language_detection() {
    // Tests language detection configuration
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"language_detection": {"enabled": true}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        format!("{:?}", result.detected_languages).contains(r#"eng"#),
        "expected to contain: {}",
        r#"eng"#
    );
    assert!(result.language_detection_confidence >= 0.5_f64, "expected >= 0.5_f64");
}

#[test]
fn test_config_language_detection_multi() {
    // Tests language detection with detect_multiple and min_confidence options
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json =
        serde_json::json!({"language_detection": {"detect_multiple": true, "enabled": true, "min_confidence": 0.3}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        format!("{:?}", result.detected_languages).contains(r#"eng"#),
        "expected to contain: {}",
        r#"eng"#
    );
}

#[test]
fn test_config_postprocessor() {
    // Tests postprocessor config is accepted and extraction succeeds
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"postprocessor": {"enabled": true}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.content.is_empty(), "expected non-empty value");
}

#[test]
fn test_config_processing_warnings_empty() {
    // Tests that a clean PDF extraction produces no processing warnings
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(result.processing_warnings.is_empty(), "expected empty value");
}

#[test]
fn test_config_quality_disabled() {
    // Tests extraction with quality processing explicitly disabled
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"enable_quality_processing": false});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.content.is_empty(), "expected non-empty value");
}

#[test]
fn test_config_security_limits() {
    // Tests archive extraction with custom security limits
    let path = r#"archives/documents.zip"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"security_limits": {"max_archive_size": 104857600, "max_compression_ratio": 50, "max_files_in_archive": 100}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/zip"#) || result.mime_type.contains(r#"application/x-zip-compressed"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_config_tables_content() {
    // Tests table extraction with content_contains_any assertion on DOCX with tables
    let path = r#"docx/docx_tables.docx"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(!result.tables.is_empty(), "expected >= 1");
}

#[test]
fn test_config_use_cache_false() {
    // Tests use_cache=false configuration option
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"use_cache": false});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_output_format_bytes_markdown() {
    // Tests markdown output format via bytes extraction API
    let data = Default::default();
    let mime_type = String::new();
    let config_json = serde_json::json!({"output_format": "markdown"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_bytes_sync(data, &mime_type, &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"markdown"#, "equals assertion failed");
}

#[test]
fn test_output_format_djot() {
    // Tests Djot output format
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"output_format": "djot"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"djot"#, "equals assertion failed");
}

#[test]
fn test_output_format_html() {
    // Tests HTML output format
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"output_format": "html"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"html"#, "equals assertion failed");
}

#[test]
fn test_output_format_markdown() {
    // Tests Markdown output format
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"output_format": "markdown"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"markdown"#, "equals assertion failed");
}

#[test]
fn test_output_format_plain() {
    // Tests Plain output format
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"output_format": "plain"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.output_format.trim(), r#"plain"#, "equals assertion failed");
}

#[test]
fn test_result_format_element_based() {
    // Tests ElementBased result format with element assertions
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"result_format": "element_based"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert_eq!(
        result.result_format.trim(),
        r#"element_based"#,
        "equals assertion failed"
    );
    assert!(!result.elements.is_empty(), "expected >= 1");
}

#[test]
fn test_result_format_unified() {
    // Tests Unified result format (default)
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"result_format": "unified"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert_eq!(result.result_format.trim(), r#"unified"#, "equals assertion failed");
}
