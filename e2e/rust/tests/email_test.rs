// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1d1bcc7b53ef5649d8c56b05d177196ee06b5f6ce5610ac346422ea26a2cf8ea
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: email

use kreuzberg::extract_file;

#[tokio::test]
async fn test_email_eml_html_body() {
    // EML with HTML body content.
    let path = r#"email/html_only.eml"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"message/rfc822"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_email_eml_multipart() {
    // EML with multipart MIME content.
    let path = r#"email/html_email_multipart.eml"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"message/rfc822"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_email_eml_utf16() {
    // UTF-16 encoded EML file with BOM.
    let path = r#"vendored/unstructured/eml/fake-email-utf-16.eml"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"message/rfc822"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Test Email"#) || result.content.contains(r#"Roses are red"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_email_msg_basic() {
    // Outlook MSG file extraction.
    let path = r#"email/fake_email.msg"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.ms-outlook"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_email_pst_empty() {
    // Empty Outlook PST archive with no messages.
    let path = r#"email/empty.pst"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.ms-outlook-pst"#,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_email_sample_eml() {
    // Sample EML email file to verify email parsing.
    let path = r#"email/sample_email.eml"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"message/rfc822"#, "equals assertion failed");
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}
