// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: mime_utilities

use kreuzberg::{extract_file, get_extensions_for_mime, list_supported_formats};

#[tokio::test]
async fn test_mime_detect_bytes() {
    // Detect MIME type from file bytes
    let path = String::new();
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"pdf"#),
        "expected to contain: {}",
        r#"pdf"#
    );
}

#[tokio::test]
async fn test_mime_detect_image() {
    // Detect MIME type from PNG image bytes
    let path = String::new();
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"png"#),
        "expected to contain: {}",
        r#"png"#
    );
}

#[tokio::test]
async fn test_mime_detect_path() {
    // Detect MIME type from file path
    let path = String::new();
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"text"#),
        "expected to contain: {}",
        r#"text"#
    );
}

#[test]
fn test_mime_get_extensions() {
    // Get file extensions for a MIME type
    let mime_type = String::new();
    let result = get_extensions_for_mime(&mime_type).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"pdf"#),
        "expected to contain: {}",
        r#"pdf"#
    );
}

#[test]
fn test_mime_list_supported() {
    // List all supported MIME types and file extensions
    let _ = list_supported_formats().expect("should succeed");
}
