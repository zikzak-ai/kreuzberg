// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f65a3ced783deba7fbb6db09b417a18969753583c176b33880d42b943cdb793b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: validate

use kreuzberg::{
    extract_file_sync, validate_binarization_method, validate_chunking_params, validate_confidence, validate_host,
    validate_ocr_backend, validate_port, validate_token_reduction_level,
};

#[test]
fn test_validate_binarization_invalid() {
    // Invalid binarization
    let method = r#"nonexistent"#;
    let result = validate_binarization_method(&method);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_binarization_valid() {
    // Valid binarization
    let method = r#"otsu"#;
    let _ = validate_binarization_method(&method);
}

#[test]
fn test_validate_chunking_overlap_exceeds() {
    // Reject chunking config where overlap exceeds max_characters
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"chunking": {"max_characters": 10, "max_overlap": 100}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_chunking_params_invalid() {
    // Chunking overlap >= max
    let max_characters = 50;
    let overlap = 60;
    let result = validate_chunking_params(max_characters, overlap);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_chunking_params_valid() {
    // Valid chunking params
    let max_characters = 500;
    let overlap = 50;
    let _ = validate_chunking_params(max_characters, overlap);
}

#[test]
fn test_validate_chunking_valid() {
    // Accept valid chunking config
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"chunking": {"max_characters": 1000, "max_overlap": 200}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_validate_confidence_boundary() {
    // Confidence at 1.0
    let confidence = 1_f64;
    let _ = validate_confidence(confidence);
}

#[test]
fn test_validate_confidence_negative() {
    // Negative confidence
    let confidence = -0.5_f64;
    let result = validate_confidence(confidence);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_confidence_over() {
    // Confidence > 1
    let confidence = 1.5_f64;
    let result = validate_confidence(confidence);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_confidence_valid() {
    // Accept valid confidence threshold
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"ocr": {"confidence_threshold": 0.8}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let _ = extract_file_sync(&path, mime_type.as_deref(), &config);
}

#[test]
fn test_validate_confidence_zero() {
    // Confidence at 0.0
    let confidence = 0_f64;
    let _ = validate_confidence(confidence);
}

#[test]
fn test_validate_dpi_negative() {
    // Reject negative DPI in OCR config
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"ocr": {"dpi": -1}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_dpi_valid() {
    // Accept valid DPI in OCR config
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"ocr": {"dpi": 300}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let _ = extract_file_sync(&path, mime_type.as_deref(), &config);
}

#[test]
fn test_validate_host_empty() {
    // Empty host rejected
    let host = r#""#;
    let result = validate_host(&host);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_ocr_backend_invalid() {
    // Invalid OCR backend
    let backend = r#"nonexistent"#;
    let result = validate_ocr_backend(&backend);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_ocr_backend_valid() {
    // Valid OCR backend
    let backend = r#"tesseract"#;
    let _ = validate_ocr_backend(&backend);
}

#[test]
fn test_validate_output_format_invalid() {
    // Reject invalid output format
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"output_format": "nonexistent_format"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_output_format_valid() {
    // Accept valid output format
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"output_format": "markdown"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let _ = extract_file_sync(&path, mime_type.as_deref(), &config);
}

#[test]
fn test_validate_port_max() {
    // Port 65535 accepted
    let port = 65535;
    let _ = validate_port(port);
}

#[test]
fn test_validate_port_over() {
    // Port over 65535
    let port = 70000;
    let result = validate_port(port);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_port_zero() {
    // Port 0 rejected
    let port = 0;
    let result = validate_port(port);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_reduction_level_invalid() {
    // Invalid reduction level
    let level = r#"nonexistent"#;
    let result = validate_token_reduction_level(&level);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_reduction_level_valid() {
    // Valid reduction level
    let level = r#"moderate"#;
    let _ = validate_token_reduction_level(&level);
}
