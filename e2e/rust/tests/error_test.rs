// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: error

use kreuzberg::{extract_bytes_sync, extract_file, extract_file_sync, render_page};

#[test]
fn test_error_empty_bytes() {
    // Graceful handling of empty bytes (should not error)
    let data = r#"text/empty.txt"#;
    let mime_type = r#"text/plain"#;
    let config = Default::default();
    let _ = extract_bytes_sync(data.as_bytes(), &mime_type, &config).expect("should succeed");
}

#[test]
fn test_error_empty_mime() {
    // Error when extracting with empty MIME type
    let data = r#"text/plain.txt"#;
    let mime_type = r#""#;
    let config = Default::default();
    let result = extract_bytes_sync(data.as_bytes(), &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_extract_bytes_conflicting_ocr() {
    // extract_bytes force+disable OCR
    let data = r#"SGVsbG8="#;
    let mime_type = r#"text/plain"#;
    let config_json = serde_json::json!({"disable_ocr": true, "force_ocr": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_bytes_sync(data.as_bytes(), &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_file_not_found() {
    // Error when file does not exist (sync)
    let path = r#"nonexistent/file.pdf"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[tokio::test]
async fn test_error_file_not_found_async() {
    // Error when file does not exist (async)
    let path = r#"nonexistent/file.pdf"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await;
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_invalid_chunk_overlap() {
    // Error when chunk overlap exceeds max_characters
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"chunking": {"max_characters": 10, "overlap_characters": 100}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_invalid_confidence() {
    // Error when OCR confidence threshold is out of range
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"ocr": {"confidence_threshold": 2.0}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_invalid_dpi() {
    // Error when OCR DPI is invalid (negative)
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"ocr": {"dpi": -1}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_invalid_mime_format() {
    // Error when extracting with invalid MIME type format
    let data = r#"text/plain.txt"#;
    let mime_type = r#"not-a-mime"#;
    let config = Default::default();
    let result = extract_bytes_sync(data.as_bytes(), &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_invalid_ocr_language() {
    // Error when OCR language pack is not available
    let path = r#"images/balance_sheet_1.png"#;
    let mime_type = None;
    let config_json = serde_json::json!({"ocr": {"force_ocr": true, "language": "zzz_nonexistent"}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_pdf_corrupt() {
    // Error when PDF is truncated/corrupt
    let path = r#"pdf/corrupt_truncated.pdf"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_pdf_needs_password() {
    // Error when password-protected PDF is accessed without password
    let path = r#"pdf/password_protected.pdf"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_pdf_page_out_of_range() {
    // Graceful handling of out-of-range page extraction
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"pages": "999-1000"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let _ = extract_file_sync(&path, &mime_type, &config).expect("should succeed");
}

#[test]
fn test_error_pdf_wrong_password() {
    // Error when PDF password is incorrect
    let path = r#"pdf/password_protected.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"pdf": {"password": "wrongpassword"}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_render_corrupt_pdf() {
    // Error when rendering page from corrupt PDF
    let path = r#"pdf/corrupt_truncated.pdf"#;
    let page = 0;
    let config = Default::default();
    let result = render_page(&path, page, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_render_invalid_page() {
    // Error when rendering page number exceeds document page count
    let path = r#"pdf/fake_memo.pdf"#;
    let page = 999;
    let config = Default::default();
    let result = render_page(&path, page, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_security_archive_files() {
    // Error when archive file count exceeds security limit
    let path = r#"archives/documents.zip"#;
    let mime_type = None;
    let config_json = serde_json::json!({"security_limits": {"max_files_in_archive": 0}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_security_archive_size() {
    // Error when archive size exceeds security limit
    let path = r#"archives/documents.zip"#;
    let mime_type = None;
    let config_json = serde_json::json!({"security_limits": {"max_archive_size": 1}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_security_content_too_large() {
    // Error when content exceeds security limit for max size
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"security_limits": {"max_content_size": 10}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_security_nesting_depth() {
    // Error when XML nesting depth exceeds security limit
    let path = r#"html/complex_table.html"#;
    let mime_type = None;
    let config_json = serde_json::json!({"security_limits": {"max_nesting_depth": 1}});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_timeout_extraction() {
    // Error when extraction times out
    let path = r#"pdf/large.pdf"#;
    let mime_type = None;
    let config_json = serde_json::json!({"extraction_timeout_secs": 1});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_error_unsupported_mime() {
    // Error when extracting with unsupported MIME type
    let data = r#"text/plain.txt"#;
    let mime_type = r#"application/x-nonexistent"#;
    let config = Default::default();
    let result = extract_bytes_sync(data.as_bytes(), &mime_type, &config);
    assert!(result.is_err(), "expected call to fail");
}
