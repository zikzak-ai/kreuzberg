// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: configuration

use kreuzberg::extract_file;

#[tokio::test]
async fn test_config_discover() {
    // Discover configuration from current or parent directories
    let path = String::new();
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(!result.result.chunking.is_empty(), "expected non-empty value");
    assert_eq!(result.result.chunking.max_chars, 50, "equals assertion failed");
}

#[tokio::test]
async fn test_config_from_file() {
    // Load configuration from a TOML file
    let path = String::new();
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(!result.result.chunking.is_empty(), "expected non-empty value");
    assert_eq!(result.result.chunking.max_chars, 100, "equals assertion failed");
    assert_eq!(result.result.chunking.max_overlap, 20, "equals assertion failed");
    assert!(!result.result.language_detection.is_empty(), "expected non-empty value");
    assert!(!result.result.language_detection.enabled, "equals assertion failed");
}
