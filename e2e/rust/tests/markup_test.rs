// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ba3fda7cf58497522f08441c104233af4d84102204d664785a823fdef7a94e35
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: markup

use kreuzberg::{bold, code, italic, link, strikethrough, underline};

#[test]
fn test_bold_basic() {
    // Create bold markdown formatting
    let text = r#"important"#;
    let result = bold(&text).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"important"#),
        "expected to contain: {}",
        r#"important"#
    );
}

#[test]
fn test_bold_empty() {
    // Bold empty string
    let text = r#""#;
    let _ = bold(&text);
}

#[test]
fn test_bold_special_chars() {
    // Bold special chars
    let text = r#"hello **world** & <tag>"#;
    let result = bold(&text).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"hello"#),
        "expected to contain: {}",
        r#"hello"#
    );
}

#[test]
fn test_code_basic() {
    // Create inline code formatting
    let text = r#"variable"#;
    let result = code(&text).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"variable"#),
        "expected to contain: {}",
        r#"variable"#
    );
}

#[test]
fn test_code_multiline() {
    // Code multiline
    let text = r#"line1
line2"#;
    let _ = code(&text);
}

#[test]
fn test_italic_basic() {
    // Create italic markdown formatting
    let text = r#"emphasis"#;
    let result = italic(&text).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"emphasis"#),
        "expected to contain: {}",
        r#"emphasis"#
    );
}

#[test]
fn test_italic_empty() {
    // Italic empty string
    let text = r#""#;
    let _ = italic(&text);
}

#[test]
fn test_italic_special_chars() {
    // Italic special chars
    let text = r#"emphasized *text*"#;
    let _ = italic(&text);
}

#[test]
fn test_link_basic() {
    // Create markdown link with text and URL
    let text = r#"Click here"#;
    let url = r#"https://example.com"#;
    let result = link(&text, &url).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"Click here"#),
        "expected to contain: {}",
        r#"Click here"#
    );
    assert!(
        format!("{:?}", result.result).contains(r#"example.com"#),
        "expected to contain: {}",
        r#"example.com"#
    );
}

#[test]
fn test_link_empty_text() {
    // Create markdown link with empty text
    let text = r#""#;
    let url = r#"https://example.com"#;
    let result = link(&text, &url).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"example.com"#),
        "expected to contain: {}",
        r#"example.com"#
    );
}

#[test]
fn test_link_special_url() {
    // Link with query params
    let text = r#"click"#;
    let url = r#"https://example.com?q=1&b=2"#;
    let _ = link(&text, &url);
}

#[test]
fn test_strikethrough_basic() {
    // Create strikethrough formatting
    let text = r#"deleted"#;
    let result = strikethrough(&text).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"deleted"#),
        "expected to contain: {}",
        r#"deleted"#
    );
}

#[test]
fn test_strikethrough_empty() {
    // Strikethrough empty
    let text = r#""#;
    let _ = strikethrough(&text);
}

#[test]
fn test_underline_basic() {
    // Create underline formatting
    let text = r#"underlined"#;
    let result = underline(&text).expect("should succeed");
    assert!(
        format!("{:?}", result.result).contains(r#"underlined"#),
        "expected to contain: {}",
        r#"underlined"#
    );
}

#[test]
fn test_underline_empty() {
    // Underline empty
    let text = r#""#;
    let _ = underline(&text);
}
