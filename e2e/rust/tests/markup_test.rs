// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:21b846c30676576e237b3a5e3bc8f9d837283d814d404f1bcada24c3526c83ca
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: markup

use kreuzberg::{bold, code, italic, link, strikethrough, underline};

#[test]
fn test_bold_basic() {
    // Create bold markdown formatting
    let start = 0;
    let end = 9;
    let result = bold(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 9, "equals assertion failed");
}

#[test]
fn test_bold_empty() {
    // Bold empty string
    let start = 0;
    let end = 0;
    let _ = bold(start, end);
}

#[test]
fn test_bold_special_chars() {
    // Bold special chars
    let start = 0;
    let end = 23;
    let result = bold(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 23, "equals assertion failed");
}

#[test]
fn test_code_basic() {
    // Create inline code formatting
    let start = 0;
    let end = 8;
    let result = code(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 8, "equals assertion failed");
}

#[test]
fn test_code_multiline() {
    // Code multiline
    let start = 0;
    let end = 11;
    let result = code(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 11, "equals assertion failed");
}

#[test]
fn test_italic_basic() {
    // Create italic markdown formatting
    let start = 0;
    let end = 8;
    let result = italic(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 8, "equals assertion failed");
}

#[test]
fn test_italic_empty() {
    // Italic empty string
    let start = 0;
    let end = 0;
    let _ = italic(start, end);
}

#[test]
fn test_italic_special_chars() {
    // Italic special chars
    let start = 0;
    let end = 17;
    let result = italic(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 17, "equals assertion failed");
}

#[test]
fn test_link_basic() {
    // Create markdown link with text and URL
    let start = 0;
    let end = 10;
    let url = r#"https://example.com"#;
    let title: Option<String> = None;
    let result = link(start, end, url, title.as_deref());
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 10, "equals assertion failed");
}

#[test]
fn test_link_empty_text() {
    // Create markdown link with empty text
    let start = 0;
    let end = 0;
    let url = r#"https://example.com"#;
    let title: Option<String> = None;
    let result = link(start, end, url, title.as_deref());
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 0, "equals assertion failed");
}

#[test]
fn test_link_special_url() {
    // Link with query params
    let start = 0;
    let end = 5;
    let url = r#"https://example.com?q=1&b=2"#;
    let title: Option<String> = None;
    let result = link(start, end, url, title.as_deref());
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 5, "equals assertion failed");
}

#[test]
fn test_strikethrough_basic() {
    // Create strikethrough formatting
    let start = 0;
    let end = 7;
    let result = strikethrough(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 7, "equals assertion failed");
}

#[test]
fn test_strikethrough_empty() {
    // Strikethrough empty
    let start = 0;
    let end = 0;
    let _ = strikethrough(start, end);
}

#[test]
fn test_underline_basic() {
    // Create underline formatting
    let start = 0;
    let end = 10;
    let result = underline(start, end);
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 10, "equals assertion failed");
}

#[test]
fn test_underline_empty() {
    // Underline empty
    let start = 0;
    let end = 0;
    let _ = underline(start, end);
}
