// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8d8d52ec2dfc57dace3893432a511bf00358efc50c62f53c00bf8a5e2ff04c47
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: markup

use kreuzberg::extract_file;

#[tokio::test]
async fn test_bold_basic() {
    // Create bold markdown formatting
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 9, "equals assertion failed");
}

#[tokio::test]
async fn test_bold_empty() {
    // Bold empty string
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let _ = extract_file(path, mime_type.as_deref(), &config).await;
}

#[tokio::test]
async fn test_bold_special_chars() {
    // Bold special chars
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 23, "equals assertion failed");
}

#[tokio::test]
async fn test_code_basic() {
    // Create inline code formatting
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 8, "equals assertion failed");
}

#[tokio::test]
async fn test_code_multiline() {
    // Code multiline
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 11, "equals assertion failed");
}

#[tokio::test]
async fn test_italic_basic() {
    // Create italic markdown formatting
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 8, "equals assertion failed");
}

#[tokio::test]
async fn test_italic_empty() {
    // Italic empty string
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let _ = extract_file(path, mime_type.as_deref(), &config).await;
}

#[tokio::test]
async fn test_italic_special_chars() {
    // Italic special chars
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 17, "equals assertion failed");
}

#[tokio::test]
async fn test_link_basic() {
    // Create markdown link with text and URL
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 10, "equals assertion failed");
}

#[tokio::test]
async fn test_link_empty_text() {
    // Create markdown link with empty text
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 0, "equals assertion failed");
}

#[tokio::test]
async fn test_link_special_url() {
    // Link with query params
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 5, "equals assertion failed");
}

#[tokio::test]
async fn test_strikethrough_basic() {
    // Create strikethrough formatting
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 7, "equals assertion failed");
}

#[tokio::test]
async fn test_strikethrough_empty() {
    // Strikethrough empty
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let _ = extract_file(path, mime_type.as_deref(), &config).await;
}

#[tokio::test]
async fn test_underline_basic() {
    // Create underline formatting
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.start, 0, "equals assertion failed");
    assert_eq!(result.end, 10, "equals assertion failed");
}

#[tokio::test]
async fn test_underline_empty() {
    // Underline empty
    let path = Default::default();
    let mime_type: Option<String> = None;
    let config = Default::default();
    let _ = extract_file(path, mime_type.as_deref(), &config).await;
}
