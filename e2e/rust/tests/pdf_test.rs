// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0513a45c0a3224b356bae2ff6a08144e6df5c289ea1855d4dd0992a73f007417
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: pdf

use kreuzberg::extract_file;

#[tokio::test]
async fn test_pdf_assembly_technical() {
    // Assembly language technical manual with large body of text.
    let path = r#"pdf/assembly_language_for_beginners_al4_b_en.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 5000,
        "expected length >= 5000, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"assembly"#)
            || result.content.contains(r#"register"#)
            || result.content.contains(r#"instruction"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_bayesian_data_analysis() {
    // Bayesian data analysis textbook PDF with large content volume.
    let path = r#"pdf/bayesian_data_analysis_third_edition_13th_feb_2020.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10000,
        "expected length >= 10000, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Bayesian"#)
            || result.content.contains(r#"probability"#)
            || result.content.contains(r#"distribution"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_code_and_formula() {
    // PDF containing code snippets and formulas should retain substantial content.
    let path = r#"pdf/code_and_formula.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_deep_learning() {
    // Deep learning textbook PDF to ensure long-form extraction quality.
    let path = r#"pdf/fundamentals_of_deep_learning_2014.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 1000,
        "expected length >= 1000, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"neural"#)
            || result.content.contains(r#"network"#)
            || result.content.contains(r#"deep learning"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_embedded_images() {
    // PDF with embedded images should extract text and tables when present.
    let path = r#"pdf/embedded_images_tables.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(!result.tables.is_empty(), "expected >= 0");
}

#[tokio::test]
async fn test_pdf_google_doc() {
    // Google Docs exported PDF to verify conversion fidelity.
    let path = r#"pdf/google_doc_document.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_large_ciml() {
    // Large machine learning textbook PDF to stress extraction length.
    let path = r#"pdf/a_course_in_machine_learning_ciml_v0_9_all.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10000,
        "expected length >= 10000, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"machine learning"#)
            || result.content.contains(r#"algorithm"#)
            || result.content.contains(r#"training"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_non_english_german() {
    // German technical PDF to ensure non-ASCII content extraction.
    let path = r#"pdf/5_level_paging_and_5_level_ept_intel_revision_1_1_may_2017.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Intel"#) || result.content.contains(r#"paging"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_right_to_left() {
    // Right-to-left language PDF to verify RTL extraction.
    let path = r#"pdf/right_to_left_01.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_simple_text() {
    // Simple text-heavy PDF should extract content without OCR or tables.
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#)
            || result.content.contains(r#"To Whom it May Concern"#)
            || result.content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_tables_large() {
    // Large PDF with extensive tables to stress table extraction.
    let path = r#"pdf/large.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 500,
        "expected length >= 500, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_tables_medium() {
    // Medium-sized PDF with multiple tables.
    let path = r#"pdf/medium.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_technical_stat_learning() {
    // Technical statistical learning PDF requiring substantial extraction.
    let path = r#"pdf/an_introduction_to_statistical_learning_with_applications_in_r_islr_sixth_printing.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10000,
        "expected length >= 10000, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"statistical"#)
            || result.content.contains(r#"regression"#)
            || result.content.contains(r#"learning"#),
        "expected to contain at least one of the specified values"
    );
}
