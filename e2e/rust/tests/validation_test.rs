// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bca4ab33982ccb81d86149e3f83fdbd994d362a454dfe6b605e9e40172e83c28
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: validation

use kreuzberg::{
    validate_confidence, validate_host, validate_language_code, validate_mime_type, validate_output_format,
    validate_port, validate_tesseract_oem, validate_tesseract_psm,
};

#[test]
fn test_validate_confidence_fn_invalid() {
    // Reject confidence threshold > 1.0
    let confidence = 2_f64;
    let result = validate_confidence(confidence);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_confidence_fn_valid() {
    // Accept valid confidence threshold
    let confidence = 0.95_f64;
    let _ = validate_confidence(confidence);
}

#[test]
fn test_validate_format_invalid() {
    // Reject invalid output format
    let format = r#"nonexistent"#;
    let result = validate_output_format(&format);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_format_valid() {
    // Accept valid output format
    let format = r#"markdown"#;
    let _ = validate_output_format(&format);
}

#[test]
fn test_validate_host_valid() {
    // Accept valid host address
    let host = r#"127.0.0.1"#;
    let _ = validate_host(&host);
}

#[test]
fn test_validate_lang_invalid() {
    // Reject invalid language code
    let code = r#"zzz_nonexistent_9999"#;
    let result = validate_language_code(&code);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_lang_valid() {
    // Accept valid language code
    let code = r#"eng"#;
    let _ = validate_language_code(&code);
}

#[test]
fn test_validate_mime_invalid() {
    // Reject invalid MIME type
    let mime_type = r#"invalid"#;
    let result = validate_mime_type(&mime_type);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_mime_valid() {
    // Accept valid MIME type
    let mime_type = r#"application/pdf"#;
    let _ = validate_mime_type(&mime_type);
}

#[test]
fn test_validate_oem_invalid() {
    // Reject invalid Tesseract OEM mode
    let oem = 999;
    let result = validate_tesseract_oem(oem);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_oem_valid() {
    // Accept valid Tesseract OEM mode
    let oem = 1;
    let _ = validate_tesseract_oem(oem);
}

#[test]
fn test_validate_port_invalid() {
    // Reject invalid port number (zero)
    let port = 0;
    let result = validate_port(port);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_port_valid() {
    // Accept valid port number
    let port = 8080;
    let _ = validate_port(port);
}

#[test]
fn test_validate_psm_invalid() {
    // Reject invalid Tesseract PSM mode
    let psm = 999;
    let result = validate_tesseract_psm(psm);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_validate_psm_valid() {
    // Accept valid Tesseract PSM mode
    let psm = 6;
    let _ = validate_tesseract_psm(psm);
}
