// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b2ac95a534d3bbce92842a7bbd665f6836e53d955de57d366f5d663ab8bc57c2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: chunking

use kreuzberg::{chunk_text, chunk_texts_batch};

#[test]
fn test_chunk_batch_basic() {
    // Batch chunking of multiple texts in a single call
    let texts_json = serde_json::json!(["Hello world. This is text one.", "Goodbye world. This is text two."]);
    let texts = serde_json::from_value(texts_json).unwrap();
    let config_json = serde_json::json!({"max_characters": 100, "max_overlap": 10});
    let config = serde_json::from_value(config_json).unwrap();
    let _ = chunk_texts_batch(&texts, &config);
}

#[test]
fn test_chunk_text_basic() {
    // Basic text chunking with small chunk size and overlap
    let text = r#"The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text."#;
    let config_json = serde_json::json!({"max_characters": 50, "max_overlap": 10});
    let config = serde_json::from_value(config_json).unwrap();
    let _ = chunk_text(&text, &config);
}

#[test]
fn test_chunk_text_large() {
    // Text chunking with large chunk size to test handling of long content
    let text = r#"The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text."#;
    let config_json = serde_json::json!({"max_characters": 1000, "max_overlap": 10});
    let config = serde_json::from_value(config_json).unwrap();
    let _ = chunk_text(&text, &config);
}

#[test]
fn test_chunk_text_no_overlap() {
    // Text chunking with no overlap between chunks
    let text = r#"The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text."#;
    let config_json = serde_json::json!({"max_characters": 50, "max_overlap": 0});
    let config = serde_json::from_value(config_json).unwrap();
    let _ = chunk_text(&text, &config);
}

#[test]
fn test_chunk_text_overlap_exceeds() {
    // chunk_text overlap >= max
    let text = r#"Some text."#;
    let config_json = serde_json::json!({"max_characters": 50, "overlap": 60});
    let config = serde_json::from_value(config_json).unwrap();
    let result = chunk_text(&text, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_chunk_text_yaml() {
    // Chunk with YAML chunker
    let text = r#"key1: value1
key2: value2
---
key3: value3"#;
    let config_json = serde_json::json!({"chunker_type": "yaml", "max_characters": 50});
    let config = serde_json::from_value(config_json).unwrap();
    let _ = chunk_text(&text, &config);
}

#[test]
fn test_chunk_text_zero_max() {
    // chunk_text max_characters zero
    let text = r#"Some text."#;
    let config_json = serde_json::json!({"max_characters": 0});
    let config = serde_json::from_value(config_json).unwrap();
    let result = chunk_text(&text, &config);
    assert!(result.is_err(), "expected call to fail");
}

#[test]
fn test_chunk_texts_batch_multiple() {
    // Batch chunk multiple texts
    let texts_json = serde_json::json!(["First doc text.", "Second doc text."]);
    let texts = serde_json::from_value(texts_json).unwrap();
    let config_json = serde_json::json!({"max_characters": 100});
    let config = serde_json::from_value(config_json).unwrap();
    let _ = chunk_texts_batch(&texts, &config);
}
