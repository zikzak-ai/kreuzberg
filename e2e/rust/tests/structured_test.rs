// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: structured

use kreuzberg::extract_file;

#[tokio::test]
async fn test_structured_csv_basic() {
    // CSV data file extraction.
    let path = r#"csv/stanley_cups.csv"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/csv"#, "equals assertion failed");
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_structured_enw_basic() {
    // EndNote ENW citation format extraction.
    let path = r#"data_formats/sample.enw"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-endnote-refer"#)
            || result.mime_type.contains(r#"application/x-endnote+xml"#)
            || result.mime_type.contains(r#"text/plain"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_structured_json_basic() {
    // Structured JSON extraction should stream and preserve content.
    let path = r#"json/sample_document.json"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/json"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 20, "expected length >= 20, got {}", content.len());
    assert!(
        content.contains(r#"Sample Document"#) || content.contains(r#"Test Author"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_structured_json_simple() {
    // Simple JSON document to verify structured extraction.
    let path = r#"json/simple.json"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/json"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 10, "expected length >= 10, got {}", content.len());
    assert!(
        content.contains(r#"{"#) || content.contains(r#"name"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_structured_nbib_basic() {
    // PubMed NBIB citation format extraction.
    let path = r#"data_formats/sample.nbib"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/nbib"#)
            || result.mime_type.contains(r#"application/x-pubmed"#)
            || result.mime_type.contains(r#"text/plain"#),
        "expected to contain at least one of the specified values"
    );
    assert!(result.content.is_some(), "expected content to be present");
}

#[tokio::test]
async fn test_structured_ris_basic() {
    // RIS citation format extraction.
    let path = r#"data_formats/sample.ris"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-research-info-systems"#)
            || result.mime_type.contains(r#"text/plain"#),
        "expected to contain at least one of the specified values"
    );
    assert!(result.content.is_some(), "expected content to be present");
}

#[tokio::test]
async fn test_structured_toml_basic() {
    // TOML configuration file extraction.
    let path = r#"data_formats/cargo.toml"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/toml"#) || result.mime_type.contains(r#"text/toml"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_structured_tsv_basic() {
    // TSV (tab-separated values) data file extraction.
    let path = r#"data_formats/employees.tsv"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"text/tab-separated-values"#) || result.mime_type.contains(r#"text/plain"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_structured_yaml_basic() {
    // YAML file text extraction.
    let path = r#"yaml/simple.yaml"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/yaml"#)
            || result.mime_type.contains(r#"text/yaml"#)
            || result.mime_type.contains(r#"text/x-yaml"#)
            || result.mime_type.contains(r#"application/x-yaml"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_structured_yaml_simple() {
    // Simple YAML document to validate structured extraction.
    let path = r#"yaml/simple.yaml"#;
    let mime_type = None;
    let config = Default::default();
    let result = extract_file(&path, &mime_type, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/x-yaml"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}
