// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8b4dbe8e0c610e595e2c870f5963a40dccea0b962e57b166df627b716a865c6f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: document

use kreuzberg::extract_file_sync;

#[test]
fn test_doc_metadata_docx() {
    // Extract DOCX metadata
    let path = r#"vendored/markitdown/test.docx"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(!result.metadata.is_empty(), "expected non-empty value");
}

#[test]
fn test_doc_metadata_pdf() {
    // Extract PDF metadata
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(!result.metadata.is_empty(), "expected non-empty value");
}

#[test]
fn test_doc_structure_docx() {
    // Extract DOCX document structure with include_document_structure config
    let path = r#"vendored/markitdown/test.docx"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"include_document_structure": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(!result.document_structure.is_empty(), "expected non-empty value");
}

#[test]
fn test_doc_structure_html() {
    // Extract HTML document structure with include_document_structure config
    let path = r#"html/html.html"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"include_document_structure": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(!result.document_structure.is_empty(), "expected non-empty value");
}

#[test]
fn test_doc_structure_pdf() {
    // Extract PDF document structure with include_document_structure config
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"include_document_structure": true});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(!result.document_structure.is_empty(), "expected non-empty value");
}
