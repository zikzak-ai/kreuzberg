// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2bb40d9f33f351051605f5fd55a9610218e8aa3af5ee7c9a4f53459f44319179
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: async

use kreuzberg::{extract_bytes, extract_file};

#[tokio::test]
async fn test_async_extract_bytes() {
    // Async extract_bytes call on PDF document
    let data = Default::default();
    let mime_type = r#"application/pdf"#;
    let config = Default::default();
    let result = extract_bytes(data, &mime_type, &config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_async_extract_bytes_empty_mime() {
    // extract_bytes empty MIME async
    let data = r#"SGVsbG8="#;
    let mime_type = r#""#;
    let config_json = serde_json::json!({});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_bytes(data.as_bytes(), &mime_type, &config).await;
    assert!(result.is_err(), "expected call to fail");
}

#[tokio::test]
async fn test_async_extract_bytes_invalid_mime() {
    // extract_bytes unsupported MIME async
    let data = r#"SGVsbG8="#;
    let mime_type = r#"application/x-nonexistent"#;
    let config_json = serde_json::json!({});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_bytes(data.as_bytes(), &mime_type, &config).await;
    assert!(result.is_err(), "expected call to fail");
}

#[tokio::test]
async fn test_async_extract_file() {
    // Async extract_file call on PDF document
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = Some(r#"application/pdf"#);
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_async_extract_file_docx() {
    // Async extract_file call on DOCX document
    let path = r#"docx/fake.docx"#;
    let mime_type = Some(r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#);
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_async_extract_file_html() {
    // Async extract_file call on HTML document
    let path = r#"html/simple_table.html"#;
    let mime_type = Some(r#"text/html"#);
    let config = Default::default();
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_async_extract_file_image() {
    // Async extract_file call on image document
    let path = r#"images/test_hello_world.png"#;
    let mime_type = Some(r#"image/png"#);
    let config = Default::default();
    let _ = extract_file(&path, mime_type.as_deref(), &config).await;
}

#[tokio::test]
async fn test_async_extract_file_with_config() {
    // Async extract_file call with output format and chunking config
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type = Some(r#"application/pdf"#);
    let config_json =
        serde_json::json!({"chunking": {"max_characters": 500, "max_overlap": 50}, "output_format": "markdown"});
    let config = Some(serde_json::from_value(config_json).unwrap());
    let result = extract_file(&path, mime_type.as_deref(), &config)
        .await
        .expect("should succeed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}
