// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:41ef82fbde5430db802cc44b0489612fd038f332ec540126c089cf25987d3964
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: format_specific

use kreuzberg::{extract_bytes_sync, extract_file_sync};

#[test]
fn test_format_csv() {
    // CSV file extraction using extract_file_sync
    let path = r#"csv/data_table.csv"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[test]
fn test_format_docx_standalone() {
    // Standalone DOCX extraction using extract_bytes_sync
    let data = Default::default();
    let mime_type = r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#;
    let config = Default::default();
    let result = extract_bytes_sync(data, &mime_type, &config).expect("should succeed");
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[test]
fn test_format_email_eml() {
    // Email EML file extraction using extract_file_sync
    let path = r#"email/html_email_multipart.eml"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_format_pdf_text() {
    // Standalone PDF text extraction using extract_bytes_sync
    let data = Default::default();
    let mime_type = r#"application/pdf"#;
    let config = Default::default();
    let result = extract_bytes_sync(data, &mime_type, &config).expect("should succeed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Mallori"#) || result.content.contains(r#"May"#),
        "expected to contain at least one of the specified values"
    );
}

#[test]
fn test_format_pptx() {
    // PPTX presentation extraction using extract_bytes_sync
    let data = Default::default();
    let mime_type = r#"application/vnd.openxmlformats-officedocument.presentationml.presentation"#;
    let config = Default::default();
    let _ = extract_bytes_sync(data, &mime_type, &config);
}

#[test]
fn test_format_rtf() {
    // RTF file extraction using extract_file_sync
    let path = r#"rtf/extraction_test.rtf"#;
    let mime_type: Option<String> = None;
    let config = Default::default();
    let result = extract_file_sync(&path, mime_type.as_deref(), &config).expect("should succeed");
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[test]
fn test_format_xlsx() {
    // XLSX spreadsheet extraction using extract_bytes_sync
    let data = Default::default();
    let mime_type = r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#;
    let config = Default::default();
    let _ = extract_bytes_sync(data, &mime_type, &config);
}
