// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b1d1ea3c46065bf7c85cb3762b2efc9940ce5312dfbb9ec61b5694b2d05d738d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: serialization

use kreuzberg::{extract_file_to_json, extract_file_to_toon};

#[test]
fn test_serialize_json_basic() {
    // Serialize extraction result to JSON
    let path = r#"test_documents/text/plain.txt"#;
    let config_json = serde_json::json!({});
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract_file_to_json(path, &config).expect("should succeed");
    assert!(result.len() >= 1, "expected length >= 1, got {}", result.len());
}

#[test]
fn test_serialize_json_pdf() {
    // Serialize PDF extraction to JSON
    let path = r#"test_documents/pdf/sample.pdf"#;
    let config_json = serde_json::json!({});
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract_file_to_json(path, &config).expect("should succeed");
    assert!(
        format!("{:?}", result).contains(r#"content"#),
        "expected to contain: {}",
        r#"content"#
    );
}

#[test]
fn test_serialize_toon_basic() {
    // Serialize extraction result to TOON format
    let path = r#"test_documents/text/plain.txt"#;
    let config_json = serde_json::json!({});
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract_file_to_toon(path, &config).expect("should succeed");
    assert!(result.len() >= 1, "expected length >= 1, got {}", result.len());
}

#[test]
fn test_serialize_toon_pdf() {
    // Serialize PDF extraction to TOON
    let path = r#"test_documents/pdf/sample.pdf"#;
    let config_json = serde_json::json!({});
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract_file_to_toon(path, &config).expect("should succeed");
    assert!(result.len() >= 10, "expected length >= 10, got {}", result.len());
}
