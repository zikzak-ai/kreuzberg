// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0f5b0bbfb06f681e69e74c6afe18351e6e60c2b52ad2394458c46df231adbe17
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: extraction

use kreuzberg::{extract_doc_text, extract_email_content, extract_pptx_from_bytes, extract_text_from_pdf};

#[test]
fn test_extract_doc_basic() {
    // Extract text from a DOC file containing unit test lists
    let content = r#"doc/unit_test_lists.doc"#;
    let result = extract_doc_text(content.as_bytes()).expect("should succeed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_extract_email_basic() {
    // Extract content from an email file in EML format
    let bytes = r#"email/html_email_multipart.eml"#;
    let _ = extract_email_content(bytes.as_bytes());
}

#[test]
fn test_extract_pdf_basic() {
    // Extract text from a basic PDF document containing memo content
    let pdf_bytes = r#"pdf/fake_memo.pdf"#;
    let result = extract_text_from_pdf(pdf_bytes.as_bytes()).expect("should succeed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[test]
fn test_extract_pdf_code() {
    // Extract text from PDF containing code snippets and formulas
    let pdf_bytes = r#"pdf/code_and_formula.pdf"#;
    let result = extract_text_from_pdf(pdf_bytes.as_bytes()).expect("should succeed");
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[test]
fn test_extract_pptx_basic() {
    // Extract text from a PowerPoint presentation file
    let bytes = r#"pptx/powerpoint_sample.pptx"#;
    let _ = extract_pptx_from_bytes(bytes.as_bytes());
}
