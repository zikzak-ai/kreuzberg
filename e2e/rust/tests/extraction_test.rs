// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:98692f45d15d8a690e33f2e55d2207b10f23f721b74c79a0119655707c586bf1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: extraction

use kreuzberg::{extract_doc_text, extract_email_content, extract_pptx_from_bytes, extract_text_from_pdf};

#[test]
fn test_extract_doc_basic() {
    // Extract text from a DOC file containing unit test lists
    let content = std::fs::read(concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/../../test_documents/",
        "doc/unit_test_lists.doc"
    ))
    .expect("fixture file should exist");
    let result = extract_doc_text(&content).expect("should succeed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[test]
fn test_extract_email_basic() {
    // Extract content from an email file in EML format
    let data = std::fs::read(concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/../../test_documents/",
        "email/html_email_multipart.eml"
    ))
    .expect("fixture file should exist");
    let mime_type = String::new();
    let fallback_codepage = None;
    let _ = extract_email_content(&data, &mime_type, fallback_codepage);
}

#[test]
fn test_extract_pdf_basic() {
    // Extract text from a basic PDF document containing memo content
    let pdf_bytes = std::fs::read(concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/../../test_documents/",
        "pdf/fake_memo.pdf"
    ))
    .expect("fixture file should exist");
    let result = extract_text_from_pdf(&pdf_bytes).expect("should succeed");
    assert!(result.len() >= 50, "expected length >= 50, got {}", result.len());
}

#[test]
fn test_extract_pdf_code() {
    // Extract text from PDF containing code snippets and formulas
    let pdf_bytes = std::fs::read(concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/../../test_documents/",
        "pdf/code_and_formula.pdf"
    ))
    .expect("fixture file should exist");
    let result = extract_text_from_pdf(&pdf_bytes).expect("should succeed");
    assert!(result.len() >= 20, "expected length >= 20, got {}", result.len());
}

#[test]
fn test_extract_pptx_basic() {
    // Extract text from a PowerPoint presentation file
    let bytes = std::fs::read(concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/../../test_documents/",
        "pptx/powerpoint_sample.pptx"
    ))
    .expect("fixture file should exist");
    let _ = extract_pptx_from_bytes(&bytes);
}
