<?php
// This file is auto-generated by alef. DO NOT EDIT.

declare(strict_types=1);

namespace Kreuzberg\E2e;

use PHPUnit\Framework\TestCase;
use Kreuzberg\Kreuzberg\Kreuzberg;

/** E2e tests for category: office. */
final class OfficeTest extends TestCase
{
    /** BibTeX bibliography extraction. */
    public function test_office_bibtex_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("bibtex/comprehensive.bib");
        $found = false;
        if (str_contains($result->mime_type, "application/x-bibtex")) { $found = true; }
        if (str_contains($result->mime_type, "text/x-bibtex")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** CommonMark (.commonmark) text extraction. */
    public function test_office_commonmark_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("markdown/sample.commonmark");
        $found = false;
        if (str_contains($result->mime_type, "text/markdown")) { $found = true; }
        if (str_contains($result->mime_type, "text/plain")) { $found = true; }
        if (str_contains($result->mime_type, "text/x-commonmark")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(5, strlen($result->content));
    }

    /** Djot markup text extraction. */
    public function test_office_djot_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("markdown/tables.djot");
        $found = false;
        if (str_contains($result->mime_type, "text/x-djot")) { $found = true; }
        if (str_contains($result->mime_type, "text/djot")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** DocBook XML document extraction. */
    public function test_office_docbook_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docbook/docbook-reader.docbook");
        $found = false;
        if (str_contains($result->mime_type, "application/docbook+xml")) { $found = true; }
        if (str_contains($result->mime_type, "text/docbook")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** DOCX document extraction baseline. */
    public function test_office_docx_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/sample_document.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** DOCX file containing equations to validate math extraction. */
    public function test_office_docx_equations(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/equations.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** Simple DOCX document to verify baseline extraction. */
    public function test_office_docx_fake(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/fake.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** DOCX document heavy on formatting for style preservation. */
    public function test_office_docx_formatting(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/unit_test_formatting.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** DOCX document with complex headers. */
    public function test_office_docx_headers(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/unit_test_headers.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** DOCX document emphasizing list formatting. */
    public function test_office_docx_lists(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/unit_test_lists.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** DOCX document containing tables for table-aware extraction. */
    public function test_office_docx_tables(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("docx/docx_tables.docx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", $result->mime_type);
        $this->assertGreaterThanOrEqual(50, strlen($result->content));
        $this->assertStringContainsString("Simple uniform table", $result->content);
        $this->assertStringContainsString("Nested Table", $result->content);
        $this->assertStringContainsString("merged cells", $result->content);
        $this->assertStringContainsString("Header Col", $result->content);
        $this->assertGreaterThanOrEqual(1, count($result->tables));
    }

    /** EPUB book extraction with text content. */
    public function test_office_epub_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("epub/features.epub");
        $this->assertEquals("application/epub+zip", $result->mime_type);
        $this->assertGreaterThanOrEqual(50, strlen($result->content));
    }

    /** FictionBook (FB2) document extraction baseline. */
    public function test_office_fb2_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("fictionbook/basic.fb2");
        $this->assertEquals("application/x-fictionbook+xml", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** FictionBook (.fb2) text extraction. */
    public function test_office_fictionbook_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("fictionbook/basic.fb2");
        $found = false;
        if (str_contains($result->mime_type, "application/x-fictionbook+xml")) { $found = true; }
        if (str_contains($result->mime_type, "application/x-fictionbook")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** JATS scientific article extraction. */
    public function test_office_jats_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("jats/sample_article.jats");
        $found = false;
        if (str_contains($result->mime_type, "application/x-jats+xml")) { $found = true; }
        if (str_contains($result->mime_type, "text/jats")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Jupyter notebook extraction. */
    public function test_office_jupyter_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("jupyter/rank.ipynb");
        $this->assertEquals("application/x-ipynb+json", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Keynote document extraction baseline. */
    public function test_office_keynote_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("iwork/test.key");
        $this->assertEquals("application/x-iwork-keynote-sffkey", $result->mime_type);
        $this->assertGreaterThanOrEqual(5, strlen($result->content));
    }

    /** LaTeX document text extraction. */
    public function test_office_latex_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("latex/basic_sections.tex");
        $found = false;
        if (str_contains($result->mime_type, "application/x-latex")) { $found = true; }
        if (str_contains($result->mime_type, "text/x-latex")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** Markdown document extraction baseline. */
    public function test_office_markdown_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("markdown/comprehensive.md");
        $this->assertEquals("text/markdown", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Numbers document extraction baseline. */
    public function test_office_numbers_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("iwork/test.numbers");
        $this->assertEquals("application/x-iwork-numbers-sffnumbers", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Basic ODS spreadsheet extraction. */
    public function test_office_ods_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("data_formats/test_01.ods");
        $this->assertEquals("application/vnd.oasis.opendocument.spreadsheet", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** ODT document with bold formatting. */
    public function test_office_odt_bold(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("odt/bold.odt");
        $this->assertEquals("application/vnd.oasis.opendocument.text", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** ODT document containing unordered lists with nesting. */
    public function test_office_odt_list(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("odt/unorderedList.odt");
        $this->assertEquals("application/vnd.oasis.opendocument.text", $result->mime_type);
        $this->assertGreaterThanOrEqual(30, strlen($result->content));
        $found = false;
        if (str_contains($result->content, "list item")) { $found = true; }
        if (str_contains($result->content, "New level")) { $found = true; }
        if (str_contains($result->content, "Pushed us")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
    }

    /** Basic ODT document with paragraphs and headings. */
    public function test_office_odt_simple(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("odt/simple.odt");
        $this->assertEquals("application/vnd.oasis.opendocument.text", $result->mime_type);
        $this->assertGreaterThanOrEqual(50, strlen($result->content));
        $found = false;
        if (str_contains($result->content, "favorite things")) { $found = true; }
        if (str_contains($result->content, "Parrots")) { $found = true; }
        if (str_contains($result->content, "Analysis")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
    }

    /** ODT document with a table structure. */
    public function test_office_odt_table(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("odt/table.odt");
        $this->assertEquals("application/vnd.oasis.opendocument.text", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
        $this->assertGreaterThanOrEqual(1, count($result->tables));
    }

    /** OPML outline document extraction. */
    public function test_office_opml_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("opml/outline.opml");
        $found = false;
        if (str_contains($result->mime_type, "application/xml+opml")) { $found = true; }
        if (str_contains($result->mime_type, "text/x-opml")) { $found = true; }
        if (str_contains($result->mime_type, "application/x-opml+xml")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Org-mode document text extraction. */
    public function test_office_org_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("org/comprehensive.org");
        $found = false;
        if (str_contains($result->mime_type, "text/x-org")) { $found = true; }
        if (str_contains($result->mime_type, "text/org")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** Pages document extraction baseline. */
    public function test_office_pages_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("iwork/test.pages");
        $this->assertEquals("application/x-iwork-pages-sffpages", $result->mime_type);
        $this->assertGreaterThanOrEqual(5, strlen($result->content));
    }

    /** PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2. */
    public function test_office_ppsx_slideshow(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("pptx/sample.ppsx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.presentationml.slideshow", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** PowerPoint macro-enabled presentation (.pptm) extraction. */
    public function test_office_pptm_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("pptx/powerpoint_with_image.pptm");
        $found = false;
        if (str_contains($result->mime_type, "application/vnd.ms-powerpoint.presentation.macroEnabled.12")) { $found = true; }
        if (str_contains($result->mime_type, "application/vnd.openxmlformats-officedocument.presentationml.presentation")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertNotEmpty($result->content);
    }

    /** PPTX deck should extract slides content. */
    public function test_office_pptx_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("pptx/simple.pptx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", $result->mime_type);
        $this->assertGreaterThanOrEqual(50, strlen($result->content));
    }

    /** PPTX presentation containing images to ensure metadata extraction. */
    public function test_office_pptx_images(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("pptx/powerpoint_with_image.pptx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", $result->mime_type);
        $this->assertGreaterThanOrEqual(15, strlen($result->content));
    }

    /** Pitch deck PPTX used to validate large slide extraction. */
    public function test_office_pptx_pitch_deck(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("pptx/pitch_deck_presentation.pptx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", $result->mime_type);
        $this->assertGreaterThanOrEqual(100, strlen($result->content));
    }

    /** reStructuredText document extraction. */
    public function test_office_rst_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("rst/restructured_text.rst");
        $found = false;
        if (str_contains($result->mime_type, "text/x-rst")) { $found = true; }
        if (str_contains($result->mime_type, "text/prs.fallenstein.rst")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** RTF document text extraction. */
    public function test_office_rtf_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("rtf/extraction_test.rtf");
        $found = false;
        if (str_contains($result->mime_type, "application/rtf")) { $found = true; }
        if (str_contains($result->mime_type, "text/rtf")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Typst document text extraction. */
    public function test_office_typst_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("typst/headings.typ");
        $found = false;
        if (str_contains($result->mime_type, "application/x-typst")) { $found = true; }
        if (str_contains($result->mime_type, "text/x-typst")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Legacy XLS spreadsheet to ensure backward compatibility. */
    public function test_office_xls_legacy(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("xls/test_excel.xls");
        $this->assertEquals("application/vnd.ms-excel", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Excel binary workbook (.xlsb) extraction. */
    public function test_office_xlsb_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("xlsx/test_xlsb.xlsb");
        $found = false;
        if (str_contains($result->mime_type, "application/vnd.ms-excel.sheet.binary.macroEnabled.12")) { $found = true; }
        if (str_contains($result->mime_type, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertNotEmpty($result->content);
    }

    /** Excel macro-enabled workbook (.xlsm) extraction. */
    public function test_office_xlsm_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("xlsx/test_01.xlsm");
        $found = false;
        if (str_contains($result->mime_type, "application/vnd.ms-excel.sheet.macroEnabled.12")) { $found = true; }
        if (str_contains($result->mime_type, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertNotEmpty($result->content);
    }

    /** XLSX spreadsheet should produce metadata and table content. */
    public function test_office_xlsx_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("xlsx/stanley_cups.xlsx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", $result->mime_type);
        $this->assertGreaterThanOrEqual(100, strlen($result->content));
        $this->assertStringContainsString("Team", $result->content);
        $this->assertStringContainsString("Location", $result->content);
        $this->assertStringContainsString("Stanley Cups", $result->content);
        $this->assertGreaterThanOrEqual(1, count($result->tables));
        $this->assertGreaterThanOrEqual(2, $result->metadata->sheet_count);
        $this->assertStringContainsString("Stanley Cups", $result->metadata->sheet_names);
    }

    /** XLSX workbook with multiple sheets. */
    public function test_office_xlsx_multi_sheet(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("xlsx/excel_multi_sheet.xlsx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
        $this->assertGreaterThanOrEqual(2, $result->metadata->sheet_count);
    }

    /** Simple XLSX spreadsheet shipped alongside office integration tests. */
    public function test_office_xlsx_office_example(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("xlsx/test_01.xlsx");
        $this->assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }
}
