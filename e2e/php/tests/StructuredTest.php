<?php
// This file is auto-generated by alef. DO NOT EDIT.

declare(strict_types=1);

namespace Kreuzberg\E2e;

use PHPUnit\Framework\TestCase;
use Kreuzberg\Kreuzberg\Kreuzberg;

/** E2e tests for category: structured. */
final class StructuredTest extends TestCase
{
    /** CSV data file extraction. */
    public function test_structured_csv_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("csv/stanley_cups.csv");
        $this->assertEquals("text/csv", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
    }

    /** EndNote ENW citation format extraction. */
    public function test_structured_enw_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("data_formats/sample.enw");
        $found = false;
        if (str_contains($result->mime_type, "application/x-endnote-refer")) { $found = true; }
        if (str_contains($result->mime_type, "application/x-endnote+xml")) { $found = true; }
        if (str_contains($result->mime_type, "text/plain")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
    }

    /** Structured JSON extraction should stream and preserve content. */
    public function test_structured_json_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("json/sample_document.json");
        $this->assertEquals("application/json", $result->mime_type);
        $this->assertGreaterThanOrEqual(20, strlen($result->content));
        $found = false;
        if (str_contains($result->content, "Sample Document")) { $found = true; }
        if (str_contains($result->content, "Test Author")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
    }

    /** Simple JSON document to verify structured extraction. */
    public function test_structured_json_simple(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("json/simple.json");
        $this->assertEquals("application/json", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
        $found = false;
        if (str_contains($result->content, "{")) { $found = true; }
        if (str_contains($result->content, "name")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
    }

    /** PubMed NBIB citation format extraction. */
    public function test_structured_nbib_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("data_formats/sample.nbib");
        $found = false;
        if (str_contains($result->mime_type, "application/nbib")) { $found = true; }
        if (str_contains($result->mime_type, "application/x-pubmed")) { $found = true; }
        if (str_contains($result->mime_type, "text/plain")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertNotEmpty($result->content);
    }

    /** RIS citation format extraction. */
    public function test_structured_ris_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("data_formats/sample.ris");
        $found = false;
        if (str_contains($result->mime_type, "application/x-research-info-systems")) { $found = true; }
        if (str_contains($result->mime_type, "text/plain")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertNotEmpty($result->content);
    }

    /** TOML configuration file extraction. */
    public function test_structured_toml_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("data_formats/cargo.toml");
        $found = false;
        if (str_contains($result->mime_type, "application/toml")) { $found = true; }
        if (str_contains($result->mime_type, "text/toml")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** TSV (tab-separated values) data file extraction. */
    public function test_structured_tsv_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("data_formats/employees.tsv");
        $found = false;
        if (str_contains($result->mime_type, "text/tab-separated-values")) { $found = true; }
        if (str_contains($result->mime_type, "text/plain")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** YAML file text extraction. */
    public function test_structured_yaml_basic(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("yaml/simple.yaml");
        $found = false;
        if (str_contains($result->mime_type, "application/yaml")) { $found = true; }
        if (str_contains($result->mime_type, "text/yaml")) { $found = true; }
        if (str_contains($result->mime_type, "text/x-yaml")) { $found = true; }
        if (str_contains($result->mime_type, "application/x-yaml")) { $found = true; }
        $this->assertTrue($found, 'expected to contain at least one of the specified values');
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }

    /** Simple YAML document to validate structured extraction. */
    public function test_structured_yaml_simple(): void
    {
        $result = Kreuzberg\Kreuzberg::extractFile_async("yaml/simple.yaml");
        $this->assertEquals("application/x-yaml", $result->mime_type);
        $this->assertGreaterThanOrEqual(10, strlen($result->content));
    }
}
