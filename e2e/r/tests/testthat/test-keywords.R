# Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
# To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang r
#
# Tests for keywords fixtures.
# nolint start

test_that("keywords_rake", {
  skip_if_feature_unavailable("keywords-rake")
  result <- run_fixture(
    "keywords_rake",
    "pdf/fake_memo.pdf",
    list(keywords = list(algorithm = "rake", max_keywords = 10L)),
    requirements = c("keywords-rake"),
    notes = NULL,
    skip_if_missing = TRUE
  )
      assert_expected_mime(result, c("application/pdf"))
      assert_min_content_length(result, 10L)
      assert_keywords(result, has_keywords = TRUE, min_count = 1L, max_count = 10L)
})

test_that("keywords_yake", {
  skip_if_feature_unavailable("keywords-yake")
  result <- run_fixture(
    "keywords_yake",
    "pdf/fake_memo.pdf",
    list(keywords = list(algorithm = "yake", max_keywords = 10L)),
    requirements = c("keywords-yake"),
    notes = NULL,
    skip_if_missing = TRUE
  )
      assert_expected_mime(result, c("application/pdf"))
      assert_min_content_length(result, 10L)
      assert_keywords(result, has_keywords = TRUE, min_count = 1L, max_count = 10L)
})
# nolint end
