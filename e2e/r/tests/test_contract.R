# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:b7dd6a949e481626160bb5e453595ef653b348431e3e7cbb470953d3dc7f9bc8
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: contract

test_that("api_batch_bytes_async: Tests async batch bytes extraction API (batch_extract_bytes)", {
  result <- batch_extract_bytes()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_batch_bytes_sync: Tests sync batch bytes extraction API (batch_extract_bytes_sync)", {
  result <- batch_extract_bytes_sync()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_batch_bytes_with_configs_async: Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)", {
  result <- batch_extract_bytes()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "markdown")
})

test_that("api_batch_bytes_with_configs_sync: Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)", {
  result <- batch_extract_bytes_sync()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "markdown")
})

test_that("api_batch_file_async: Tests async batch file extraction API (batch_extract_file)", {
  result <- batch_extract_file()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_batch_file_sync: Tests sync batch file extraction API (batch_extract_file_sync)", {
  result <- batch_extract_file_sync()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_batch_file_with_configs_async: Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)", {
  result <- batch_extract_file()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "markdown")
})

test_that("api_batch_file_with_configs_sync: Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)", {
  result <- batch_extract_file_sync()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "markdown")
})

test_that("api_batch_file_with_timeout_sync: Tests sync batch file extraction with per-file timeout config override", {
  result <- batch_extract_file_sync(config = list("extraction_timeout_secs" = 300))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
})

test_that("api_extract_bytes_async: Tests async bytes extraction API (extract_bytes)", {
  result <- extract_bytes()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_extract_bytes_sync: Tests sync bytes extraction API (extract_bytes_sync)", {
  result <- extract_bytes_sync()
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_extract_file_async: Tests async file extraction API (extract_file)", {
  result <- extract_file(path = "pdf/fake_memo.pdf")
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("api_extract_file_sync: Tests sync file extraction API (extract_file_sync)", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf")
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("May 5, 2023", "Mallori"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("config_acceleration_cpu_provider: Tests explicit CPU acceleration provider configuration", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("acceleration" = list("device_id" = 0, "provider" = "cpu")))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 50)
  expect_true(any(sapply(c("May 5, 2023", "To Whom it May Concern"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("config_chunking: Tests chunking configuration with chunk assertions", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("chunking" = list("max_chars" = 500, "max_overlap" = 50)))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(length(result$chunks) >= 1)
  expect_true(result$chunks_have_content)
})

test_that("config_chunking_text: Tests text chunker type (generic whitespace/punctuation splitter)", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("chunking" = list("chunker_type" = "text", "max_chars" = 500, "max_overlap" = 50)))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(length(result$chunks) >= 1)
  expect_true(result$chunks_have_content)
})

test_that("config_disable_ocr: Tests disable_ocr configuration option - OCR is skipped for images", {
  result <- extract_file_sync(path = "images/test_hello_world.png", config = list("disable_ocr" = TRUE))
  expect_equal(trimws(result$mime_type), "image/png")
  expect_true(nchar(result$content) <= 5)
})

test_that("config_document_structure: Tests include_document_structure config produces document tree", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("include_document_structure" = TRUE))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(if (is.character(result$document_structure)) nchar(result$document_structure) > 0 else length(result$document_structure) > 0)
  expect_true(length(result$document_structure$nodes) >= 1)
  expect_true(grepl("paragraph", result$document_structure$node_types, fixed = TRUE))
})

test_that("config_document_structure_disabled: Tests document field is null when include_document_structure is false", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf")
  expect_equal(trimws(result$mime_type), "application/pdf")
})

test_that("config_document_structure_with_headings: Tests document structure with DOCX heading-driven nesting", {
  result <- extract_file_sync(path = "docx/fake.docx", config = list("include_document_structure" = TRUE))
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(if (is.character(result$document_structure)) nchar(result$document_structure) > 0 else length(result$document_structure) > 0)
  expect_true(length(result$document_structure$nodes) >= 1)
})

test_that("config_email_msg_fallback_codepage: Tests MSG extraction with custom fallback codepage for Cyrillic", {
  result <- extract_file(path = "email/fake_email.msg", config = list("email" = list("msg_fallback_codepage" = 1251)))
  expect_equal(trimws(result$mime_type), "application/vnd.ms-outlook")
  expect_true(nchar(result$content) >= 10)
})

test_that("config_extraction_timeout: Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("extraction_timeout_secs" = 300))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
})

test_that("config_html_options: Tests extraction with HTML conversion options configured", {
  result <- extract_file_sync(path = "html/complex_table.html", config = list("html_options" = list("extract_metadata" = TRUE)))
  expect_true(any(sapply(c("text/html"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("config_html_styled_custom_css: HTML output with inline user CSS string is embedded verbatim in the style block", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("html_output" = list("css" = ".kb-p { color: red; }", "embed_css" = TRUE, "theme" = "unstyled"), "output_format" = "html"))
  expect_equal(trimws(result$output_format), "html")
  expect_true(grepl(".kb-p { color: red; }", result$content, fixed = TRUE))
  expect_true(grepl("kb-doc", result$content, fixed = TRUE))
})

test_that("config_html_styled_default: HTML output with default theme emits kb-* class hooks and an embedded style block", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("html_output" = list("embed_css" = TRUE, "theme" = "default"), "output_format" = "html"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_equal(trimws(result$output_format), "html")
  expect_true(grepl("kb-doc", result$content, fixed = TRUE))
  expect_true(grepl("kb-content", result$content, fixed = TRUE))
  expect_true(grepl("kb-p", result$content, fixed = TRUE))
  expect_true(grepl("<style>", result$content, fixed = TRUE))
})

test_that("config_html_styled_no_embed: HTML output with embed_css false emits class hooks but no style block", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("html_output" = list("embed_css" = FALSE, "theme" = "default"), "output_format" = "html"))
  expect_equal(trimws(result$output_format), "html")
  expect_true(grepl("kb-doc", result$content, fixed = TRUE))
  expect_true(grepl("kb-content", result$content, fixed = TRUE))
})

test_that("config_images: Tests image extraction configuration with image assertions", {
  result <- extract_file_sync(path = "pdf/embedded_images_tables.pdf", config = list("images" = list("extract_images" = TRUE)))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(length(result$images) >= 1)
})

test_that("config_images_with_formats: Tests image extraction on PPTX containing embedded images", {
  result <- extract_file_sync(path = "pptx/powerpoint_with_image.pptx", config = list("images" = list("extract_images" = TRUE)))
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
  expect_true(length(result$images) >= 1)
})

test_that("config_language_detection: Tests language detection configuration", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("language_detection" = list("enabled" = TRUE)))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(grepl("eng", result$detected_languages, fixed = TRUE))
  expect_true(result$language_detection_confidence >= 0.5)
})

test_that("config_language_detection_multi: Tests language detection with detect_multiple and min_confidence options", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("language_detection" = list("detect_multiple" = TRUE, "enabled" = TRUE, "min_confidence" = 0.3)))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(grepl("eng", result$detected_languages, fixed = TRUE))
})

test_that("config_postprocessor: Tests postprocessor config is accepted and extraction succeeds", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("postprocessor" = list("enabled" = TRUE)))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("config_processing_warnings_empty: Tests that a clean PDF extraction produces no processing warnings", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf")
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(result$processing_warnings, "")
})

test_that("config_quality_disabled: Tests extraction with quality processing explicitly disabled", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("enable_quality_processing" = FALSE))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("config_security_limits: Tests archive extraction with custom security limits", {
  result <- extract_file_sync(path = "archives/documents.zip", config = list("security_limits" = list("max_archive_size" = 104857600, "max_compression_ratio" = 50, "max_files_in_archive" = 100)))
  expect_true(any(sapply(c("application/zip", "application/x-zip-compressed"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("config_tables_content: Tests table extraction with content_contains_any assertion on DOCX with tables", {
  result <- extract_file_sync(path = "docx/docx_tables.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(length(result$tables) >= 1)
})

test_that("config_use_cache_false: Tests use_cache=false configuration option", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("use_cache" = FALSE))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
})

test_that("output_format_bytes_markdown: Tests markdown output format via bytes extraction API", {
  result <- extract_bytes_sync(config = list("output_format" = "markdown"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "markdown")
})

test_that("output_format_djot: Tests Djot output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "djot"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "djot")
})

test_that("output_format_html: Tests HTML output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "html"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "html")
})

test_that("output_format_markdown: Tests Markdown output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "markdown"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "markdown")
})

test_that("output_format_plain: Tests Plain output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "plain"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$output_format), "plain")
})

test_that("result_format_element_based: Tests ElementBased result format with element assertions", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("result_format" = "element_based"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_equal(trimws(result$result_format), "element_based")
  expect_true(length(result$elements) >= 1)
})

test_that("result_format_unified: Tests Unified result format (default)", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("result_format" = "unified"))
  expect_equal(trimws(result$mime_type), "application/pdf")
  expect_true(nchar(result$content) >= 10)
  expect_equal(trimws(result$result_format), "unified")
})
