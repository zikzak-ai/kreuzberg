# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e60882100fbfba615b74177ccf43721280c30b60971b06bc7489c501b4e6b7e8
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: chunking

test_that("chunk_batch_basic: Batch chunking of multiple texts in a single call", {
  result <- (list("config" = list("max_characters" = 100, "max_overlap" = 10), "texts" = c("hello world. this is text one.", "goodbye world. this is text two.")))
})

test_that("chunk_text_basic: Basic text chunking with small chunk size and overlap", {
  result <- (list("config" = list("max_characters" = 50, "max_overlap" = 10), "text" = "the quick brown fox jumps over the lazy dog. this is a long paragraph that should be split into multiple chunks when the chunk size is small enough. each chunk should contain coherent text."))
})

test_that("chunk_text_large: Text chunking with large chunk size to test handling of long content", {
  result <- (list("config" = list("max_characters" = 1000, "max_overlap" = 10), "text" = "the quick brown fox jumps over the lazy dog. this is a long paragraph that should be split into multiple chunks when the chunk size is small enough. each chunk should contain coherent text."))
})

test_that("chunk_text_no_overlap: Text chunking with no overlap between chunks", {
  result <- (list("config" = list("max_characters" = 50, "max_overlap" = 0), "text" = "the quick brown fox jumps over the lazy dog. this is a long paragraph that should be split into multiple chunks when the chunk size is small enough. each chunk should contain coherent text."))
})

test_that("chunk_text_overlap_exceeds: chunk_text overlap >= max", {
  expect_error((list("config" = list("max_characters" = 50, "overlap" = 60), "text" = "some text.")))
})

test_that("chunk_text_yaml: Chunk with YAML chunker", {
  result <- (list("config" = list("chunker_type" = "yaml", "max_characters" = 50), "text" = "key1: value1\nkey2: value2\n---\nkey3: value3"))
})

test_that("chunk_text_zero_max: chunk_text max_characters zero", {
  expect_error((list("config" = list("max_characters" = 0), "text" = "some text.")))
})

test_that("chunk_texts_batch_multiple: Batch chunk multiple texts", {
  result <- (list("config" = list("max_characters" = 100), "texts" = c("first doc text.", "second doc text.")))
})
