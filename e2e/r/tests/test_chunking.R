# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:b82c8a5c6dde2f8d782f292874e4ce057c9a8a7d866b34a5d2e55fd7f030e7fb
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: chunking

test_that("chunk_batch_basic: Batch chunking of multiple texts in a single call", {
  result <- chunk_texts_batch(texts = c("hello world. this is text one.", "goodbye world. this is text two."), config = list("max_characters" = 100, "max_overlap" = 10))
})

test_that("chunk_text_basic: Basic text chunking with small chunk size and overlap", {
  result <- chunk_text(text = "the quick brown fox jumps over the lazy dog. this is a long paragraph that should be split into multiple chunks when the chunk size is small enough. each chunk should contain coherent text.", config = list("max_characters" = 50, "max_overlap" = 10))
})

test_that("chunk_text_large: Text chunking with large chunk size to test handling of long content", {
  result <- chunk_text(text = "the quick brown fox jumps over the lazy dog. this is a long paragraph that should be split into multiple chunks when the chunk size is small enough. each chunk should contain coherent text.", config = list("max_characters" = 1000, "max_overlap" = 10))
})

test_that("chunk_text_no_overlap: Text chunking with no overlap between chunks", {
  result <- chunk_text(text = "the quick brown fox jumps over the lazy dog. this is a long paragraph that should be split into multiple chunks when the chunk size is small enough. each chunk should contain coherent text.", config = list("max_characters" = 50, "max_overlap" = 0))
})

test_that("chunk_text_overlap_exceeds: chunk_text overlap >= max", {
  expect_error(chunk_text(text = "some text.", config = list("max_characters" = 50, "overlap" = 60)))
})

test_that("chunk_text_yaml: Chunk with YAML chunker", {
  result <- chunk_text(text = "key1: value1\nkey2: value2\n---\nkey3: value3", config = list("chunker_type" = "yaml", "max_characters" = 50))
})

test_that("chunk_text_zero_max: chunk_text max_characters zero", {
  expect_error(chunk_text(text = "some text.", config = list("max_characters" = 0)))
})

test_that("chunk_texts_batch_multiple: Batch chunk multiple texts", {
  result <- chunk_texts_batch(texts = c("first doc text.", "second doc text."), config = list("max_characters" = 100))
})
