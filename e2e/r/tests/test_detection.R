# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:9dd848f998f7fc355f44b5b3ccbe3083aabbd95b98834afb0f164186e5a4477f
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: detection

test_that("detect_image_jpeg: Detect JPEG from bytes", {
  result <- detect_image_format(data = "/9j/4AAQ")
})

test_that("detect_image_png: Detect PNG image format", {
  result <- detect_image_format(data = "images/hello_world.png")
})

test_that("detect_lang_english: Detect English language in text", {
  result <- detect_languages(text = "the quick brown fox jumps over the lazy dog.")
})

test_that("detect_lang_french: Detect French language in text", {
  result <- detect_languages(text = "bonjour le monde, comment allez-vous aujourd'hui?")
})

test_that("detect_lang_mixed: Detect mixed languages in text", {
  result <- detect_languages(text = "hello world. bonjour le monde. hallo welt.")
})

test_that("detect_mime_bytes_html: Detect HTML MIME from bytes", {
  result <- detect_mime_type_from_bytes(content = "<!DOCTYPE html><html><body>Hello</body></html>")
})

test_that("detect_mime_bytes_pdf: Detect PDF MIME type from bytes", {
  result <- detect_mime_type_from_bytes(content = "pdf/fake_memo.pdf")
})

test_that("detect_mime_bytes_png: Detect PNG MIME type from bytes", {
  result <- detect_mime_type_from_bytes(content = "images/hello_world.png")
})

test_that("detect_mime_docx: Detect DOCX MIME type from extension", {
  result <- detect_mime_type(path = "document.docx")
})

test_that("detect_mime_html: Detect HTML MIME type from extension", {
  result <- detect_mime_type(path = "page.html")
})

test_that("detect_mime_not_found: detect_mime_type file not found", {
  expect_error(detect_mime_type(path = "/nonexistent/path/file.pdf", check_exists = TRUE))
})

test_that("detect_mime_pdf: Detect PDF MIME type from extension", {
  result <- detect_mime_type(path = "test.pdf")
})

test_that("detect_mime_unknown_ext: detect_mime_type unknown extension", {
  expect_error(detect_mime_type(path = "file.zzz_unknown"))
})

test_that("detect_or_validate_both: detect_or_validate with both", {
  result <- detect_or_validate(path = "file.pdf", mime_type = "application/pdf")
})

test_that("detect_or_validate_invalid_mime: detect_or_validate unsupported MIME", {
  expect_error(detect_or_validate(mime_type = "application/x-nonexistent"))
})

test_that("detect_or_validate_mime: Validate known MIME", {
  result <- detect_or_validate(mime_type = "application/pdf")
})

test_that("detect_or_validate_neither: detect_or_validate errors with no args", {
  expect_error(detect_or_validate())
})

test_that("detect_or_validate_path: Detect MIME from path", {
  result <- detect_or_validate(path = "document.pdf")
})

test_that("get_extensions_unknown_mime: get_extensions unknown MIME", {
  expect_error(get_extensions_for_mime(mime_type = "application/x-totally-unknown"))
})

test_that("is_valid_format_field_false: Invalid format field", {
  result <- is_valid_format_field(field = "nonexistent_field_xyz")
})

test_that("is_valid_format_field_true: Valid format field", {
  result <- is_valid_format_field(field = "content")
})
