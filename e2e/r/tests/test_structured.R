# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: structured

test_that("structured_csv_basic: CSV data file extraction.", {
  result <- extract_file(path = "csv/stanley_cups.csv")
  expect_equal(trimws(result$mime_type), "text/csv")
  expect_true(nchar(result$content) >= 20)
})

test_that("structured_enw_basic: EndNote ENW citation format extraction.", {
  result <- extract_file(path = "data_formats/sample.enw")
  expect_true(any(sapply(c("application/x-endnote-refer", "application/x-endnote+xml", "text/plain"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
})

test_that("structured_json_basic: Structured JSON extraction should stream and preserve content.", {
  result <- extract_file(path = "json/sample_document.json")
  expect_equal(trimws(result$mime_type), "application/json")
  expect_true(nchar(result$content) >= 20)
  expect_true(any(sapply(c("Sample Document", "Test Author"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("structured_json_simple: Simple JSON document to verify structured extraction.", {
  result <- extract_file(path = "json/simple.json")
  expect_equal(trimws(result$mime_type), "application/json")
  expect_true(nchar(result$content) >= 10)
  expect_true(any(sapply(c("{", "name"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("structured_nbib_basic: PubMed NBIB citation format extraction.", {
  result <- extract_file(path = "data_formats/sample.nbib")
  expect_true(any(sapply(c("application/nbib", "application/x-pubmed", "text/plain"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("structured_ris_basic: RIS citation format extraction.", {
  result <- extract_file(path = "data_formats/sample.ris")
  expect_true(any(sapply(c("application/x-research-info-systems", "text/plain"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("structured_toml_basic: TOML configuration file extraction.", {
  result <- extract_file(path = "data_formats/cargo.toml")
  expect_true(any(sapply(c("application/toml", "text/toml"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("structured_tsv_basic: TSV (tab-separated values) data file extraction.", {
  result <- extract_file(path = "data_formats/employees.tsv")
  expect_true(any(sapply(c("text/tab-separated-values", "text/plain"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("structured_yaml_basic: YAML file text extraction.", {
  result <- extract_file(path = "yaml/simple.yaml")
  expect_true(any(sapply(c("application/yaml", "text/yaml", "text/x-yaml", "application/x-yaml"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("structured_yaml_simple: Simple YAML document to validate structured extraction.", {
  result <- extract_file(path = "yaml/simple.yaml")
  expect_equal(trimws(result$mime_type), "application/x-yaml")
  expect_true(nchar(result$content) >= 10)
})
