# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4672f6ec9ebda64dc952fa8cf786a5dd51d05deba76c666ddaa3171d2dae0fe7
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: document

test_that("doc_metadata_docx: Extract DOCX metadata", {
  result <- extract_file_sync(path = "vendored/markitdown/test.docx")
  expect_true(if (is.character(result$metadata)) nchar(result$metadata) > 0 else length(result$metadata) > 0)
})

test_that("doc_metadata_pdf: Extract PDF metadata", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf")
  expect_true(if (is.character(result$metadata)) nchar(result$metadata) > 0 else length(result$metadata) > 0)
})

test_that("doc_structure_docx: Extract DOCX document structure with include_document_structure config", {
  result <- extract_file_sync(path = "vendored/markitdown/test.docx", config = list("include_document_structure" = TRUE))
  expect_true(if (is.character(result$document_structure)) nchar(result$document_structure) > 0 else length(result$document_structure) > 0)
})

test_that("doc_structure_html: Extract HTML document structure with include_document_structure config", {
  result <- extract_file_sync(path = "html/html.html", config = list("include_document_structure" = TRUE))
  expect_true(if (is.character(result$document_structure)) nchar(result$document_structure) > 0 else length(result$document_structure) > 0)
})

test_that("doc_structure_pdf: Extract PDF document structure with include_document_structure config", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("include_document_structure" = TRUE))
  expect_true(if (is.character(result$document_structure)) nchar(result$document_structure) > 0 else length(result$document_structure) > 0)
})
