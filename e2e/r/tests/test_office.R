# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: office

test_that("office_bibtex_basic: BibTeX bibliography extraction.", {
  result <- extract_file(path = "bibtex/comprehensive.bib")
  expect_true(any(sapply(c("application/x-bibtex", "text/x-bibtex"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_commonmark_basic: CommonMark (.commonmark) text extraction.", {
  result <- extract_file(path = "markdown/sample.commonmark")
  expect_true(any(sapply(c("text/markdown", "text/plain", "text/x-commonmark"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 5)
})

test_that("office_djot_basic: Djot markup text extraction.", {
  result <- extract_file(path = "markdown/tables.djot")
  expect_true(any(sapply(c("text/x-djot", "text/djot"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_docbook_basic: DocBook XML document extraction.", {
  result <- extract_file(path = "docbook/docbook-reader.docbook")
  expect_true(any(sapply(c("application/docbook+xml", "text/docbook"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_docx_basic: DOCX document extraction baseline.", {
  result <- extract_file(path = "docx/sample_document.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_docx_equations: DOCX file containing equations to validate math extraction.", {
  result <- extract_file(path = "docx/equations.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 20)
})

test_that("office_docx_fake: Simple DOCX document to verify baseline extraction.", {
  result <- extract_file(path = "docx/fake.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 20)
})

test_that("office_docx_formatting: DOCX document heavy on formatting for style preservation.", {
  result <- extract_file(path = "docx/unit_test_formatting.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 20)
})

test_that("office_docx_headers: DOCX document with complex headers.", {
  result <- extract_file(path = "docx/unit_test_headers.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 20)
})

test_that("office_docx_lists: DOCX document emphasizing list formatting.", {
  result <- extract_file(path = "docx/unit_test_lists.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 20)
})

test_that("office_docx_tables: DOCX document containing tables for table-aware extraction.", {
  result <- extract_file(path = "docx/docx_tables.docx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 50)
  expect_true(grepl("Simple uniform table", result$content, fixed = TRUE))
  expect_true(grepl("Nested Table", result$content, fixed = TRUE))
  expect_true(grepl("merged cells", result$content, fixed = TRUE))
  expect_true(grepl("Header Col", result$content, fixed = TRUE))
  expect_true(length(result$tables) >= 1)
})

test_that("office_epub_basic: EPUB book extraction with text content.", {
  result <- extract_file(path = "epub/features.epub")
  expect_equal(trimws(result$mime_type), "application/epub+zip")
  expect_true(nchar(result$content) >= 50)
})

test_that("office_fb2_basic: FictionBook (FB2) document extraction baseline.", {
  result <- extract_file(path = "fictionbook/basic.fb2")
  expect_equal(trimws(result$mime_type), "application/x-fictionbook+xml")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_fictionbook_basic: FictionBook (.fb2) text extraction.", {
  result <- extract_file(path = "fictionbook/basic.fb2")
  expect_true(any(sapply(c("application/x-fictionbook+xml", "application/x-fictionbook"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_jats_basic: JATS scientific article extraction.", {
  result <- extract_file(path = "jats/sample_article.jats")
  expect_true(any(sapply(c("application/x-jats+xml", "text/jats"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_jupyter_basic: Jupyter notebook extraction.", {
  result <- extract_file(path = "jupyter/rank.ipynb")
  expect_equal(trimws(result$mime_type), "application/x-ipynb+json")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_keynote_basic: Keynote document extraction baseline.", {
  result <- extract_file(path = "iwork/test.key")
  expect_equal(trimws(result$mime_type), "application/x-iwork-keynote-sffkey")
  expect_true(nchar(result$content) >= 5)
})

test_that("office_latex_basic: LaTeX document text extraction.", {
  result <- extract_file(path = "latex/basic_sections.tex")
  expect_true(any(sapply(c("application/x-latex", "text/x-latex"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 20)
})

test_that("office_markdown_basic: Markdown document extraction baseline.", {
  result <- extract_file(path = "markdown/comprehensive.md")
  expect_equal(trimws(result$mime_type), "text/markdown")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_numbers_basic: Numbers document extraction baseline.", {
  result <- extract_file(path = "iwork/test.numbers")
  expect_equal(trimws(result$mime_type), "application/x-iwork-numbers-sffnumbers")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_ods_basic: Basic ODS spreadsheet extraction.", {
  result <- extract_file(path = "data_formats/test_01.ods")
  expect_equal(trimws(result$mime_type), "application/vnd.oasis.opendocument.spreadsheet")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_odt_bold: ODT document with bold formatting.", {
  result <- extract_file(path = "odt/bold.odt")
  expect_equal(trimws(result$mime_type), "application/vnd.oasis.opendocument.text")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_odt_list: ODT document containing unordered lists with nesting.", {
  result <- extract_file(path = "odt/unorderedList.odt")
  expect_equal(trimws(result$mime_type), "application/vnd.oasis.opendocument.text")
  expect_true(nchar(result$content) >= 30)
  expect_true(any(sapply(c("list item", "New level", "Pushed us"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("office_odt_simple: Basic ODT document with paragraphs and headings.", {
  result <- extract_file(path = "odt/simple.odt")
  expect_equal(trimws(result$mime_type), "application/vnd.oasis.opendocument.text")
  expect_true(nchar(result$content) >= 50)
  expect_true(any(sapply(c("favorite things", "Parrots", "Analysis"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("office_odt_table: ODT document with a table structure.", {
  result <- extract_file(path = "odt/table.odt")
  expect_equal(trimws(result$mime_type), "application/vnd.oasis.opendocument.text")
  expect_true(nchar(result$content) >= 10)
  expect_true(length(result$tables) >= 1)
})

test_that("office_opml_basic: OPML outline document extraction.", {
  result <- extract_file(path = "opml/outline.opml")
  expect_true(any(sapply(c("application/xml+opml", "text/x-opml", "application/x-opml+xml"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_org_basic: Org-mode document text extraction.", {
  result <- extract_file(path = "org/comprehensive.org")
  expect_true(any(sapply(c("text/x-org", "text/org"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 20)
})

test_that("office_pages_basic: Pages document extraction baseline.", {
  result <- extract_file(path = "iwork/test.pages")
  expect_equal(trimws(result$mime_type), "application/x-iwork-pages-sffpages")
  expect_true(nchar(result$content) >= 5)
})

test_that("office_ppsx_slideshow: PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.", {
  result <- extract_file(path = "pptx/sample.ppsx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.presentationml.slideshow")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_pptm_basic: PowerPoint macro-enabled presentation (.pptm) extraction.", {
  result <- extract_file(path = "pptx/powerpoint_with_image.pptm")
  expect_true(any(sapply(c("application/vnd.ms-powerpoint.presentation.macroEnabled.12", "application/vnd.openxmlformats-officedocument.presentationml.presentation"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("office_pptx_basic: PPTX deck should extract slides content.", {
  result <- extract_file(path = "pptx/simple.pptx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
  expect_true(nchar(result$content) >= 50)
})

test_that("office_pptx_images: PPTX presentation containing images to ensure metadata extraction.", {
  result <- extract_file(path = "pptx/powerpoint_with_image.pptx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
  expect_true(nchar(result$content) >= 15)
})

test_that("office_pptx_pitch_deck: Pitch deck PPTX used to validate large slide extraction.", {
  result <- extract_file(path = "pptx/pitch_deck_presentation.pptx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
  expect_true(nchar(result$content) >= 100)
})

test_that("office_rst_basic: reStructuredText document extraction.", {
  result <- extract_file(path = "rst/restructured_text.rst")
  expect_true(any(sapply(c("text/x-rst", "text/prs.fallenstein.rst"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 20)
})

test_that("office_rtf_basic: RTF document text extraction.", {
  result <- extract_file(path = "rtf/extraction_test.rtf")
  expect_true(any(sapply(c("application/rtf", "text/rtf"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_typst_basic: Typst document text extraction.", {
  result <- extract_file(path = "typst/headings.typ")
  expect_true(any(sapply(c("application/x-typst", "text/x-typst"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(nchar(result$content) >= 10)
})

test_that("office_xls_legacy: Legacy XLS spreadsheet to ensure backward compatibility.", {
  result <- extract_file(path = "xls/test_excel.xls")
  expect_equal(trimws(result$mime_type), "application/vnd.ms-excel")
  expect_true(nchar(result$content) >= 10)
})

test_that("office_xlsb_basic: Excel binary workbook (.xlsb) extraction.", {
  result <- extract_file(path = "xlsx/test_xlsb.xlsb")
  expect_true(any(sapply(c("application/vnd.ms-excel.sheet.binary.macroEnabled.12", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("office_xlsm_basic: Excel macro-enabled workbook (.xlsm) extraction.", {
  result <- extract_file(path = "xlsx/test_01.xlsm")
  expect_true(any(sapply(c("application/vnd.ms-excel.sheet.macroEnabled.12", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), function(v) grepl(v, result$mime_type, fixed = TRUE))))
  expect_true(if (is.character(result$content)) nchar(result$content) > 0 else length(result$content) > 0)
})

test_that("office_xlsx_basic: XLSX spreadsheet should produce metadata and table content.", {
  result <- extract_file(path = "xlsx/stanley_cups.xlsx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  expect_true(nchar(result$content) >= 100)
  expect_true(grepl("Team", result$content, fixed = TRUE))
  expect_true(grepl("Location", result$content, fixed = TRUE))
  expect_true(grepl("Stanley Cups", result$content, fixed = TRUE))
  expect_true(length(result$tables) >= 1)
  expect_true(result$metadata$sheet_count >= 2)
  expect_true(grepl("Stanley Cups", result$metadata$sheet_names, fixed = TRUE))
})

test_that("office_xlsx_multi_sheet: XLSX workbook with multiple sheets.", {
  result <- extract_file(path = "xlsx/excel_multi_sheet.xlsx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  expect_true(nchar(result$content) >= 20)
  expect_true(result$metadata$sheet_count >= 2)
})

test_that("office_xlsx_office_example: Simple XLSX spreadsheet shipped alongside office integration tests.", {
  result <- extract_file(path = "xlsx/test_01.xlsx")
  expect_equal(trimws(result$mime_type), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  expect_true(nchar(result$content) >= 10)
})
