# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:25ad42f2d7673082d1df9faca02e351d166120486c3c5bec8863eec60cef103a
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: email

test_that("email_eml_html_body: EML with HTML body content.", {
  result <- extract_file(path = "email/html_only.eml")
  expect_equal(trimws(result$mime_type), "message/rfc822")
  expect_true(nchar(result$content) >= 10)
})

test_that("email_eml_multipart: EML with multipart MIME content.", {
  result <- extract_file(path = "email/html_email_multipart.eml")
  expect_equal(trimws(result$mime_type), "message/rfc822")
  expect_true(nchar(result$content) >= 10)
})

test_that("email_eml_utf16: UTF-16 encoded EML file with BOM.", {
  result <- extract_file(path = "vendored/unstructured/eml/fake-email-utf-16.eml")
  expect_equal(trimws(result$mime_type), "message/rfc822")
  expect_true(nchar(result$content) >= 50)
  expect_true(any(sapply(c("Test Email", "Roses are red"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("email_msg_basic: Outlook MSG file extraction.", {
  result <- extract_file(path = "email/fake_email.msg")
  expect_equal(trimws(result$mime_type), "application/vnd.ms-outlook")
  expect_true(nchar(result$content) >= 10)
})

test_that("email_pst_empty: Empty Outlook PST archive with no messages.", {
  result <- extract_file(path = "email/empty.pst")
  expect_equal(trimws(result$mime_type), "application/vnd.ms-outlook-pst")
})

test_that("email_sample_eml: Sample EML email file to verify email parsing.", {
  result <- extract_file(path = "email/sample_email.eml")
  expect_equal(trimws(result$mime_type), "message/rfc822")
  expect_true(nchar(result$content) >= 20)
})
