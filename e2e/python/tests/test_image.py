# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: image."""

import pytest

from kreuzberg import ExtractionConfig, extract_file


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_bmp_basic() -> None:
    """BMP image extraction via OCR."""
    path = "images/bmp_24.bmp"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "image/bmp"
    assert result.content


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_gif_basic() -> None:
    """GIF image extraction via OCR."""
    path = "images_extra/ocr_image.gif"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "image/gif"
    assert result.content


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_jp2_basic() -> None:
    """JPEG 2000 image extraction via OCR."""
    path = "images_extra/ocr_image.jp2"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["image/jp2", "image/jpeg2000"])
    assert result.content


@pytest.mark.asyncio
async def test_image_metadata_only() -> None:
    """JPEG image to validate metadata extraction without OCR."""
    path = "images/example.jpg"
    config = ExtractionConfig(disable_ocr=True, ocr=None)
    result = await extract_file(path=path, config=config)
    assert result.mime_type.strip() == "image/jpeg"
    assert len(result.content) <= 200


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_pbm_basic() -> None:
    """PBM (portable bitmap) image extraction via OCR."""
    path = "images_extra/ocr_image.pbm"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["image/x-portable-bitmap", "image/x-pbm"])
    assert result.content


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_pgm_basic() -> None:
    """PGM (portable graymap) image extraction via OCR."""
    path = "images_extra/ocr_image.pgm"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["image/x-portable-graymap", "image/x-pgm"])
    assert result.content


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_ppm_basic() -> None:
    """PPM (portable pixmap) image extraction via OCR."""
    path = "images_extra/ocr_image.ppm"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["image/x-portable-pixmap", "image/x-ppm"])
    assert result.content


@pytest.mark.asyncio
async def test_image_svg_basic() -> None:
    """SVG image extraction."""
    path = "xml/simple_svg.svg"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "image/svg+xml"
    assert len(result.content) >= 5


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_tiff_basic() -> None:
    """TIFF image extraction via OCR."""
    path = "images_extra/ocr_image.tif"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "image/tiff"
    assert result.content


@pytest.mark.skip(reason="Requires features: tesseract")
@pytest.mark.asyncio
async def test_image_webp_basic() -> None:
    """WebP image extraction via OCR."""
    path = "images_extra/ocr_image.webp"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "image/webp"
    assert result.content
