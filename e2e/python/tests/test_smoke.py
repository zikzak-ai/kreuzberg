# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: smoke."""

import pytest

from kreuzberg import ExtractionConfig, extract_file


@pytest.mark.asyncio
async def test_smoke_cache_namespace() -> None:
    """Smoke test: Extraction with cache namespace and TTL configuration."""
    path = "text/report.txt"
    config = ExtractionConfig(cache_namespace="test_tenant", cache_ttl_secs=3600, use_cache=True)
    result = await extract_file(path=path, config=config)
    assert result.mime_type.strip() == "text/plain"
    assert len(result.content) >= 5


@pytest.mark.asyncio
async def test_smoke_docx_basic() -> None:
    """Smoke test: DOCX with formatted text."""
    path = "docx/fake.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 20
    assert result.content is not None
    assert any(v in result.content for v in ["Lorem", "ipsum", "document", "text"])


@pytest.mark.asyncio
async def test_smoke_html_basic() -> None:
    """Smoke test: HTML table extraction."""
    path = "html/simple_table.html"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/html"
    assert len(result.content) >= 10
    assert result.content is not None
    assert any(v in result.content for v in ["Sample Data Table", "Laptop", "Electronics", "Product"])


@pytest.mark.skip(reason="Skip if document missing; Image extraction requires image processing dependencies")
@pytest.mark.asyncio
async def test_smoke_image_png() -> None:
    """Smoke test: PNG image (without OCR, metadata only)."""
    path = "images/sample.png"
    config = ExtractionConfig(disable_ocr=True)
    result = await extract_file(path=path, config=config)
    assert result.mime_type.strip() == "image/png"
    assert result.metadata.format.strip() == "PNG"


@pytest.mark.asyncio
async def test_smoke_json_basic() -> None:
    """Smoke test: JSON file extraction."""
    path = "json/simple.json"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/json"
    assert len(result.content) >= 5


@pytest.mark.asyncio
async def test_smoke_pdf_basic() -> None:
    """Smoke test: PDF with simple text extraction."""
    path = "pdf/fake_memo.pdf"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/pdf"
    assert len(result.content) >= 50
    assert result.content is not None
    assert any(v in result.content for v in ["May 5, 2023", "To Whom it May Concern"])


@pytest.mark.asyncio
async def test_smoke_txt_basic() -> None:
    """Smoke test: Plain text file."""
    path = "text/report.txt"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/plain"
    assert len(result.content) >= 5


@pytest.mark.asyncio
async def test_smoke_xlsx_basic() -> None:
    """Smoke test: XLSX with basic spreadsheet data including tables."""
    path = "xlsx/stanley_cups.xlsx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    assert len(result.content) >= 100
    assert result.content is not None
    assert "Team" in result.content
    assert result.content is not None
    assert "Location" in result.content
    assert result.content is not None
    assert "Stanley Cups" in result.content
    assert result.content is not None
    assert "Blues" in result.content
    assert result.content is not None
    assert "Flyers" in result.content
    assert result.content is not None
    assert "Maple Leafs" in result.content
    assert result.content is not None
    assert "STL" in result.content
    assert result.content is not None
    assert "PHI" in result.content
    assert result.content is not None
    assert "TOR" in result.content
    assert len(result.tables) >= 1
    assert result.metadata.sheet_count >= 2
    assert "Stanley Cups" in result.metadata.sheet_names
