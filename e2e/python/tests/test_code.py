# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: code."""

import pytest

from kreuzberg import ExtractionConfig, extract_bytes_sync, extract_file


@pytest.mark.skip(reason="Requires features: tree-sitter")
@pytest.mark.asyncio
async def test_code_javascript_basic() -> None:
    """Smoke test: JavaScript source code file extraction."""
    path = "code/app.js"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/x-source-code"
    assert len(result.content) >= 10
    assert result.content is not None
    assert "class Application" in result.content
    assert result.content is not None
    assert "export function" in result.content
    assert result.content is not None
    assert "import" in result.content


@pytest.mark.skip(reason="Requires features: tree-sitter")
@pytest.mark.asyncio
async def test_code_python_basic() -> None:
    """Smoke test: Python source code file extraction."""
    path = "code/hello.py"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/x-source-code"
    assert len(result.content) >= 10
    assert result.content is not None
    assert "def greet" in result.content
    assert result.content is not None
    assert "class Greeter" in result.content
    assert result.content is not None
    assert "import os" in result.content


@pytest.mark.skip(reason="Requires features: tree-sitter")
@pytest.mark.asyncio
async def test_code_rust_basic() -> None:
    """Smoke test: Rust source code file extraction."""
    path = "code/main.rs"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/x-source-code"
    assert len(result.content) >= 10
    assert result.content is not None
    assert "struct Store" in result.content
    assert result.content is not None
    assert "fn main" in result.content
    assert result.content is not None
    assert "HashMap" in result.content


@pytest.mark.skip(reason="Requires features: tree-sitter")
def test_code_shebang_detection() -> None:
    """Test language detection from shebang line via bytes input."""
    data = None
    mime_type = ""
    result = extract_bytes_sync(data=data, mime_type=mime_type)
    assert result.mime_type.strip() == "text/x-source-code"
    assert len(result.content) >= 10
    assert result.content is not None
    assert "build" in result.content
    assert result.content is not None
    assert "clean" in result.content


@pytest.mark.skip(reason="Requires features: tree-sitter")
@pytest.mark.asyncio
async def test_code_tree_sitter_config() -> None:
    """Test tree-sitter config with structure, comments, and docstrings enabled."""
    path = "code/hello.py"
    config = ExtractionConfig(
        tree_sitter={
            "process": {"comments": True, "docstrings": True, "exports": True, "imports": True, "structure": True}
        }
    )
    result = await extract_file(path=path, config=config)
    assert result.mime_type.strip() == "text/x-source-code"
    assert len(result.content) >= 10
    assert result.content is not None
    assert "def greet" in result.content
    assert result.content is not None
    assert "class Greeter" in result.content
