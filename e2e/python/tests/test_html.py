# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: html."""

import pytest

from kreuzberg import extract_file


@pytest.mark.asyncio
async def test_html_complex_layout() -> None:
    """Large Wikipedia HTML page to validate complex conversion."""
    path = "html/taylor_swift.html"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/html"
    assert len(result.content) >= 1000


@pytest.mark.asyncio
async def test_html_simple_table() -> None:
    """HTML table converted to markdown should retain structure."""
    path = "html/simple_table.html"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/html"
    assert len(result.content) >= 100
    assert result.content is not None
    assert "Product" in result.content
    assert result.content is not None
    assert "Category" in result.content
    assert result.content is not None
    assert "Price" in result.content
    assert result.content is not None
    assert "Stock" in result.content
    assert result.content is not None
    assert "Laptop" in result.content
    assert result.content is not None
    assert "Electronics" in result.content
    assert result.content is not None
    assert "Sample Data Table" in result.content
