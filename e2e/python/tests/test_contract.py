# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:28e91a41a1f48ac029a93a74bff3b5360242cfdf759594e9845fdb0861f4e456
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: contract."""

import pytest  # noqa: F401
from kreuzberg import (
    batch_extract_bytes,
    batch_extract_bytes_sync,
    batch_extract_file,
    batch_extract_file_sync,
    extract_bytes,
    extract_bytes_sync,
    extract_file,
    extract_file_sync,
    ExtractionConfig,
)


@pytest.mark.asyncio
async def test_api_batch_bytes_async() -> None:
    """Tests async batch bytes extraction API (batch_extract_bytes)."""
    items = None
    result = await batch_extract_bytes(items=items)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


def test_api_batch_bytes_sync() -> None:
    """Tests sync batch bytes extraction API (batch_extract_bytes_sync)."""
    items = None
    result = batch_extract_bytes_sync(items=items)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


@pytest.mark.asyncio
async def test_api_batch_bytes_with_configs_async() -> None:
    """Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)."""
    items = None
    result = await batch_extract_bytes(items=items)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "markdown"  # noqa: S101


def test_api_batch_bytes_with_configs_sync() -> None:
    """Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)."""
    items = None
    result = batch_extract_bytes_sync(items=items)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "markdown"  # noqa: S101


@pytest.mark.asyncio
async def test_api_batch_file_async() -> None:
    """Tests async batch file extraction API (batch_extract_file)."""
    paths = None
    result = await batch_extract_file(paths=paths)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


def test_api_batch_file_sync() -> None:
    """Tests sync batch file extraction API (batch_extract_file_sync)."""
    paths = None
    result = batch_extract_file_sync(paths=paths)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


@pytest.mark.asyncio
async def test_api_batch_file_with_configs_async() -> None:
    """Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)."""
    paths = None
    result = await batch_extract_file(paths=paths)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "markdown"  # noqa: S101


def test_api_batch_file_with_configs_sync() -> None:
    """Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)."""
    paths = None
    result = batch_extract_file_sync(paths=paths)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "markdown"  # noqa: S101


def test_api_batch_file_with_timeout_sync() -> None:
    """Tests sync batch file extraction with per-file timeout config override."""
    paths = None
    config = ExtractionConfig(extraction_timeout_secs=300)
    result = batch_extract_file_sync(paths=paths, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101


@pytest.mark.asyncio
async def test_api_extract_bytes_async() -> None:
    """Tests async bytes extraction API (extract_bytes)."""
    data = None
    mime_type = ""
    result = await extract_bytes(data=data, mime_type=mime_type)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


def test_api_extract_bytes_sync() -> None:
    """Tests sync bytes extraction API (extract_bytes_sync)."""
    data = None
    mime_type = ""
    result = extract_bytes_sync(data=data, mime_type=mime_type)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


@pytest.mark.asyncio
async def test_api_extract_file_async() -> None:
    """Tests async file extraction API (extract_file)."""
    path = "pdf/fake_memo.pdf"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


def test_api_extract_file_sync() -> None:
    """Tests sync file extraction API (extract_file_sync)."""
    path = "pdf/fake_memo.pdf"
    result = extract_file_sync(path=path)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "Mallori"])  # noqa: S101


def test_config_acceleration_cpu_provider() -> None:
    """Tests explicit CPU acceleration provider configuration."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(acceleration={"device_id": 0, "provider": "cpu"})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 50  # noqa: S101
    assert any(v in result.content for v in ["May 5, 2023", "To Whom it May Concern"])  # noqa: S101


def test_config_chunking() -> None:
    """Tests chunking configuration with chunk assertions."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(chunking={"max_chars": 500, "max_overlap": 50})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 1  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


@pytest.mark.skip(reason="Requires features: chunking")
def test_config_chunking_heading_context() -> None:
    """Tests markdown chunker populates heading context on chunks."""
    path = "markdown/extraction_test.md"
    config = ExtractionConfig(
        chunking={"chunker_type": "markdown", "max_chars": 300, "max_overlap": 50}
    )
    result = extract_file_sync(path=path, config=config)
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 2  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101
    assert result.chunks_have_heading_context is True  # noqa: S101


@pytest.mark.skip(reason="Requires features: chunking")
def test_config_chunking_markdown() -> None:
    """Tests markdown-aware chunker type."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        chunking={"chunker_type": "markdown", "max_chars": 500, "max_overlap": 50}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 1  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


@pytest.mark.skip(reason="Requires features: chunking")
def test_config_chunking_no_headings() -> None:
    """Tests markdown chunker on text with no headings produces null heading_context."""
    path = "text/book_war_and_peace_1p.txt"
    config = ExtractionConfig(
        chunking={"chunker_type": "markdown", "max_chars": 300, "max_overlap": 50}
    )
    result = extract_file_sync(path=path, config=config)
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 2  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


@pytest.mark.skip(reason="Requires features: chunking")
def test_config_chunking_prepend_heading_context() -> None:
    """Tests markdown chunker prepends heading hierarchy to chunk content."""
    path = "markdown/extraction_test.md"
    config = ExtractionConfig(
        chunking={
            "chunker_type": "markdown",
            "max_chars": 300,
            "max_overlap": 50,
            "prepend_heading_context": True,
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 2  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101
    assert result.chunks_have_heading_context is True  # noqa: S101
    assert result.first_chunk_starts_with_heading is True  # noqa: S101


@pytest.mark.skip(reason="Requires chunking feature")
def test_config_chunking_semantic() -> None:
    """Semantic chunker with defaults on a multi-section document."""
    path = "semantic/annual_report.txt"
    config = ExtractionConfig(chunking={"chunker_type": "semantic"})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "text/plain"  # noqa: S101
    assert len(result.content) >= 100  # noqa: S101


@pytest.mark.skip(reason="Requires chunking feature")
def test_config_chunking_semantic_small() -> None:
    """Semantic chunker with small budget produces more granular chunks."""
    path = "semantic/annual_report.txt"
    config = ExtractionConfig(chunking={"chunker_type": "semantic", "max_chars": 200})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "text/plain"  # noqa: S101
    assert len(result.content) >= 100  # noqa: S101


@pytest.mark.skip(reason="Requires chunking feature")
def test_config_chunking_semantic_threshold() -> None:
    """Semantic chunker with explicit topic threshold on mixed-topic text."""
    path = "semantic/mixed_topics.txt"
    config = ExtractionConfig(
        chunking={"chunker_type": "semantic", "topic_threshold": 0.5}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "text/plain"  # noqa: S101
    assert len(result.content) >= 100  # noqa: S101


@pytest.mark.skip(
    reason="Semantic chunking with a plugin embedder requires the plugin to be registered at runtime via register_embedding_backend — the e2e harness cannot do this. Covers config round-trip only."
)
def test_config_chunking_semantic_with_plugin() -> None:
    """Tests semantic chunking config with EmbeddingModelType::Plugin — verifies each binding's EmbeddingConfig accepts the combined semantic-chunker + plugin-embedder shape."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        chunking={
            "chunker_type": "semantic",
            "embedding": {
                "max_embed_duration_secs": 60,
                "model": {"name": "test-semantic-plugin", "type": "plugin"},
                "normalize": True,
            },
            "max_chars": 1024,
            "max_overlap": 100,
            "topic_threshold": 0.75,
        }
    )
    _ = extract_file_sync(path=path, config=config)


@pytest.mark.skip(reason="Requires features: chunking")
def test_config_chunking_small() -> None:
    """Tests chunking with very small chunk size produces more chunks."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(chunking={"max_chars": 100, "max_overlap": 20})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 2  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


def test_config_chunking_text() -> None:
    """Tests text chunker type (generic whitespace/punctuation splitter)."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        chunking={"chunker_type": "text", "max_chars": 500, "max_overlap": 50}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 1  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


@pytest.mark.skip(
    reason="Requires features: chunking-tokenizers; Requires network access for HuggingFace Hub tokenizer download"
)
def test_config_chunking_tokenizer() -> None:
    """Tests token-based chunk sizing with HuggingFace tokenizer."""
    path = "markdown/comprehensive.md"
    config = ExtractionConfig(
        chunking={
            "max_chars": 200,
            "max_overlap": 40,
            "sizing": {"model": "Xenova/gpt-4o", "type": "tokenizer"},
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 2  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


def test_config_disable_ocr() -> None:
    """Tests disable_ocr configuration option - OCR is skipped for images."""
    path = "images/test_hello_world.png"
    config = ExtractionConfig(disable_ocr=True)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "image/png"  # noqa: S101
    assert len(result.content) <= 5  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_djot_content() -> None:
    """Tests djot output format converts content to djot markup."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(output_format="djot")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "djot"  # noqa: S101


def test_config_document_structure() -> None:
    """Tests include_document_structure config produces document tree."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(include_document_structure=True)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert result.document_structure  # noqa: S101
    assert len(result.document_structure.nodes) >= 1  # noqa: S101
    assert "paragraph" in result.document_structure.node_types  # noqa: S101


def test_config_document_structure_disabled() -> None:
    """Tests document field is null when include_document_structure is false."""
    path = "pdf/fake_memo.pdf"
    result = extract_file_sync(path=path)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101


@pytest.mark.skip(reason="Requires features: office")
def test_config_document_structure_groups() -> None:
    """Tests document structure extraction with group node assertion on DOCX with headings."""
    path = "docx/unit_test_headers.docx"
    config = ExtractionConfig(include_document_structure=True)
    result = extract_file_sync(path=path, config=config)
    assert any(
        v in result.mime_type
        for v in [
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
        ]
    )  # noqa: S101
    assert result.document_structure  # noqa: S101
    assert result.document_structure_has_groups is True  # noqa: S101


@pytest.mark.skip(reason="Requires features: office")
def test_config_document_structure_headings() -> None:
    """Tests document structure extraction with heading nodes on a DOCX."""
    path = "docx/unit_test_headers.docx"
    config = ExtractionConfig(include_document_structure=True)
    result = extract_file_sync(path=path, config=config)
    assert any(
        v in result.mime_type
        for v in [
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
        ]
    )  # noqa: S101
    assert result.document_structure  # noqa: S101
    assert len(result.document_structure.nodes) >= 1  # noqa: S101
    assert "heading" in result.document_structure.node_types  # noqa: S101
    assert "paragraph" in result.document_structure.node_types  # noqa: S101


def test_config_document_structure_with_headings() -> None:
    """Tests document structure with DOCX heading-driven nesting."""
    path = "docx/fake.docx"
    config = ExtractionConfig(include_document_structure=True)
    result = extract_file_sync(path=path, config=config)
    assert (
        result.mime_type.strip()
        == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    )  # noqa: S101
    assert result.document_structure  # noqa: S101
    assert len(result.document_structure.nodes) >= 1  # noqa: S101


@pytest.mark.skip(reason="Requires features: office")
def test_config_element_types() -> None:
    """Tests element-based result format with element type assertions on DOCX."""
    path = "docx/unit_test_headers.docx"
    config = ExtractionConfig(result_format="element_based")
    result = extract_file_sync(path=path, config=config)
    assert any(
        v in result.mime_type
        for v in [
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
        ]
    )  # noqa: S101
    assert result.result_format.strip() == "element_based"  # noqa: S101
    assert len(result.elements) >= 1  # noqa: S101
    assert "narrative_text" in result.element_types  # noqa: S101


@pytest.mark.asyncio
async def test_config_email_msg_fallback_codepage() -> None:
    """Tests MSG extraction with custom fallback codepage for Cyrillic."""
    path = "email/fake_email.msg"
    config = ExtractionConfig(email={"msg_fallback_codepage": 1251})
    result = await extract_file(path=path, config=config)
    assert result.mime_type.strip() == "application/vnd.ms-outlook"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101


@pytest.mark.skip(
    reason='EmbeddingModelType::Plugin requires a host-language backend registered via register_embedding_backend before dispatch; the e2e harness cannot register one. This fixture validates config round-trip (the {"type":"plugin","name":...} shape is accepted by every binding\'s EmbeddingConfig).'
)
def test_config_embedding_plugin() -> None:
    """Tests EmbeddingModelType::Plugin variant deserialization in ChunkingConfig — config accepts the plugin variant shape; actual dispatch requires a host-language backend registered via register_embedding_backend at runtime."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        chunking={
            "embedding": {
                "max_embed_duration_secs": 30,
                "model": {"name": "test-plugin-backend", "type": "plugin"},
                "normalize": True,
            },
            "max_chars": 500,
            "max_overlap": 50,
        }
    )
    _ = extract_file_sync(path=path, config=config)


def test_config_extraction_timeout() -> None:
    """Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(extraction_timeout_secs=300)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101


@pytest.mark.skip(reason="Requires features: tesseract")
def test_config_force_ocr() -> None:
    """Tests force_ocr configuration option."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(force_ocr=True)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 5  # noqa: S101


@pytest.mark.skip(reason="Requires features: ocr")
def test_config_force_ocr_pages() -> None:
    """Tests that force_ocr_pages config field is accepted for selective page OCR."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        force_ocr_pages=[1], ocr={"backend": "tesseract", "language": "eng"}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 1  # noqa: S101


def test_config_html_options() -> None:
    """Tests extraction with HTML conversion options configured."""
    path = "html/complex_table.html"
    config = ExtractionConfig(html_options={"extract_metadata": True})
    result = extract_file_sync(path=path, config=config)
    assert any(v in result.mime_type for v in ["text/html"])  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.content  # noqa: S101


def test_config_html_styled_custom_css() -> None:
    """HTML output with inline user CSS string is embedded verbatim in the style block."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        html_output={
            "css": ".kb-p { color: red; }",
            "embed_css": True,
            "theme": "unstyled",
        },
        output_format="html",
    )
    result = extract_file_sync(path=path, config=config)
    assert result.output_format.strip() == "html"  # noqa: S101
    assert ".kb-p { color: red; }" in result.content  # noqa: S101
    assert "kb-doc" in result.content  # noqa: S101


def test_config_html_styled_default() -> None:
    """HTML output with default theme emits kb-* class hooks and an embedded style block."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        html_output={"embed_css": True, "theme": "default"}, output_format="html"
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert result.output_format.strip() == "html"  # noqa: S101
    assert "kb-doc" in result.content  # noqa: S101
    assert "kb-content" in result.content  # noqa: S101
    assert "kb-p" in result.content  # noqa: S101
    assert "<style>" in result.content  # noqa: S101


def test_config_html_styled_no_embed() -> None:
    """HTML output with embed_css false emits class hooks but no style block."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        html_output={"embed_css": False, "theme": "default"}, output_format="html"
    )
    result = extract_file_sync(path=path, config=config)
    assert result.output_format.strip() == "html"  # noqa: S101
    assert "kb-doc" in result.content  # noqa: S101
    assert "kb-content" in result.content  # noqa: S101


def test_config_images() -> None:
    """Tests image extraction configuration with image assertions."""
    path = "pdf/embedded_images_tables.pdf"
    config = ExtractionConfig(images={"extract_images": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.images) >= 1  # noqa: S101


def test_config_images_with_formats() -> None:
    """Tests image extraction on PPTX containing embedded images."""
    path = "pptx/powerpoint_with_image.pptx"
    config = ExtractionConfig(images={"extract_images": True})
    result = extract_file_sync(path=path, config=config)
    assert (
        result.mime_type.strip()
        == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
    )  # noqa: S101
    assert len(result.images) >= 1  # noqa: S101


@pytest.mark.skip(reason="Requires features: keywords-yake")
def test_config_keywords() -> None:
    """Tests keyword extraction via YAKE algorithm."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(keywords={"algorithm": "yake", "max_keywords": 10})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.keywords  # noqa: S101
    assert len(result.keywords) >= 1  # noqa: S101


def test_config_language_detection() -> None:
    """Tests language detection configuration."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(language_detection={"enabled": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert "eng" in result.detected_languages  # noqa: S101
    assert result.language_detection_confidence >= 0.5  # noqa: S101


def test_config_language_detection_multi() -> None:
    """Tests language detection with detect_multiple and min_confidence options."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        language_detection={
            "detect_multiple": True,
            "enabled": True,
            "min_confidence": 0.3,
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert "eng" in result.detected_languages  # noqa: S101


@pytest.mark.skip(reason="Requires features: language-detection")
def test_config_language_multi() -> None:
    """Tests multi-language detection config."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        language_detection={"detect_multiple": True, "enabled": True}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert "eng" in result.detected_languages  # noqa: S101


@pytest.mark.skip(
    reason="Requires features: liter-llm; Requires liter-llm feature and KREUZBERG_LLM_API_KEY env var"
)
def test_config_llm_embeddings() -> None:
    """Tests VLM embeddings via liter-llm provider-hosted embedding model."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        chunking={
            "embedding": {
                "model": {
                    "llm": {"model": "openai/text-embedding-3-small"},
                    "type": "llm",
                },
                "normalize": True,
            },
            "max_chars": 500,
            "max_overlap": 50,
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.chunks) >= 1  # noqa: S101
    assert result.chunks_have_embeddings is True  # noqa: S101
    assert result.chunks_have_content is True  # noqa: S101


@pytest.mark.skip(
    reason="Requires features: liter-llm; Requires liter-llm feature and KREUZBERG_LLM_API_KEY env var"
)
def test_config_llm_structured_extraction() -> None:
    """Tests structured extraction via liter-llm with JSON schema."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        structured_extraction={
            "llm": {"model": "openai/gpt-4o"},
            "schema": {
                "properties": {
                    "date": {"type": "string"},
                    "summary": {"type": "string"},
                    "title": {"type": "string"},
                },
                "required": ["title"],
                "type": "object",
            },
            "schema_name": "memo_data",
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.structured_output  # noqa: S101


@pytest.mark.skip(
    reason="Requires features: liter-llm; Requires liter-llm feature and KREUZBERG_LLM_API_KEY env var"
)
def test_config_llm_structured_extraction_with_prompt() -> None:
    """Tests structured extraction with custom prompt."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        structured_extraction={
            "llm": {"model": "openai/gpt-4o"},
            "prompt": "Extract the sender and recipient from this memo document. If not found, use 'unknown'.",
            "schema": {
                "properties": {
                    "recipient": {"type": "string"},
                    "sender": {"type": "string"},
                    "subject": {"type": "string"},
                },
                "required": ["sender", "recipient"],
                "type": "object",
            },
            "schema_name": "memo_parties",
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.structured_output  # noqa: S101
    assert result.structured_output.sender  # noqa: S101
    assert result.structured_output.recipient  # noqa: S101


@pytest.mark.skip(
    reason="Requires features: liter-llm; Requires liter-llm feature and KREUZBERG_LLM_API_KEY env var"
)
def test_config_llm_vlm_ocr() -> None:
    """Tests VLM OCR backend via liter-llm vision model."""
    path = "images/test_hello_world.png"
    config = ExtractionConfig(
        ocr={
            "backend": "vlm",
            "language": "eng",
            "vlm_config": {"model": "openai/gpt-4o"},
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.content  # noqa: S101
    assert len(result.content) >= 5  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_pages() -> None:
    """Tests page extraction and page marker configuration."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        pages={"extract_pages": True, "insert_page_markers": True}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["PAGE"])  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_pages_exact_count() -> None:
    """Tests page extraction with exact page count assertion on multi-page PDF."""
    path = "pdf/multi_page.pdf"
    config = ExtractionConfig(pages={"extract_pages": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.pages) == 5  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_pages_extract() -> None:
    """Tests page extraction config producing per-page content array."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(pages={"extract_pages": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert len(result.pages) >= 1  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_pages_markers() -> None:
    """Tests page marker insertion in extracted content."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(pages={"insert_page_markers": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert any(v in result.content for v in ["PAGE"])  # noqa: S101


@pytest.mark.skip(
    reason="Requires features: pdf; Skip on platforms: aarch64-unknown-linux-gnu; PDFium ARM Linux binary does not support annotation extraction"
)
def test_config_pdf_annotations_count() -> None:
    """Tests PDF annotation extraction with min_count assertion."""
    path = "vendored/pdfplumber/pdf/annotations.pdf"
    config = ExtractionConfig(pdf_options={"extract_annotations": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert result.annotations  # noqa: S101
    assert len(result.annotations) >= 3  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_pdf_hierarchy() -> None:
    """Tests PDF hierarchy extraction config with block-level structure."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        pages={"extract_pages": True},
        pdf_options={"hierarchy": {"enabled": True, "include_bbox": True}},
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 50  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_pdf_margins() -> None:
    """Tests PDF margin exclusion configuration."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(
        pdf_options={"bottom_margin_fraction": 0.1, "top_margin_fraction": 0.1}
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 5  # noqa: S101


def test_config_postprocessor() -> None:
    """Tests postprocessor config is accepted and extraction succeeds."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(postprocessor={"enabled": True})
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.content  # noqa: S101


def test_config_processing_warnings_empty() -> None:
    """Tests that a clean PDF extraction produces no processing warnings."""
    path = "pdf/fake_memo.pdf"
    result = extract_file_sync(path=path)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert not result.processing_warnings  # noqa: S101


def test_config_quality_disabled() -> None:
    """Tests extraction with quality processing explicitly disabled."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(enable_quality_processing=False)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.content  # noqa: S101


@pytest.mark.skip(reason="Requires features: quality")
def test_config_quality_enabled() -> None:
    """Tests quality scoring produces a score value in [0.0, 1.0]."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(enable_quality_processing=True)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.quality_score  # noqa: S101
    assert result.quality_score >= 0.0  # noqa: S101
    assert result.quality_score <= 1.0  # noqa: S101


@pytest.mark.skip(reason="Requires features: quality")
def test_config_quality_score_range() -> None:
    """Tests quality scoring produces a score with minimum bound assertion."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(enable_quality_processing=True)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert result.quality_score  # noqa: S101
    assert result.quality_score >= 0.1  # noqa: S101


def test_config_security_limits() -> None:
    """Tests archive extraction with custom security limits."""
    path = "archives/documents.zip"
    config = ExtractionConfig(
        security_limits={
            "max_archive_size": 104857600,
            "max_compression_ratio": 50,
            "max_files_in_archive": 100,
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert any(
        v in result.mime_type
        for v in ["application/zip", "application/x-zip-compressed"]
    )  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101


@pytest.mark.skip(reason="Requires features: pdf")
def test_config_structured_output() -> None:
    """Tests structured (JSON) output format config."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(output_format="structured")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "structured"  # noqa: S101


def test_config_tables_content() -> None:
    """Tests table extraction with content_contains_any assertion on DOCX with tables."""
    path = "docx/docx_tables.docx"
    result = extract_file_sync(path=path)
    assert (
        result.mime_type.strip()
        == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    )  # noqa: S101
    assert len(result.tables) >= 1  # noqa: S101


@pytest.mark.skip(reason="Requires features: tree-sitter")
def test_config_tree_sitter() -> None:
    """Tests tree-sitter configuration round-trip."""
    path = "code/hello.py"
    config = ExtractionConfig(
        tree_sitter={
            "groups": ["web"],
            "languages": ["python", "rust"],
            "process": {
                "comments": False,
                "diagnostics": False,
                "docstrings": False,
                "exports": True,
                "imports": True,
                "structure": True,
                "symbols": False,
            },
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "text/x-source-code"  # noqa: S101
    assert len(result.content) >= 5  # noqa: S101


@pytest.mark.skip(reason="Requires features: tree-sitter")
def test_config_tree_sitter_process() -> None:
    """Tests tree-sitter process config with all options enabled."""
    path = "code/hello.py"
    config = ExtractionConfig(
        tree_sitter={
            "process": {
                "chunk_max_size": 2000,
                "comments": True,
                "diagnostics": True,
                "docstrings": True,
                "exports": True,
                "imports": True,
                "structure": True,
                "symbols": True,
            }
        }
    )
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "text/x-source-code"  # noqa: S101
    assert len(result.content) >= 5  # noqa: S101


def test_config_use_cache_false() -> None:
    """Tests use_cache=false configuration option."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(use_cache=False)
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101


def test_output_format_bytes_markdown() -> None:
    """Tests markdown output format via bytes extraction API."""
    data = None
    mime_type = ""
    config = ExtractionConfig(output_format="markdown")
    result = extract_bytes_sync(data=data, mime_type=mime_type, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "markdown"  # noqa: S101


def test_output_format_djot() -> None:
    """Tests Djot output format."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(output_format="djot")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "djot"  # noqa: S101


def test_output_format_html() -> None:
    """Tests HTML output format."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(output_format="html")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "html"  # noqa: S101


def test_output_format_markdown() -> None:
    """Tests Markdown output format."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(output_format="markdown")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "markdown"  # noqa: S101


def test_output_format_plain() -> None:
    """Tests Plain output format."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(output_format="plain")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.output_format.strip() == "plain"  # noqa: S101


def test_result_format_element_based() -> None:
    """Tests ElementBased result format with element assertions."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(result_format="element_based")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert result.result_format.strip() == "element_based"  # noqa: S101
    assert len(result.elements) >= 1  # noqa: S101


def test_result_format_unified() -> None:
    """Tests Unified result format (default)."""
    path = "pdf/fake_memo.pdf"
    config = ExtractionConfig(result_format="unified")
    result = extract_file_sync(path=path, config=config)
    assert result.mime_type.strip() == "application/pdf"  # noqa: S101
    assert len(result.content) >= 10  # noqa: S101
    assert result.result_format.strip() == "unified"  # noqa: S101
