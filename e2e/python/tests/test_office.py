# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: office."""

import pytest

from kreuzberg import extract_file


@pytest.mark.asyncio
async def test_office_bibtex_basic() -> None:
    """BibTeX bibliography extraction."""
    path = "bibtex/comprehensive.bib"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/x-bibtex", "text/x-bibtex"])
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_commonmark_basic() -> None:
    """CommonMark (.commonmark) text extraction."""
    path = "markdown/sample.commonmark"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/markdown", "text/plain", "text/x-commonmark"])
    assert len(result.content) >= 5


@pytest.mark.skip(reason="Requires features: office; Skip if document missing; Requires the office feature.")
@pytest.mark.asyncio
async def test_office_dbf_basic() -> None:
    """dBASE (.dbf) table extraction as markdown."""
    path = "dbf/stations.dbf"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-dbf"
    assert len(result.content) >= 10
    assert result.content is not None
    assert any(v in result.content for v in ["|"])


@pytest.mark.asyncio
async def test_office_djot_basic() -> None:
    """Djot markup text extraction."""
    path = "markdown/tables.djot"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/x-djot", "text/djot"])
    assert len(result.content) >= 10


@pytest.mark.skip(reason="Requires features: office; Skip if document missing; Requires the office feature.")
@pytest.mark.asyncio
async def test_office_doc_legacy() -> None:
    """Legacy .doc document extraction via native OLE/CFB parsing."""
    path = "doc/unit_test_lists.doc"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/msword"
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_docbook_basic() -> None:
    """DocBook XML document extraction."""
    path = "docbook/docbook-reader.docbook"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/docbook+xml", "text/docbook"])
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_docx_basic() -> None:
    """DOCX document extraction baseline."""
    path = "docx/sample_document.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_docx_equations() -> None:
    """DOCX file containing equations to validate math extraction."""
    path = "docx/equations.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_docx_fake() -> None:
    """Simple DOCX document to verify baseline extraction."""
    path = "docx/fake.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_docx_formatting() -> None:
    """DOCX document heavy on formatting for style preservation."""
    path = "docx/unit_test_formatting.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_docx_headers() -> None:
    """DOCX document with complex headers."""
    path = "docx/unit_test_headers.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_docx_lists() -> None:
    """DOCX document emphasizing list formatting."""
    path = "docx/unit_test_lists.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_docx_tables() -> None:
    """DOCX document containing tables for table-aware extraction."""
    path = "docx/docx_tables.docx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
    assert len(result.content) >= 50
    assert result.content is not None
    assert "Simple uniform table" in result.content
    assert result.content is not None
    assert "Nested Table" in result.content
    assert result.content is not None
    assert "merged cells" in result.content
    assert result.content is not None
    assert "Header Col" in result.content
    assert len(result.tables) >= 1


@pytest.mark.asyncio
async def test_office_epub_basic() -> None:
    """EPUB book extraction with text content."""
    path = "epub/features.epub"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/epub+zip"
    assert len(result.content) >= 50


@pytest.mark.asyncio
async def test_office_fb2_basic() -> None:
    """FictionBook (FB2) document extraction baseline."""
    path = "fictionbook/basic.fb2"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-fictionbook+xml"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_fictionbook_basic() -> None:
    """FictionBook (.fb2) text extraction."""
    path = "fictionbook/basic.fb2"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/x-fictionbook+xml", "application/x-fictionbook"])
    assert len(result.content) >= 10


@pytest.mark.skip(reason="Requires features: office; Skip if document missing; Requires the office feature.")
@pytest.mark.asyncio
async def test_office_hwp_basic() -> None:
    """Hangul Word Processor (.hwp) text extraction."""
    path = "hwp/converted_output.hwp"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-hwp"
    assert len(result.content) >= 10


@pytest.mark.skip(
    reason="Requires features: hwp; Skip on platforms: aarch64-unknown-linux-gnu; Skip if document missing; HWP styled doc yields no extractable plain text with current parser. Extraction returns empty content on ARM Linux."
)
@pytest.mark.asyncio
async def test_office_hwp_styled() -> None:
    """Hangul Word Processor (.hwp) styled document extraction."""
    path = "hwp/styled_document.hwp"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-hwp"


@pytest.mark.asyncio
async def test_office_jats_basic() -> None:
    """JATS scientific article extraction."""
    path = "jats/sample_article.jats"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/x-jats+xml", "text/jats"])
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_jupyter_basic() -> None:
    """Jupyter notebook extraction."""
    path = "jupyter/rank.ipynb"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-ipynb+json"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_keynote_basic() -> None:
    """Keynote document extraction baseline."""
    path = "iwork/test.key"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-iwork-keynote-sffkey"
    assert len(result.content) >= 5


@pytest.mark.asyncio
async def test_office_latex_basic() -> None:
    """LaTeX document text extraction."""
    path = "latex/basic_sections.tex"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/x-latex", "text/x-latex"])
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_markdown_basic() -> None:
    """Markdown document extraction baseline."""
    path = "markdown/comprehensive.md"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "text/markdown"
    assert len(result.content) >= 10


@pytest.mark.skip(reason="Requires features: mdx")
@pytest.mark.asyncio
async def test_office_mdx_basic() -> None:
    """MDX document extraction with JSX stripping and frontmatter."""
    path = "markdown/sample.mdx"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/mdx", "text/x-mdx"])
    assert len(result.content) >= 50
    assert result.content is not None
    assert "MDX Sample Document" in result.content
    assert result.content is not None
    assert "warning message" in result.content
    assert result.content is not None
    assert "Conclusion" in result.content


@pytest.mark.skip(reason="Requires features: mdx")
@pytest.mark.asyncio
async def test_office_mdx_getting_started() -> None:
    """Real-world MDX extraction from mdx-js/mdx getting-started.mdx with imports, exports, JSX components, code blocks, and reference links."""
    path = "markdown/mdx_getting_started.mdx"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/mdx", "text/x-mdx"])
    assert len(result.content) >= 2000
    assert result.content is not None
    assert "Getting started" in result.content
    assert result.content is not None
    assert "Prerequisites" in result.content
    assert result.content is not None
    assert "Quick start" in result.content
    assert result.content is not None
    assert "Bundler" in result.content
    assert result.content is not None
    assert "Security" in result.content
    assert result.content is not None
    assert "Integrations" in result.content
    assert result.content is not None
    assert "esbuild" in result.content
    assert result.content is not None
    assert "Rollup" in result.content
    assert result.content is not None
    assert "webpack" in result.content
    assert result.content is not None
    assert "React" in result.content


@pytest.mark.skip(reason="Requires features: mdx")
@pytest.mark.asyncio
async def test_office_mdx_troubleshooting() -> None:
    """Real-world MDX extraction from mdx-js/mdx troubleshooting-mdx.mdx with error documentation, code blocks, and JSX comments."""
    path = "markdown/mdx_troubleshooting.mdx"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/mdx", "text/x-mdx"])
    assert len(result.content) >= 2000
    assert result.content is not None
    assert "Troubleshooting MDX" in result.content
    assert result.content is not None
    assert "Problems integrating MDX" in result.content
    assert result.content is not None
    assert "ESM" in result.content
    assert result.content is not None
    assert "Problems using MDX" in result.content
    assert result.content is not None
    assert "Problems writing MDX" in result.content


@pytest.mark.skip(reason="Requires features: mdx")
@pytest.mark.asyncio
async def test_office_mdx_using_mdx() -> None:
    """Real-world MDX extraction from mdx-js/mdx using-mdx.mdx with complex JSX examples, component passing, and MDX provider patterns."""
    path = "markdown/mdx_using_mdx.mdx"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/mdx", "text/x-mdx"])
    assert len(result.content) >= 2000
    assert result.content is not None
    assert "Using MDX" in result.content
    assert result.content is not None
    assert "How MDX works" in result.content
    assert result.content is not None
    assert "MDX content" in result.content
    assert result.content is not None
    assert "Props" in result.content
    assert result.content is not None
    assert "Components" in result.content
    assert result.content is not None
    assert "Layout" in result.content
    assert result.content is not None
    assert "MDX provider" in result.content


@pytest.mark.asyncio
async def test_office_numbers_basic() -> None:
    """Numbers document extraction baseline."""
    path = "iwork/test.numbers"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-iwork-numbers-sffnumbers"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_ods_basic() -> None:
    """Basic ODS spreadsheet extraction."""
    path = "data_formats/test_01.ods"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.oasis.opendocument.spreadsheet"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_odt_bold() -> None:
    """ODT document with bold formatting."""
    path = "odt/bold.odt"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.oasis.opendocument.text"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_odt_list() -> None:
    """ODT document containing unordered lists with nesting."""
    path = "odt/unorderedList.odt"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.oasis.opendocument.text"
    assert len(result.content) >= 30
    assert result.content is not None
    assert any(v in result.content for v in ["list item", "New level", "Pushed us"])


@pytest.mark.asyncio
async def test_office_odt_simple() -> None:
    """Basic ODT document with paragraphs and headings."""
    path = "odt/simple.odt"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.oasis.opendocument.text"
    assert len(result.content) >= 50
    assert result.content is not None
    assert any(v in result.content for v in ["favorite things", "Parrots", "Analysis"])


@pytest.mark.asyncio
async def test_office_odt_table() -> None:
    """ODT document with a table structure."""
    path = "odt/table.odt"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.oasis.opendocument.text"
    assert len(result.content) >= 10
    assert len(result.tables) >= 1


@pytest.mark.asyncio
async def test_office_opml_basic() -> None:
    """OPML outline document extraction."""
    path = "opml/outline.opml"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/xml+opml", "text/x-opml", "application/x-opml+xml"])
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_org_basic() -> None:
    """Org-mode document text extraction."""
    path = "org/comprehensive.org"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/x-org", "text/org"])
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_pages_basic() -> None:
    """Pages document extraction baseline."""
    path = "iwork/test.pages"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/x-iwork-pages-sffpages"
    assert len(result.content) >= 5


@pytest.mark.asyncio
async def test_office_ppsx_slideshow() -> None:
    """PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2."""
    path = "pptx/sample.ppsx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.presentationml.slideshow"
    assert len(result.content) >= 10


@pytest.mark.skip(reason="Requires features: office; Requires the office feature.")
@pytest.mark.asyncio
async def test_office_ppt_legacy() -> None:
    """Legacy PowerPoint .ppt extraction via native OLE/CFB parsing."""
    path = "ppt/simple.ppt"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.ms-powerpoint"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_pptm_basic() -> None:
    """PowerPoint macro-enabled presentation (.pptm) extraction."""
    path = "pptx/powerpoint_with_image.pptm"
    result = await extract_file(path=path)
    assert any(
        v in result.mime_type
        for v in [
            "application/vnd.ms-powerpoint.presentation.macroEnabled.12",
            "application/vnd.openxmlformats-officedocument.presentationml.presentation",
        ]
    )
    assert result.content


@pytest.mark.asyncio
async def test_office_pptx_basic() -> None:
    """PPTX deck should extract slides content."""
    path = "pptx/simple.pptx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
    assert len(result.content) >= 50


@pytest.mark.asyncio
async def test_office_pptx_images() -> None:
    """PPTX presentation containing images to ensure metadata extraction."""
    path = "pptx/powerpoint_with_image.pptx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
    assert len(result.content) >= 15


@pytest.mark.asyncio
async def test_office_pptx_pitch_deck() -> None:
    """Pitch deck PPTX used to validate large slide extraction."""
    path = "pptx/pitch_deck_presentation.pptx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
    assert len(result.content) >= 100


@pytest.mark.asyncio
async def test_office_rst_basic() -> None:
    """reStructuredText document extraction."""
    path = "rst/restructured_text.rst"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["text/x-rst", "text/prs.fallenstein.rst"])
    assert len(result.content) >= 20


@pytest.mark.asyncio
async def test_office_rtf_basic() -> None:
    """RTF document text extraction."""
    path = "rtf/extraction_test.rtf"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/rtf", "text/rtf"])
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_typst_basic() -> None:
    """Typst document text extraction."""
    path = "typst/headings.typ"
    result = await extract_file(path=path)
    assert any(v in result.mime_type for v in ["application/x-typst", "text/x-typst"])
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_xls_legacy() -> None:
    """Legacy XLS spreadsheet to ensure backward compatibility."""
    path = "xls/test_excel.xls"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.ms-excel"
    assert len(result.content) >= 10


@pytest.mark.asyncio
async def test_office_xlsb_basic() -> None:
    """Excel binary workbook (.xlsb) extraction."""
    path = "xlsx/test_xlsb.xlsb"
    result = await extract_file(path=path)
    assert any(
        v in result.mime_type
        for v in [
            "application/vnd.ms-excel.sheet.binary.macroEnabled.12",
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        ]
    )
    assert result.content


@pytest.mark.asyncio
async def test_office_xlsm_basic() -> None:
    """Excel macro-enabled workbook (.xlsm) extraction."""
    path = "xlsx/test_01.xlsm"
    result = await extract_file(path=path)
    assert any(
        v in result.mime_type
        for v in [
            "application/vnd.ms-excel.sheet.macroEnabled.12",
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        ]
    )
    assert result.content


@pytest.mark.asyncio
async def test_office_xlsx_basic() -> None:
    """XLSX spreadsheet should produce metadata and table content."""
    path = "xlsx/stanley_cups.xlsx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    assert len(result.content) >= 100
    assert result.content is not None
    assert "Team" in result.content
    assert result.content is not None
    assert "Location" in result.content
    assert result.content is not None
    assert "Stanley Cups" in result.content
    assert len(result.tables) >= 1
    assert result.metadata.sheet_count >= 2
    assert "Stanley Cups" in result.metadata.sheet_names


@pytest.mark.asyncio
async def test_office_xlsx_multi_sheet() -> None:
    """XLSX workbook with multiple sheets."""
    path = "xlsx/excel_multi_sheet.xlsx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    assert len(result.content) >= 20
    assert result.metadata.sheet_count >= 2


@pytest.mark.asyncio
async def test_office_xlsx_office_example() -> None:
    """Simple XLSX spreadsheet shipped alongside office integration tests."""
    path = "xlsx/test_01.xlsx"
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    assert len(result.content) >= 10
