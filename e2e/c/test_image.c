/* Code generated by kreuzberg-e2e-generator. DO NOT EDIT. */
/* Category: image */

#include "helpers.h"
#include <stdio.h>

static void test_image_image_bmp_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images/bmp_24.bmp", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/bmp"}, 1);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_gif_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.gif", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/gif"}, 1);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_jp2_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.jp2", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/jp2", "image/jpeg2000"}, 2);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_metadata_only(void) {
    CExtractionResult *result = run_extraction("images/example.jpg", "{\"ocr\":null,\"disable_ocr\":true}");
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/jpeg"}, 1);
    assert_max_content_length(result, 200);
    kreuzberg_free_result(result);
}

static void test_image_image_pbm_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.pbm", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/x-portable-bitmap", "image/x-pbm"}, 2);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_pgm_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.pgm", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/x-portable-graymap", "image/x-pgm"}, 2);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_ppm_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.ppm", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/x-portable-pixmap", "image/x-ppm"}, 2);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_svg_basic(void) {
    CExtractionResult *result = run_extraction("xml/simple_svg.svg", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/svg+xml"}, 1);
    assert_min_content_length(result, 5);
    kreuzberg_free_result(result);
}

static void test_image_image_tiff_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.tif", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/tiff"}, 1);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

static void test_image_image_webp_basic(void) {
    if (skip_if_feature_unavailable("tesseract")) return;
    CExtractionResult *result = run_extraction("images_extra/ocr_image.webp", NULL);
    if (!result) return; /* skipped */
    assert_expected_mime(result, (const char *[]){"image/webp"}, 1);
    assert_content_not_empty(result);
    kreuzberg_free_result(result);
}

int main(void) {
    test_image_image_bmp_basic();
    test_image_image_gif_basic();
    test_image_image_jp2_basic();
    test_image_image_metadata_only();
    test_image_image_pbm_basic();
    test_image_image_pgm_basic();
    test_image_image_ppm_basic();
    test_image_image_svg_basic();
    test_image_image_tiff_basic();
    test_image_image_webp_basic();
    printf("test_image: all tests passed\n");
    return 0;
}
