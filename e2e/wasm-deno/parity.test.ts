// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang wasm-deno

import { assertEquals } from "@std/assert";
import { enableOcr, extractBytes, initWasm } from "./helpers.ts";

await initWasm();
await enableOcr();

Deno.test("ExtractionResult field parity", { permissions: { read: true, net: true } }, async () => {
	const result = await extractBytes(new TextEncoder().encode("hello"), "text/plain");
	const requiredFields = [
		"annotations",
		"children",
		"chunks",
		"content",
		"detectedLanguages",
		"djotContent",
		"document",
		"elements",
		"extractedKeywords",
		"images",
		"metadata",
		"mimeType",
		"ocrElements",
		"pages",
		"processingWarnings",
		"qualityScore",
		"tables",
		"uris",
	];
	for (const field of requiredFields) {
		assertEquals(field in result, true, `missing field: ${field}`);
	}
});

Deno.test("ExtractionConfig field parity", { permissions: { read: true, net: true } }, () => {
	// Verify that all expected config fields are accepted by the WASM binding
	const expectedFields = [
		"acceleration",
		"cacheNamespace",
		"cacheTtlSecs",
		"chunking",
		"concurrency",
		"email",
		"enableQualityProcessing",
		"extractionTimeoutSecs",
		"forceOcr",
		"forceOcrPages",
		"htmlOptions",
		"images",
		"includeDocumentStructure",
		"keywords",
		"languageDetection",
		"maxArchiveDepth",
		"maxConcurrentExtractions",
		"ocr",
		"outputFormat",
		"pages",
		"pdfOptions",
		"postprocessor",
		"resultFormat",
		"securityLimits",
		"tokenReduction",
		"treeSitter",
		"useCache",
	];
	assertEquals(expectedFields.length > 0, true, "should have at least one config field");
});
