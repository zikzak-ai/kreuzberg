// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang wasm-deno

// Tests for image fixtures. Run with: deno test --allow-read

import {
	assertions,
	buildConfig,
	enableOcr,
	extractBytes,
	initWasm,
	resolveDocument,
	shouldSkipFixture,
} from "./helpers.ts";
import type { ExtractionResult } from "./helpers.ts";

// Initialize WASM module and enable OCR once at module load time
await initWasm();
await enableOcr();

Deno.test("image_bmp_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images/bmp_24.bmp");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/bmp", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_bmp_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/bmp"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_gif_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.gif");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/gif", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_gif_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/gif"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_jp2_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.jp2");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/jp2", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_jp2_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/jp2", "image/jpeg2000"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_metadata_only", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig({ ocr: null });
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images/example.jpg");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/jpeg", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_metadata_only", [], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/jpeg"]);
	assertions.assertMaxContentLength(result, 200);
});

Deno.test("image_pbm_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.pbm");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/x-portable-bitmap", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_pbm_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/x-portable-bitmap", "image/x-pbm"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_pgm_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.pgm");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/x-portable-graymap", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_pgm_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/x-portable-graymap", "image/x-pgm"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_ppm_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.ppm");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/x-portable-pixmap", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_ppm_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/x-portable-pixmap", "image/x-ppm"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_svg_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("xml/simple_svg.svg");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/svg+xml", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_svg_basic", [], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/svg+xml"]);
	assertions.assertMinContentLength(result, 5);
});

Deno.test("image_tiff_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.tif");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/tiff", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_tiff_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/tiff"]);
	assertions.assertContentNotEmpty(result);
});

Deno.test("image_webp_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("images_extra/ocr_image.webp");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "image/webp", config);
	} catch (error) {
		if (shouldSkipFixture(error, "image_webp_basic", ["tesseract"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["image/webp"]);
	assertions.assertContentNotEmpty(result);
});
