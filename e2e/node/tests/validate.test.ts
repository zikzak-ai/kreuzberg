// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:badd7fbe170e62daafedafd9c16a76df6e40a76f16f4ef3c57cddf9685a31912
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, extractFileSync, , type JsExtractionConfig } from 'kreuzberg';

describe('validate', () => {
  it('validate_binarization_invalid: Invalid binarization', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_binarization_valid: Valid binarization', async () => {
    await extractFile(null, {} as unknown as JsExtractionConfig);
  });

  it('validate_chunking_overlap_exceeds: Reject chunking config where overlap exceeds max_characters', async () => {
    await expect(async () => {
      await extractFileSync("pdf/fake_memo.pdf", { chunking: { maxCharacters: 10, maxOverlap: 100 } } as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_chunking_params_invalid: Chunking overlap >= max', async () => {
    await expect(async () => {
      await ({ max_characters: 50, overlap: 60 });
    }).rejects.toThrow();
  });

  it('validate_chunking_params_valid: Valid chunking params', () => {
    ({ max_characters: 500, overlap: 50 });
  });

  it('validate_chunking_valid: Accept valid chunking config', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", { chunking: { maxCharacters: 1000, maxOverlap: 200 } } as unknown as JsExtractionConfig);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('validate_confidence_boundary: Confidence at 1.0', async () => {
    await extractFile(null, {} as unknown as JsExtractionConfig);
  });

  it('validate_confidence_negative: Negative confidence', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_confidence_over: Confidence > 1', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_confidence_valid: Accept valid confidence threshold', () => {
    extractFileSync("pdf/fake_memo.pdf", { ocr: { confidenceThreshold: 0.8 } } as unknown as JsExtractionConfig);
  });

  it('validate_confidence_zero: Confidence at 0.0', async () => {
    await extractFile(null, {} as unknown as JsExtractionConfig);
  });

  it('validate_dpi_negative: Reject negative DPI in OCR config', async () => {
    await expect(async () => {
      await extractFileSync("pdf/fake_memo.pdf", { ocr: { dpi: -1 } } as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_dpi_valid: Accept valid DPI in OCR config', () => {
    extractFileSync("pdf/fake_memo.pdf", { ocr: { dpi: 300 } } as unknown as JsExtractionConfig);
  });

  it('validate_host_empty: Empty host rejected', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_ocr_backend_invalid: Invalid OCR backend', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_ocr_backend_valid: Valid OCR backend', async () => {
    await extractFile(null, {} as unknown as JsExtractionConfig);
  });

  it('validate_output_format_invalid: Reject invalid output format', async () => {
    await expect(async () => {
      await extractFileSync("pdf/fake_memo.pdf", { outputFormat: "nonexistent_format" } as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_output_format_valid: Accept valid output format', () => {
    extractFileSync("pdf/fake_memo.pdf", { outputFormat: "markdown" } as unknown as JsExtractionConfig);
  });

  it('validate_port_max: Port 65535 accepted', async () => {
    await extractFile(null, {} as unknown as JsExtractionConfig);
  });

  it('validate_port_over: Port over 65535', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_port_zero: Port 0 rejected', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_reduction_level_invalid: Invalid reduction level', async () => {
    await expect(async () => {
      await extractFile(null, {} as unknown as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_reduction_level_valid: Valid reduction level', async () => {
    await extractFile(null, {} as unknown as JsExtractionConfig);
  });
});
