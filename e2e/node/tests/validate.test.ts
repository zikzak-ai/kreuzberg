// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extractFile, type ExtractionConfig } from 'kreuzberg';

describe('validate', () => {
  it('validate_binarization_invalid: Invalid binarization', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_binarization_valid: Valid binarization', async () => {
    await extractFile("");
  });

  it('validate_chunking_overlap_exceeds: Reject chunking config where overlap exceeds max_characters', async () => {
    await expect(async () => {
      await extractFile("pdf/fake_memo.pdf", { chunking: { max_characters: 10, max_overlap: 100 } } as ExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_chunking_params_invalid: Chunking overlap >= max', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_chunking_params_valid: Valid chunking params', async () => {
    await extractFile("");
  });

  it('validate_chunking_valid: Accept valid chunking config', async () => {
    const result = await extractFile("pdf/fake_memo.pdf", { chunking: { max_characters: 1000, max_overlap: 200 } } as ExtractionConfig);
    // TODO: unsupported assertion type: min_length
  });

  it('validate_confidence_boundary: Confidence at 1.0', async () => {
    await extractFile("");
  });

  it('validate_confidence_negative: Negative confidence', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_confidence_over: Confidence > 1', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_confidence_valid: Accept valid confidence threshold', async () => {
    await extractFile("pdf/fake_memo.pdf", { ocr: { confidence_threshold: 0.8 } } as ExtractionConfig);
  });

  it('validate_confidence_zero: Confidence at 0.0', async () => {
    await extractFile("");
  });

  it('validate_dpi_negative: Reject negative DPI in OCR config', async () => {
    await expect(async () => {
      await extractFile("pdf/fake_memo.pdf", { ocr: { dpi: -1 } } as ExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_dpi_valid: Accept valid DPI in OCR config', async () => {
    await extractFile("pdf/fake_memo.pdf", { ocr: { dpi: 300 } } as ExtractionConfig);
  });

  it('validate_host_empty: Empty host rejected', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_ocr_backend_invalid: Invalid OCR backend', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_ocr_backend_valid: Valid OCR backend', async () => {
    await extractFile("");
  });

  it('validate_output_format_invalid: Reject invalid output format', async () => {
    await expect(async () => {
      await extractFile("pdf/fake_memo.pdf", { output_format: "nonexistent_format" } as ExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_output_format_valid: Accept valid output format', async () => {
    await extractFile("pdf/fake_memo.pdf", { output_format: "markdown" } as ExtractionConfig);
  });

  it('validate_port_max: Port 65535 accepted', async () => {
    await extractFile("");
  });

  it('validate_port_over: Port over 65535', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_port_zero: Port 0 rejected', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_reduction_level_invalid: Invalid reduction level', async () => {
    await expect(async () => {
      await extractFile("");
    }).rejects.toThrow();
  });

  it('validate_reduction_level_valid: Valid reduction level', async () => {
    await extractFile("");
  });
});
