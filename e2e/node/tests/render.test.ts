// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extractFile, type ExtractionConfig } from 'kreuzberg';

describe('render', () => {
  it('render_custom_dpi: Render PDF page with custom DPI', async () => {
    const result = await extractFile("pdf/tiny.pdf");
    expect(result.isPng).toBe(true);
    expect(result.byteLength).toBeGreaterThanOrEqual(50);
  });

  it('render_high_dpi: Render PDF page with high DPI setting (300 DPI)', async () => {
    await extractFile("pdf/fake_memo.pdf", { dpi: 300 } as ExtractionConfig);
  });

  it('render_iterator: Iterate all PDF pages as PNG images', async () => {
    const result = await extractFile("pdf/tiny.pdf");
    expect(result.isPng).toBe(true);
    expect(result.pageCount).toBeGreaterThanOrEqual(1);
  });

  it('render_multi_page: Render a page from a multi-page PDF document', async () => {
    const result = await extractFile("pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('render_output_djot: Extract PDF with Djot output format', async () => {
    const result = await extractFile("pdf/fake_memo.pdf", { output_format: "djot" } as ExtractionConfig);
    expect(result.outputFormat.trim()).toBe("djot");
    // TODO: unsupported assertion type: min_length
  });

  it('render_output_html: Extract PDF with HTML output format', async () => {
    const result = await extractFile("pdf/fake_memo.pdf", { output_format: "html" } as ExtractionConfig);
    expect(result.outputFormat.trim()).toBe("html");
    expect(["<", ">", "</"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('render_output_markdown: Extract PDF with Markdown output format', async () => {
    const result = await extractFile("pdf/fake_memo.pdf", { output_format: "markdown" } as ExtractionConfig);
    expect(result.outputFormat.trim()).toBe("markdown");
    expect(["#", "**", "*"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('render_output_plain: Extract PDF with plain text output format', async () => {
    const result = await extractFile("pdf/fake_memo.pdf", { output_format: "plain" } as ExtractionConfig);
    expect(result.outputFormat.trim()).toBe("plain");
    // TODO: unsupported assertion type: min_length
  });

  it('render_single_page: Render a single PDF page to PNG', async () => {
    const result = await extractFile("pdf/tiny.pdf");
    expect(result.isPng).toBe(true);
    expect(result.byteLength).toBeGreaterThanOrEqual(100);
  });
});
