// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a5737bb338ead750f17b9a3b414c859ced190160f0dbf8f15247aa65cc711dfd
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import {
  extractFile,
  batchExtractBytesSync,
  batchExtractBytes,
  batchExtractFiles,
  batchExtractFilesSync,
  type JsExtractionConfig,
} from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("batch", () => {
  it("batch_bytes_invalid_mime: batch_extract_bytes_sync invalid MIME", () => {
    batchExtractBytesSync([
      { content: [72, 101, 108, 108, 111], mimeType: "application/x-nonexistent" },
    ] as JsExtractionConfig);
  });

  it("batch_extract_bytes_happy: batch_extract_bytes: happy path with mixed inputs", async () => {
    const result = await batchExtractBytes([
      { content: "Hello, world!", mimeType: "text/plain" },
      { content: "<html><body>Test</body></html>", mimeType: "text/html" },
    ] as JsExtractionConfig);
    expect(result.results.length).toBeGreaterThanOrEqual(1);
  });

  it("batch_extract_bytes_mixed_format: batch_extract_bytes: handles unsupported MIME gracefully", async () => {
    await batchExtractBytes([
      { content: "PDF placeholder", mimeType: "application/x-unknown" },
    ] as JsExtractionConfig);
  });

  it("batch_extract_bytes_size_cap: batch_extract_bytes: respects security limits", async () => {
    await expect(async () => {
      await batchExtractBytes(
        [
          {
            content:
              "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
            mimeType: "text/plain",
          },
        ] as JsExtractionConfig,
        { securityLimits: { maxExtractionSize: 50 } } as JsExtractionConfig,
      );
    }).rejects.toThrow();
  });

  it("batch_extract_bytes_sync_empty_list: batch_extract_bytes_sync: empty batch", () => {
    const result = batchExtractBytesSync([] as JsExtractionConfig);
    expect(result.results.length).toBe(0);
  });

  it("batch_extract_bytes_sync_invalid_mime: batch_extract_bytes_sync: unsupported MIME", () => {
    batchExtractBytesSync([
      { content: "data", mimeType: "application/x-unknown" },
    ] as JsExtractionConfig);
  });

  it("batch_file_async_basic: Extract text from multiple files asynchronously", async () => {
    await batchExtractFiles([
      { path: "pdf/fake_memo.pdf" },
      { path: "text/fake_text.txt" },
    ] as JsExtractionConfig);
  });

  it("batch_file_async_not_found: batch_extract_file async nonexistent", async () => {
    await batchExtractFiles([{ path: "/nonexistent/a.pdf" }] as JsExtractionConfig);
  });

  it("batch_file_not_found: batch_extract_file_sync nonexistent", () => {
    batchExtractFilesSync([
      { path: "/nonexistent/a.pdf" },
      { path: "/nonexistent/b.txt" },
    ] as JsExtractionConfig);
  });

  it("batch_file_partial: batch_extract_file_sync mixed", () => {
    batchExtractFilesSync([
      { path: "text/plain.txt" },
      { path: "/nonexistent/missing.pdf" },
    ] as JsExtractionConfig);
  });

  it("batch_file_sync_basic: Extract text from multiple files synchronously", () => {
    batchExtractFilesSync([
      { path: "pdf/fake_memo.pdf" },
      { path: "text/fake_text.txt" },
    ] as JsExtractionConfig);
  });
});
