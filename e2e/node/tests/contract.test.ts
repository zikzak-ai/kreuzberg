// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6a4809f13704d2e41a1e52d75f16de040b63890e080dfa9d226ee9b999c65a67
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, batchExtractBytes, batchExtractBytesSync, batchExtractFile, batchExtractFileSync, extractBytes, extractBytesSync, extractFileSync, type JsExtractionConfig } from 'kreuzberg';

describe('contract', () => {
  it('api_batch_bytes_async: Tests async batch bytes extraction API (batch_extract_bytes)', async () => {
    const result = await batchExtractBytes(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_bytes_sync: Tests sync batch bytes extraction API (batch_extract_bytes_sync)', () => {
    const result = batchExtractBytesSync(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_bytes_with_configs_async: Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)', async () => {
    const result = await batchExtractBytes(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
  });

  it('api_batch_bytes_with_configs_sync: Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)', () => {
    const result = batchExtractBytesSync(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
  });

  it('api_batch_file_async: Tests async batch file extraction API (batch_extract_file)', async () => {
    const result = await batchExtractFile(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_file_sync: Tests sync batch file extraction API (batch_extract_file_sync)', () => {
    const result = batchExtractFileSync(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_file_with_configs_async: Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)', async () => {
    const result = await batchExtractFile(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
  });

  it('api_batch_file_with_configs_sync: Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)', () => {
    const result = batchExtractFileSync(null);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
  });

  it('api_batch_file_with_timeout_sync: Tests sync batch file extraction with per-file timeout config override', () => {
    const result = batchExtractFileSync(null, { extractionTimeoutSecs: 300 } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('api_extract_bytes_async: Tests async bytes extraction API (extract_bytes)', async () => {
    const result = await extractBytes(null, "");
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_extract_bytes_sync: Tests sync bytes extraction API (extract_bytes_sync)', () => {
    const result = extractBytesSync(null, "");
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_extract_file_async: Tests async file extraction API (extract_file)', async () => {
    const result = await extractFile("pdf/fake_memo.pdf");
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_extract_file_sync: Tests sync file extraction API (extract_file_sync)', () => {
    const result = extractFileSync("pdf/fake_memo.pdf");
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('config_acceleration_cpu_provider: Tests explicit CPU acceleration provider configuration', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { acceleration: { deviceId: 0, provider: "cpu" } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
    expect(["May 5, 2023", "To Whom it May Concern"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('config_chunking: Tests chunking configuration with chunk assertions', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { chunking: { maxChars: 500, maxOverlap: 50 } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    expect((result.chunks ?? []).every((c: { content?: string }) => !!c.content)).toBe(true);
  });

  it('config_chunking_text: Tests text chunker type (generic whitespace/punctuation splitter)', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { chunking: { chunkerType: "text", maxChars: 500, maxOverlap: 50 } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    expect((result.chunks ?? []).every((c: { content?: string }) => !!c.content)).toBe(true);
  });

  it('config_disable_ocr: Tests disable_ocr configuration option - OCR is skipped for images', () => {
    const result = extractFileSync("images/test_hello_world.png", undefined, { disableOcr: true } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("image/png");
    expect(result.content.length).toBeLessThanOrEqual(5);
  });

  it('config_document_structure: Tests include_document_structure config produces document tree', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { includeDocumentStructure: true } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect((result.document ?? "").length).toBeGreaterThan(0);
    expect(result.document.nodes.length).toBeGreaterThanOrEqual(1);
    expect(result.document.nodeTypes).toContain("paragraph");
  });

  it('config_document_structure_disabled: Tests document field is null when include_document_structure is false', () => {
    const result = extractFileSync("pdf/fake_memo.pdf");
    expect(result.mimeType.trim()).toBe("application/pdf");
  });

  it('config_document_structure_with_headings: Tests document structure with DOCX heading-driven nesting', () => {
    const result = extractFileSync("docx/fake.docx", undefined, { includeDocumentStructure: true } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.wordprocessingml.document");
    expect((result.document ?? "").length).toBeGreaterThan(0);
    expect(result.document.nodes.length).toBeGreaterThanOrEqual(1);
  });

  it('config_email_msg_fallback_codepage: Tests MSG extraction with custom fallback codepage for Cyrillic', async () => {
    const result = await extractFile("email/fake_email.msg", undefined, { email: { msgFallbackCodepage: 1251 } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/vnd.ms-outlook");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('config_extraction_timeout: Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { extractionTimeoutSecs: 300 } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('config_html_options: Tests extraction with HTML conversion options configured', () => {
    const result = extractFileSync("html/complex_table.html", undefined, { htmlOptions: { extractMetadata: true } } as JsExtractionConfig);
    expect(["text/html"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('config_html_styled_custom_css: HTML output with inline user CSS string is embedded verbatim in the style block', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { htmlOutput: { css: ".kb-p { color: red; }", embedCss: true, theme: "unstyled" }, outputFormat: "html" } as JsExtractionConfig);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("html");
    expect(result.content).toContain(".kb-p { color: red; }");
    expect(result.content).toContain("kb-doc");
  });

  it('config_html_styled_default: HTML output with default theme emits kb-* class hooks and an embedded style block', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { htmlOutput: { embedCss: true, theme: "default" }, outputFormat: "html" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect((result.metadata.outputFormat ?? "").trim()).toBe("html");
    expect(result.content).toContain("kb-doc");
    expect(result.content).toContain("kb-content");
    expect(result.content).toContain("kb-p");
    expect(result.content).toContain("<style>");
  });

  it('config_html_styled_no_embed: HTML output with embed_css false emits class hooks but no style block', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { htmlOutput: { embedCss: false, theme: "default" }, outputFormat: "html" } as JsExtractionConfig);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("html");
    expect(result.content).toContain("kb-doc");
    expect(result.content).toContain("kb-content");
  });

  it('config_images: Tests image extraction configuration with image assertions', () => {
    const result = extractFileSync("pdf/embedded_images_tables.pdf", undefined, { images: { extractImages: true } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.images.length).toBeGreaterThanOrEqual(1);
  });

  it('config_images_with_formats: Tests image extraction on PPTX containing embedded images', () => {
    const result = extractFileSync("pptx/powerpoint_with_image.pptx", undefined, { images: { extractImages: true } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.presentationml.presentation");
    expect(result.images.length).toBeGreaterThanOrEqual(1);
  });

  it('config_language_detection: Tests language detection configuration', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { languageDetection: { enabled: true } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.detectedLanguages).toContain("eng");
  });

  it('config_language_detection_multi: Tests language detection with detect_multiple and min_confidence options', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { languageDetection: { detectMultiple: true, enabled: true, minConfidence: 0.3 } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.detectedLanguages).toContain("eng");
  });

  it('config_postprocessor: Tests postprocessor config is accepted and extraction succeeds', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { postprocessor: { enabled: true } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('config_processing_warnings_empty: Tests that a clean PDF extraction produces no processing warnings', () => {
    const result = extractFileSync("pdf/fake_memo.pdf");
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.processingWarnings).toHaveLength(0);
  });

  it('config_quality_disabled: Tests extraction with quality processing explicitly disabled', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { enableQualityProcessing: false } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('config_security_limits: Tests archive extraction with custom security limits', () => {
    const result = extractFileSync("archives/documents.zip", undefined, { securityLimits: { maxArchiveSize: 104857600, maxCompressionRatio: 50, maxFilesInArchive: 100 } } as JsExtractionConfig);
    expect(["application/zip", "application/x-zip-compressed"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('config_tables_content: Tests table extraction with content_contains_any assertion on DOCX with tables', () => {
    const result = extractFileSync("docx/docx_tables.docx");
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.wordprocessingml.document");
    expect(result.tables.length).toBeGreaterThanOrEqual(1);
  });

  it('config_use_cache_false: Tests use_cache=false configuration option', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { useCache: false } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('output_format_bytes_markdown: Tests markdown output format via bytes extraction API', () => {
    const result = extractBytesSync(null, "", { outputFormat: "markdown" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
  });

  it('output_format_djot: Tests Djot output format', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { outputFormat: "djot" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("djot");
  });

  it('output_format_html: Tests HTML output format', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { outputFormat: "html" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("html");
  });

  it('output_format_markdown: Tests Markdown output format', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { outputFormat: "markdown" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
  });

  it('output_format_plain: Tests Plain output format', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { outputFormat: "plain" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("plain");
  });

  it('result_format_element_based: Tests ElementBased result format with element assertions', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { resultFormat: "element_based" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.elements.length).toBeGreaterThanOrEqual(1);
  });

  it('result_format_unified: Tests Unified result format (default)', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { resultFormat: "unified" } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.elements ?? "").toHaveLength(0);
  });
});
