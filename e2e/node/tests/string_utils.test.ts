// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extractFile } from 'kreuzberg';

describe('string_utils', () => {
  it('camel_to_snake_basic: Convert camelCase to snake_case', async () => {
    const result = await extractFile("");
    expect(result.result.trim()).toBe("my_variable_name");
  });

  it('camel_to_snake_single: Convert single word (no camelCase) remains unchanged', async () => {
    const result = await extractFile("");
    expect(result.result.trim()).toBe("hello");
  });

  it('sanitize_filename_basic: Sanitize path traversal attempts in filename', async () => {
    const result = await extractFile("");
    // TODO: unsupported assertion type: min_length
  });

  it('sanitize_filename_special: Sanitize special characters in filename', async () => {
    const result = await extractFile("");
    // TODO: unsupported assertion type: min_length
  });

  it('sanitize_path_basic: Sanitize path traversal in file path', async () => {
    const result = await extractFile("../../../secret/file.txt");
    // TODO: unsupported assertion type: min_length
  });

  it('sanitize_path_spaces: Sanitize path with spaces', async () => {
    await extractFile("my folder/file.txt");
  });

  it('sanitize_path_traversal: Sanitize path traversal', async () => {
    await extractFile("../../../etc/passwd");
  });

  it('snake_to_camel_basic: Convert snake_case to camelCase', async () => {
    const result = await extractFile("");
    expect(result.result.trim()).toBe("myVariableName");
  });

  it('snake_to_camel_single: Convert single word remains unchanged', async () => {
    const result = await extractFile("");
    expect(result.result.trim()).toBe("hello");
  });
});
