// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c815075c4bb1e478b4326dc5a9ba5cb5401fcf5b89345cf5ebfa50761f987157
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import {
  extractFile,
  embedTextsAsync,
  embedTexts,
  getEmbeddingPreset,
  listEmbeddingPresets,
  type JsExtractionConfig,
} from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("embeddings", () => {
  it("embed_texts_async_empty_input: embed_texts_async: empty text list", async () => {
    const result = await embedTextsAsync([] as JsExtractionConfig);
    expect(result.length).toBe(0);
  });

  it("embed_texts_async_happy: embed_texts_async: basic async embedding", async () => {
    const result = await embedTextsAsync(["First", "Second"] as JsExtractionConfig);
    expect(result.length).toBeGreaterThanOrEqual(2);
  });

  it("embed_texts_async_preset_switch: embed_texts_async: preset override", async () => {
    await embedTextsAsync(
      ["Text"] as JsExtractionConfig,
      { embeddingModel: "default" } as JsExtractionConfig,
    );
  });

  it("embed_texts_different_preset: embed_texts: multilingual preset", () => {
    const result = embedTexts(
      ["Hello world", "Test"] as JsExtractionConfig,
      { embeddingModel: "multilingual" } as JsExtractionConfig,
    );
    expect(result.length).toBeGreaterThanOrEqual(2);
  });

  it("get_embedding_preset_known: get_embedding_preset: known preset", () => {
    getEmbeddingPreset("default");
  });

  it("get_embedding_preset_nominal: get_embedding_preset: nominal case", () => {
    getEmbeddingPreset("default");
  });

  it("get_embedding_preset_unknown: get_embedding_preset: unknown preset fails", async () => {
    await expect(async () => {
      await getEmbeddingPreset("nonexistent-xyz");
    }).rejects.toThrow();
  });

  it("list_embedding_presets_sanity: list_embedding_presets: returns at least one", () => {
    const result = listEmbeddingPresets({});
    expect(result.presets.length).toBeGreaterThanOrEqual(1);
  });
});
