// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:81690228ffa7fba4ea75870113b72c9e493b98af6e5b5c4a702c2493956d8a34
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, extract_file_sync, reduce_tokens, type ExtractionConfig } from 'kreuzberg';

describe('token_reduction', () => {
  it('token_reduction_aggressive: Tests aggressive token reduction mode significantly reduces content', () => {
    const result = extract_file_sync("pdf/fake_memo.pdf", { token_reduction: { mode: "aggressive" } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(150);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_basic: Tests basic token reduction on PDF document', () => {
    const result = extract_file_sync("pdf/fake_memo.pdf", { token_reduction: { mode: "moderate" } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(200);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_light: Tests light token reduction mode preserves most content', () => {
    const result = extract_file_sync("pdf/fake_memo.pdf", { token_reduction: { mode: "light" } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_reduce_aggressive: Reduce tokens with aggressive compression level', () => {
    const result = reduce_tokens("The quick brown fox jumps over the lazy dog.", "aggressive");
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it('token_reduction_reduce_basic: Reduce tokens with light compression level', () => {
    const result = reduce_tokens("The quick brown fox jumps over the lazy dog. This is a test sentence.", "light");
    expect(result.result).toContain("quick");
  });

  it('token_reduction_with_chunking: Tests token reduction combined with chunking', () => {
    const result = extract_file_sync("pdf/fake_memo.pdf", { chunking: { max_chars: 500, max_overlap: 50 }, token_reduction: { mode: "moderate" } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(200);
    expect(result.content.length).toBeGreaterThan(0);
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    expect(result.chunksHaveContent).toBe(true);
  });
});
