// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2d39147eb92900f85b67dd1072caf9adceb80aeee2ba979acc32a04136628a27
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, validate_confidence, validate_output_format, validate_host, validate_language_code, validate_mime_type, validate_tesseract_oem, validate_port, validate_tesseract_psm } from 'kreuzberg';

describe('validation', () => {
  it('validate_confidence_fn_invalid: Reject confidence threshold > 1.0', async () => {
    await expect(async () => {
      await validate_confidence(2.0);
    }).rejects.toThrow();
  });

  it('validate_confidence_fn_valid: Accept valid confidence threshold', () => {
    validate_confidence(0.95);
  });

  it('validate_format_invalid: Reject invalid output format', async () => {
    await expect(async () => {
      await validate_output_format("nonexistent");
    }).rejects.toThrow();
  });

  it('validate_format_valid: Accept valid output format', () => {
    validate_output_format("markdown");
  });

  it('validate_host_valid: Accept valid host address', () => {
    validate_host("127.0.0.1");
  });

  it('validate_lang_invalid: Reject invalid language code', async () => {
    await expect(async () => {
      await validate_language_code("zzz_nonexistent_9999");
    }).rejects.toThrow();
  });

  it('validate_lang_valid: Accept valid language code', () => {
    validate_language_code("eng");
  });

  it('validate_mime_invalid: Reject invalid MIME type', async () => {
    await expect(async () => {
      await validate_mime_type("invalid");
    }).rejects.toThrow();
  });

  it('validate_mime_valid: Accept valid MIME type', () => {
    validate_mime_type("application/pdf");
  });

  it('validate_oem_invalid: Reject invalid Tesseract OEM mode', async () => {
    await expect(async () => {
      await validate_tesseract_oem(999);
    }).rejects.toThrow();
  });

  it('validate_oem_valid: Accept valid Tesseract OEM mode', () => {
    validate_tesseract_oem(1);
  });

  it('validate_port_invalid: Reject invalid port number (zero)', async () => {
    await expect(async () => {
      await validate_port(0);
    }).rejects.toThrow();
  });

  it('validate_port_valid: Accept valid port number', () => {
    validate_port(8080);
  });

  it('validate_psm_invalid: Reject invalid Tesseract PSM mode', async () => {
    await expect(async () => {
      await validate_tesseract_psm(999);
    }).rejects.toThrow();
  });

  it('validate_psm_valid: Accept valid Tesseract PSM mode', () => {
    validate_tesseract_psm(6);
  });
});
