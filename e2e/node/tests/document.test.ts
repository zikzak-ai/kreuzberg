// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3ed40584cf41439de7d3e88e50f512ad35bfaab4dc38bfd2757f0e8d3bfb283e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile, extractFileSync, type JsExtractionConfig } from "kreuzberg";

describe("document", () => {
  it("doc_metadata_docx: Extract DOCX metadata", () => {
    const result = extractFileSync(
      "vendored/markitdown/test.docx",
      {} as unknown as JsExtractionConfig,
    );
    expect(
      (() => {
        const v = result.metadata as unknown;
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("doc_metadata_pdf: Extract PDF metadata", () => {
    const result = extractFileSync("pdf/fake_memo.pdf", {} as unknown as JsExtractionConfig);
    expect(
      (() => {
        const v = result.metadata as unknown;
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("doc_structure_docx: Extract DOCX document structure with include_document_structure config", () => {
    const result = extractFileSync("vendored/markitdown/test.docx", {
      includeDocumentStructure: true,
    } as unknown as JsExtractionConfig);
    expect(
      (() => {
        const v = (result.document as unknown) ?? "";
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("doc_structure_html: Extract HTML document structure with include_document_structure config", () => {
    const result = extractFileSync("html/html.html", {
      includeDocumentStructure: true,
    } as unknown as JsExtractionConfig);
    expect(
      (() => {
        const v = (result.document as unknown) ?? "";
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });

  it("doc_structure_pdf: Extract PDF document structure with include_document_structure config", () => {
    const result = extractFileSync("pdf/fake_memo.pdf", {
      includeDocumentStructure: true,
    } as unknown as JsExtractionConfig);
    expect(
      (() => {
        const v = (result.document as unknown) ?? "";
        return typeof v === "string" || Array.isArray(v)
          ? (v as { length: number }).length
          : Object.keys(v as object).length;
      })(),
    ).toBeGreaterThan(0);
  });
});
