// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extractFile, type ExtractionConfig } from 'kreuzberg';

describe('chunking', () => {
  it('chunk_batch_basic: Batch chunking of multiple texts in a single call', async () => {
    await extractFile("", { max_characters: 100, max_overlap: 10 } as ExtractionConfig);
  });

  it('chunk_text_basic: Basic text chunking with small chunk size and overlap', async () => {
    await extractFile("", { max_characters: 50, max_overlap: 10 } as ExtractionConfig);
  });

  it('chunk_text_large: Text chunking with large chunk size to test handling of long content', async () => {
    await extractFile("", { max_characters: 1000, max_overlap: 10 } as ExtractionConfig);
  });

  it('chunk_text_no_overlap: Text chunking with no overlap between chunks', async () => {
    await extractFile("", { max_characters: 50, max_overlap: 0 } as ExtractionConfig);
  });

  it('chunk_text_overlap_exceeds: chunk_text overlap >= max', async () => {
    await expect(async () => {
      await extractFile("", { max_characters: 50, overlap: 60 } as ExtractionConfig);
    }).rejects.toThrow();
  });

  it('chunk_text_yaml: Chunk with YAML chunker', async () => {
    await extractFile("", { chunker_type: "yaml", max_characters: 50 } as ExtractionConfig);
  });

  it('chunk_text_zero_max: chunk_text max_characters zero', async () => {
    await expect(async () => {
      await extractFile("", { max_characters: 0 } as ExtractionConfig);
    }).rejects.toThrow();
  });

  it('chunk_texts_batch_multiple: Batch chunk multiple texts', async () => {
    await extractFile("", { max_characters: 100 } as ExtractionConfig);
  });
});
