// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ff6580c22c4467139539d37c54c10f7f719b64863e6d452ee8077ec3f15bccb7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, convert_html_to_markdown, djot_to_html, render_djot, render_html, render_json, render_markdown, render_plain } from 'kreuzberg';

describe('rendering', () => {
  it('convert_html_basic: Convert basic HTML to Markdown format', () => {
    const result = convert_html_to_markdown("<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>");
    expect(result.content).toContain("Title");
  });

  it('convert_html_complex: Convert complex HTML to markdown', () => {
    const result = convert_html_to_markdown("<h1>Title</h1><p>Text with <strong>bold</strong> and <a href=\"https://example.com\">link</a>.</p>");
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it('convert_html_empty: Convert empty HTML to markdown', () => {
    convert_html_to_markdown("");
  });

  it('convert_html_table: Convert HTML table to Markdown table format', () => {
    const result = convert_html_to_markdown("<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>");
    expect(result.content).toContain("|");
  });

  it('djot_to_html_basic: Convert Djot format to HTML', () => {
    const result = djot_to_html("# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('djot_to_html_complex: Complex djot to HTML', () => {
    const result = djot_to_html("# Heading\n\nParagraph.\n\n- Item one\n- Item two");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('djot_to_html_empty: Convert empty djot to HTML', () => {
    djot_to_html("");
  });

  it('render_djot_basic: Render HTML document to Djot format', () => {
    const result = render_djot("<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_djot_formatted: Render HTML to djot', () => {
    render_djot("<h2>Section</h2><p>Text with <code>code</code>.</p>");
  });

  it('render_html_basic: Render a simple HTML document to formatted output', () => {
    const result = render_html("<h1>Test Document</h1><p>This is a paragraph with some test content.</p>");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_html_styled: Render HTML preserving structure', () => {
    const result = render_html("<h1>Heading</h1><p><strong>bold</strong></p>");
    expect(result.content).toContain("Heading");
  });

  it('render_json_basic: Render HTML document to JSON format', () => {
    const result = render_json("<h1>Structured Data</h1><p>Content for JSON rendering.</p>");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_json_complex: Render HTML to JSON', () => {
    const result = render_json("<h1>Title</h1><p>Text.</p>");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_markdown_basic: Render HTML document to Markdown format', () => {
    const result = render_markdown("<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_markdown_complex: Render complex HTML to markdown', () => {
    const result = render_markdown("<h1>Title</h1><ul><li>A</li><li>B</li></ul>");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('render_plain_basic: Render HTML document to plain text format', () => {
    const result = render_plain("<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it('render_plain_empty: Render empty HTML', () => {
    render_plain("");
  });
});
