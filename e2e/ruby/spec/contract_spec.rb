# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f1757dbc8f3d44e294ad07f8c023682c6f332f3be5f64c532e9a00f94699a34b
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# frozen_string_literal: true

require 'kreuzberg'
require 'json'

RSpec.describe 'contract' do
  it 'api_batch_bytes_async: Tests async batch bytes extraction API (batch_extract_bytes)' do
    result = Kreuzberg.batch_extract_bytes(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_batch_bytes_sync: Tests sync batch bytes extraction API (batch_extract_bytes_sync)' do
    result = Kreuzberg.batch_extract_bytes_sync(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_batch_bytes_with_configs_async: Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)' do
    result = Kreuzberg.batch_extract_bytes(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('markdown')
  end

  it 'api_batch_bytes_with_configs_sync: Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)' do
    result = Kreuzberg.batch_extract_bytes_sync(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('markdown')
  end

  it 'api_batch_file_async: Tests async batch file extraction API (batch_extract_file)' do
    result = Kreuzberg.batch_extract_file(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_batch_file_sync: Tests sync batch file extraction API (batch_extract_file_sync)' do
    result = Kreuzberg.batch_extract_file_sync(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_batch_file_with_configs_async: Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)' do
    result = Kreuzberg.batch_extract_file(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('markdown')
  end

  it 'api_batch_file_with_configs_sync: Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)' do
    result = Kreuzberg.batch_extract_file_sync(nil)
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('markdown')
  end

  it 'api_batch_file_with_timeout_sync: Tests sync batch file extraction with per-file timeout config override' do
    result = Kreuzberg.batch_extract_file_sync(nil, Kreuzberg::ExtractionConfig.new(extraction_timeout_secs: 300))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
  end

  it 'api_extract_bytes_async: Tests async bytes extraction API (extract_bytes)' do
    result = Kreuzberg.extract_bytes(nil, '')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_extract_bytes_sync: Tests sync bytes extraction API (extract_bytes_sync)' do
    result = Kreuzberg.extract_bytes_sync(nil, '')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_extract_file_async: Tests async file extraction API (extract_file)' do
    result = Kreuzberg.extract_file('pdf/fake_memo.pdf')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'api_extract_file_sync: Tests sync file extraction API (extract_file_sync)' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(['May 5, 2023', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'config_acceleration_cpu_provider: Tests explicit CPU acceleration provider configuration' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(acceleration: { 'device_id' => 0, 'provider' => 'cpu' }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 50
    expect(['May 5, 2023', 'To Whom it May Concern'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'config_chunking: Tests chunking configuration with chunk assertions' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(chunking: { 'max_chars' => 500, 'max_overlap' => 50 }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.chunks.length).to be >= 1
    expect(result.chunks_have_content).to be true
  end

  it 'config_chunking_text: Tests text chunker type (generic whitespace/punctuation splitter)' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(chunking: { 'chunker_type' => 'text', 'max_chars' => 500, 'max_overlap' => 50 }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.chunks.length).to be >= 1
    expect(result.chunks_have_content).to be true
  end

  it 'config_disable_ocr: Tests disable_ocr configuration option - OCR is skipped for images' do
    result = Kreuzberg.extract_file_sync('images/test_hello_world.png', Kreuzberg::ExtractionConfig.new(disable_ocr: true))
    expect(result.mime_type).to eq('image/png')
    expect(result.content.length).to be <= 5
  end

  it 'config_document_structure: Tests include_document_structure config produces document tree' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(include_document_structure: true))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.document_structure).not_to be_empty
    expect(result.document_structure.nodes.length).to be >= 1
    expect(result.document_structure.node_types.to_s).to include('paragraph')
  end

  it 'config_document_structure_disabled: Tests document field is null when include_document_structure is false' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf')
    expect(result.mime_type).to eq('application/pdf')
  end

  it 'config_document_structure_with_headings: Tests document structure with DOCX heading-driven nesting' do
    result = Kreuzberg.extract_file_sync('docx/fake.docx', Kreuzberg::ExtractionConfig.new(include_document_structure: true))
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.document_structure).not_to be_empty
    expect(result.document_structure.nodes.length).to be >= 1
  end

  it 'config_email_msg_fallback_codepage: Tests MSG extraction with custom fallback codepage for Cyrillic' do
    result = Kreuzberg.extract_file('email/fake_email.msg', Kreuzberg::ExtractionConfig.new(email: { 'msg_fallback_codepage' => 1251 }))
    expect(result.mime_type).to eq('application/vnd.ms-outlook')
    expect(result.content.length).to be >= 10
  end

  it 'config_extraction_timeout: Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(extraction_timeout_secs: 300))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
  end

  it 'config_html_options: Tests extraction with HTML conversion options configured' do
    result = Kreuzberg.extract_file_sync('html/complex_table.html', Kreuzberg::ExtractionConfig.new(html_options: { 'extract_metadata' => true }))
    expect(['text/html'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
    expect(result.content).not_to be_empty
  end

  it 'config_html_styled_custom_css: HTML output with inline user CSS string is embedded verbatim in the style block' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(html_output: { 'css' => '.kb-p { color: red; }', 'embed_css' => true, 'theme' => 'unstyled' }, output_format: 'html'))
    expect(result.output_format).to eq('html')
    expect(result.content.to_s).to include('.kb-p { color: red; }')
    expect(result.content.to_s).to include('kb-doc')
  end

  it 'config_html_styled_default: HTML output with default theme emits kb-* class hooks and an embedded style block' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(html_output: { 'embed_css' => true, 'theme' => 'default' }, output_format: 'html'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.output_format).to eq('html')
    expect(result.content.to_s).to include('kb-doc')
    expect(result.content.to_s).to include('kb-content')
    expect(result.content.to_s).to include('kb-p')
    expect(result.content.to_s).to include('<style>')
  end

  it 'config_html_styled_no_embed: HTML output with embed_css false emits class hooks but no style block' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(html_output: { 'embed_css' => false, 'theme' => 'default' }, output_format: 'html'))
    expect(result.output_format).to eq('html')
    expect(result.content.to_s).to include('kb-doc')
    expect(result.content.to_s).to include('kb-content')
  end

  it 'config_images: Tests image extraction configuration with image assertions' do
    result = Kreuzberg.extract_file_sync('pdf/embedded_images_tables.pdf', Kreuzberg::ExtractionConfig.new(images: { 'extract_images' => true }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.images.length).to be >= 1
  end

  it 'config_images_with_formats: Tests image extraction on PPTX containing embedded images' do
    result = Kreuzberg.extract_file_sync('pptx/powerpoint_with_image.pptx', Kreuzberg::ExtractionConfig.new(images: { 'extract_images' => true }))
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.presentationml.presentation')
    expect(result.images.length).to be >= 1
  end

  it 'config_language_detection: Tests language detection configuration' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(language_detection: { 'enabled' => true }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.detected_languages.to_s).to include('eng')
    expect(result.language_detection_confidence).to be >= 0.5
  end

  it 'config_language_detection_multi: Tests language detection with detect_multiple and min_confidence options' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(language_detection: { 'detect_multiple' => true, 'enabled' => true, 'min_confidence' => 0.3 }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.detected_languages.to_s).to include('eng')
  end

  it 'config_postprocessor: Tests postprocessor config is accepted and extraction succeeds' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(postprocessor: { 'enabled' => true }))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.content).not_to be_empty
  end

  it 'config_processing_warnings_empty: Tests that a clean PDF extraction produces no processing warnings' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.processing_warnings.nil? || result.processing_warnings.empty?).to be(true)
  end

  it 'config_quality_disabled: Tests extraction with quality processing explicitly disabled' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(enable_quality_processing: false))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.content).not_to be_empty
  end

  it 'config_security_limits: Tests archive extraction with custom security limits' do
    result = Kreuzberg.extract_file_sync('archives/documents.zip', Kreuzberg::ExtractionConfig.new(security_limits: { 'max_archive_size' => 104857600, 'max_compression_ratio' => 50, 'max_files_in_archive' => 100 }))
    expect(['application/zip', 'application/x-zip-compressed'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'config_tables_content: Tests table extraction with content_contains_any assertion on DOCX with tables' do
    result = Kreuzberg.extract_file_sync('docx/docx_tables.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.tables.length).to be >= 1
  end

  it 'config_use_cache_false: Tests use_cache=false configuration option' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(use_cache: false))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
  end

  it 'output_format_bytes_markdown: Tests markdown output format via bytes extraction API' do
    result = Kreuzberg.extract_bytes_sync(nil, '', Kreuzberg::ExtractionConfig.new(output_format: 'markdown'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('markdown')
  end

  it 'output_format_djot: Tests Djot output format' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(output_format: 'djot'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('djot')
  end

  it 'output_format_html: Tests HTML output format' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(output_format: 'html'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('html')
  end

  it 'output_format_markdown: Tests Markdown output format' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(output_format: 'markdown'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('markdown')
  end

  it 'output_format_plain: Tests Plain output format' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(output_format: 'plain'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.output_format).to eq('plain')
  end

  it 'result_format_element_based: Tests ElementBased result format with element assertions' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(result_format: 'element_based'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.result_format).to eq('element_based')
    expect(result.elements.length).to be >= 1
  end

  it 'result_format_unified: Tests Unified result format (default)' do
    result = Kreuzberg.extract_file_sync('pdf/fake_memo.pdf', Kreuzberg::ExtractionConfig.new(result_format: 'unified'))
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10
    expect(result.result_format).to eq('unified')
  end
end
