# This file is auto-generated by alef. DO NOT EDIT.
# frozen_string_literal: true

require 'kreuzberg'
require 'json'

RSpec.describe 'office' do
  it 'office_bibtex_basic: BibTeX bibliography extraction.' do
    result = Kreuzberg.extract_file('bibtex/comprehensive.bib')
    expect(['application/x-bibtex', 'text/x-bibtex'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_commonmark_basic: CommonMark (.commonmark) text extraction.' do
    result = Kreuzberg.extract_file('markdown/sample.commonmark')
    expect(['text/markdown', 'text/plain', 'text/x-commonmark'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 5
  end

  it 'office_djot_basic: Djot markup text extraction.' do
    result = Kreuzberg.extract_file('markdown/tables.djot')
    expect(['text/x-djot', 'text/djot'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_docbook_basic: DocBook XML document extraction.' do
    result = Kreuzberg.extract_file('docbook/docbook-reader.docbook')
    expect(['application/docbook+xml', 'text/docbook'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_docx_basic: DOCX document extraction baseline.' do
    result = Kreuzberg.extract_file('docx/sample_document.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 10
  end

  it 'office_docx_equations: DOCX file containing equations to validate math extraction.' do
    result = Kreuzberg.extract_file('docx/equations.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 20
  end

  it 'office_docx_fake: Simple DOCX document to verify baseline extraction.' do
    result = Kreuzberg.extract_file('docx/fake.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 20
  end

  it 'office_docx_formatting: DOCX document heavy on formatting for style preservation.' do
    result = Kreuzberg.extract_file('docx/unit_test_formatting.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 20
  end

  it 'office_docx_headers: DOCX document with complex headers.' do
    result = Kreuzberg.extract_file('docx/unit_test_headers.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 20
  end

  it 'office_docx_lists: DOCX document emphasizing list formatting.' do
    result = Kreuzberg.extract_file('docx/unit_test_lists.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 20
  end

  it 'office_docx_tables: DOCX document containing tables for table-aware extraction.' do
    result = Kreuzberg.extract_file('docx/docx_tables.docx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.wordprocessingml.document')
    expect(result.content.length).to be >= 50
    expect(result.content.to_s).to include('Simple uniform table')
    expect(result.content.to_s).to include('Nested Table')
    expect(result.content.to_s).to include('merged cells')
    expect(result.content.to_s).to include('Header Col')
    expect(result.tables.length).to be >= 1
  end

  it 'office_epub_basic: EPUB book extraction with text content.' do
    result = Kreuzberg.extract_file('epub/features.epub')
    expect(result.mime_type).to eq('application/epub+zip')
    expect(result.content.length).to be >= 50
  end

  it 'office_fb2_basic: FictionBook (FB2) document extraction baseline.' do
    result = Kreuzberg.extract_file('fictionbook/basic.fb2')
    expect(result.mime_type).to eq('application/x-fictionbook+xml')
    expect(result.content.length).to be >= 10
  end

  it 'office_fictionbook_basic: FictionBook (.fb2) text extraction.' do
    result = Kreuzberg.extract_file('fictionbook/basic.fb2')
    expect(['application/x-fictionbook+xml', 'application/x-fictionbook'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_jats_basic: JATS scientific article extraction.' do
    result = Kreuzberg.extract_file('jats/sample_article.jats')
    expect(['application/x-jats+xml', 'text/jats'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_jupyter_basic: Jupyter notebook extraction.' do
    result = Kreuzberg.extract_file('jupyter/rank.ipynb')
    expect(result.mime_type).to eq('application/x-ipynb+json')
    expect(result.content.length).to be >= 10
  end

  it 'office_keynote_basic: Keynote document extraction baseline.' do
    result = Kreuzberg.extract_file('iwork/test.key')
    expect(result.mime_type).to eq('application/x-iwork-keynote-sffkey')
    expect(result.content.length).to be >= 5
  end

  it 'office_latex_basic: LaTeX document text extraction.' do
    result = Kreuzberg.extract_file('latex/basic_sections.tex')
    expect(['application/x-latex', 'text/x-latex'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 20
  end

  it 'office_markdown_basic: Markdown document extraction baseline.' do
    result = Kreuzberg.extract_file('markdown/comprehensive.md')
    expect(result.mime_type).to eq('text/markdown')
    expect(result.content.length).to be >= 10
  end

  it 'office_numbers_basic: Numbers document extraction baseline.' do
    result = Kreuzberg.extract_file('iwork/test.numbers')
    expect(result.mime_type).to eq('application/x-iwork-numbers-sffnumbers')
    expect(result.content.length).to be >= 10
  end

  it 'office_ods_basic: Basic ODS spreadsheet extraction.' do
    result = Kreuzberg.extract_file('data_formats/test_01.ods')
    expect(result.mime_type).to eq('application/vnd.oasis.opendocument.spreadsheet')
    expect(result.content.length).to be >= 10
  end

  it 'office_odt_bold: ODT document with bold formatting.' do
    result = Kreuzberg.extract_file('odt/bold.odt')
    expect(result.mime_type).to eq('application/vnd.oasis.opendocument.text')
    expect(result.content.length).to be >= 10
  end

  it 'office_odt_list: ODT document containing unordered lists with nesting.' do
    result = Kreuzberg.extract_file('odt/unorderedList.odt')
    expect(result.mime_type).to eq('application/vnd.oasis.opendocument.text')
    expect(result.content.length).to be >= 30
    expect(['list item', 'New level', 'Pushed us'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'office_odt_simple: Basic ODT document with paragraphs and headings.' do
    result = Kreuzberg.extract_file('odt/simple.odt')
    expect(result.mime_type).to eq('application/vnd.oasis.opendocument.text')
    expect(result.content.length).to be >= 50
    expect(['favorite things', 'Parrots', 'Analysis'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'office_odt_table: ODT document with a table structure.' do
    result = Kreuzberg.extract_file('odt/table.odt')
    expect(result.mime_type).to eq('application/vnd.oasis.opendocument.text')
    expect(result.content.length).to be >= 10
    expect(result.tables.length).to be >= 1
  end

  it 'office_opml_basic: OPML outline document extraction.' do
    result = Kreuzberg.extract_file('opml/outline.opml')
    expect(['application/xml+opml', 'text/x-opml', 'application/x-opml+xml'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_org_basic: Org-mode document text extraction.' do
    result = Kreuzberg.extract_file('org/comprehensive.org')
    expect(['text/x-org', 'text/org'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 20
  end

  it 'office_pages_basic: Pages document extraction baseline.' do
    result = Kreuzberg.extract_file('iwork/test.pages')
    expect(result.mime_type).to eq('application/x-iwork-pages-sffpages')
    expect(result.content.length).to be >= 5
  end

  it 'office_ppsx_slideshow: PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.' do
    result = Kreuzberg.extract_file('pptx/sample.ppsx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.presentationml.slideshow')
    expect(result.content.length).to be >= 10
  end

  it 'office_pptm_basic: PowerPoint macro-enabled presentation (.pptm) extraction.' do
    result = Kreuzberg.extract_file('pptx/powerpoint_with_image.pptm')
    expect(['application/vnd.ms-powerpoint.presentation.macroEnabled.12', 'application/vnd.openxmlformats-officedocument.presentationml.presentation'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content).not_to be_empty
  end

  it 'office_pptx_basic: PPTX deck should extract slides content.' do
    result = Kreuzberg.extract_file('pptx/simple.pptx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.presentationml.presentation')
    expect(result.content.length).to be >= 50
  end

  it 'office_pptx_images: PPTX presentation containing images to ensure metadata extraction.' do
    result = Kreuzberg.extract_file('pptx/powerpoint_with_image.pptx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.presentationml.presentation')
    expect(result.content.length).to be >= 15
  end

  it 'office_pptx_pitch_deck: Pitch deck PPTX used to validate large slide extraction.' do
    result = Kreuzberg.extract_file('pptx/pitch_deck_presentation.pptx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.presentationml.presentation')
    expect(result.content.length).to be >= 100
  end

  it 'office_rst_basic: reStructuredText document extraction.' do
    result = Kreuzberg.extract_file('rst/restructured_text.rst')
    expect(['text/x-rst', 'text/prs.fallenstein.rst'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 20
  end

  it 'office_rtf_basic: RTF document text extraction.' do
    result = Kreuzberg.extract_file('rtf/extraction_test.rtf')
    expect(['application/rtf', 'text/rtf'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_typst_basic: Typst document text extraction.' do
    result = Kreuzberg.extract_file('typst/headings.typ')
    expect(['application/x-typst', 'text/x-typst'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content.length).to be >= 10
  end

  it 'office_xls_legacy: Legacy XLS spreadsheet to ensure backward compatibility.' do
    result = Kreuzberg.extract_file('xls/test_excel.xls')
    expect(result.mime_type).to eq('application/vnd.ms-excel')
    expect(result.content.length).to be >= 10
  end

  it 'office_xlsb_basic: Excel binary workbook (.xlsb) extraction.' do
    result = Kreuzberg.extract_file('xlsx/test_xlsb.xlsb')
    expect(['application/vnd.ms-excel.sheet.binary.macroEnabled.12', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content).not_to be_empty
  end

  it 'office_xlsm_basic: Excel macro-enabled workbook (.xlsm) extraction.' do
    result = Kreuzberg.extract_file('xlsx/test_01.xlsm')
    expect(['application/vnd.ms-excel.sheet.macroEnabled.12', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'].any? { |v| result.mime_type.to_s.include?(v) }).to be(true)
    expect(result.content).not_to be_empty
  end

  it 'office_xlsx_basic: XLSX spreadsheet should produce metadata and table content.' do
    result = Kreuzberg.extract_file('xlsx/stanley_cups.xlsx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')
    expect(result.content.length).to be >= 100
    expect(result.content.to_s).to include('Team')
    expect(result.content.to_s).to include('Location')
    expect(result.content.to_s).to include('Stanley Cups')
    expect(result.tables.length).to be >= 1
    expect(result.metadata.sheet_count).to be >= 2
    expect(result.metadata.sheet_names.to_s).to include('Stanley Cups')
  end

  it 'office_xlsx_multi_sheet: XLSX workbook with multiple sheets.' do
    result = Kreuzberg.extract_file('xlsx/excel_multi_sheet.xlsx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')
    expect(result.content.length).to be >= 20
    expect(result.metadata.sheet_count).to be >= 2
  end

  it 'office_xlsx_office_example: Simple XLSX spreadsheet shipped alongside office integration tests.' do
    result = Kreuzberg.extract_file('xlsx/test_01.xlsx')
    expect(result.mime_type).to eq('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')
    expect(result.content.length).to be >= 10
  end
end
