# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: structured
defmodule E2e.StructuredTest do
  use ExUnit.Case, async: true

  describe "structured_csv_basic" do
    test "CSV data file extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("csv/stanley_cups.csv")
      assert String.trim(result.mime_type) == "text/csv"
      assert String.length(result.content) >= 20
    end
  end

  describe "structured_enw_basic" do
    test "EndNote ENW citation format extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("data_formats/sample.enw")
      assert Enum.any?(["application/x-endnote-refer", "application/x-endnote+xml", "text/plain"], fn v -> String.contains?(to_string(result.mime_type), v) end)
    end
  end

  describe "structured_json_basic" do
    test "Structured JSON extraction should stream and preserve content." do
      {:ok, result} = Kreuzberg.extract_file_async("json/sample_document.json")
      assert String.trim(result.mime_type) == "application/json"
      assert String.length(result.content) >= 20
      assert Enum.any?(["Sample Document", "Test Author"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "structured_json_simple" do
    test "Simple JSON document to verify structured extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("json/simple.json")
      assert String.trim(result.mime_type) == "application/json"
      assert String.length(result.content) >= 10
      assert Enum.any?(["{", "name"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "structured_nbib_basic" do
    test "PubMed NBIB citation format extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("data_formats/sample.nbib")
      assert Enum.any?(["application/nbib", "application/x-pubmed", "text/plain"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert result.content != ""
    end
  end

  describe "structured_ris_basic" do
    test "RIS citation format extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("data_formats/sample.ris")
      assert Enum.any?(["application/x-research-info-systems", "text/plain"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert result.content != ""
    end
  end

  describe "structured_toml_basic" do
    test "TOML configuration file extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("data_formats/cargo.toml")
      assert Enum.any?(["application/toml", "text/toml"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "structured_tsv_basic" do
    test "TSV (tab-separated values) data file extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("data_formats/employees.tsv")
      assert Enum.any?(["text/tab-separated-values", "text/plain"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "structured_yaml_basic" do
    test "YAML file text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("yaml/simple.yaml")
      assert Enum.any?(["application/yaml", "text/yaml", "text/x-yaml", "application/x-yaml"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "structured_yaml_simple" do
    test "Simple YAML document to validate structured extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("yaml/simple.yaml")
      assert String.trim(result.mime_type) == "application/x-yaml"
      assert String.length(result.content) >= 10
    end
  end
end
