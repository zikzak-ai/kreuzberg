# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e884665d101725b8707a566a05e85644cf46dfcdd26c210ffdc20252a16f9644
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: validate
defmodule E2e.ValidateTest do
  use ExUnit.Case, async: true

  describe "validate_binarization_invalid" do
    test "Invalid binarization" do
      assert {:error, _} = Kreuzberg.validate_binarization_method("")
    end
  end

  describe "validate_binarization_valid" do
    test "Valid binarization" do
      {:ok, result} = Kreuzberg.validate_binarization_method("")
    end
  end

  describe "validate_chunking_overlap_exceeds" do
    test "Reject chunking config where overlap exceeds max_characters" do
      assert {:error, _} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"chunking" => %{"max_characters" => 10, "max_overlap" => 100}})
    end
  end

  describe "validate_chunking_params_invalid" do
    test "Chunking overlap >= max" do
      assert {:error, _} = Kreuzberg.validate_chunking_params("")
    end
  end

  describe "validate_chunking_params_valid" do
    test "Valid chunking params" do
      {:ok, result} = Kreuzberg.validate_chunking_params("")
    end
  end

  describe "validate_chunking_valid" do
    test "Accept valid chunking config" do
      {:ok, result} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"chunking" => %{"max_characters" => 1000, "max_overlap" => 200}})
      assert String.length(result.content) >= 10
    end
  end

  describe "validate_confidence_boundary" do
    test "Confidence at 1.0" do
      {:ok, result} = Kreuzberg.validate_confidence("")
    end
  end

  describe "validate_confidence_negative" do
    test "Negative confidence" do
      assert {:error, _} = Kreuzberg.validate_confidence("")
    end
  end

  describe "validate_confidence_over" do
    test "Confidence > 1" do
      assert {:error, _} = Kreuzberg.validate_confidence("")
    end
  end

  describe "validate_confidence_valid" do
    test "Accept valid confidence threshold" do
      {:ok, result} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"ocr" => %{"confidence_threshold" => 0.8}})
    end
  end

  describe "validate_confidence_zero" do
    test "Confidence at 0.0" do
      {:ok, result} = Kreuzberg.validate_confidence("")
    end
  end

  describe "validate_dpi_negative" do
    test "Reject negative DPI in OCR config" do
      assert {:error, _} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"ocr" => %{"dpi" => -1}})
    end
  end

  describe "validate_dpi_valid" do
    test "Accept valid DPI in OCR config" do
      {:ok, result} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"ocr" => %{"dpi" => 300}})
    end
  end

  describe "validate_host_empty" do
    test "Empty host rejected" do
      assert {:error, _} = Kreuzberg.validate_host("")
    end
  end

  describe "validate_ocr_backend_invalid" do
    test "Invalid OCR backend" do
      assert {:error, _} = Kreuzberg.validate_ocr_backend("")
    end
  end

  describe "validate_ocr_backend_valid" do
    test "Valid OCR backend" do
      {:ok, result} = Kreuzberg.validate_ocr_backend("")
    end
  end

  describe "validate_output_format_invalid" do
    test "Reject invalid output format" do
      assert {:error, _} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"output_format" => "nonexistent_format"})
    end
  end

  describe "validate_output_format_valid" do
    test "Accept valid output format" do
      {:ok, result} = Kreuzberg.extract_file_sync("pdf/fake_memo.pdf", %{"output_format" => "markdown"})
    end
  end

  describe "validate_port_max" do
    test "Port 65535 accepted" do
      {:ok, result} = Kreuzberg.validate_port("")
    end
  end

  describe "validate_port_over" do
    test "Port over 65535" do
      assert {:error, _} = Kreuzberg.validate_port("")
    end
  end

  describe "validate_port_zero" do
    test "Port 0 rejected" do
      assert {:error, _} = Kreuzberg.validate_port("")
    end
  end

  describe "validate_reduction_level_invalid" do
    test "Invalid reduction level" do
      assert {:error, _} = Kreuzberg.validate_token_reduction_level("")
    end
  end

  describe "validate_reduction_level_valid" do
    test "Valid reduction level" do
      {:ok, result} = Kreuzberg.validate_token_reduction_level("")
    end
  end
end
