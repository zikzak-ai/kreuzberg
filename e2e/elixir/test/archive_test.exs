# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: archive
defmodule E2e.ArchiveTest do
  use ExUnit.Case, async: true

  describe "archive_gz_basic" do
    test "Gzip compressed file extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("archives/book_war_and_peace_1p.txt.gz")
      assert Enum.any?(["application/gzip", "application/x-gzip"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "archive_sevenz_basic" do
    test "7-Zip archive extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("archives/documents.7z")
      assert String.trim(result.mime_type) == "application/x-7z-compressed"
      assert String.length(result.content) >= 10
    end
  end

  describe "archive_tar_basic" do
    test "TAR archive extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("archives/documents.tar")
      assert Enum.any?(["application/x-tar", "application/tar"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "archive_zip_basic" do
    test "ZIP archive extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("archives/documents.zip")
      assert Enum.any?(["application/zip", "application/x-zip-compressed"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end
end
