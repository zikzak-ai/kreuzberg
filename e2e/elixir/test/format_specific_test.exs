# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:bb6214b20b5295d60002900dc6c9db40797e3eae2b334e45ebd3b11b59bd11a2
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: format_specific
defmodule E2e.FormatSpecificTest do
  use ExUnit.Case, async: true

  describe "format_csv" do
    test "CSV file extraction using extract_file_sync" do
      {:ok, result} = Kreuzberg.extract_file_sync("csv/data_table.csv")
      assert String.length(result.content) >= 5
    end
  end

  describe "format_docx_standalone" do
    test "Standalone DOCX extraction using extract_bytes_sync" do
      {:ok, result} = Kreuzberg.extract_bytes_sync("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
      assert String.length(result.content) >= 20
    end
  end

  describe "format_email_eml" do
    test "Email EML file extraction using extract_file_sync" do
      {:ok, result} = Kreuzberg.extract_file_sync("email/html_email_multipart.eml")
      assert String.length(result.content) >= 10
    end
  end

  describe "format_pdf_text" do
    test "Standalone PDF text extraction using extract_bytes_sync" do
      {:ok, result} = Kreuzberg.extract_bytes_sync("pdf/fake_memo.pdf", "application/pdf")
      assert String.length(result.content) >= 50
      assert Enum.any?(["Mallori", "May"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "format_pptx" do
    test "PPTX presentation extraction using extract_bytes_sync" do
      {:ok, result} = Kreuzberg.extract_bytes_sync("pptx/simple.pptx", "application/vnd.openxmlformats-officedocument.presentationml.presentation")
    end
  end

  describe "format_rtf" do
    test "RTF file extraction using extract_file_sync" do
      {:ok, result} = Kreuzberg.extract_file_sync("rtf/extraction_test.rtf")
      assert String.length(result.content) >= 5
    end
  end

  describe "format_xlsx" do
    test "XLSX spreadsheet extraction using extract_bytes_sync" do
      {:ok, result} = Kreuzberg.extract_bytes_sync("xlsx/stanley_cups.xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
    end
  end
end
