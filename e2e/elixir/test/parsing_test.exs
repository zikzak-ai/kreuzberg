# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: parsing
defmodule E2e.ParsingTest do
  use ExUnit.Case, async: true

  describe "parse_json" do
    test "Parse JSON document format" do
      {:ok, result} = Kreuzberg.extract_file_async("json/simple.json")
      assert String.length(result.content) >= 5
    end
  end

  describe "parse_toml" do
    test "Parse TOML document format" do
      {:ok, result} = Kreuzberg.extract_file_async("data_formats/cargo.toml")
      assert String.length(result.content) >= 5
    end
  end

  describe "parse_xml" do
    test "Parse XML document format" do
      {:ok, result} = Kreuzberg.extract_file_async("xml/simple_note.xml")
      assert String.length(result.content) >= 5
    end
  end

  describe "parse_yaml" do
    test "Parse YAML document format" do
      {:ok, result} = Kreuzberg.extract_file_async("test_fixtures/config.yaml")
      assert String.length(result.content) >= 5
    end
  end
end
