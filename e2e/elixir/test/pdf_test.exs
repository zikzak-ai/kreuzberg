# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: pdf
defmodule E2e.PdfTest do
  use ExUnit.Case, async: true

  describe "pdf_assembly_technical" do
    test "Assembly language technical manual with large body of text." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/assembly_language_for_beginners_al4_b_en.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 5000
      assert Enum.any?(["assembly", "register", "instruction"], fn v -> String.contains?(to_string(result.content), v) end)
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_bayesian_data_analysis" do
    test "Bayesian data analysis textbook PDF with large content volume." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/bayesian_data_analysis_third_edition_13th_feb_2020.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 10000
      assert Enum.any?(["Bayesian", "probability", "distribution"], fn v -> String.contains?(to_string(result.content), v) end)
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_code_and_formula" do
    test "PDF containing code snippets and formulas should retain substantial content." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/code_and_formula.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 100
    end
  end

  describe "pdf_deep_learning" do
    test "Deep learning textbook PDF to ensure long-form extraction quality." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/fundamentals_of_deep_learning_2014.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 1000
      assert Enum.any?(["neural", "network", "deep learning"], fn v -> String.contains?(to_string(result.content), v) end)
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_embedded_images" do
    test "PDF with embedded images should extract text and tables when present." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/embedded_images_tables.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 50
      assert length(result.tables) >= 0
    end
  end

  describe "pdf_google_doc" do
    test "Google Docs exported PDF to verify conversion fidelity." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/google_doc_document.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 50
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_large_ciml" do
    test "Large machine learning textbook PDF to stress extraction length." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/a_course_in_machine_learning_ciml_v0_9_all.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 10000
      assert Enum.any?(["machine learning", "algorithm", "training"], fn v -> String.contains?(to_string(result.content), v) end)
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_non_english_german" do
    test "German technical PDF to ensure non-ASCII content extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/5_level_paging_and_5_level_ept_intel_revision_1_1_may_2017.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 100
      assert Enum.any?(["Intel", "paging"], fn v -> String.contains?(to_string(result.content), v) end)
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_right_to_left" do
    test "Right-to-left language PDF to verify RTL extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/right_to_left_01.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 50
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end

  describe "pdf_simple_text" do
    test "Simple text-heavy PDF should extract content without OCR or tables." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/fake_memo.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 50
      assert Enum.any?(["May 5, 2023", "To Whom it May Concern", "Mallori"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "pdf_tables_large" do
    test "Large PDF with extensive tables to stress table extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/large.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 500
    end
  end

  describe "pdf_tables_medium" do
    test "Medium-sized PDF with multiple tables." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/medium.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 100
    end
  end

  describe "pdf_technical_stat_learning" do
    test "Technical statistical learning PDF requiring substantial extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/an_introduction_to_statistical_learning_with_applications_in_r_islr_sixth_printing.pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 10000
      assert Enum.any?(["statistical", "regression", "learning"], fn v -> String.contains?(to_string(result.content), v) end)
      assert String.trim(result.metadata.format_type) == "pdf"
    end
  end
end
