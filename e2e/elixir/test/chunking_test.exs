# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:75969b548fc82b3077390f25e5a61cf7ec087dc9f947d2c9d0b330d3127e5cec
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: chunking
defmodule E2e.ChunkingTest do
  use ExUnit.Case, async: true

  describe "chunk_batch_basic" do
    test "Batch chunking of multiple texts in a single call" do
      {:ok, result} = Kreuzberg.chunk_texts_batch("", %{"max_characters" => 100, "max_overlap" => 10})
    end
  end

  describe "chunk_text_basic" do
    test "Basic text chunking with small chunk size and overlap" do
      {:ok, result} = Kreuzberg.chunk_text("", %{"max_characters" => 50, "max_overlap" => 10})
    end
  end

  describe "chunk_text_large" do
    test "Text chunking with large chunk size to test handling of long content" do
      {:ok, result} = Kreuzberg.chunk_text("", %{"max_characters" => 1000, "max_overlap" => 10})
    end
  end

  describe "chunk_text_no_overlap" do
    test "Text chunking with no overlap between chunks" do
      {:ok, result} = Kreuzberg.chunk_text("", %{"max_characters" => 50, "max_overlap" => 0})
    end
  end

  describe "chunk_text_overlap_exceeds" do
    test "chunk_text overlap >= max" do
      assert {:error, _} = Kreuzberg.chunk_text("", %{"max_characters" => 50, "overlap" => 60})
    end
  end

  describe "chunk_text_yaml" do
    test "Chunk with YAML chunker" do
      {:ok, result} = Kreuzberg.chunk_text("", %{"chunker_type" => "yaml", "max_characters" => 50})
    end
  end

  describe "chunk_text_zero_max" do
    test "chunk_text max_characters zero" do
      assert {:error, _} = Kreuzberg.chunk_text("", %{"max_characters" => 0})
    end
  end

  describe "chunk_texts_batch_multiple" do
    test "Batch chunk multiple texts" do
      {:ok, result} = Kreuzberg.chunk_texts_batch("", %{"max_characters" => 100})
    end
  end
end
