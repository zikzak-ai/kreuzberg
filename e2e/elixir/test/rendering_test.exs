# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: rendering
defmodule E2e.RenderingTest do
  use ExUnit.Case, async: true

  describe "convert_html_basic" do
    test "Convert basic HTML to Markdown format" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.contains?(to_string(result.content), "Title")
    end
  end

  describe "convert_html_complex" do
    test "Convert complex HTML to markdown" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 20
    end
  end

  describe "convert_html_empty" do
    test "Convert empty HTML to markdown" do
      {:ok, result} = Kreuzberg.extract_file_async("")
    end
  end

  describe "convert_html_table" do
    test "Convert HTML table to Markdown table format" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.contains?(to_string(result.content), "|")
    end
  end

  describe "djot_to_html_basic" do
    test "Convert Djot format to HTML" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 10
    end
  end

  describe "djot_to_html_complex" do
    test "Complex djot to HTML" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 10
    end
  end

  describe "djot_to_html_empty" do
    test "Convert empty djot to HTML" do
      {:ok, result} = Kreuzberg.extract_file_async("")
    end
  end

  describe "render_djot_basic" do
    test "Render HTML document to Djot format" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 5
    end
  end

  describe "render_djot_formatted" do
    test "Render HTML to djot" do
      {:ok, result} = Kreuzberg.extract_file_async("")
    end
  end

  describe "render_html_basic" do
    test "Render a simple HTML document to formatted output" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 5
    end
  end

  describe "render_html_styled" do
    test "Render HTML preserving structure" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.contains?(to_string(result.content), "Heading")
    end
  end

  describe "render_json_basic" do
    test "Render HTML document to JSON format" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 5
    end
  end

  describe "render_json_complex" do
    test "Render HTML to JSON" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 5
    end
  end

  describe "render_markdown_basic" do
    test "Render HTML document to Markdown format" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 5
    end
  end

  describe "render_markdown_complex" do
    test "Render complex HTML to markdown" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 10
    end
  end

  describe "render_plain_basic" do
    test "Render HTML document to plain text format" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.length(result.content) >= 5
    end
  end

  describe "render_plain_empty" do
    test "Render empty HTML" do
      {:ok, result} = Kreuzberg.extract_file_async("")
    end
  end
end
