// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7aa45858ccc431004b8330d86f3e2a51c8d4c9016c52bc946a536cbedd7acfdd
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg
import RustBridge

/// E2e tests for category: contract.
final class ContractTests: XCTestCase {
    func testConfigDocumentStructureWithHeadings() throws {
        // Tests document structure with DOCX heading-driven nesting
        let result = try RustBridge.extractFileSync("docx/fake.docx", "{\"include_document_structure\":true}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertFalse(result.document.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 1)
    }

    func testConfigExtractionTimeout() throws {
        // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
        let result = try RustBridge.extractFileSync("pdf/fake_memo.pdf", "{\"extraction_timeout_secs\":300}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testConfigSecurityLimits() throws {
        // Tests archive extraction with custom security limits
        let result = try RustBridge.extractFileSync("archives/documents.zip", "{\"security_limits\":{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}}")
        XCTAssertTrue(result.mime_type.contains("application/zip") || result.mime_type.contains("application/x-zip-compressed"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOutputFormatBytesMarkdown() throws {
        // Tests markdown output format via bytes extraction API
        let result = try RustBridge.extractBytesSync("pdf/fake_memo.pdf", "application/pdf", "{\"output_format\":\"markdown\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual((result.metadata.output_format ?? "").trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testOutputFormatMarkdown() throws {
        // Tests Markdown output format
        let result = try RustBridge.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual((result.metadata.output_format ?? "").trimmingCharacters(in: .whitespaces), "markdown")
    }

}
