// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:6bff042f37f228edde976deb3809496517c045247e83e6423da178d55086e58e
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;using System.Text.Json;
using System.Text.Json.Serialization;
namespace Kreuzberg;

internal sealed class TessdataManagerSafeHandle : SafeHandle
{
    internal TessdataManagerSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.TessdataManagerFree(handle);
        return true;
    }
}
/// <summary>/// Manages tessdata file downloading, caching, and manifest generation./// </summary>
public sealed class TessdataManager : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };
    private readonly TessdataManagerSafeHandle _safeHandle;

    internal TessdataManager(IntPtr handle)
    {
        _safeHandle = new TessdataManagerSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Get the cache directory path.
    /// </summary>
    public string CacheDir()
    {
        var nativeResult = NativeMethods.TessdataManagerCacheDir(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "TessdataManagerCacheDir failed";
            throw new KreuzbergException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Check if a specific language traineddata file is cached.
    /// </summary>
    public bool IsLanguageCached(string lang)
    {
        var nativeResult = NativeMethods.TessdataManagerIsLanguageCached(
            Handle,
            lang
        );
        var returnValue = nativeResult != 0;
        return returnValue;
    }

    /// <summary>
    /// Downloads all tessdata_fast traineddata files to the cache directory.
    ///
    /// Skips files that already exist. Returns the count of newly downloaded files.
    ///
    /// Requires the `paddle-ocr` feature for HTTP download support (ureq).
    /// </summary>
    public ulong EnsureAllLanguages()
    {
        var nativeResult = NativeMethods.TessdataManagerEnsureAllLanguages(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }
}
