// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// Format-specific metadata (discriminated union).
///
/// Only one format type can exist per extraction result. This provides
/// type-safe, clean metadata without nested optionals.
/// </summary>
[JsonConverter(typeof(FormatMetadataJsonConverter))]
public abstract record FormatMetadata
{
    public sealed record Pdf(
        string Value
    ) : FormatMetadata;

    public sealed record Docx(
        DocxMetadata Value
    ) : FormatMetadata;

    public sealed record Excel(
        ExcelMetadata Value
    ) : FormatMetadata;

    public sealed record Email(
        EmailMetadata Value
    ) : FormatMetadata;

    public sealed record Pptx(
        PptxMetadata Value
    ) : FormatMetadata;

    public sealed record Archive(
        ArchiveMetadata Value
    ) : FormatMetadata;

    public sealed record Image(
        string Value
    ) : FormatMetadata;

    public sealed record Xml(
        XmlMetadata Value
    ) : FormatMetadata;

    public sealed record Text(
        TextMetadata Value
    ) : FormatMetadata;

    public sealed record Html(
        HtmlMetadata Value
    ) : FormatMetadata;

    public sealed record Ocr(
        OcrMetadata Value
    ) : FormatMetadata;

    public sealed record Csv(
        CsvMetadata Value
    ) : FormatMetadata;

    public sealed record Bibtex(
        BibtexMetadata Value
    ) : FormatMetadata;

    public sealed record Citation(
        CitationMetadata Value
    ) : FormatMetadata;

    public sealed record FictionBook(
        FictionBookMetadata Value
    ) : FormatMetadata;

    public sealed record Dbf(
        DbfMetadata Value
    ) : FormatMetadata;

    public sealed record Jats(
        JatsMetadata Value
    ) : FormatMetadata;

    public sealed record Epub(
        EpubMetadata Value
    ) : FormatMetadata;

    public sealed record Pst(
        PstMetadata Value
    ) : FormatMetadata;

    public sealed record Code(
        string Value
    ) : FormatMetadata;

}

/// <summary>Custom JSON converter for <see cref="FormatMetadata"/> that reads the "format_type" discriminator from any position.</summary>
internal sealed class FormatMetadataJsonConverter : JsonConverter<FormatMetadata>
{
    public override FormatMetadata Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;
        if (!root.TryGetProperty("format_type", out var tagEl))
            throw new JsonException("FormatMetadata: missing \"format_type\" discriminator");
        var tag = tagEl.GetString();
        var json = root.GetRawText();
        return tag switch
        {
            "pdf" => new FormatMetadata.Pdf(
                JsonSerializer.Deserialize<string>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Pdf.Value")),
            "docx" => new FormatMetadata.Docx(
                JsonSerializer.Deserialize<DocxMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Docx.Value")),
            "excel" => new FormatMetadata.Excel(
                JsonSerializer.Deserialize<ExcelMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Excel.Value")),
            "email" => new FormatMetadata.Email(
                JsonSerializer.Deserialize<EmailMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Email.Value")),
            "pptx" => new FormatMetadata.Pptx(
                JsonSerializer.Deserialize<PptxMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Pptx.Value")),
            "archive" => new FormatMetadata.Archive(
                JsonSerializer.Deserialize<ArchiveMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Archive.Value")),
            "image" => new FormatMetadata.Image(
                JsonSerializer.Deserialize<string>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Image.Value")),
            "xml" => new FormatMetadata.Xml(
                JsonSerializer.Deserialize<XmlMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Xml.Value")),
            "text" => new FormatMetadata.Text(
                JsonSerializer.Deserialize<TextMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Text.Value")),
            "html" => new FormatMetadata.Html(
                JsonSerializer.Deserialize<HtmlMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Html.Value")),
            "ocr" => new FormatMetadata.Ocr(
                JsonSerializer.Deserialize<OcrMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Ocr.Value")),
            "csv" => new FormatMetadata.Csv(
                JsonSerializer.Deserialize<CsvMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Csv.Value")),
            "bibtex" => new FormatMetadata.Bibtex(
                JsonSerializer.Deserialize<BibtexMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Bibtex.Value")),
            "citation" => new FormatMetadata.Citation(
                JsonSerializer.Deserialize<CitationMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Citation.Value")),
            "fiction_book" => new FormatMetadata.FictionBook(
                JsonSerializer.Deserialize<FictionBookMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.FictionBook.Value")),
            "dbf" => new FormatMetadata.Dbf(
                JsonSerializer.Deserialize<DbfMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Dbf.Value")),
            "jats" => new FormatMetadata.Jats(
                JsonSerializer.Deserialize<JatsMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Jats.Value")),
            "epub" => new FormatMetadata.Epub(
                JsonSerializer.Deserialize<EpubMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Epub.Value")),
            "pst" => new FormatMetadata.Pst(
                JsonSerializer.Deserialize<PstMetadata>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Pst.Value")),
            "code" => new FormatMetadata.Code(
                JsonSerializer.Deserialize<string>(json, options)!
                    ?? throw new JsonException("Failed to deserialize FormatMetadata.Code.Value")),
            _ => throw new JsonException($"Unknown FormatMetadata discriminator: {tag}")
        };
    }

    public override void Write(Utf8JsonWriter writer, FormatMetadata value, JsonSerializerOptions options)
    {
        // Serialize the concrete type, then inject the discriminator
        switch (value)
        {
            case FormatMetadata.Pdf v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "pdf");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Docx v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "docx");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Excel v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "excel");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Email v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "email");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Pptx v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "pptx");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Archive v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "archive");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Image v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "image");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Xml v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "xml");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Text v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "text");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Html v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "html");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Ocr v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "ocr");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Csv v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "csv");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Bibtex v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "bibtex");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Citation v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "citation");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.FictionBook v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "fiction_book");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Dbf v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "dbf");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Jats v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "jats");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Epub v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "epub");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Pst v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "pst");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            case FormatMetadata.Code v:
                {
                    var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                    writer.WriteStartObject();
                    writer.WriteString("format_type", "code");
                    foreach (var prop in doc.RootElement.EnumerateObject())
                        if (prop.Name != "format_type") prop.WriteTo(writer);
                    writer.WriteEndObject();
                    break;
                }
            default: throw new JsonException($"Unknown FormatMetadata subtype: {value.GetType().Name}");
        }
    }
}
