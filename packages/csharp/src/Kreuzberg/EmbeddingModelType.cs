// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7b10ac4bbaf91cb1741cf64a17a08d9beeacfeea7a1872da836b1d2575785a4a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// Embedding model types supported by Kreuzberg.
/// </summary>
[JsonConverter(typeof(EmbeddingModelTypeJsonConverter))]
public abstract record EmbeddingModelType
{
    /// <summary>
    /// Use a preset model configuration (recommended)
    /// </summary>
    public sealed record Preset(
        [property: JsonPropertyName("name")] string Name
    ) : EmbeddingModelType;

    /// <summary>
    /// Use a custom ONNX model from HuggingFace
    /// </summary>
    public sealed record Custom(
        [property: JsonPropertyName("model_id")] string ModelId,
        [property: JsonPropertyName("dimensions")] ulong Dimensions
    ) : EmbeddingModelType;

    /// <summary>
    /// Provider-hosted embedding model via liter-llm.
    ///
    /// Uses the model specified in the nested `LlmConfig` (e.g.,
    /// `"openai/text-embedding-3-small"`).
    /// </summary>
    public sealed record Llm(
        LlmConfig Value
    ) : EmbeddingModelType;

}

/// <summary>Custom JSON converter for <see cref="EmbeddingModelType"/> that reads the "type" discriminator from any position.</summary>
internal sealed class EmbeddingModelTypeJsonConverter : JsonConverter<EmbeddingModelType>
{
    public override EmbeddingModelType Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;
        if (!root.TryGetProperty("type", out var tagEl))
            throw new JsonException("EmbeddingModelType: missing \"type\" discriminator");
        var tag = tagEl.GetString();
        var json = root.GetRawText();
        return tag switch
        {
            "preset" => JsonSerializer.Deserialize<EmbeddingModelType.Preset>(json, options)!
                ?? throw new JsonException("Failed to deserialize EmbeddingModelType.Preset"),
            "custom" => JsonSerializer.Deserialize<EmbeddingModelType.Custom>(json, options)!
                ?? throw new JsonException("Failed to deserialize EmbeddingModelType.Custom"),
            "llm" => new EmbeddingModelType.Llm(
                JsonSerializer.Deserialize<LlmConfig>(json, options)!
                    ?? throw new JsonException("Failed to deserialize EmbeddingModelType.Llm.Value")),
            _ => throw new JsonException($"Unknown EmbeddingModelType discriminator: {tag}")
        };
    }

    public override void Write(Utf8JsonWriter writer, EmbeddingModelType value, JsonSerializerOptions options)
    {
        // Serialize the concrete type, then inject the discriminator
        switch (value)
        {
            case EmbeddingModelType.Preset v:
            {
                var doc = JsonSerializer.SerializeToDocument(v, options);
                writer.WriteStartObject();
                writer.WriteString("type", "preset");
                foreach (var prop in doc.RootElement.EnumerateObject())
                    if (prop.Name != "type") prop.WriteTo(writer);
                writer.WriteEndObject();
                break;
            }
            case EmbeddingModelType.Custom v:
            {
                var doc = JsonSerializer.SerializeToDocument(v, options);
                writer.WriteStartObject();
                writer.WriteString("type", "custom");
                foreach (var prop in doc.RootElement.EnumerateObject())
                    if (prop.Name != "type") prop.WriteTo(writer);
                writer.WriteEndObject();
                break;
            }
            case EmbeddingModelType.Llm v:
            {
                var doc = JsonSerializer.SerializeToDocument(v.Value, options);
                writer.WriteStartObject();
                writer.WriteString("type", "llm");
                foreach (var prop in doc.RootElement.EnumerateObject())
                    if (prop.Name != "type") prop.WriteTo(writer);
                writer.WriteEndObject();
                break;
            }
            default: throw new JsonException($"Unknown EmbeddingModelType subtype: {value.GetType().Name}");
        }
    }
}
