// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:bfe5966d331d9763334872bd49eaa1a80c1820eb68a66d57a128b74d5b84bb02
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Collections.Generic;
using System.Threading.Tasks;
namespace Kreuzberg;

internal sealed class HwpxExtractorSafeHandle : SafeHandle
{
    internal HwpxExtractorSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.HwpxExtractorFree(handle);
        return true;
    }
}
/// <summary>/// Extractor for Hangul Word Processor XML (.hwpx) files./// /// Supports HWPX (Open HWPML), the ZIP-based XML successor to the binary HWP 5.0 format./// </summary>
public sealed class HwpxExtractor : IDisposable
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };
    private readonly HwpxExtractorSafeHandle _safeHandle;

    internal HwpxExtractor(IntPtr handle)
    {
        _safeHandle = new HwpxExtractorSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    public static HwpxExtractor Default()
    {
        var nativeResult = NativeMethods.HwpxExtractorDefault(

        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorDefault failed";
            throw new KreuzbergException(ec, msg);
        }
        var returnValue = new HwpxExtractor(nativeResult);
        return returnValue;
    }

    public string Name()
    {
        var nativeResult = NativeMethods.HwpxExtractorName(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorName failed";
            throw new KreuzbergException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    public string Version()
    {
        var nativeResult = NativeMethods.HwpxExtractorVersion(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorVersion failed";
            throw new KreuzbergException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    public void Initialize()
    {
        NativeMethods.HwpxExtractorInitialize(
            Handle
        );
    }

    public void Shutdown()
    {
        NativeMethods.HwpxExtractorShutdown(
            Handle
        );
    }

    public string Description()
    {
        var nativeResult = NativeMethods.HwpxExtractorDescription(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorDescription failed";
            throw new KreuzbergException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    public string Author()
    {
        var nativeResult = NativeMethods.HwpxExtractorAuthor(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorAuthor failed";
            throw new KreuzbergException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    public async Task<string> ExtractBytes(byte[] content, string mimeType, ExtractionConfig config)
    {
        var contentHandle = GCHandle.Alloc(content, GCHandleType.Pinned);
        var configJson = JsonSerializer.Serialize((config ?? new ExtractionConfig()), JsonOptions);
        var configHandle = NativeMethods.ExtractionConfigFromJson(configJson);
        if (configHandle == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "ExtractionConfigFromJson failed";
            throw new KreuzbergException(ec, msg);
        }
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.HwpxExtractorExtractBytes(
                Handle,
                contentHandle.AddrOfPinnedObject(),
                (nuint)content.Length,
                mimeType,
                configHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                var ec = NativeMethods.LastErrorCode();
                var ctxPtr = NativeMethods.LastErrorContext();
                var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorExtractBytes failed";
                throw new KreuzbergException(ec, msg);
            }
            var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
            NativeMethods.FreeString(nativeResult);
            contentHandle.Free();
            NativeMethods.ExtractionConfigFree(configHandle);
            return returnValue;
        });
    }

    public List<string> SupportedMimeTypes()
    {
        var nativeResult = NativeMethods.HwpxExtractorSupportedMimeTypes(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "HwpxExtractorSupportedMimeTypes failed";
            throw new KreuzbergException(ec, msg);
        }
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    public int Priority()
    {
        var nativeResult = NativeMethods.HwpxExtractorPriority(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }
}
