// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a71617c2bf26fa95930aa844039270e07170bd047b9594cdda0c542db77ecbba
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg;

import java.util.Optional;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonPOJOBuilder;
import org.jspecify.annotations.Nullable;

/**
 * Embedding configuration for text chunks.
 *
 * Configures embedding generation using ONNX models via the vendored embedding engine.
 * Requires the {@code embeddings} feature to be enabled.
 */
@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = EmbeddingConfig.Builder.class)
public record EmbeddingConfig(
    /**
     * The embedding model to use (defaults to "balanced" preset if not specified)
     */
    EmbeddingModelType model,
    /**
     * Whether to normalize embedding vectors (recommended for cosine similarity)
     */
    boolean normalize,
    /**
     * Batch size for embedding generation
     */
    @JsonProperty("batch_size") long batchSize,
    /**
     * Show model download progress
     */
    @JsonProperty("show_download_progress") boolean showDownloadProgress,
    /**
     * Custom cache directory for model files
     *
     * Defaults to {@code ~/.cache/kreuzberg/embeddings/} if not specified.
     * Allows full customization of model download location.
     */
    @JsonProperty("cache_dir") java.nio.file.@Nullable Path cacheDir,
    /**
     * Hardware acceleration for the embedding ONNX model.
     *
     * When set, controls which execution provider (CPU, CUDA, CoreML, TensorRT)
     * is used for inference. Defaults to {@code None} (auto-select per platform).
     */
    @Nullable AccelerationConfig acceleration,
    /**
     * Maximum wall-clock duration (in seconds) for a single {@code embed()} call when
     * using EmbeddingModelType::Plugin.
     *
     * Applies only to the in-process plugin path — protects against hung
     * host-language backends (e.g. a Python callback deadlocked on the GIL,
     * a model stuck on CUDA OOM retries, etc.). On timeout, the dispatcher
     * returns {@code Plugin} instead of blocking forever.
     *
     * {@code None} disables the timeout. The default (60 seconds) is conservative
     * for common in-process inference; increase for large batches on slow
     * hardware.
     */
    @Nullable @JsonProperty("max_embed_duration_secs") Long maxEmbedDurationSecs
) {
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Parse a {@code EmbeddingConfig} from a JSON string.
     *
     * @param json JSON serialisation matching the Rust-side field names (snake_case).
     * @throws KreuzbergRsException if the JSON cannot be deserialised.
     */
    public static EmbeddingConfig fromJson(String json) throws KreuzbergRsException {
        try {
            return new com.fasterxml.jackson.databind.ObjectMapper()
                .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
                .findAndRegisterModules()
                .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
                .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
                .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true)
                .readValue(json, EmbeddingConfig.class);
        } catch (Exception e) {
            throw new KreuzbergRsException("Failed to parse EmbeddingConfig from JSON: " + e.getMessage(), e);
        }
    }
    public EmbeddingConfig{
        if (batchSize == 0) batchSize = 32;
    }

    @JsonPOJOBuilder(withPrefix = "with")
    public static final class Builder {

        @JsonProperty("model")
        private EmbeddingModelType model = null;
        @JsonProperty("normalize")
        private boolean normalize = true;
        @JsonProperty("batch_size")
        private long batchSize = 0;
        @JsonProperty("show_download_progress")
        private boolean showDownloadProgress = false;
        @JsonProperty("cache_dir")
        private Optional<java.nio.file.Path> cacheDir = Optional.empty();
        @JsonProperty("acceleration")
        private Optional<AccelerationConfig> acceleration = Optional.empty();
        @JsonProperty("max_embed_duration_secs")
        private Optional<Long> maxEmbedDurationSecs = Optional.empty();

        /** Sets the model field. */
        @JsonProperty("model")
        public Builder withModel(final EmbeddingModelType value) {
            this.model = value;
            return this;
        }

        /** Sets the normalize field. */
        @JsonProperty("normalize")
        public Builder withNormalize(final boolean value) {
            this.normalize = value;
            return this;
        }

        /** Sets the batchSize field. */
        @JsonProperty("batch_size")
        public Builder withBatchSize(final long value) {
            this.batchSize = value;
            return this;
        }

        /** Sets the showDownloadProgress field. */
        @JsonProperty("show_download_progress")
        public Builder withShowDownloadProgress(final boolean value) {
            this.showDownloadProgress = value;
            return this;
        }

        /** Sets the cacheDir field. */
        @JsonProperty("cache_dir")
        public Builder withCacheDir(final Optional<java.nio.file.Path> value) {
            this.cacheDir = value;
            return this;
        }

        /** Sets the acceleration field. */
        @JsonProperty("acceleration")
        public Builder withAcceleration(final Optional<AccelerationConfig> value) {
            this.acceleration = value;
            return this;
        }

        /** Sets the maxEmbedDurationSecs field. */
        @JsonProperty("max_embed_duration_secs")
        public Builder withMaxEmbedDurationSecs(final Optional<Long> value) {
            this.maxEmbedDurationSecs = value;
            return this;
        }

        /** Builds the EmbeddingConfig instance. */
        public EmbeddingConfig build() {
            return new EmbeddingConfig(
                model,
                normalize,
                batchSize,
                showDownloadProgress,
                cacheDir.orElse(null),
                acceleration.orElse(null),
                maxEmbedDurationSecs.orElse(null)
            );
        }
    }
}
