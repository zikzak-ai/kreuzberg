// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2a60f72452e251592e68253a3abeb4eaf532476225272d38ab0e3911a0d5a42c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg;

import java.util.List;

public final class Kreuzberg {
    private Kreuzberg() {
    }

    /**
     * Extract content from a byte array.
     *
     * This is the main entry point for in-memory extraction. It performs the following steps: 1. Validate MIME type 2.
     * Handle legacy format conversion if needed 3. Select appropriate extractor from registry 4. Extract content 5. Run
     * post-processing pipeline
     *
     * # Arguments
     *
     * * {@code content} - The byte array to extract * {@code mime_type} - MIME type of the content * {@code config} -
     * Extraction configuration
     *
     * # Returns
     *
     * An {@code ExtractionResult} containing the extracted content and metadata.
     *
     * # Errors
     *
     * Returns {@code KreuzbergError::Validation} if MIME type is invalid. Returns
     * {@code KreuzbergError::UnsupportedFormat} if MIME type is not supported.
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::extract_bytes; use
     * kreuzberg::core::config::ExtractionConfig;
     *
     * # async fn example() -&gt; kreuzberg::Result&lt;()&gt; { let config = ExtractionConfig::default(); let bytes =
     * b"Hello, world!"; let result = extract_bytes(bytes, "text/plain", &amp;config).await?; println!("Content: {}",
     * result.content); # Ok(()) # } {@code }{@code }
     */
    public static ExtractionResult extractBytes(final byte[] content, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(content, "content must not be null");
        java.util.Objects.requireNonNull(mimeType, "mimeType must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractBytes(content, mimeType, config);
    }

    /**
     * Extract content from a file.
     *
     * This is the main entry point for file-based extraction. It performs the following steps: 1. Check cache for
     * existing result (if caching enabled) 2. Detect or validate MIME type 3. Select appropriate extractor from
     * registry 4. Extract content 5. Run post-processing pipeline 6. Store result in cache (if caching enabled)
     *
     * # Arguments
     *
     * * {@code path} - Path to the file to extract * {@code mime_type} - Optional MIME type override. If None, will be
     * auto-detected * {@code config} - Extraction configuration
     *
     * # Returns
     *
     * An {@code ExtractionResult} containing the extracted content and metadata.
     *
     * # Errors
     *
     * Returns {@code KreuzbergError::Io} if the file doesn't exist (NotFound) or for other file I/O errors. Returns
     * {@code KreuzbergError::UnsupportedFormat} if MIME type is not supported.
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::extract_file; use
     * kreuzberg::core::config::ExtractionConfig;
     *
     * # async fn example() -&gt; kreuzberg::Result&lt;()&gt; { let config = ExtractionConfig::default(); let result =
     * extract_file("document.pdf", None, &amp;config).await?; println!("Content: {}", result.content); # Ok(()) # }
     * {@code }{@code }
     */
    public static ExtractionResult extractFile(final java.nio.file.Path path, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractFile(path, mimeType, config);
    }

    public static ExtractionResult extractFile(final java.nio.file.Path path, final ExtractionConfig config)
            throws KreuzbergRsException {
        return KreuzbergRs.extractFile(path, null, config);
    }

    /**
     * Synchronous wrapper for {@code extract_file}.
     *
     * This is a convenience function that blocks the current thread until extraction completes. For async code, use
     * {@code extract_file} directly.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating a new runtime per call. Always uses
     * the global runtime to avoid nested runtime issues.
     *
     * This function is only available with the {@code tokio-runtime} feature. For WASM targets, use a truly synchronous
     * extraction approach instead.
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::extract_file_sync; use
     * kreuzberg::core::config::ExtractionConfig;
     *
     * let config = ExtractionConfig::default(); let result = extract_file_sync("document.pdf", None, &amp;config)?;
     * println!("Content: {}", result.content); # Ok::&lt;(), kreuzberg::KreuzbergError&gt;(()) {@code }{@code }
     */
    public static ExtractionResult extractFileSync(final java.nio.file.Path path, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractFileSync(path, mimeType, config);
    }

    public static ExtractionResult extractFileSync(final java.nio.file.Path path, final ExtractionConfig config)
            throws KreuzbergRsException {
        return KreuzbergRs.extractFileSync(path, null, config);
    }

    /**
     * Synchronous wrapper for {@code extract_bytes}.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating a new runtime per call.
     *
     * With the {@code tokio-runtime} feature, this blocks the current thread using the global Tokio runtime. Without it
     * (WASM), this calls a truly synchronous implementation.
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::extract_bytes_sync; use
     * kreuzberg::core::config::ExtractionConfig;
     *
     * let config = ExtractionConfig::default(); let bytes = b"Hello, world!"; let result = extract_bytes_sync(bytes,
     * "text/plain", &amp;config)?; println!("Content: {}", result.content); # Ok::&lt;(),
     * kreuzberg::KreuzbergError&gt;(()) {@code }{@code }
     */
    public static ExtractionResult extractBytesSync(final byte[] content, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(content, "content must not be null");
        java.util.Objects.requireNonNull(mimeType, "mimeType must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractBytesSync(content, mimeType, config);
    }

    /**
     * Synchronous wrapper for {@code batch_extract_file}.
     *
     * Uses the global Tokio runtime for optimal performance. Only available with {@code tokio-runtime} (WASM has no
     * filesystem).
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::batch_extract_file_sync; use
     * kreuzberg::core::config::ExtractionConfig; use kreuzberg::FileExtractionConfig; use std::path::PathBuf;
     *
     * let config = ExtractionConfig::default(); let items: Vec&lt;(PathBuf, Option&lt;FileExtractionConfig&gt;)&gt; =
     * vec![ ("doc1.pdf".into(), Some(FileExtractionConfig { force_ocr: Some(true), ..Default::default() })),
     * ("doc2.pdf".into(), None), ]; let results = batch_extract_file_sync(items, &amp;config)?; # Ok::&lt;(),
     * kreuzberg::KreuzbergError&gt;(()) {@code }{@code }
     */
    public static List<ExtractionResult> batchExtractFileSync(final List<String> items, final ExtractionConfig config)
            throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractFileSync(items, config);
    }

    /**
     * Synchronous wrapper for {@code batch_extract_bytes}.
     *
     * Uses the global Tokio runtime for optimal performance. With the {@code tokio-runtime} feature, this blocks the
     * current thread using the global Tokio runtime. Without it (WASM), this calls a truly synchronous implementation
     * that iterates through items and calls {@code extract_bytes_sync()}.
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::batch_extract_bytes_sync; use
     * kreuzberg::core::config::ExtractionConfig; use kreuzberg::FileExtractionConfig;
     *
     * let config = ExtractionConfig::default(); let items = vec![ (b"content".to_vec(), "text/plain".to_string(),
     * None), (b"other".to_vec(), "text/plain".to_string(), Some(FileExtractionConfig { force_ocr: Some(true),
     * ..Default::default() })), ]; let results = batch_extract_bytes_sync(items, &amp;config)?; # Ok::&lt;(),
     * kreuzberg::KreuzbergError&gt;(()) {@code }{@code }
     */
    public static List<ExtractionResult> batchExtractBytesSync(final List<String> items, final ExtractionConfig config)
            throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractBytesSync(items, config);
    }

    /**
     * Extract content from multiple files concurrently.
     *
     * This function processes multiple files in parallel, automatically managing concurrency to prevent resource
     * exhaustion. The concurrency limit can be configured via {@code ExtractionConfig::max_concurrent_extractions} or
     * defaults to {@code (num_cpus * 1.5).ceil()}.
     *
     * Each file can optionally specify a [{@code FileExtractionConfig}] that overrides specific fields from the
     * batch-level {@code config}. Pass {@code None} for a file to use the batch defaults. Batch-level settings like
     * {@code max_concurrent_extractions} and {@code use_cache} are always taken from the batch-level {@code config}.
     *
     * # Arguments
     *
     * * {@code items} - Vector of {@code (path, optional_file_config)} tuples. Pass {@code None} as the config to use
     * the batch-level defaults for that file. * {@code config} - Batch-level extraction configuration (provides
     * defaults and batch settings)
     *
     * # Returns
     *
     * A vector of {@code ExtractionResult} in the same order as the input items.
     *
     * # Errors
     *
     * Individual file errors are captured in the result metadata. System errors (IO, RuntimeError equivalents) will
     * bubble up and fail the entire batch.
     *
     * # Examples
     *
     * Simple usage with no per-file overrides:
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::batch_extract_file; use
     * kreuzberg::core::config::ExtractionConfig; use std::path::PathBuf;
     *
     * # async fn example() -&gt; kreuzberg::Result&lt;()&gt; { let config = ExtractionConfig::default(); let items:
     * Vec&lt;(PathBuf, Option&lt;kreuzberg::FileExtractionConfig&gt;)&gt; = vec![ ("doc1.pdf".into(), None),
     * ("doc2.pdf".into(), None), ]; let results = batch_extract_file(items, &amp;config).await?; println!("Processed {}
     * files", results.len()); # Ok(()) # } {@code }{@code }
     *
     * Per-file configuration overrides:
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::batch_extract_file; use
     * kreuzberg::core::config::ExtractionConfig; use kreuzberg::FileExtractionConfig; use std::path::PathBuf;
     *
     * # async fn example() -&gt; kreuzberg::Result&lt;()&gt; { let config = ExtractionConfig::default(); let items:
     * Vec&lt;(PathBuf, Option&lt;FileExtractionConfig&gt;)&gt; = vec![ ("scan.pdf".into(), Some(FileExtractionConfig {
     * force_ocr: Some(true), ..Default::default() })), ("notes.txt".into(), None), ]; let results =
     * batch_extract_file(items, &amp;config).await?; # Ok(()) # } {@code }{@code }
     */
    public static List<ExtractionResult> batchExtractFile(final List<String> items, final ExtractionConfig config)
            throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractFile(items, config);
    }

    /**
     * Extract content from multiple byte arrays concurrently.
     *
     * This function processes multiple byte arrays in parallel, automatically managing concurrency to prevent resource
     * exhaustion. The concurrency limit can be configured via {@code ExtractionConfig::max_concurrent_extractions} or
     * defaults to {@code (num_cpus * 1.5).ceil()}.
     *
     * Each item can optionally specify a [{@code FileExtractionConfig}] that overrides specific fields from the
     * batch-level {@code config}. Pass {@code None} as the config to use the batch-level defaults for that item.
     *
     * # Arguments
     *
     * * {@code items} - Vector of {@code (bytes, mime_type, optional_file_config)} tuples * {@code config} -
     * Batch-level extraction configuration
     *
     * # Returns
     *
     * A vector of {@code ExtractionResult} in the same order as the input items.
     *
     * # Examples
     *
     * Simple usage with no per-item overrides:
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::batch_extract_bytes; use
     * kreuzberg::core::config::ExtractionConfig;
     *
     * # async fn example() -&gt; kreuzberg::Result&lt;()&gt; { let config = ExtractionConfig::default(); let items =
     * vec![ (b"content 1".to_vec(), "text/plain".to_string(), None), (b"content 2".to_vec(), "text/plain".to_string(),
     * None), ]; let results = batch_extract_bytes(items, &amp;config).await?; println!("Processed {} items",
     * results.len()); # Ok(()) # } {@code }{@code }
     *
     * Per-item configuration overrides:
     *
     * {@code }{@code rust,no_run} use kreuzberg::core::extractor::batch_extract_bytes; use
     * kreuzberg::core::config::ExtractionConfig; use kreuzberg::FileExtractionConfig;
     *
     * # async fn example() -&gt; kreuzberg::Result&lt;()&gt; { let config = ExtractionConfig::default(); let items =
     * vec![ (b"content".to_vec(), "text/plain".to_string(), None), (b"&lt;html&gt;test&lt;/html&gt;".to_vec(),
     * "text/html".to_string(), Some(FileExtractionConfig { force_ocr: Some(true), ..Default::default() })), ]; let
     * results = batch_extract_bytes(items, &amp;config).await?; # Ok(()) # } {@code }{@code }
     */
    public static List<ExtractionResult> batchExtractBytes(final List<String> items, final ExtractionConfig config)
            throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractBytes(items, config);
    }

    /**
     * Detect MIME type from raw file bytes.
     *
     * Uses magic byte signatures to detect file type from content. Falls back to {@code infer} crate for comprehensive
     * detection.
     *
     * For ZIP-based files, inspects contents to distinguish Office Open XML formats (DOCX, XLSX, PPTX) from plain ZIP
     * archives.
     *
     * # Arguments
     *
     * * {@code content} - Raw file bytes
     *
     * # Returns
     *
     * The detected MIME type string.
     *
     * # Errors
     *
     * Returns {@code KreuzbergError::UnsupportedFormat} if MIME type cannot be determined.
     */
    public static String detectMimeTypeFromBytes(final byte[] content) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(content, "content must not be null");
        return KreuzbergRs.detectMimeTypeFromBytes(content);
    }

    /**
     * Get file extensions for a given MIME type.
     *
     * Returns all known file extensions that map to the specified MIME type.
     *
     * # Arguments
     *
     * * {@code mime_type} - The MIME type to look up
     *
     * # Returns
     *
     * A vector of file extensions (without leading dot) for the MIME type.
     *
     * # Example
     *
     * {@code }{@code } use kreuzberg::core::mime::get_extensions_for_mime;
     *
     * let extensions = get_extensions_for_mime("application/pdf").unwrap(); assert_eq!(extensions, vec!["pdf"]);
     *
     * let doc_extensions =
     * get_extensions_for_mime("application/vnd.openxmlformats-officedocument.wordprocessingml.document").unwrap();
     * assert!(doc_extensions.contains(&amp;"docx".to_string())); {@code }{@code }
     */
    public static List<String> getExtensionsForMime(final String mimeType) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(mimeType, "mimeType must not be null");
        return KreuzbergRs.getExtensionsForMime(mimeType);
    }

    /**
     * List names of all registered document extractors.
     *
     * Re-exported at the crate root as {@code list_document_extractors}.
     */
    public static List<String> listExtractors() throws KreuzbergRsException {
        return KreuzbergRs.listExtractors();
    }

    /**
     * List all registered OCR backends.
     *
     * Returns the names of all OCR backends currently registered in the global registry.
     *
     * # Returns
     *
     * A vector of OCR backend names.
     *
     * # Example
     *
     * {@code }{@code rust} use kreuzberg::plugins::list_ocr_backends;
     *
     * # tokio_test::block_on(async { let backends = list_ocr_backends()?; for name in backends { println!("Registered
     * OCR backend: {}", name); } # Ok::&lt;(), kreuzberg::KreuzbergError&gt;(()) # }); {@code }{@code }
     */
    public static List<String> listOcrBackends() throws KreuzbergRsException {
        return KreuzbergRs.listOcrBackends();
    }

    /**
     * Clear all OCR backends from the global registry.
     *
     * Removes all OCR backends and calls their {@code shutdown()} methods.
     *
     * # Returns
     *
     * - {@code Ok(())} if all backends were cleared successfully - {@code Err(...)} if any shutdown method failed
     *
     * # Example
     *
     * {@code }{@code rust} use kreuzberg::plugins::clear_ocr_backends;
     *
     * # tokio_test::block_on(async { clear_ocr_backends()?; # Ok::&lt;(), kreuzberg::KreuzbergError&gt;(()) # });
     * {@code }{@code }
     */
    public static void clearOcrBackends() throws KreuzbergRsException {
        KreuzbergRs.clearOcrBackends();
    }

    /**
     * List all registered post-processor names.
     *
     * Returns a vector of all post-processor names currently registered in the global registry.
     *
     * # Returns
     *
     * - {@code Ok(Vec&lt;String&gt;)} - Vector of post-processor names - {@code Err(...)} if the registry lock is
     * poisoned
     *
     * # Example
     *
     * {@code }{@code rust} use kreuzberg::plugins::list_post_processors;
     *
     * # tokio_test::block_on(async { let processors = list_post_processors()?; for name in processors {
     * println!("Registered post-processor: {}", name); } # Ok::&lt;(), kreuzberg::KreuzbergError&gt;(()) # });
     * {@code }{@code }
     */
    public static List<String> listPostProcessors() throws KreuzbergRsException {
        return KreuzbergRs.listPostProcessors();
    }

    /**
     * Remove all registered post-processors.
     */
    public static void clearPostProcessors() throws KreuzbergRsException {
        KreuzbergRs.clearPostProcessors();
    }

    /**
     * List names of all registered validators.
     */
    public static List<String> listValidators() throws KreuzbergRsException {
        return KreuzbergRs.listValidators();
    }

    /**
     * Remove all registered validators.
     */
    public static void clearValidators() throws KreuzbergRsException {
        KreuzbergRs.clearValidators();
    }

    /**
     * Render a single PDF page to a PNG-encoded byte buffer.
     *
     * # Errors
     *
     * Returns an error if the PDF is invalid, the page index is out of bounds, or if the page fails to render.
     *
     * # Example
     *
     * {@code }{@code rust,no_run} use kreuzberg::pdf::render_pdf_page_to_png;
     *
     * # fn example() -&gt; kreuzberg::pdf::error::Result&lt;()&gt; { let pdf_bytes = std::fs::read("document.pdf")?;
     * let png = render_pdf_page_to_png(&amp;pdf_bytes, 0, Some(150), None)?; std::fs::write("page_0.png", png)?; #
     * Ok(()) # } {@code }{@code }
     */
    public static byte[] renderPdfPageToPng(final byte[] pdfBytes, final long pageIndex, final Integer dpi,
            final String password) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(pdfBytes, "pdfBytes must not be null");
        java.util.Objects.requireNonNull(pageIndex, "pageIndex must not be null");
        return KreuzbergRs.renderPdfPageToPng(pdfBytes, pageIndex, dpi, password);
    }

    public static byte[] renderPdfPageToPng(final byte[] pdfBytes, final long pageIndex) throws KreuzbergRsException {
        return KreuzbergRs.renderPdfPageToPng(pdfBytes, pageIndex, 0, null);
    }

    /**
     * Detect the MIME type of a file at the given path.
     *
     * Uses the file extension and optionally the file content to determine the MIME type. Set {@code check_exists} to
     * {@code true} to verify the file exists before detection.
     */
    public static String detectMimeType(final String path, final boolean checkExists) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(checkExists, "checkExists must not be null");
        return KreuzbergRs.detectMimeType(path, checkExists);
    }

    /**
     * Embed a list of texts using the configured embedding model.
     *
     * Returns a 2D vector where each inner vector is the embedding for the corresponding text.
     */
    public static List<List<Float>> embedTexts(final List<String> texts, final EmbeddingConfig config)
            throws KreuzbergRsException {
        java.util.Objects.requireNonNull(texts, "texts must not be null");
        return KreuzbergRs.embedTexts(texts, config);
    }

    public static List<List<Float>> embedTexts(final List<String> texts) throws KreuzbergRsException {
        return KreuzbergRs.embedTexts(texts, null);
    }

    /**
     * Get an embedding preset by name.
     *
     * Returns {@code None} if no preset with the given name exists.
     */
    public static String getEmbeddingPreset(final String name) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(name, "name must not be null");
        return KreuzbergRs.getEmbeddingPreset(name).orElseThrow();
    }

    /**
     * List the names of all available embedding presets.
     */
    public static List<String> listEmbeddingPresets() throws KreuzbergRsException {
        return KreuzbergRs.listEmbeddingPresets();
    }

}
