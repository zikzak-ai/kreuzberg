// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:76fb35e1a4d5deb6da66ec3be1ebfde04a78721e424047e24ab32cdbab24fa12
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg;

import java.util.Optional;
import com.fasterxml.jackson.annotation.JsonProperty;

/**
 * Extracted image from a document.
 *
 * Contains raw image data, metadata, and optional nested OCR results. Raw bytes
 * allow cross-language compatibility - users can convert to PIL.Image (Python),
 * Sharp (Node.js), or other formats as needed.
 */
public record ExtractedImage(
		/** Raw image data (PNG, JPEG, WebP, etc. bytes). */
		byte[] data,
		/** Image format (e.g., "jpeg", "png", "webp") */
		String format,
		/** Zero-indexed position of this image in the document/page */
		@JsonProperty("image_index") long imageIndex,
		/** Page/slide number where image was found (1-indexed) */
		@JsonProperty("page_number") Optional<Long> pageNumber,
		/** Image width in pixels */
		Optional<Integer> width,
		/** Image height in pixels */
		Optional<Integer> height,
		/** Colorspace information (e.g., "RGB", "CMYK", "Gray") */
		Optional<String> colorspace,
		/** Bits per color component (e.g., 8, 16) */
		@JsonProperty("bits_per_component") Optional<Integer> bitsPerComponent,
		/** Whether this image is a mask image */
		@JsonProperty("is_mask") boolean isMask,
		/** Optional description of the image */
		Optional<String> description,
		/** Nested OCR extraction result (if image was OCRed) */
		@JsonProperty("ocr_result") Optional<ExtractionResult> ocrResult,
		/**
		 * Bounding box of the image on the page (PDF coordinates: x0=left, y0=bottom,
		 * x1=right, y1=top).
		 */
		@JsonProperty("bounding_box") Optional<String> boundingBox,
		/**
		 * Original source path of the image within the document archive (e.g.,
		 * "media/image1.png" in DOCX).
		 */
		@JsonProperty("source_path") Optional<String> sourcePath,
		/** Heuristic classification of what this image likely depicts. */
		@JsonProperty("image_kind") Optional<ImageKind> imageKind,
		/** Confidence score for {@code image_kind}, in [0.0, 1.0]. */
		@JsonProperty("kind_confidence") Optional<Float> kindConfidence,
		/** Identifier shared across images that form a single logical figure */
		@JsonProperty("cluster_id") Optional<Integer> clusterId) {
}
