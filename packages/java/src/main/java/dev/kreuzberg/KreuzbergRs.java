// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:dc92e746c5af123fd59e44e4e746e374feda4df194f1ed0ab661923b5ae05a52
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.util.List;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

public final class KreuzbergRs {
	private KreuzbergRs() {
	}

	public static String blake3HashBytes(final byte[] data) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BLAKE3_HASH_BYTES.invoke(data);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String blake3HashFile(final java.nio.file.Path path) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpath = arena.allocateFrom(path.toString());
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BLAKE3_HASH_FILE.invoke(cpath);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static long fastHash(final byte[] data) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var primitiveResult = (long) NativeLib.KREUZBERG_FAST_HASH.invoke(data);
			return primitiveResult;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static boolean validateCacheKey(final String key) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ckey = arena.allocateFrom(key);
			var primitiveResult = (boolean) NativeLib.KREUZBERG_VALIDATE_CACHE_KEY.invoke(ckey);
			return primitiveResult;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validatePort(final short port) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_PORT.invoke(port);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateHost(final String host) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var chost = arena.allocateFrom(host);
			NativeLib.KREUZBERG_VALIDATE_HOST.invoke(chost);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateCorsOrigin(final String origin) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var corigin = arena.allocateFrom(origin);
			NativeLib.KREUZBERG_VALIDATE_CORS_ORIGIN.invoke(corigin);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateUploadSize(final long size) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_UPLOAD_SIZE.invoke(size);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateBinarizationMethod(final String method) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmethod = arena.allocateFrom(method);
			NativeLib.KREUZBERG_VALIDATE_BINARIZATION_METHOD.invoke(cmethod);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateTokenReductionLevel(final String level) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var clevel = arena.allocateFrom(level);
			NativeLib.KREUZBERG_VALIDATE_TOKEN_REDUCTION_LEVEL.invoke(clevel);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateOcrBackend(final String backend) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cbackend = arena.allocateFrom(backend);
			NativeLib.KREUZBERG_VALIDATE_OCR_BACKEND.invoke(cbackend);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateLanguageCode(final String code) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ccode = arena.allocateFrom(code);
			NativeLib.KREUZBERG_VALIDATE_LANGUAGE_CODE.invoke(ccode);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateTesseractPsm(final int psm) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_TESSERACT_PSM.invoke(psm);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateTesseractOem(final int oem) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_TESSERACT_OEM.invoke(oem);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateOutputFormat(final String format) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cformat = arena.allocateFrom(format);
			NativeLib.KREUZBERG_VALIDATE_OUTPUT_FORMAT.invoke(cformat);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateConfidence(final double confidence) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_CONFIDENCE.invoke(confidence);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateDpi(final int dpi) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_DPI.invoke(dpi);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateChunkingParams(final long maxChars, final long maxOverlap) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_VALIDATE_CHUNKING_PARAMS.invoke(maxChars, maxOverlap);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void validateLlmConfigModel(final String model) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmodel = arena.allocateFrom(model);
			NativeLib.KREUZBERG_VALIDATE_LLM_CONFIG_MODEL.invoke(cmodel);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static ExtractionResult extractBytes(final byte[] content, final String mimeType,
			final ExtractionConfig config) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmimeType = arena.allocateFrom(mimeType);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_BYTES.invoke(content, cmimeType, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ExtractionResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static CompletableFuture<ExtractionResult> extractBytesAsync(final byte[] content, final String mimeType,
			final ExtractionConfig config) {
		return CompletableFuture.supplyAsync(() -> {
			try {
				return extractBytes(content, mimeType, config);
			} catch (Throwable e) {
				throw new CompletionException(e);
			}
		});
	}

	public static ExtractionResult extractFile(final java.nio.file.Path path, final String mimeType,
			final ExtractionConfig config) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpath = arena.allocateFrom(path.toString());
			var cmimeType = arena.allocateFrom(mimeType);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_FILE.invoke(cpath, cmimeType, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ExtractionResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static CompletableFuture<ExtractionResult> extractFileAsync(final java.nio.file.Path path,
			final String mimeType, final ExtractionConfig config) {
		return CompletableFuture.supplyAsync(() -> {
			try {
				return extractFile(path, mimeType, config);
			} catch (Throwable e) {
				throw new CompletionException(e);
			}
		});
	}

	public static ExtractionResult extractFileSync(final java.nio.file.Path path, final String mimeType,
			final ExtractionConfig config) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpath = arena.allocateFrom(path.toString());
			var cmimeType = arena.allocateFrom(mimeType);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_FILE_SYNC.invoke(cpath, cmimeType, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ExtractionResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static ExtractionResult extractBytesSync(final byte[] content, final String mimeType,
			final ExtractionConfig config) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmimeType = arena.allocateFrom(mimeType);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_BYTES_SYNC.invoke(content, cmimeType, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ExtractionResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<ExtractionResult> batchExtractFileSync(final List<String> items, final ExtractionConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var citemsJson = createObjectMapper().writeValueAsString(items);
			var citems = arena.allocateFrom(citemsJson);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BATCH_EXTRACT_FILE_SYNC.invoke(citems, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<ExtractionResult>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<ExtractionResult> batchExtractBytesSync(final List<String> items, final ExtractionConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var citemsJson = createObjectMapper().writeValueAsString(items);
			var citems = arena.allocateFrom(citemsJson);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BATCH_EXTRACT_BYTES_SYNC.invoke(citems, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<ExtractionResult>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<ExtractionResult> batchExtractFile(final List<String> items, final ExtractionConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var citemsJson = createObjectMapper().writeValueAsString(items);
			var citems = arena.allocateFrom(citemsJson);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BATCH_EXTRACT_FILE.invoke(citems, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<ExtractionResult>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static CompletableFuture<List<ExtractionResult>> batchExtractFileAsync(final List<String> items,
			final ExtractionConfig config) {
		return CompletableFuture.supplyAsync(() -> {
			try {
				return batchExtractFile(items, config);
			} catch (Throwable e) {
				throw new CompletionException(e);
			}
		});
	}

	public static List<ExtractionResult> batchExtractBytes(final List<String> items, final ExtractionConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var citemsJson = createObjectMapper().writeValueAsString(items);
			var citems = arena.allocateFrom(citemsJson);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BATCH_EXTRACT_BYTES.invoke(citems, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<ExtractionResult>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static CompletableFuture<List<ExtractionResult>> batchExtractBytesAsync(final List<String> items,
			final ExtractionConfig config) {
		return CompletableFuture.supplyAsync(() -> {
			try {
				return batchExtractBytes(items, config);
			} catch (Throwable e) {
				throw new CompletionException(e);
			}
		});
	}

	public static boolean isValidFormatField(final String field) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cfield = arena.allocateFrom(field);
			var primitiveResult = (boolean) NativeLib.KREUZBERG_IS_VALID_FORMAT_FIELD.invoke(cfield);
			return primitiveResult;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String validateMimeType(final String mimeType) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmimeType = arena.allocateFrom(mimeType);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_VALIDATE_MIME_TYPE.invoke(cmimeType);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String detectOrValidate(final String path, final String mimeType) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpath = arena.allocateFrom(path);
			var cmimeType = arena.allocateFrom(mimeType);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_DETECT_OR_VALIDATE.invoke(cpath, cmimeType);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String detectMimeTypeFromBytes(final byte[] content) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_DETECT_MIME_TYPE_FROM_BYTES.invoke(content);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> getExtensionsForMime(final String mimeType) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmimeType = arena.allocateFrom(mimeType);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_GET_EXTENSIONS_FOR_MIME.invoke(cmimeType);
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<SupportedFormat> listSupportedFormats() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_SUPPORTED_FORMATS.invoke();
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<SupportedFormat>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void clearProcessorCache() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_CLEAR_PROCESSOR_CACHE.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<Element> transformExtractionResultToElements(final ExtractionResult result)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cresultJson = result != null ? createObjectMapper().writeValueAsString(result) : null;
			var cresultJsonSeg = cresultJson != null ? arena.allocateFrom(cresultJson) : MemorySegment.NULL;
			var cresult = cresultJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_FROM_JSON.invoke(cresultJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_TRANSFORM_EXTRACTION_RESULT_TO_ELEMENTS.invoke(cresult);
			if (!cresult.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(cresult);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<Element>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static EmailExtractionResult extractEmailContent(final byte[] data, final String mimeType,
			final Integer fallbackCodepage) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmimeType = arena.allocateFrom(mimeType);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_EMAIL_CONTENT.invoke(data, cmimeType,
					fallbackCodepage);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_EMAIL_EXTRACTION_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_EMAIL_EXTRACTION_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, EmailExtractionResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String cellsToText(final List<List<String>> cells) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ccellsJson = createObjectMapper().writeValueAsString(cells);
			var ccells = arena.allocateFrom(ccellsJson);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CELLS_TO_TEXT.invoke(ccells);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String cellsToMarkdown(final List<List<String>> cells) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ccellsJson = createObjectMapper().writeValueAsString(cells);
			var ccells = arena.allocateFrom(ccellsJson);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CELLS_TO_MARKDOWN.invoke(ccells);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String djotToHtml(final String djotSource) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cdjotSource = arena.allocateFrom(djotSource);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_DJOT_TO_HTML.invoke(cdjotSource);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> dedupText(final List<String> texts) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctextsJson = createObjectMapper().writeValueAsString(texts);
			var ctexts = arena.allocateFrom(ctextsJson);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_DEDUP_TEXT.invoke(ctexts);
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String normalizeWhitespace(final String s) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cs = arena.allocateFrom(s);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_NORMALIZE_WHITESPACE.invoke(cs);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void registerDefaultExtractors() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_REGISTER_DEFAULT_EXTRACTORS.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void unregisterExtractor(final String name) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cname = arena.allocateFrom(name);
			NativeLib.KREUZBERG_UNREGISTER_EXTRACTOR.invoke(cname);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> listExtractors() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_EXTRACTORS.invoke();
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void clearExtractors() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_CLEAR_EXTRACTORS.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void unregisterOcrBackend(final String name) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cname = arena.allocateFrom(name);
			NativeLib.KREUZBERG_UNREGISTER_OCR_BACKEND.invoke(cname);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> listOcrBackends() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_OCR_BACKENDS.invoke();
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void clearOcrBackends() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_CLEAR_OCR_BACKENDS.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> listPostProcessors() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_POST_PROCESSORS.invoke();
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void unregisterRenderer(final String name) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cname = arena.allocateFrom(name);
			NativeLib.KREUZBERG_UNREGISTER_RENDERER.invoke(cname);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> listRenderers() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_RENDERERS.invoke();
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void clearRenderers() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_CLEAR_RENDERERS.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> listValidators() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_VALIDATORS.invoke();
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void clearValidators() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_CLEAR_VALIDATORS.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String sanitizeFilename(final java.nio.file.Path path) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpath = arena.allocateFrom(path.toString());
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SANITIZE_FILENAME.invoke(cpath);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String sanitizePath(final java.nio.file.Path path) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpath = arena.allocateFrom(path.toString());
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SANITIZE_PATH.invoke(cpath);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static boolean isValidUtf8(final byte[] bytes) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var primitiveResult = (boolean) NativeLib.KREUZBERG_IS_VALID_UTF8.invoke(bytes);
			return primitiveResult;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String cleanExtractedText(final String text) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CLEAN_EXTRACTED_TEXT.invoke(ctext);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String reduceTokens(final String text, final TokenReductionConfig config, final String languageHint)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_TOKEN_REDUCTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var clanguageHint = arena.allocateFrom(languageHint);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_REDUCE_TOKENS.invoke(ctext, cconfig, clanguageHint);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_TOKEN_REDUCTION_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> batchReduceTokens(final List<String> texts, final TokenReductionConfig config,
			final String languageHint) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctextsJson = createObjectMapper().writeValueAsString(texts);
			var ctexts = arena.allocateFrom(ctextsJson);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_TOKEN_REDUCTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var clanguageHint = arena.allocateFrom(languageHint);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BATCH_REDUCE_TOKENS.invoke(ctexts, cconfig,
					clanguageHint);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_TOKEN_REDUCTION_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation bold(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_BOLD.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation italic(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_ITALIC.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation underline(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_UNDERLINE.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation link(final int start, final int end, final String url, final String title)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var curl = arena.allocateFrom(url);
			var ctitle = arena.allocateFrom(title);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LINK.invoke(start, end, curl, ctitle);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation code(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CODE.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation strikethrough(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_STRIKETHROUGH.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation subscript(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SUBSCRIPT.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation superscript(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SUPERSCRIPT.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation fontSize(final int start, final int end, final String value)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cvalue = arena.allocateFrom(value);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_FONT_SIZE.invoke(start, end, cvalue);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation color(final int start, final int end, final String value) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cvalue = arena.allocateFrom(value);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_COLOR.invoke(start, end, cvalue);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static TextAnnotation highlight(final int start, final int end) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_HIGHLIGHT.invoke(start, end);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_TEXT_ANNOTATION_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_TEXT_ANNOTATION_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, TextAnnotation.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static UriKind classifyUri(final String url) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var curl = arena.allocateFrom(url);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CLASSIFY_URI.invoke(curl);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_URI_KIND_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_URI_KIND_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, UriKind.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String safeDecode(final byte[] byteData, final String encoding) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cencoding = arena.allocateFrom(encoding);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SAFE_DECODE.invoke(byteData, cencoding);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static double calculateTextConfidence(final String text) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var primitiveResult = (double) NativeLib.KREUZBERG_CALCULATE_TEXT_CONFIDENCE.invoke(ctext);
			return primitiveResult;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static StringBufferPool createStringBufferPool(final long poolSize, final long bufferCapacity)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CREATE_STRING_BUFFER_POOL.invoke(poolSize,
					bufferCapacity);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			return new StringBufferPool(resultPtr);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static ByteBufferPool createByteBufferPool(final long poolSize, final long bufferCapacity)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CREATE_BYTE_BUFFER_POOL.invoke(poolSize,
					bufferCapacity);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			return new ByteBufferPool(resultPtr);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String openapiJson() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_OPENAPI_JSON.invoke();
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void serveDefault() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			NativeLib.KREUZBERG_SERVE_DEFAULT.invoke();
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static CompletableFuture<Void> serveDefaultAsync() {
		return CompletableFuture.supplyAsync(() -> {
			try {
				serveDefault();
				return null;
			} catch (Throwable e) {
				throw new CompletionException(e);
			}
		});
	}

	public static ChunkingResult chunkText(final String text, final ChunkingConfig config,
			final List<PageBoundary> pageBoundaries) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_CHUNKING_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var cpageBoundariesJson = createObjectMapper().writeValueAsString(pageBoundaries);
			var cpageBoundaries = arena.allocateFrom(cpageBoundariesJson);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNK_TEXT.invoke(ctext, cconfig, cpageBoundaries);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_CHUNKING_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNKING_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_CHUNKING_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ChunkingResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static ChunkingResult chunkTextWithHeadingSource(final String text, final ChunkingConfig config,
			final List<PageBoundary> pageBoundaries, final String headingSource) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_CHUNKING_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var cpageBoundariesJson = createObjectMapper().writeValueAsString(pageBoundaries);
			var cpageBoundaries = arena.allocateFrom(cpageBoundariesJson);
			var cheadingSource = arena.allocateFrom(headingSource);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNK_TEXT_WITH_HEADING_SOURCE.invoke(ctext, cconfig,
					cpageBoundaries, cheadingSource);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_CHUNKING_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNKING_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_CHUNKING_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ChunkingResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<ChunkingResult> chunkTextsBatch(final List<String> texts, final ChunkingConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctextsJson = createObjectMapper().writeValueAsString(texts);
			var ctexts = arena.allocateFrom(ctextsJson);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_CHUNKING_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNK_TEXTS_BATCH.invoke(ctexts, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_CHUNKING_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<ChunkingResult>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static ChunkingResult chunkSemantic(final String text, final ChunkingConfig config,
			final List<PageBoundary> pageBoundaries) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_CHUNKING_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var cpageBoundariesJson = createObjectMapper().writeValueAsString(pageBoundaries);
			var cpageBoundaries = arena.allocateFrom(cpageBoundariesJson);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNK_SEMANTIC.invoke(ctext, cconfig, cpageBoundaries);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_CHUNKING_CONFIG_FREE.invoke(cconfig);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			var jsonPtr = (MemorySegment) NativeLib.KREUZBERG_CHUNKING_RESULT_TO_JSON.invoke(resultPtr);
			NativeLib.KREUZBERG_CHUNKING_RESULT_FREE.invoke(resultPtr);
			if (jsonPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
			return createObjectMapper().readValue(json, ChunkingResult.class);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<Float> normalize(final List<Float> v) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cvJson = createObjectMapper().writeValueAsString(v);
			var cv = arena.allocateFrom(cvJson);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_NORMALIZE.invoke(cv);
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<Float>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static Optional<String> getPreset(final String name) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cname = arena.allocateFrom(name);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_GET_PRESET.invoke(cname);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return Optional.empty();
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return Optional.of(str);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<String> listPresets() throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_LIST_PRESETS.invoke();
			return readJsonList(resultPtr, new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
			});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void warmModel(final EmbeddingModelType modelType, final java.nio.file.Path cacheDir)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmodelTypeJson = modelType != null ? createObjectMapper().writeValueAsString(modelType) : null;
			var cmodelTypeJsonSeg = cmodelTypeJson != null ? arena.allocateFrom(cmodelTypeJson) : MemorySegment.NULL;
			var cmodelType = cmodelTypeJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EMBEDDING_MODEL_TYPE_FROM_JSON.invoke(cmodelTypeJsonSeg)
					: MemorySegment.NULL;
			var ccacheDir = arena.allocateFrom(cacheDir.toString());
			NativeLib.KREUZBERG_WARM_MODEL.invoke(cmodelType, ccacheDir);
			if (!cmodelType.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EMBEDDING_MODEL_TYPE_FREE.invoke(cmodelType);
			}
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static void downloadModel(final EmbeddingModelType modelType, final java.nio.file.Path cacheDir)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cmodelTypeJson = modelType != null ? createObjectMapper().writeValueAsString(modelType) : null;
			var cmodelTypeJsonSeg = cmodelTypeJson != null ? arena.allocateFrom(cmodelTypeJson) : MemorySegment.NULL;
			var cmodelType = cmodelTypeJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EMBEDDING_MODEL_TYPE_FROM_JSON.invoke(cmodelTypeJsonSeg)
					: MemorySegment.NULL;
			var ccacheDir = arena.allocateFrom(cacheDir.toString());
			NativeLib.KREUZBERG_DOWNLOAD_MODEL.invoke(cmodelType, ccacheDir);
			if (!cmodelType.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EMBEDDING_MODEL_TYPE_FREE.invoke(cmodelType);
			}
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static int calculateOptimalDpi(final double pageWidth, final double pageHeight, final int targetDpi,
			final int maxDimension, final int minDpi, final int maxDpi) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var primitiveResult = (int) NativeLib.KREUZBERG_CALCULATE_OPTIMAL_DPI.invoke(pageWidth, pageHeight,
					targetDpi, maxDimension, minDpi, maxDpi);
			return primitiveResult;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static Optional<List<String>> detectLanguages(final String text, final LanguageDetectionConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_LANGUAGE_DETECTION_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_DETECT_LANGUAGES.invoke(ctext, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_LANGUAGE_DETECTION_CONFIG_FREE.invoke(cconfig);
			}
			return Optional.of(readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<String>>() {
					}));
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static List<Keyword> extractKeywords(final String text, final KeywordConfig config)
			throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var ctext = arena.allocateFrom(text);
			var cconfigJson = config != null ? createObjectMapper().writeValueAsString(config) : null;
			var cconfigJsonSeg = cconfigJson != null ? arena.allocateFrom(cconfigJson) : MemorySegment.NULL;
			var cconfig = cconfigJson != null
					? (MemorySegment) NativeLib.KREUZBERG_KEYWORD_CONFIG_FROM_JSON.invoke(cconfigJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_KEYWORDS.invoke(ctext, cconfig);
			if (!cconfig.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_KEYWORD_CONFIG_FREE.invoke(cconfig);
			}
			return readJsonList(resultPtr,
					new com.fasterxml.jackson.core.type.TypeReference<java.util.List<Keyword>>() {
					});
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String computeHash(final String data) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cdata = arena.allocateFrom(data);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_COMPUTE_HASH.invoke(cdata);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static byte[] renderPdfPageToPng(final byte[] pdfBytes, final long pageIndex, final Integer dpi,
			final String password) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cpassword = arena.allocateFrom(password);
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_RENDER_PDF_PAGE_TO_PNG.invoke(pdfBytes, pageIndex, dpi,
					cpassword);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			long byteLen = resultPtr.byteSize();
			byte[] result = resultPtr.reinterpret(byteLen).toArray(ValueLayout.JAVA_BYTE);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return result;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String extractTextFromPdf(final byte[] pdfBytes) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_EXTRACT_TEXT_FROM_PDF.invoke(pdfBytes);
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String serializeToToon(final ExtractionResult result) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cresultJson = result != null ? createObjectMapper().writeValueAsString(result) : null;
			var cresultJsonSeg = cresultJson != null ? arena.allocateFrom(cresultJson) : MemorySegment.NULL;
			var cresult = cresultJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_FROM_JSON.invoke(cresultJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SERIALIZE_TO_TOON.invoke(cresult);
			if (!cresult.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(cresult);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	public static String serializeToJson(final ExtractionResult result) throws KreuzbergRsException {
		try (var arena = Arena.ofConfined()) {
			var cresultJson = result != null ? createObjectMapper().writeValueAsString(result) : null;
			var cresultJsonSeg = cresultJson != null ? arena.allocateFrom(cresultJson) : MemorySegment.NULL;
			var cresult = cresultJson != null
					? (MemorySegment) NativeLib.KREUZBERG_EXTRACTION_RESULT_FROM_JSON.invoke(cresultJsonSeg)
					: MemorySegment.NULL;
			var resultPtr = (MemorySegment) NativeLib.KREUZBERG_SERIALIZE_TO_JSON.invoke(cresult);
			if (!cresult.equals(MemorySegment.NULL)) {
				NativeLib.KREUZBERG_EXTRACTION_RESULT_FREE.invoke(cresult);
			}
			if (resultPtr.equals(MemorySegment.NULL)) {
				checkLastError();
				return null;
			}
			String str = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return str;
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}

	// Helper methods for FFI marshalling

	private static void checkLastError() throws Throwable {
		int errCode = (int) NativeLib.KREUZBERG_LAST_ERROR_CODE.invoke();
		if (errCode != 0) {
			var ctxPtr = (MemorySegment) NativeLib.KREUZBERG_LAST_ERROR_CONTEXT.invoke();
			String msg = ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
			throw new KreuzbergRsException(errCode, msg);
		}
	}

	private static com.fasterxml.jackson.databind.ObjectMapper createObjectMapper() {
		return new com.fasterxml.jackson.databind.ObjectMapper()
				.registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module()).findAndRegisterModules()
				.setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
				.configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
	}

	private static <T> java.util.List<T> readJsonList(MemorySegment resultPtr,
			com.fasterxml.jackson.core.type.TypeReference<java.util.List<T>> typeRef) throws KreuzbergRsException {
		if (resultPtr.equals(MemorySegment.NULL)) {
			return java.util.List.of();
		}
		try {
			String json = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
			NativeLib.KREUZBERG_FREE_STRING.invoke(resultPtr);
			return createObjectMapper().readValue(json, typeRef);
		} catch (Throwable e) {
			throw new KreuzbergRsException("FFI call failed", e);
		}
	}
}
