name: CI Python

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/actions/**'
      - 'crates/kreuzberg/**'
      - 'crates/kreuzberg-py/**'
      - 'crates/kreuzberg-tesseract/**'
      - 'packages/python/**'
      - 'e2e/python/**'
      - 'test_documents/**'
      - 'fixtures/**'
      - 'tools/e2e-generator/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'uv.lock'
      - 'pyproject.toml'
      - 'scripts/ci/python/**'
      - '.github/workflows/ci-python.yaml'
      - '.cargo/config.toml'

concurrency:
  group: ci-python-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0
  RUST_BACKTRACE: short
  RUST_MIN_STACK: 16777216
  PDFIUM_VERSION: "7578"
  PDFIUM_STATIC_VERSION: "7442b"
  ORT_VERSION: "1.23.2"
  MACOSX_DEPLOYMENT_TARGET: "14.0"

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      python_matrix: ${{ steps.matrix.outputs.python_matrix }}
      python_matrix_has_entries: ${{ steps.matrix.outputs.python_matrix_has_entries }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            python_windows:
              - '.github/actions/**'
              - 'crates/kreuzberg/**'
              - 'crates/kreuzberg-py/**'
              - 'crates/kreuzberg-tesseract/**'
              - 'packages/python/**'
              - 'e2e/python/**'
              - 'test_documents/**'
              - 'fixtures/**'
              - 'tools/e2e-generator/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'uv.lock'
              - 'pyproject.toml'
              - 'scripts/ci/python/**'
              - '.github/workflows/ci-python.yaml'
              - '.cargo/config.toml'
            python_unix:
              - '.github/actions/**'
              - 'crates/kreuzberg/**'
              - 'crates/kreuzberg-py/**'
              - 'crates/kreuzberg-tesseract/**'
              - 'packages/python/**'
              - 'e2e/python/**'
              - 'test_documents/**'
              - 'fixtures/**'
              - 'tools/e2e-generator/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'uv.lock'
              - 'pyproject.toml'
              - 'scripts/ci/python/**'
              - '.github/workflows/ci-python.yaml'
              - '.cargo/config.toml'
      - name: Build matrix
        id: matrix
        shell: bash
        env:
          PY_WINDOWS: ${{ steps.filter.outputs.python_windows }}
          PY_UNIX: ${{ steps.filter.outputs.python_unix }}
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import os
          include = []
          if os.environ.get("PY_WINDOWS") == "true":
              include.append({
                  "os": "windows-latest",
                  "target": "x86_64-pc-windows-msvc",
                  "platform": "windows-x86_64",
              })
          if os.environ.get("PY_UNIX") == "true":
              include.extend([
                  {
                      "os": "ubuntu-latest",
                      "target": "x86_64-unknown-linux-gnu",
                      "platform": "linux-x86_64",
                  },
                  {
                      "os": "macos-latest",
                      "target": "aarch64-apple-darwin",
                      "platform": "macos-arm64",
                  },
              ])
          print(f"python_matrix={json.dumps({'include': include})}")
          print(f"python_matrix_has_entries={'true' if include else 'false'}")
          PY

  build-and-smoke-python:
    name: Python Build + Smoke (${{ matrix.target }})
    if: ${{ github.actor != 'dependabot[bot]' && needs.changes.outputs.python_matrix_has_entries == 'true' }}
    needs: changes
    timeout-minutes: 180
    strategy:
      fail-fast: true
      matrix: ${{ fromJSON(needs.changes.outputs.python_matrix) }}
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Free disk space before setup
        if: matrix.os == 'ubuntu-latest'
        uses: ./.github/actions/free-disk-space-linux
        with:
          show-initial: "true"
          show-final: "true"

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: python-${{ matrix.platform }}
          target: ${{ matrix.target }}

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.13"
          cache-prefix: wheels-${{ matrix.platform }}

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Clean previous wheel artifacts
        shell: bash
        run: scripts/ci/python/clean-artifacts.sh

      - name: Build Rust FFI for Python (PyO3)
        uses: ./.github/actions/build-rust-ffi
        with:
          crate-name: kreuzberg-py
          features: extension-module
          build-profile: release
          verbose: true

      - name: Build Python wheels
        uses: PyO3/maturin-action@v1
        with:
          working-directory: packages/python
          command: build
          args: --release --strip --out ../../target/wheels
          manylinux: off

      - name: Upload wheels
        uses: actions/upload-artifact@v6
        with:
          name: wheels-${{ matrix.platform }}
          path: target/wheels/*.whl
          retention-days: 7

      - name: Cleanup Rust cache
        if: always()
        uses: ./.github/actions/cleanup-rust-cache

      - name: Smoke test wheel
        shell: bash
        run: scripts/ci/python/smoke-test-wheel.sh

  build-python-sdist:
    name: Python Sdist
    if: ${{ github.actor != 'dependabot[bot]' }}
    timeout-minutes: 180
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: python-sdist

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.13"
          cache-prefix: sdist

      - name: Build sdist
        run: scripts/ci/python/build-sdist.sh

      - name: Upload sdist
        uses: actions/upload-artifact@v6
        with:
          name: python-sdist
          path: target/wheels/*.tar.gz
          retention-days: 7

  test-python:
    name: Python Tests (${{ matrix.python-version }}, ${{ matrix.os }})
    if: ${{ github.actor != 'dependabot[bot]' }}
    needs: build-and-smoke-python
    timeout-minutes: 180
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: ubuntu-latest
            python-version: "3.10"
            platform: linux-x86_64
            coverage: true
          - os: macos-latest
            python-version: "3.10"
            platform: macos-arm64
            coverage: false
          - os: windows-latest
            python-version: "3.10"
            platform: windows-x86_64
            coverage: false
    runs-on: ${{ matrix.os }}
    env:
      HF_HOME: ~/.cache/huggingface
      TRANSFORMERS_CACHE: ~/.cache/huggingface
      RUSTC_WRAPPER: ''
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: ${{ matrix.python-version }}
          cache-prefix: test-py-${{ matrix.python-version }}

      - name: Cache HuggingFace models
        uses: actions/cache@v5
        with:
          path: ~/.cache/huggingface
          key: huggingface-${{ runner.os }}-py-${{ matrix.python-version }}-${{ hashFiles('packages/python/pyproject.toml') || 'fallback' }}

      - name: Download wheels
        uses: actions/download-artifact@v7
        with:
          name: wheels-${{ matrix.platform }}
          path: dist/

      - name: Install wheel
        shell: bash
        run: scripts/ci/python/install-wheel.sh

      - name: Install SpaCy model (Linux 3.10 only)
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.10'
        run: scripts/ci/python/install-spacy-model.sh

      - name: Run Python tests
        shell: bash
        run: scripts/ci/python/run-tests.sh "${{ matrix.coverage }}"

      - name: Upload coverage
        if: matrix.coverage
        uses: deepsourcelabs/test-coverage-action@master
        with:
          key: python
          coverage-file: packages/python/coverage.lcov
          dsn: ${{ secrets.DEEPSOURCE_DSN }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Run E2E tests
        shell: bash
        env:
          PYTEST_TIMEOUT: 300
        run: scripts/ci/python/run-e2e.sh
