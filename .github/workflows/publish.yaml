name: Publish Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to build (e.g., v4.0.0-rc.1)"
        required: true
        type: string
      dry_run:
        description: "Prepare artifacts without publishing"
        required: false
        type: boolean
        default: false
      ref:
        description: "Git ref (branch, tag, or commit) to build; defaults to the tag"
        required: false
        type: string
      targets:
        description: "Comma-separated list of release targets (all, python, node, ruby, cli, crates, docker, homebrew, java, csharp, elixir)"
        required: false
        type: string
      force_republish:
        description: "Force re-publish even if artifacts already exist"
        required: false
        type: boolean
        default: false
  release:
    types: [published]
  repository_dispatch:
    types: [publish-release]

concurrency:
  group: ${{ github.workflow }}-${{ (github.event_name == 'workflow_dispatch' && (github.event.inputs.ref || github.event.inputs.tag)) || github.ref || github.run_id }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  PDFIUM_VERSION: "7578"
  PDFIUM_STATIC_VERSION: "7442b"
  ORT_VERSION: "1.23.2"
  MACOSX_DEPLOYMENT_TARGET: "14.0"

permissions:
  contents: read

jobs:
  prepare:
    name: Prepare metadata
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}
      npm_tag: ${{ steps.meta.outputs.npm_tag }}
      ref: ${{ steps.meta.outputs.ref }}
      dry_run: ${{ steps.meta.outputs.dry_run }}
      force_republish: ${{ steps.meta.outputs.force_republish }}
      checkout_ref: ${{ steps.meta.outputs.checkout_ref }}
      target_sha: ${{ steps.meta.outputs.target_sha }}
      matrix_ref: ${{ steps.meta.outputs.matrix_ref }}
      is_tag: ${{ steps.meta.outputs.is_tag }}
      release_targets: ${{ steps.meta.outputs.release_targets }}
      release_any: ${{ steps.meta.outputs.release_any }}
      release_python: ${{ steps.meta.outputs.release_python }}
      release_node: ${{ steps.meta.outputs.release_node }}
      release_ruby: ${{ steps.meta.outputs.release_ruby }}
      release_cli: ${{ steps.meta.outputs.release_cli }}
      release_crates: ${{ steps.meta.outputs.release_crates }}
      release_rust: ${{ steps.meta.outputs.release_crates }}
      release_homebrew: ${{ steps.meta.outputs.release_homebrew }}
      release_java: ${{ steps.meta.outputs.release_java }}
      release_csharp: ${{ steps.meta.outputs.release_csharp }}
      release_go: ${{ steps.meta.outputs.release_go }}
      release_wasm: ${{ steps.meta.outputs.release_wasm }}
      release_php: ${{ steps.meta.outputs.release_php }}
      release_elixir: ${{ steps.meta.outputs.release_elixir }}
    steps:
      - name: Checkout code (default)
        uses: actions/checkout@v4

      - name: Validate tag and compute version
        id: meta
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          INPUT_TAG: ${{ inputs.tag }}
          INPUT_DRY_RUN: ${{ inputs.dry_run }}
          INPUT_REF: ${{ inputs.ref }}
          INPUT_TARGETS: ${{ inputs.targets }}
          INPUT_FORCE_REPUBLISH: ${{ inputs.force_republish }}
          EVENT_RELEASE_TAG: ${{ github.event.release.tag_name }}
          EVENT_DISPATCH_TAG: ${{ github.event.client_payload.tag }}
          EVENT_DISPATCH_DRY_RUN: ${{ github.event.client_payload.dry_run }}
          EVENT_DISPATCH_REF: ${{ github.event.client_payload.ref }}
          EVENT_DISPATCH_TARGETS: ${{ github.event.client_payload.targets }}
        run: scripts/publish/prepare-release-metadata.sh

      - name: Re-checkout at target ref
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.meta.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Upload release metadata
        uses: actions/upload-artifact@v4
        with:
          name: release-metadata
          path: release-metadata.json
          retention-days: 14

  validate-versions:
    name: Validate version consistency
    needs: [prepare]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}

      - name: Check version consistency
        shell: bash
        env:
          EXPECTED_VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/validate-version-consistency.sh


  check-pypi:
    name: Check if Python package exists on PyPI
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_python == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check PyPI for existing version
        id: check
        run: scripts/publish/check_pypi.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-npm:
    name: Check if Node package exists on npm
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_node == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check npm for existing version
        id: check
        run: scripts/publish/check_npm.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-wasm:
    name: Check if WASM package exists on npm
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_wasm == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check npm for existing WASM package
        id: check
        run: scripts/publish/check_wasm.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-rubygems:
    name: Check if Ruby gem exists on RubyGems
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_ruby == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check RubyGems for existing version
        id: check
        run: scripts/publish/check_rubygems.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-maven:
    name: Check if Java package exists on Maven Central
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_java == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Maven Central for existing version
        id: check
        run: scripts/publish/check_maven.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-nuget:
    name: Check if C# package exists on NuGet
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_csharp == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check NuGet for existing version
        id: check
        run: scripts/publish/check_nuget.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-packagist:
    name: Check if PHP package exists on Packagist
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_php == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Packagist for existing version
        id: check
        run: scripts/publish/check_packagist.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-cli:
    name: Check if CLI assets already published
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_cli == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Check release for existing CLI assets
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.tag }}
          ASSET_PREFIX: kreuzberg-cli-
          REQUIRED_ASSETS: |
            kreuzberg-cli-aarch64-apple-darwin.tar.gz
            kreuzberg-cli-aarch64-unknown-linux-gnu.tar.gz
            kreuzberg-cli-x86_64-unknown-linux-gnu.tar.gz
            kreuzberg-cli-x86_64-pc-windows-msvc.zip
          SUMMARY_LABEL: CLI
        run: scripts/publish/check-release-assets.sh

  check-go:
    name: Check if Go FFI assets already published
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_go == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Check release for existing Go artifacts
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.tag }}
          ASSET_PREFIX: go-ffi-
          REQUIRED_ASSETS: |
            go-ffi-linux-aarch64.tar.gz
            go-ffi-linux-x86_64.tar.gz
            go-ffi-macos-arm64.tar.gz
            go-ffi-windows-x86_64.tar.gz
          SUMMARY_LABEL: Go FFI
        run: scripts/publish/check-release-assets.sh

  check-cratesio:
    name: Check if Rust crate exists on crates.io
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      kreuzberg_exists: ${{ steps.check.outputs.kreuzberg_exists }}
      tesseract_exists: ${{ steps.check.outputs.tesseract_exists }}
      cli_exists: ${{ steps.check.outputs.cli_exists }}
      exists: ${{ steps.check.outputs.all_exist }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check crates.io for existing versions
        id: check
        run: scripts/publish/check_cratesio.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-hex:
    name: Check if Elixir package exists on Hex.pm
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_elixir == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Hex.pm for existing version
        id: check
        run: scripts/publish/check_hex.sh "${{ needs.prepare.outputs.version }}" >> "$GITHUB_OUTPUT"

  check-homebrew:
    name: Check if Homebrew formula already published
    needs: prepare
    if: ${{ needs.prepare.outputs.is_tag == 'true' && needs.prepare.outputs.release_homebrew == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Check Homebrew tap for formula
        id: check
        timeout-minutes: 3
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          # Check if Formula/kreuzberg.rb exists and contains the current version
          # Use timeout to prevent hanging on API issues
          if timeout 120s gh api repos/kreuzberg-dev/homebrew-tap/contents/Formula/kreuzberg.rb --jq '.content' 2>/dev/null | base64 -d | grep -q "version \"${VERSION}\""; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Homebrew formula already contains version ${VERSION}" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "Homebrew formula does not contain version ${VERSION} or check failed" >> "$GITHUB_STEP_SUMMARY"
          fi

  python-wheels:
    name: Build Python wheels (${{ matrix.label }})
    needs: [prepare, check-pypi]
    if: ${{ needs.prepare.outputs.release_python == 'true' && needs.check-pypi.outputs.exists != 'true' }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: linux-x86_64
            os: ubuntu-latest
            artifact: python-wheels-linux-x86_64
            rust_target: x86_64-unknown-linux-gnu
            manylinux: auto
            python: "3.10"
            needs_qemu: false
            extra_args: ""
          - label: linux-aarch64
            os: ubuntu-24.04-arm
            artifact: python-wheels-linux-aarch64
            rust_target: aarch64-unknown-linux-gnu
            manylinux: auto
            python: "3.10"
            needs_qemu: false
            extra_args: ""
          - label: macos-arm64
            os: macos-latest
            artifact: python-wheels-macos
            needs_qemu: false
            python: "3.10"
            rust_target: aarch64-apple-darwin
            manylinux: ""
            extra_args: ""
          - label: windows-amd64
            os: windows-latest
            artifact: python-wheels-windows
            needs_qemu: false
            python: "3.10"
            rust_target: x86_64-pc-windows-msvc
            manylinux: ""
            extra_args: ""
    steps:
      - name: Maximize build disk space
        if: matrix.os == 'ubuntu-latest'
        uses: easimon/maximize-build-space@v10
        with:
          root-reserve-mb: 2048
          temp-reserve-mb: 512
          swap-size-mb: 4096
          overprovision-lvm: 'true'
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Setup QEMU
        if: matrix.os == 'ubuntu-latest' && matrix.needs_qemu
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: Cache Pdfium binaries
        if: matrix.os == 'ubuntu-latest' && matrix.needs_qemu
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download Pdfium runtime
        if: matrix.os == 'ubuntu-latest' && matrix.needs_qemu
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Free up disk space for wheel build
        if: matrix.os == 'ubuntu-latest'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Prepare wheel staging directory
        shell: bash
        run: mkdir -p target/wheels

      - name: Setup Tesseract cache
        id: tesseract-cache
        uses: ./.github/actions/setup-tesseract-cache
        with:
          label: ${{ matrix.label }}
          enable-cache: "true"
          rust-target: ${{ matrix.rust_target }}

      - name: Setup Rust toolchain (native)
        if: matrix.os != 'ubuntu-latest'
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: python-wheels-${{ matrix.label }}
          target: ${{ matrix.rust_target }}

      - name: Clean previous wheel artifacts
        shell: bash
        run: rm -rf target/wheels target/maturin && rm -f packages/python/kreuzberg/_internal_bindings.*

      - name: Build Linux wheels
        if: matrix.os == 'ubuntu-latest'
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release -i python${{ matrix.python }} --out ../../target/wheels ${{ matrix.extra_args }}
          target: ${{ matrix.rust_target }}
          manylinux: ${{ matrix.manylinux }}
          working-directory: packages/python
          sccache: true
          docker-options: ${{ steps.tesseract-cache.outputs.docker-options }}
          before-script-linux: |
            set -euo pipefail
            rustup component add rust-src || true
            # Ensure OpenSSL is available inside the manylinux container for openssl-sys
            if command -v yum >/dev/null 2>&1; then
              yum install -y openssl openssl-devel pkgconfig
              export OPENSSL_LIB_DIR=/usr/lib64
              export OPENSSL_INCLUDE_DIR=/usr/include
            elif command -v apt-get >/dev/null 2>&1; then
              apt-get update && apt-get install -y pkg-config libssl-dev
              export OPENSSL_LIB_DIR=/usr/lib/x86_64-linux-gnu
              export OPENSSL_INCLUDE_DIR=/usr/include
            fi

      - name: Build wheels (native)
        if: matrix.os != 'ubuntu-latest'
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release -i python${{ matrix.python }} --out ../../target/wheels ${{ matrix.extra_args }}
          target: ${{ matrix.rust_target }}
          working-directory: packages/python
          sccache: true

      - name: Setup Python (macOS)
        if: matrix.label == 'macos-arm64'
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install wheel CLI (macOS)
        if: matrix.label == 'macos-arm64'
        run: python3 -m pip install wheel

      - name: Codesign macOS wheel binaries
        if: matrix.label == 'macos-arm64'
        shell: bash
        env:
          MACOS_CODESIGN_IDENTITY: ${{ secrets.MACOS_CODESIGN_IDENTITY }}
        run: python3 scripts/sign_macos_wheels.py --wheel-dir target/wheels

      - name: List built wheels
        shell: bash
        run: ls -lh target/wheels

      - name: Setup Python for smoke test
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Smoke test wheel
        shell: bash
        run: scripts/ci/python/smoke-test-wheel.sh

      - name: Clean up Docker resources (Ubuntu)
        if: matrix.os == 'ubuntu-latest' && always()
        run: scripts/publish/ubuntu/docker-prune.sh

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: target/wheels/*.whl
          retention-days: 14

  python-sdist:
    name: Build Python sdist
    needs: [prepare, check-pypi]
    if: ${{ needs.prepare.outputs.release_python == 'true' && needs.check-pypi.outputs.exists != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
    steps:
      - name: Show initial disk space
        run: echo "=== Initial disk space ===" && df -h /

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Free up disk space
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.13"
          cache-prefix: python-sdist

      - name: Setup Rust toolchain
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-python-sdist

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Build sdist
        env:
          DIST_DIR: dist
        run: task python:build:sdist:publish

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: python-sdist
          path: dist/*.tar.gz
          retention-days: 14

  node-bindings:
    name: Build Node bindings (${{ matrix.settings.target }})
    needs: [prepare, check-npm]
    if: ${{ needs.prepare.outputs.release_node == 'true' && needs.check-npm.outputs.exists != 'true' }}
    runs-on: ${{ matrix.settings.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        settings:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            rust_target: ""
            use_cross: false
            use_napi_cross: false
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            rust_target: ""
            use_cross: false
            use_napi_cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            rust_target: ""
            use_cross: false
            use_napi_cross: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            rust_target: ""
            use_cross: false
            use_napi_cross: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Show initial disk space (Linux)
        if: runner.os == 'Linux'
        run: echo "=== Initial disk space ===" && df -h /

      - name: Free up disk space (Linux)
        if: runner.os == 'Linux'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: package.json

      - name: Setup Rust toolchain
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-node-${{ matrix.settings.target }}
          use-sccache: 'false'

      - name: Add Rust target
        if: ${{ matrix.settings.rust_target != '' }}
        run: rustup target add ${{ matrix.settings.rust_target }}

      - name: Install cross
        if: ${{ matrix.settings.use_cross }}
        run: cargo install cross --git https://github.com/cross-rs/cross --locked

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          check-latest: true

      - name: Prepare ONNX Runtime (macOS)
        if: startsWith(matrix.settings.target, 'aarch64-apple-darwin') || startsWith(matrix.settings.target, 'x86_64-apple-darwin')
        uses: ./.github/actions/setup-prebuilt-onnx
        with:
          target: ${{ matrix.settings.target }}

      - name: Install Node dependencies
        shell: bash
        run: scripts/publish/node/install-deps.sh

      - name: Clean npm directory
        if: runner.os != 'Windows'
        run: rm -rf crates/kreuzberg-node/npm

      - name: Clean npm directory (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: Remove-Item -Recurse -Force crates/kreuzberg-node\npm -ErrorAction SilentlyContinue

      - name: Create npm package structure
        run: pnpm --filter @kreuzberg/node exec napi create-npm-dirs

      - name: Build native module
        if: runner.os != 'Windows'
        env:
          TARGET: ${{ matrix.settings.target }}
          USE_CROSS: ${{ matrix.settings.use_cross }}
          USE_NAPI_CROSS: ${{ matrix.settings.use_napi_cross }}
        shell: bash
        run: scripts/publish/node/build-native-module.sh

      - name: Build native module (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          TARGET: ${{ matrix.settings.target }}
          USE_CROSS: ${{ matrix.settings.use_cross }}
          USE_NAPI_CROSS: ${{ matrix.settings.use_napi_cross }}
        run: ./scripts/publish/node/build-native-module.ps1

      - name: Build TypeScript wrapper
        shell: bash
        run: pnpm -C crates/kreuzberg-node build:ts

      - name: Package artifacts
        if: runner.os != 'Windows'
        env:
          TARGET: ${{ matrix.settings.target }}
        shell: bash
        run: scripts/publish/node/package-artifacts.sh

      - name: Package artifacts (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          TARGET: ${{ matrix.settings.target }}
        run: ./scripts/publish/node/package-artifacts.ps1

      - name: Generate and upload TypeScript definitions
        if: matrix.settings.target == 'x86_64-unknown-linux-gnu'
        run: scripts/publish/node/generate-typescript-defs.sh

      - name: Upload TypeScript definitions
        if: matrix.settings.target == 'x86_64-unknown-linux-gnu'
        uses: actions/upload-artifact@v4
        with:
          name: node-typescript-defs
          path: typescript-defs
          retention-days: 14

      - name: Upload Node artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-bindings-${{ matrix.settings.target }}
          path: node-bindings-${{ matrix.settings.target }}.tar.gz
          retention-days: 14

  cli-binaries:
    name: Build CLI binaries (${{ matrix.target }})
    needs: [prepare, check-cli]
    if: ${{ needs.prepare.outputs.release_cli == 'true' && (needs.check-cli.outputs.exists != 'true' || needs.prepare.outputs.dry_run == 'true') }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            use_cross: false
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            use_cross: false
          # TODO: Re-enable x86_64-unknown-linux-musl once build issues are resolved
          - os: macos-latest
            target: aarch64-apple-darwin
            use_cross: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            use_cross: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Show initial disk space (Linux)
        if: runner.os == 'Linux'
        run: echo "=== Initial disk space ===" && df -h /

      - name: Free up disk space (Linux)
        if: runner.os == 'Linux'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Setup Rust toolchain
        uses: ./.github/actions/setup-rust
        with:
          target: ${{ matrix.target }}
          cache-key-prefix: publish-cli-${{ matrix.target }}

      - name: Add compilation target
        run: rustup target add ${{ matrix.target }}

      - name: Install build dependencies
        if: runner.os == 'Linux'
        env:
          CLI_TARGET: ${{ matrix.target }}
        run: scripts/publish/cli/install-build-deps.sh

      - name: Configure cross linker
        if: ${{ matrix.target == 'aarch64-unknown-linux-gnu' }}
        run: scripts/publish/cli/configure-cross-linker.sh

      - name: Build CLI
        shell: bash
        run: scripts/build_cli_binary.sh ${{ matrix.target }}

      - name: Package CLI artifact
        if: runner.os != 'Windows'
        run: scripts/package_cli_artifact.sh ${{ matrix.target }}

      - name: Package CLI artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          CLI_TARGET: ${{ matrix.target }}
        run: ./scripts/publish/cli/package-cli-windows.ps1

      - name: Upload CLI artifact
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: cli-${{ matrix.target }}
          path: kreuzberg-cli-${{ matrix.target }}.tar.gz
          retention-days: 14

      - name: Upload CLI artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: cli-${{ matrix.target }}
          path: kreuzberg-cli-${{ matrix.target }}.zip
          retention-days: 14

  homebrew-bottles:
    name: Build Homebrew bottles (${{ matrix.bottle_tag }})
    needs: [prepare, check-homebrew]
    if: |
      needs.prepare.outputs.release_homebrew == 'true' &&
      needs.check-homebrew.outputs.exists != 'true'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-latest
            bottle_tag: arm64_sequoia
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Setup Rust toolchain
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-homebrew-${{ matrix.bottle_tag }}

      - name: Setup Homebrew
        run: |
          brew tap homebrew/core
          brew update

      - name: Extract version
        id: version
        env:
          TAG: ${{ needs.prepare.outputs.tag }}
        run: |
          VERSION="${TAG#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Build CLI for bottle
        run: |
          cargo build --release \
            -p kreuzberg-cli \
            --features api,mcp,mcp-http

      - name: Create Homebrew bottle
        id: bottle
        env:
          VERSION: ${{ steps.version.outputs.version }}
          TAG: ${{ needs.prepare.outputs.tag }}
        run: |
          # Prepare bottle directory structure
          bottle_dir="/tmp/kreuzberg-bottle"
          mkdir -p "${bottle_dir}/bin"

          # Copy the built binary
          cp target/release/kreuzberg "${bottle_dir}/bin/"

          # Create bottle tarball
          cd "${bottle_dir}"
          bottle_filename="kreuzberg-${VERSION}.${{ matrix.bottle_tag }}.bottle.tar.gz"
          tar -czf "${bottle_filename}" bin/

          # Calculate SHA256
          sha256=$(shasum -a 256 "${bottle_filename}" | cut -d' ' -f1)
          echo "sha256=${sha256}" >> "$GITHUB_OUTPUT"
          echo "filename=${bottle_filename}" >> "$GITHUB_OUTPUT"

          # Copy to workspace for artifact upload
          cp "${bottle_filename}" "${{ github.workspace }}/"
          echo "Bottle created: ${bottle_filename}"
          echo "SHA256: ${sha256}"

      - name: Upload bottle artifact
        uses: actions/upload-artifact@v4
        with:
          name: homebrew-bottle-${{ matrix.bottle_tag }}
          path: kreuzberg-${{ steps.version.outputs.version }}.${{ matrix.bottle_tag }}.bottle.tar.gz
          retention-days: 14

  upload-homebrew-bottles:
    name: Upload Homebrew bottles to GitHub Release
    needs: [prepare, check-homebrew, homebrew-bottles]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_homebrew == 'true' &&
      needs.check-homebrew.outputs.exists != 'true' &&
      needs.homebrew-bottles.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Ensure GitHub release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/ensure-github-release-exists.sh "${{ needs.prepare.outputs.tag }}"

      - name: Download bottle artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: homebrew-bottle-*
          path: dist/homebrew
          merge-multiple: true

      - name: Upload bottles (idempotent)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/upload-homebrew-bottles.sh "${{ needs.prepare.outputs.tag }}" dist/homebrew

  ruby-gem:
    name: Build Ruby gem (${{ matrix.label }})
    needs: [prepare, check-rubygems]
    if: ${{ needs.prepare.outputs.release_ruby == 'true' && needs.check-rubygems.outputs.exists != 'true' }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            label: linux
          - os: ubuntu-24.04-arm
            label: linux-arm64
          - os: macos-latest
            label: macos-arm64
          - os: windows-latest
            label: windows-x64
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    env:
      RB_SYS_CARGO_PROFILE: release
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Show initial disk space (Linux)
        if: runner.os == 'Linux'
        run: echo "=== Initial disk space ===" && df -h /

      - name: Free up disk space (Linux)
        if: runner.os == 'Linux'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Install MSYS2 toolchain
        if: runner.os == 'Windows'
        shell: pwsh
        run: ridk exec pacman -S --needed --noconfirm base-devel mingw-w64-ucrt-x86_64-toolchain

      - name: Install Rust (GNU on Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: rustup toolchain install stable-gnu --profile minimal --no-self-update

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler: none
          bundler-cache: false

      - name: Install Bundler
        shell: bash
        run: (gem install bundler -v 4.0.0 --no-document || gem install bundler --no-document) && bundler --version

      - name: Configure short paths for Windows MAX_PATH mitigation
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/publish/ruby/windows-configure-short-paths.ps1

      - name: Configure bindgen compatibility headers (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/publish/ruby/windows-configure-bindgen-headers.ps1

      - name: Cache Pdfium binaries
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download Pdfium runtime
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime artifacts
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: packages/ruby/ext/kreuzberg_rb/native
          additional-destinations: |
            packages/ruby
            packages/ruby/lib

      - name: Download ONNX Runtime (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/publish/ruby/windows-download-onnx-runtime.ps1

      - name: Install Ruby dependencies (Unix)
        if: runner.os != 'Windows'
        run: scripts/ci/ruby/install-ruby-deps.sh packages/ruby

      - name: Install Ruby dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: $workspace = ridk exec bash -lc "cygpath -au '$env:GITHUB_WORKSPACE'"; ridk exec bash -lc "cd $workspace && bash scripts/ci/ruby/install-ruby-deps.sh packages/ruby"

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Build kreuzberg-ffi (Unix)
        if: runner.os != 'Windows'
        run: task rust:ffi:build

      - name: Build kreuzberg-ffi (Windows)
        if: runner.os == 'Windows'
        run: task rust:ffi:build

      - name: Vendor kreuzberg core crate
        shell: bash
        run: scripts/ci/ruby/vendor-kreuzberg-core.sh

      - name: Ensure Ruby bundle config (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: packages/ruby
        run: |
          bundle config set --local path vendor/bundle
          bundle check || bundle install

      - name: Build gem (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: packages/ruby
        timeout-minutes: 60
        run: |
          echo "Starting gem build..."
          if ! bundle exec rake build 2>&1 | tee build.log; then
            echo "ERROR: Rake build command failed with exit code ${PIPESTATUS[0]}" >&2
            exit 1
          fi
          echo "Rake build command completed"

      - name: Verify gem was built
        if: runner.os != 'Windows'
        shell: bash
        working-directory: packages/ruby
        run: |
          if [ ! -d pkg ]; then
            echo "ERROR: pkg directory does not exist" >&2
            echo "Working directory contents:" >&2
            ls -la >&2
            exit 1
          fi
          if [ -z "$(ls -A pkg/*.gem 2>/dev/null)" ]; then
            echo "ERROR: No .gem files found in pkg directory" >&2
            echo "pkg directory contents:" >&2
            ls -la pkg/ >&2
            echo "" >&2
            echo "Last 50 lines of build log:" >&2
            tail -50 build.log >&2 || echo "build.log not found" >&2
            exit 1
          fi
          echo "✓ Gem built successfully:"
          ls -lh pkg/*.gem

      - name: Check disk space after build (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          echo "=== Disk space after gem build ===" >&2
          df -h / >&2
          if [ -d packages/ruby/pkg ]; then
            du -sh packages/ruby/pkg/ >&2
            ls -lah packages/ruby/pkg/ >&2
          else
            echo "WARNING: packages/ruby/pkg/ directory does not exist" >&2
          fi

      - name: Ensure Ruby bundle config (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $workspace = ridk exec bash -lc "cygpath -au '$env:GITHUB_WORKSPACE'"
          $gemdir = "$workspace/packages/ruby"
          ridk exec bash -lc "cd $gemdir && bundle config set --local path vendor/bundle"
          ridk exec bash -lc "cd $gemdir && bundle check || bundle install"

      - name: Build gem (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/publish/ruby/build-gem-windows.ps1

      - name: Check disk space after build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Write-Host "=== Disk space after gem build ===" -ForegroundColor Yellow
          Get-Volume | Select-Object DriveLetter, Size, SizeRemaining | Format-Table

      - name: Verify gem integrity (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd packages/ruby/pkg
          echo "=== Gem Files in pkg directory ==="
          ls -lah
          echo ""
          for gem in *.gem; do
            if [ ! -f "$gem" ]; then
              echo "No gem file found" >&2
              exit 1
            fi
            echo "=========================================="
            echo "Validating: $gem"
            echo "=========================================="
            file "$gem"
            echo "File size: $(stat -f%z "$gem" 2>/dev/null || stat -c%s "$gem") bytes"
            echo ""
            echo "Primary validation: gem spec check..."
            if ! gem spec "$gem" >/dev/null 2>&1; then
              echo "WARNING: gem spec check failed, attempting tar validation..."
              echo "Trying tar -tzf (gzipped)..."
              if tar -tzf "$gem" >/dev/null 2>&1; then
                echo "✓ Gzipped tar verified"
              else
                echo "Trying tar -tf (uncompressed)..."
                if tar -tf "$gem" >/dev/null 2>&1; then
                  echo "✓ Uncompressed tar verified"
                else
                  echo "ERROR: Corrupted gem file: $gem" >&2
                  echo "First 100 bytes (hex):"
                  xxd -l 100 "$gem" || od -t x1 -N 100 "$gem"
                  exit 1
                fi
              fi
            else
              echo "✓ gem spec validation passed"
            fi
            echo ""
          done

      - name: Verify gem integrity (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cd packages/ruby/pkg
          Get-ChildItem *.gem | ForEach-Object {
            $gemPath = $_.FullName
            Write-Host "Validating $($_.Name)..."
            $gemSpec = & gem spec $gemPath 2>&1
            if ($LASTEXITCODE -ne 0) {
              Write-Warning "gem spec check failed for $($_.Name), attempting tar validation..."
              # Windows doesn't have tar by default, just check file size
              if ($_.Length -le 100KB) {
                Write-Error "Gem file appears to be corrupted or too small: $($_.Name)" -ErrorAction Stop
              }
            }
            Write-Host "Size: $($_.Length) bytes"
          }

      - name: Upload gem artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rubygems-${{ matrix.label }}
          path: packages/ruby/pkg/*.gem
          retention-days: 14
          compression-level: 0

  go-ffi-libraries:
    name: Build Go FFI libraries (${{ matrix.settings.label }})
    needs: [prepare, check-go]
    if: ${{ needs.prepare.outputs.release_go == 'true' && (needs.check-go.outputs.exists != 'true' || needs.prepare.outputs.dry_run == 'true') }}
    runs-on: ${{ matrix.settings.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        settings:
          - os: ubuntu-24.04-arm
            label: linux-aarch64
            target: aarch64-unknown-linux-gnu
            build_features: default
          - os: ubuntu-latest
            label: linux-x86_64
            target: x86_64-unknown-linux-gnu
            build_features: default
          - os: macos-latest
            label: macos-arm64
            target: aarch64-apple-darwin
            build_features: default
          - os: windows-latest
            label: windows-x86_64
            target: x86_64-pc-windows-gnu
            build_features: core
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Free up disk space (Linux)
        if: runner.os == 'Linux'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Configure Windows GNU toolchain
        if: matrix.settings.os == 'windows-latest'
        shell: bash
        run: scripts/publish/go/configure-windows-gnu-toolchain.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: ${{ matrix.settings.os == 'windows-latest' && 'publish-go-windows-gnu' || format('publish-go-{0}', matrix.settings.os) }}
          use-sccache: ${{ matrix.settings.os != 'windows-latest' }}
          target: ${{ matrix.settings.target }}
          toolchain: ${{ matrix.settings.os == 'windows-latest' && 'stable-x86_64-pc-windows-gnu' || '' }}

      - name: Set GNU as default (Windows)
        if: matrix.settings.os == 'windows-latest'
        shell: pwsh
        run: rustup default stable-x86_64-pc-windows-gnu

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime (Unix only)
        if: matrix.settings.build_features == 'default'
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup MSYS2 (Windows)
        if: matrix.settings.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-pkg-config
            mingw-w64-ucrt-x86_64-nasm
            mingw-w64-ucrt-x86_64-zstd
            mingw-w64-ucrt-x86_64-binutils

      - name: Verify MSYS2 toolchain and add to PATH (Windows)
        if: matrix.settings.os == 'windows-latest'
        shell: pwsh
        run: scripts/ci/go/setup-msys2-windows.ps1

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Build FFI library (Windows)
        if: matrix.settings.os == 'windows-latest'
        shell: bash
        run: task rust:ffi:build

      - name: Build FFI library (Unix)
        if: matrix.settings.os != 'windows-latest'
        shell: bash
        run: task rust:ffi:build

      - name: Create artifact staging directory
        shell: bash
        run: scripts/publish/go/create-artifact-staging-dir.sh

      - name: Stage FFI artifacts (Unix)
        if: matrix.settings.os != 'windows-latest'
        shell: bash
        run: scripts/ci/go/stage-ffi-artifacts.sh artifact-staging/kreuzberg-ffi "${{ matrix.settings.build_features }}"

      - name: Stage FFI artifacts (Windows)
        if: matrix.settings.os == 'windows-latest'
        shell: pwsh
        run: scripts/ci/go/stage-ffi-artifacts-windows.ps1 "artifact-staging\kreuzberg-ffi"

      - name: Generate installation README
        shell: bash
        run: scripts/ci/go/generate-install-readme.sh artifact-staging/kreuzberg-ffi/README.md

      - name: Package FFI library
        shell: bash
        run: tar -czf "go-ffi-${{ matrix.settings.label }}.tar.gz" -C artifact-staging kreuzberg-ffi/

      - name: Verify artifact completeness
        shell: bash
        run: scripts/ci/go/verify-ffi-artifact.sh "go-ffi-${{ matrix.settings.label }}.tar.gz"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: go-ffi-${{ matrix.settings.label }}
          path: go-ffi-${{ matrix.settings.label }}.tar.gz
          retention-days: 14

  java-natives:
    name: Build Java native libs (${{ matrix.settings.label }})
    needs: [prepare, check-maven]
    if: ${{ needs.prepare.outputs.release_java == 'true' && (needs.check-maven.outputs.exists != 'true' || needs.prepare.outputs.dry_run == 'true') }}
    runs-on: ${{ matrix.settings.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        settings:
          - os: ubuntu-24.04-arm
            label: linux-arm64
            rid: linux-arm64
          - os: ubuntu-latest
            label: linux-x86_64
            rid: linux-x86_64
          - os: macos-latest
            label: macos-arm64
            rid: macos-arm64
          - os: windows-latest
            label: windows-x86_64
            rid: windows-x86_64
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Tesseract cache
        id: tesseract-cache
        uses: ./.github/actions/setup-tesseract-cache
        with:
          label: ${{ matrix.settings.label }}
          enable-cache: "true"

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-java-natives-${{ matrix.settings.label }}
          use-sccache: ${{ runner.os != 'Windows' }}

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium into target/release
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime (embeddings)
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}
          dest-dir: target/release

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Build FFI library
        shell: bash
        run: task rust:ffi:build

      - name: Fix macOS dylib references
        shell: bash
        run: scripts/ci/java/fix-macos-dylib-references.sh

      - name: Collect native artifacts
        shell: bash
        run: scripts/ci/java/collect-native-artifacts.sh "${{ matrix.settings.rid }}" "java-natives/${{ matrix.settings.rid }}"

      - name: Upload native artifact
        uses: actions/upload-artifact@v4
        with:
          name: java-natives-${{ matrix.settings.label }}
          path: java-natives/${{ matrix.settings.rid }}
          retention-days: 14

  java-package:
    name: Build Java package
    needs: [prepare, check-maven, java-natives]
    if: ${{ needs.prepare.outputs.release_java == 'true' && needs.check-maven.outputs.exists != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      PDFIUM_VERSION: "7529"
    steps:
      - name: Show initial disk space
        run: echo "=== Initial disk space ===" && df -h /

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Free up disk space
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-java

      - name: Setup Java
        uses: actions/setup-java@v4
        id: setup-java
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'maven'

      - name: Setup Maven 3.9.11
        uses: ./.github/actions/setup-maven-4

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Download Java native artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: java-natives-*
          path: artifacts/java-natives
          merge-multiple: false

      - name: Stage Java native resources
        shell: bash
        run: scripts/ci/java/stage-native-resources.sh "artifacts/java-natives" "packages/java/src/main/resources/natives"

      - name: Verify bundled native resources
        shell: bash
        run: |
          set -euo pipefail
          test -f packages/java/src/main/resources/natives/linux-x86_64/libkreuzberg_ffi.so
          test -f packages/java/src/main/resources/natives/linux-arm64/libkreuzberg_ffi.so
          test -f packages/java/src/main/resources/natives/macos-arm64/libkreuzberg_ffi.dylib
          test -f packages/java/src/main/resources/natives/windows-x86_64/kreuzberg_ffi.dll

      - name: Build and package
        working-directory: packages/java
        run: task java:build:release

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: maven-packages
          path: packages/java/target/*.jar
          retention-days: 14

  php-package:
    name: Build PHP PIE package (${{ matrix.settings.label }})
    needs: [prepare, check-packagist]
    if: ${{ needs.prepare.outputs.release_php == 'true' && needs.check-packagist.outputs.exists != 'true' }}
    runs-on: ${{ matrix.settings.os }}
    timeout-minutes: 60
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        settings:
          - os: ubuntu-latest
            label: linux-x86_64
            platform: linux-x86_64
          - os: ubuntu-24.04-arm
            label: linux-arm64
            platform: linux-arm64
          - os: macos-latest
            label: macos-arm64
            platform: macos-arm64
          - os: windows-latest
            label: windows-x86_64
            platform: windows-x86_64
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Free up disk space (Linux)
        if: runner.os == 'Linux'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Tesseract cache
        id: tesseract-cache
        uses: ./.github/actions/setup-tesseract-cache
        with:
          label: ${{ matrix.settings.label }}
          enable-cache: "true"

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-php-${{ matrix.settings.label }}
          use-sccache: ${{ runner.os != 'Windows' }}

      - name: Detect partial cache hit and clean stale fingerprints
        shell: bash
        run: |
          # Check if cache was a partial hit (restored from fallback key)
          # For now, we'll do a more conservative check: always clean before build
          # since the current Cargo.lock changes frequently
          echo "Preparing clean Rust build environment..."
          cargo clean --release -p kreuzberg-php 2>/dev/null || true
          cargo clean --release -p kreuzberg 2>/dev/null || true

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium into target/release
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}
          dest-dir: target/release

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml
          coverage: none

      - name: Build PHP extension (kreuzberg-php)
        shell: bash
        run: task php:build

      - name: Build PIE package
        shell: bash
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/php/build-pie-package.sh "${{ matrix.settings.platform }}" "dist/php"

      - name: Upload PHP PIE package artifact
        uses: actions/upload-artifact@v4
        with:
          name: php-pie-${{ matrix.settings.label }}
          path: dist/php/kreuzberg-*.tar.gz*
          retention-days: 14

  csharp-package:
    name: Build C# package
    needs: [prepare, check-nuget, csharp-native-assets]
    if: ${{ needs.prepare.outputs.release_csharp == 'true' && needs.check-nuget.outputs.exists != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Show initial disk space
        run: echo "=== Initial disk space ===" && df -h /

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Free up disk space
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-csharp

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Ensure dotnet is available
        run: scripts/publish/csharp/ensure-dotnet.sh

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Download native assets
        uses: actions/download-artifact@v4
        with:
          pattern: csharp-native-assets-*
          path: packages/csharp/Kreuzberg/runtimes
          merge-multiple: true

      - name: Verify native assets were downloaded
        shell: bash
        run: |
          echo "=== Downloaded native assets ===" >&2
          find packages/csharp/Kreuzberg -type f \( -name '*.so' -o -name '*.dylib' -o -name '*.dll' \) 2>/dev/null | head -20 || true
          if [ -z "$(find packages/csharp/Kreuzberg -type f \( -name '*.so' -o -name '*.dylib' -o -name '*.dll' \) 2>/dev/null)" ]; then
            echo "ERROR: No native assets downloaded!" >&2
            echo "Directory structure of packages/csharp/Kreuzberg:" >&2
            find packages/csharp/Kreuzberg -type f | head -30 || echo "  (empty)"
            exit 1
          fi

      - name: Build C# bindings
        run: task csharp:build:release

      - name: Pack NuGet package
        run: task csharp:package -- -p:PackageVersion=${{ needs.prepare.outputs.version }} -o artifacts/csharp

      - name: Verify package contains native assets
        shell: bash
        run: scripts/publish/csharp/verify-nuget-native-assets.sh

      - name: Upload NuGet artifact
        uses: actions/upload-artifact@v4
        with:
          name: csharp-nuget
          path: packages/csharp/artifacts/csharp/*.nupkg
          retention-days: 14
          if-no-files-found: error

  csharp-native-assets:
    name: Build C# native assets (${{ matrix.rid }})
    needs: [prepare, check-nuget]
    if: ${{ needs.prepare.outputs.release_csharp == 'true' && needs.check-nuget.outputs.exists != 'true' }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - rid: linux-arm64
            os: ubuntu-24.04-arm
            rust_target: aarch64-unknown-linux-gnu
            ort_arch: arm64
            pdfium_platform: linux
            pdfium_arch: arm64
            ffi_name: libkreuzberg_ffi.so
          - rid: linux-x64
            os: ubuntu-latest
            rust_target: x86_64-unknown-linux-gnu
            ort_arch: x64
            pdfium_platform: linux
            pdfium_arch: x64
            ffi_name: libkreuzberg_ffi.so
          - rid: osx-arm64
            os: macos-latest
            rust_target: aarch64-apple-darwin
            ort_arch: arm64
            pdfium_platform: mac
            pdfium_arch: arm64
            ffi_name: libkreuzberg_ffi.dylib
          - rid: win-x64
            os: windows-latest
            rust_target: x86_64-pc-windows-msvc
            ort_arch: x64
            pdfium_platform: win
            pdfium_arch: x64
            ffi_name: kreuzberg_ffi.dll
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-csharp-native-${{ matrix.rid }}

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}
          platform-id: ${{ matrix.pdfium_platform }}
          arch-id: ${{ matrix.pdfium_arch }}


      - name: Stage PDFium runtime
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/csharp-native/${{ matrix.rid }}

      - name: Add Rust target
        shell: bash
        run: rustup target add "${{ matrix.rust_target }}"

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Build FFI library
        shell: bash
        run: task rust:ffi:build -- --target "${{ matrix.rust_target }}"

      - name: Assemble native assets for NuGet
        shell: bash
        env:
          RID: ${{ matrix.rid }}
          RUST_TARGET: ${{ matrix.rust_target }}
          FFI_NAME: ${{ matrix.ffi_name }}
        run: scripts/publish/csharp/assemble-native-assets.sh

      - name: Verify native assets exist
        shell: bash
        env:
          RID: ${{ matrix.rid }}
        run: scripts/publish/csharp/verify-native-assets-dir.sh

      - name: Upload native assets artifact
        uses: actions/upload-artifact@v4
        with:
          name: csharp-native-assets-${{ matrix.rid }}
          path: runtimes/
          retention-days: 14

  upload-cli-release:
    name: Upload CLI binaries to GitHub Release
    needs: [prepare, validate-versions, check-cli, cli-binaries]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_cli == 'true' &&
      needs.check-cli.outputs.exists != 'true' &&
      needs.cli-binaries.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Ensure GitHub release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/ensure-github-release-exists.sh "${{ needs.prepare.outputs.tag }}"

      - name: Download CLI artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: cli-*
          path: dist/cli
          merge-multiple: true

      - name: Upload CLI binaries (idempotent)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/upload-cli-binaries.sh "${{ needs.prepare.outputs.tag }}" dist/cli

  upload-go-release:
    name: Upload Go FFI libraries to GitHub Release
    needs: [prepare, validate-versions, check-go, go-ffi-libraries]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_go == 'true' &&
      needs.check-go.outputs.exists != 'true' &&
      needs.go-ffi-libraries.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Ensure GitHub release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/ensure-github-release-exists.sh "${{ needs.prepare.outputs.tag }}"

      - name: Download Go FFI artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: go-ffi-*
          path: dist/go
          merge-multiple: true

      - name: Upload Go FFI libraries
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/upload-go-libraries.sh "${{ needs.prepare.outputs.tag }}" dist/go

  publish-crates:
    name: Publish crates.io packages
    needs: [prepare, validate-versions, check-cratesio]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_rust == 'true' &&
      needs.check-cratesio.result == 'success' &&
      needs.check-cratesio.outputs.exists != 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Setup Rust toolchain
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-crates

      - name: Request OIDC token for crates.io
        id: oidc
        uses: rust-lang/crates-io-auth-action@v1

      - name: Verify Cargo.toml version matches tag
        run: scripts/publish/verify-cargo-version.sh "${{ needs.prepare.outputs.version }}"

      - name: Check crates.io for existing release
        id: crate_check
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/check_cratesio.sh "${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Publish kreuzberg-tesseract
        if: ${{ steps.crate_check.outputs.tesseract_exists != 'true' && needs.prepare.outputs.dry_run != 'true' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.oidc.outputs.token }}
        run: scripts/publish/publish-cargo-crate.sh kreuzberg-tesseract

      - name: Wait for tesseract indexing
        if: ${{ steps.crate_check.outputs.tesseract_exists != 'true' && needs.prepare.outputs.dry_run != 'true' }}
        run: scripts/publish/wait-for-package.sh cratesio "kreuzberg-tesseract" "${{ needs.prepare.outputs.version }}" 10

      - name: Publish kreuzberg
        if: ${{ steps.crate_check.outputs.core_exists != 'true' && needs.prepare.outputs.dry_run != 'true' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.oidc.outputs.token }}
        run: scripts/publish/publish-cargo-crate.sh kreuzberg

      - name: Wait for indexing
        if: ${{ steps.crate_check.outputs.core_exists != 'true' && needs.prepare.outputs.dry_run != 'true' }}
        run: scripts/publish/wait-for-package.sh cratesio "kreuzberg" "${{ needs.prepare.outputs.version }}" 10

      - name: Publish kreuzberg-cli
        if: ${{ steps.crate_check.outputs.cli_exists != 'true' && needs.prepare.outputs.dry_run != 'true' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.oidc.outputs.token }}
        run: scripts/publish/publish-cargo-crate.sh kreuzberg-cli

      - name: Crates dry-run summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        run: echo "Dry run requested; cargo publish skipped." >> "$GITHUB_STEP_SUMMARY"

  elixir-natives:
    name: Build Elixir native libs (${{ matrix.settings.label }})
    needs: [prepare, check-hex]
    if: ${{ needs.prepare.outputs.release_elixir == 'true' && (needs.check-hex.outputs.exists != 'true' || needs.prepare.outputs.dry_run == 'true') }}
    runs-on: ${{ matrix.settings.os }}
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        settings:
          - os: ubuntu-24.04-arm
            label: linux-aarch64
            target: aarch64-unknown-linux-gnu
          - os: ubuntu-latest
            label: linux-x86_64
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            label: macos-arm64
            target: aarch64-apple-darwin
          - os: windows-latest
            label: windows-x86_64
            target: x86_64-pc-windows-gnu
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      - name: Free up disk space (Linux)
        if: runner.os == 'Linux'
        run: scripts/publish/ubuntu/free-disk-space.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Tesseract cache
        id: tesseract-cache
        uses: ./.github/actions/setup-tesseract-cache
        with:
          label: ${{ matrix.settings.label }}
          enable-cache: "true"

      - name: Setup Rust
        uses: ./.github/actions/setup-rust

      - name: Restore Cargo cache
        uses: ./.github/actions/restore-cargo-cache
        with:
          shared-key: elixir-${{ matrix.settings.label }}
          prefix: elixir

      - name: Build Elixir native library
        working-directory: packages/elixir/native/kreuzberg_rustler
        env:
          CARGO_BUILD_TARGET: ${{ matrix.settings.target }}
        run: cargo build --release --target ${{ matrix.settings.target }}

      - name: Package native libraries (NIF 2.16)
        shell: bash
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          TARGET="${{ matrix.settings.target }}"
          NIF_VERSION="2.16"

          if [[ "${{ runner.os }}" == "Windows" ]]; then
            EXT="dll"
            LIB_NAME="kreuzberg_rustler.dll"
          else
            EXT="so"
            LIB_NAME="libkreuzberg_rustler.so"
          fi

          mkdir -p dist/elixir
          ARTIFACT_NAME="libkreuzberg_rustler-v${VERSION}-nif-${NIF_VERSION}-${TARGET}.${EXT}"
          TARBALL="${ARTIFACT_NAME}.tar.gz"

          # Copy the built library
          cp "packages/elixir/native/kreuzberg_rustler/target/${TARGET}/release/${LIB_NAME}" "${ARTIFACT_NAME}"

          # Create tarball
          tar -czf "dist/elixir/${TARBALL}" "${ARTIFACT_NAME}"
          echo "Created: dist/elixir/${TARBALL}"

      - name: Package native libraries (NIF 2.17)
        shell: bash
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          TARGET="${{ matrix.settings.target }}"
          NIF_VERSION="2.17"

          if [[ "${{ runner.os }}" == "Windows" ]]; then
            EXT="dll"
            LIB_NAME="kreuzberg_rustler.dll"
          else
            EXT="so"
            LIB_NAME="libkreuzberg_rustler.so"
          fi

          ARTIFACT_NAME="libkreuzberg_rustler-v${VERSION}-nif-${NIF_VERSION}-${TARGET}.${EXT}"
          TARBALL="${ARTIFACT_NAME}.tar.gz"

          # Copy the built library (same binary works for both NIF versions)
          cp "packages/elixir/native/kreuzberg_rustler/target/${TARGET}/release/${LIB_NAME}" "${ARTIFACT_NAME}"

          # Create tarball
          tar -czf "dist/elixir/${TARBALL}" "${ARTIFACT_NAME}"
          echo "Created: dist/elixir/${TARBALL}"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: elixir-${{ matrix.settings.label }}
          path: dist/elixir/*.tar.gz
          if-no-files-found: error
          retention-days: 1

  upload-elixir-release:
    name: Upload Elixir binaries to GitHub Release
    needs: [prepare, validate-versions, check-hex, elixir-natives]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_elixir == 'true' &&
      needs.check-hex.outputs.exists != 'true' &&
      needs.elixir-natives.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Ensure GitHub release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/ensure-github-release-exists.sh "${{ needs.prepare.outputs.tag }}"

      - name: Download Elixir artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: elixir-*
          path: dist/elixir
          merge-multiple: true

      - name: Upload binaries to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ needs.prepare.outputs.tag }}"
          for file in dist/elixir/*.tar.gz; do
            echo "Uploading $(basename "$file")..."
            gh release upload "$TAG" "$file" --clobber
          done

  publish-hex:
    name: Publish Elixir package to Hex.pm
    needs: [prepare, validate-versions, check-hex, publish-crates, upload-elixir-release]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_elixir == 'true' &&
      needs.check-hex.outputs.exists != 'true'
    runs-on: ubuntu-latest
    environment: hex
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}
          fetch-depth: 0

      - name: Setup Elixir
        uses: erlef/setup-elixir@v1
        with:
          elixir-version: "1.17"
          otp-version: "26"

      - name: Install Elixir dependencies
        working-directory: packages/elixir
        run: mix deps.get

      - name: Build documentation
        working-directory: packages/elixir
        env:
          RUSTLER_PRECOMPILATION_EXAMPLE_BUILD: "true"
          RUSTLER_PRECOMPILED_FORCE_BUILD_ALL: "true"
        run: mix docs

      - name: Publish to Hex.pm
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        working-directory: packages/elixir
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
        run: mix hex.publish --yes --replace

      - name: Hex.pm dry-run summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        run: echo "Dry run requested; Hex.pm publish skipped." >> "$GITHUB_STEP_SUMMARY"

  publish-pypi:
    name: Publish Python packages to PyPI
    needs: [prepare, validate-versions, check-pypi, python-wheels, python-sdist]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_python == 'true' &&
      needs.check-pypi.outputs.exists != 'true' &&
      needs.python-wheels.result == 'success' &&
      needs.python-sdist.result == 'success'
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download wheel artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: python-wheels-*
          path: dist
          merge-multiple: true

      - name: Download sdist artifact
        uses: actions/download-artifact@v4
        with:
          name: python-sdist
          path: dist

      - name: List artifacts
        run: ls -R dist

      - name: Publish to PyPI
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          skip-existing: true

      - name: Dry run summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        run: (echo "Dry run requested; artifacts staged for PyPI:" && ls -1 dist) >> "$GITHUB_STEP_SUMMARY"

  publish-rubygems:
    name: Publish Ruby gems
    needs: [prepare, validate-versions, check-rubygems, ruby-gem]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_ruby == 'true' &&
      needs.check-rubygems.outputs.exists != 'true' &&
      needs.ruby-gem.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}
          fetch-depth: 0
          submodules: recursive

      - name: Download Ruby gem artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: rubygems-*
          path: dist
          merge-multiple: true

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler: none
          bundler-cache: false

      - name: Install Bundler
        shell: bash
        run: (gem install bundler -v 4.0.0 --no-document || gem install bundler --no-document) && bundler --version

      - name: Cache Pdfium binaries
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download Pdfium runtime
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime artifacts
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: packages/ruby/ext/kreuzberg_rb/native
          additional-destinations: |
            packages/ruby
            packages/ruby/lib

      - name: Install Ruby dependencies
        shell: bash
        run: scripts/ci/ruby/install-ruby-deps.sh packages/ruby

      - name: Vendor kreuzberg core crate
        shell: bash
        run: scripts/ci/ruby/vendor-kreuzberg-core.sh

      - name: Rebuild gems from source
        shell: bash
        working-directory: packages/ruby
        run: bundle exec rake build

      - name: Copy rebuilt gems to dist directory
        shell: bash
        run: |
          mkdir -p dist
          cp packages/ruby/pkg/*.gem dist/

      - name: Configure trusted publishing credentials
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        uses: rubygems/configure-rubygems-credentials@v1.0.0

      - name: Publish gems to RubyGems
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        env:
          RUBYGEMS_VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/publish-gems-direct.sh dist

      - name: Dry run summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        working-directory: dist
        run: (echo "Dry run requested; gem artifacts ready:" && ls -1 kreuzberg-*.gem) >> "$GITHUB_STEP_SUMMARY"

  publish-node:
    name: Publish Node packages
    needs: [prepare, validate-versions, check-npm, node-bindings]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_node == 'true' &&
      needs.check-npm.outputs.exists != 'true' &&
      needs.node-bindings.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: package.json

      - name: Download Node artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: node-bindings-*
          path: node-artifacts
          merge-multiple: true

      - name: Download TypeScript definitions
        uses: actions/download-artifact@v4
        with:
          name: node-typescript-defs
          path: typescript-defs

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org/

      - name: Update NPM
        run: npm install -g npm
        shell: bash

      - name: Prepare artifact directory
        run: scripts/publish/prepare-node-artifacts.sh node-artifacts typescript-defs crates/kreuzberg-node

      - name: Node artifacts summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        run: (echo "Dry run requested; Node binding tarballs staged:" && find node-artifacts -name '*.tar.gz' -print) >> "$GITHUB_STEP_SUMMARY"

      - name: Install workspace dependencies
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        run: pnpm install -C crates/kreuzberg-node

      - name: Build TypeScript bundle
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        run: pnpm -C crates/kreuzberg-node build:ts

      - name: Pack platform packages
        run: scripts/publish/pack-node-platform-packages.sh crates/kreuzberg-node/npm

      - name: Publish native binary packages
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        run: scripts/publish/publish-node-packages.sh crates/kreuzberg-node/npm "${{ needs.prepare.outputs.npm_tag }}"

      - name: Wait for npm indexing
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: scripts/publish/wait-for-package.sh npm "@kreuzberg/node-linux-x64-gnu" "${{ needs.prepare.outputs.version }}" 25

      - name: Wait for npm indexing (ARM64)
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: scripts/publish/wait-for-package.sh npm "@kreuzberg/node-linux-arm64-gnu" "${{ needs.prepare.outputs.version }}" 25

      - name: Prepare main Node package metadata
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        run: scripts/publish/node/prepublish-main-package.sh crates/kreuzberg-node

      - name: Publish main Node package
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        run: scripts/publish/publish-main-node-package.sh crates/kreuzberg-node "${{ needs.prepare.outputs.npm_tag }}"

  publish-wasm:
    name: Publish WASM package
    needs: [prepare, validate-versions, check-wasm, publish-node]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_wasm == 'true' &&
      needs.check-wasm.outputs.exists != 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22.6.0
          registry-url: https://registry.npmjs.org/

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: package.json
          run_install: false

      - name: Setup Rust toolchain
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-wasm

      - name: Add wasm32 target
        run: rustup target add wasm32-unknown-unknown

      - name: Install wasm-pack
        run: cargo install wasm-pack --locked

      - name: Install pnpm dependencies
        run: pnpm install --frozen-lockfile

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Build WASM for web target
        run: task wasm:build:web
        working-directory: crates/kreuzberg-wasm

      - name: Build WASM for bundler target
        run: task wasm:build:bundler
        working-directory: crates/kreuzberg-wasm

      - name: Build WASM for Node.js target
        run: task wasm:build:nodejs
        working-directory: crates/kreuzberg-wasm

      - name: Build WASM for Deno target
        run: task wasm:build:deno
        working-directory: crates/kreuzberg-wasm

      - name: Build TypeScript wrapper
        run: pnpm build
        working-directory: crates/kreuzberg-wasm

      - name: Extract version from package.json
        id: version
        run: echo "version=$(jq -r '.version' crates/kreuzberg-wasm/package.json)" >> "$GITHUB_OUTPUT"

      - name: Check if version already published to npm
        id: npm_check
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: scripts/publish/check-npm-version.sh "$VERSION" "@kreuzberg/wasm" >> "$GITHUB_OUTPUT"

      - name: Publish to npm
        if: ${{ steps.npm_check.outputs.exists != 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        working-directory: crates/kreuzberg-wasm
        run: npm publish --access public --provenance --ignore-scripts --tag "${{ needs.prepare.outputs.npm_tag }}"

      - name: Wait for npm indexing
        if: ${{ steps.npm_check.outputs.exists != 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: scripts/publish/wait-for-package.sh npm "@kreuzberg/wasm" "${{ needs.prepare.outputs.version }}" 25

      - name: Publish summary
        env:
          WASM_ALREADY_PUBLISHED: ${{ steps.npm_check.outputs.exists }}
          WASM_VERSION: ${{ steps.version.outputs.version }}
        run: scripts/publish/wasm/write-summary.sh

  publish-maven:
    name: Publish Maven packages
    needs: [prepare, validate-versions, check-maven, java-package]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_java == 'true' &&
      needs.check-maven.outputs.exists != 'true' &&
      needs.java-package.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      PDFIUM_VERSION: "7529"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Check Maven Central for existing release
        id: maven_check
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/check_maven.sh "${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Install system dependencies
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/setup-openssl

      - name: Setup Rust
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: publish-maven

      - name: Patch legacy Maven GPG arguments
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        run: scripts/publish/patch-maven-gpg-args.sh packages/java/pom.xml

      - name: Setup Java
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        id: setup-java
        env:
          MAVEN_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPP_PASSPHRASE }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
          cache: maven
          server-id: ossrh
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Setup Maven 3.9.11
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/setup-maven-4

      - name: Cache PDFium
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage PDFium runtime
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Download Java native artifacts
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        uses: actions/download-artifact@v4
        with:
          pattern: java-natives-*
          path: artifacts/java-natives
          merge-multiple: false

      - name: Stage Java native resources
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        shell: bash
        run: scripts/ci/java/stage-native-resources.sh "artifacts/java-natives" "packages/java/src/main/resources/natives"

      - name: Verify bundled native resources
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          test -f packages/java/src/main/resources/natives/linux-x86_64/libkreuzberg_ffi.so
          test -f packages/java/src/main/resources/natives/linux-arm64/libkreuzberg_ffi.so
          test -f packages/java/src/main/resources/natives/macos-arm64/libkreuzberg_ffi.dylib
          test -f packages/java/src/main/resources/natives/windows-x86_64/kreuzberg_ffi.dll

      - name: Prefer gpg2 binary
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        run: scripts/publish/prefer-gpg2-binary.sh

      - name: Release Maven package
        if: ${{ needs.prepare.outputs.dry_run != 'true' && steps.maven_check.outputs.exists != 'true' }}
        env:
          MAVEN_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPP_PASSPHRASE }}
        # Note: GPG passphrase is provided via MAVEN_GPG_PASSPHRASE env var (configured in pom.xml via passphraseEnvName)
        # The -Ppublish profile activates central-publishing-maven-plugin and skips maven-deploy-plugin
        run: scripts/publish/maven/deploy-or-skip.sh --version "${{ needs.prepare.outputs.version }}" -- mvn -f packages/java/pom.xml --batch-mode --no-transfer-progress -Ppublish -DskipTests clean deploy

      - name: Maven already published summary
        if: ${{ steps.maven_check.outputs.exists == 'true' }}
        run: echo "Maven package version ${{ needs.prepare.outputs.version }} already published; skipping." >> "$GITHUB_STEP_SUMMARY"

      - name: Dry run summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        run: echo "Dry run requested; Maven publish skipped." >> "$GITHUB_STEP_SUMMARY"

  publish-nuget:
    name: Publish NuGet package
    needs: [prepare, validate-versions, check-nuget, csharp-package]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_csharp == 'true' &&
      needs.check-nuget.outputs.exists != 'true' &&
      needs.csharp-package.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download NuGet artifact
        uses: actions/download-artifact@v4
        with:
          name: csharp-nuget
          path: dist

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Ensure dotnet is available
        run: scripts/publish/csharp/ensure-dotnet.sh

      - name: Publish to NuGet
        if: ${{ needs.prepare.outputs.dry_run != 'true' }}
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.NUGET_API_KEY }}
        run: dotnet nuget push "dist/*.nupkg" --source https://api.nuget.org/v3/index.json --api-key "${{ secrets.NUGET_API_KEY }}" --skip-duplicate && echo "NuGet package published." >> "$GITHUB_STEP_SUMMARY"

      - name: Dry run summary
        if: ${{ needs.prepare.outputs.dry_run == 'true' }}
        run: (echo "Dry run requested; NuGet publish skipped." && ls -1 dist/*.nupkg) >> "$GITHUB_STEP_SUMMARY"

  publish-php:
    name: Publish PHP package and PIE packages
    needs: [prepare, validate-versions, check-packagist, php-package]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_php == 'true' &&
      needs.check-packagist.outputs.exists != 'true' &&
      needs.php-package.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}
          fetch-depth: 0

      - name: Download PHP PIE package artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: php-pie-*
          path: dist/php
          merge-multiple: true

      - name: List PHP artifacts
        run: ls -lah dist/php/

      - name: Verify Packagist auto-update
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          DRY_RUN: ${{ needs.prepare.outputs.dry_run }}
          PACKAGIST_API_TOKEN: ${{ secrets.PACKAGIST_API_TOKEN }}
        run: scripts/publish/php/publish-packagist.sh "${{ needs.prepare.outputs.version }}"

      - name: Ensure GitHub release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/publish/ensure-github-release-exists.sh "${{ needs.prepare.outputs.tag }}"

      - name: Upload PIE packages to GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.tag }}
        run: |
          echo "Uploading PHP PIE packages to GitHub release ${TAG}..."
          for file in dist/php/kreuzberg-*.tar.gz*; do
            if [ -f "$file" ]; then
              echo "Uploading: $file"
              gh release upload "$TAG" "$file" --clobber
            fi
          done
          echo "PHP PIE packages uploaded to GitHub release" >> "$GITHUB_STEP_SUMMARY"

      - name: Generate release summary
        run: |
          {
            echo "## PHP Package Published"
            echo ""
            echo "- **Package**: kreuzberg/kreuzberg"
            echo "- **Version**: ${{ needs.prepare.outputs.version }}"
            echo "- **Packagist**: https://packagist.org/packages/kreuzberg/kreuzberg#${{ needs.prepare.outputs.version }}"
            echo "- **GitHub Release**: https://github.com/${{ github.repository }}/releases/tag/${{ needs.prepare.outputs.tag }}"
            echo ""
            echo "### PIE Packages (Platform-Specific Extensions)"
            echo ""
            echo "The following platform-specific PIE packages are available:"
            echo ""
            find dist/php -maxdepth 1 -name 'kreuzberg-*.tar.gz' -print0 | while IFS= read -r -d '' file; do
              basename "$file"
            done | sed 's/^/- /'
          } >> "$GITHUB_STEP_SUMMARY"

  publish-homebrew:
    name: Update Homebrew formula
    needs: [prepare, validate-versions, check-homebrew, upload-cli-release, upload-homebrew-bottles]
    if: |
      always() &&
      needs.prepare.outputs.dry_run != 'true' &&
      needs.prepare.outputs.is_tag == 'true' &&
      needs.prepare.outputs.release_homebrew == 'true' &&
      needs.check-homebrew.outputs.exists != 'true' &&
      needs.upload-cli-release.result == 'success' &&
      needs.upload-homebrew-bottles.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}

      - name: Extract release info
        id: release
        env:
          TAG: ${{ needs.prepare.outputs.tag }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/homebrew/extract-release-info.sh

      - name: Download bottle artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: homebrew-bottle-*
          path: dist/homebrew
          merge-multiple: true

      - name: Extract bottle hashes
        id: bottles
        env:
          TAG: ${{ needs.prepare.outputs.tag }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: scripts/publish/homebrew/extract-bottle-hashes.sh dist/homebrew

      - name: Setup Git credentials
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TOKEN }}
        run: |
          git config --global credential.helper store
          echo "https://x-access-token:${GH_TOKEN}@github.com" > ~/.git-credentials
          git config --global user.name "kreuzberg-bot"
          git config --global user.email "bot@kreuzberg.dev"

      - name: Update Homebrew formula with bottles
        env:
          TAG: ${{ needs.prepare.outputs.tag }}
          VERSION: ${{ needs.prepare.outputs.version }}
          DRY_RUN: 'false'
        run: scripts/publish/homebrew/update-formula-with-bottles.sh dist/homebrew homebrew-tap

  release-finalize:
    name: Finalize release and update notes
    needs:
      - prepare
      - check-pypi
      - check-npm
      - check-rubygems
      - check-maven
      - check-nuget
      - check-packagist
      - check-cratesio
      - check-hex
      - check-homebrew
      - publish-pypi
      - publish-node
      - publish-wasm
      - publish-rubygems
      - publish-maven
      - publish-nuget
      - publish-php
      - elixir-natives
      - upload-elixir-release
      - publish-hex
      - publish-homebrew
      - upload-cli-release
      - upload-go-release
      - homebrew-bottles
      - upload-homebrew-bottles
    if: always() && needs.prepare.outputs.is_tag == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}
          fetch-depth: 0

      - name: Setup Git credentials
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create Go module tag
        if: ${{ needs.prepare.outputs.release_go == 'true' }}
        run: scripts/publish/go/tag-and-push-go-module.sh "${{ needs.prepare.outputs.tag }}"

      - name: Aggregate release status
        id: status
        env:
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
          CHECK_PYPI_EXISTS: ${{ needs.check-pypi.outputs.exists }}
          CHECK_NPM_EXISTS: ${{ needs.check-npm.outputs.exists }}
          CHECK_RUBYGEMS_EXISTS: ${{ needs.check-rubygems.outputs.exists }}
          CHECK_MAVEN_EXISTS: ${{ needs.check-maven.outputs.exists }}
          CHECK_NUGET_EXISTS: ${{ needs.check-nuget.outputs.exists }}
          CHECK_PACKAGIST_EXISTS: ${{ needs.check-packagist.outputs.exists }}
          CHECK_HEX_EXISTS: ${{ needs.check-hex.outputs.exists }}
          RESULT_PUBLISH_PYPI: ${{ needs.publish-pypi.result }}
          RESULT_PUBLISH_NODE: ${{ needs.publish-node.result }}
          RESULT_PUBLISH_WASM: ${{ needs.publish-wasm.result }}
          RESULT_PUBLISH_RUBYGEMS: ${{ needs.publish-rubygems.result }}
          RESULT_PUBLISH_MAVEN: ${{ needs.publish-maven.result }}
          RESULT_PUBLISH_NUGET: ${{ needs.publish-nuget.result }}
          RESULT_PUBLISH_PHP: ${{ needs.publish-php.result }}
          RESULT_ELIXIR_NATIVES: ${{ needs.elixir-natives.result }}
          RESULT_UPLOAD_ELIXIR_RELEASE: ${{ needs.upload-elixir-release.result }}
          RESULT_PUBLISH_HEX: ${{ needs.publish-hex.result }}
          RESULT_PUBLISH_HOMEBREW: ${{ needs.publish-homebrew.result }}
          RESULT_UPLOAD_CLI_RELEASE: ${{ needs.upload-cli-release.result }}
          RESULT_UPLOAD_GO_RELEASE: ${{ needs.upload-go-release.result }}
        run: python3 scripts/publish/aggregate-release-status.py
