# Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
# To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang python
#
# Tests for image fixtures.
from __future__ import annotations

import pytest

from kreuzberg import (
    extract_file_sync,
)

from . import helpers


def test_image_bmp_basic() -> None:
    """BMP image extraction via OCR."""

    document_path = helpers.resolve_document("images/bmp_24.bmp")
    if not document_path.exists():
        pytest.skip(f"Skipping image_bmp_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/bmp"])
    helpers.assert_content_not_empty(result)


def test_image_gif_basic() -> None:
    """GIF image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.gif")
    if not document_path.exists():
        pytest.skip(f"Skipping image_gif_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/gif"])
    helpers.assert_content_not_empty(result)


def test_image_jp2_basic() -> None:
    """JPEG 2000 image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.jp2")
    if not document_path.exists():
        pytest.skip(f"Skipping image_jp2_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/jp2", "image/jpeg2000"])
    helpers.assert_content_not_empty(result)


def test_image_metadata_only() -> None:
    """JPEG image to validate metadata extraction without OCR."""

    document_path = helpers.resolve_document("images/example.jpg")
    if not document_path.exists():
        pytest.skip(f"Skipping image_metadata_only: missing document at {document_path}")

    config = helpers.build_config({"ocr": None})

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/jpeg"])
    helpers.assert_max_content_length(result, 200)


def test_image_pbm_basic() -> None:
    """PBM (portable bitmap) image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.pbm")
    if not document_path.exists():
        pytest.skip(f"Skipping image_pbm_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/x-portable-bitmap", "image/x-pbm"])
    helpers.assert_content_not_empty(result)


def test_image_pgm_basic() -> None:
    """PGM (portable graymap) image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.pgm")
    if not document_path.exists():
        pytest.skip(f"Skipping image_pgm_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/x-portable-graymap", "image/x-pgm"])
    helpers.assert_content_not_empty(result)


def test_image_ppm_basic() -> None:
    """PPM (portable pixmap) image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.ppm")
    if not document_path.exists():
        pytest.skip(f"Skipping image_ppm_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/x-portable-pixmap", "image/x-ppm"])
    helpers.assert_content_not_empty(result)


def test_image_svg_basic() -> None:
    """SVG image extraction."""

    document_path = helpers.resolve_document("xml/simple_svg.svg")
    if not document_path.exists():
        pytest.skip(f"Skipping image_svg_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/svg+xml"])
    helpers.assert_min_content_length(result, 5)


def test_image_tiff_basic() -> None:
    """TIFF image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.tif")
    if not document_path.exists():
        pytest.skip(f"Skipping image_tiff_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/tiff"])
    helpers.assert_content_not_empty(result)


def test_image_webp_basic() -> None:
    """WebP image extraction via OCR."""

    document_path = helpers.resolve_document("images_extra/ocr_image.webp")
    if not document_path.exists():
        pytest.skip(f"Skipping image_webp_basic: missing document at {document_path}")

    config = helpers.build_config(None)

    result = extract_file_sync(document_path, None, config)

    helpers.assert_expected_mime(result, ["image/webp"])
    helpers.assert_content_not_empty(result)
